---
phase: 0.2.5-carried-tech-debt-v0-1
plan: 02
subsystem: api
tags: [fastapi, orjson, pydantic-rust-serializer, deprecation-cleanup, techdebt]

# Dependency graph
requires:
  - phase: 0.2.5-carried-tech-debt-v0-1
    provides: "Plan 01 established the comment-stripping grep gate pattern (`grep -vE '^[[:space:]]*#' ... | grep -c`) reused verbatim by this plan's acceptance criteria; no functional code dependency"
provides:
  - "FastAPI app object constructed without `default_response_class=ORJSONResponse` — relies on framework-default JSONResponse for dict-returning routes and Pydantic-Rust serializer fast path (`use_dump_json=True`) for `POST /search` (the only `response_model=` route)"
  - "Empirical confirmation that `app.router.default_response_class.value` now resolves to `starlette.responses.JSONResponse` at runtime (was `fastapi.responses.ORJSONResponse` pre-edit)"
  - "Elimination of all `FastAPIDeprecationWarning: ORJSONResponse is deprecated` emissions from the test suite (baseline: 5 occurrences per `pytest tests/test_health.py tests/integration/test_search_paginated.py`; post-edit: 0)"
affects: [api, fastapi-upgrades, future-orjson-handlers]

# Tech tracking
tech-stack:
  added: []  # FastAPI was already 0.136.3 — no new libraries
  patterns:
    - "Framework-default response serialization (Pattern 2 from 0.2.5-RESEARCH.md): no `default_response_class` kwarg → FastAPI's `Default(JSONResponse)` placeholder is preserved → `response_model=` routes auto-use Pydantic-Rust fast path (PR #14964)"
    - "Empirical runtime introspection of `app.router.default_response_class.value` as the verification artifact for FastAPI response-class chain edits — more robust than grepping source"

key-files:
  created: []
  modified:
    - src/artiscrapper/main.py

key-decisions:
  - "Full removal of `default_response_class` kwarg (per Plan 02 objective + RESEARCH Open Question 3 lock) — NOT replaced with `default_response_class=JSONResponse`. The explicit JSONResponse declaration would be deprecation-clean but adds no behavioral value because FastAPI's `Default(JSONResponse)` placeholder is what triggers the Pydantic-Rust fast-path check (`isinstance(response_class, DefaultPlaceholder)` at routing.py:692); declaring it explicitly would defeat that branch and force the slow `actual_response_class(content)` path"
  - "Same-file adjacent-line I001 isort fix (swap `from pathlib`/`from contextlib`) applied as Rule 3 deviation because the plan's acceptance criteria mandates `ruff check src/artiscrapper/main.py exits 0` and the pre-existing latent I001 only surfaces post-edit — leaving it would block the gate while clearly being within scope (same import block I touched, 1-line swap)"

patterns-established:
  - "Pattern A — FastAPI response-class debugging (when a deprecation warning fires despite source removal): inspect `app.router.default_response_class` (must be `DefaultPlaceholder`) and `placeholder.value` (must be `starlette.responses.JSONResponse`, not `fastapi.responses.ORJSONResponse`). Source greps alone are insufficient because `__pycache__/` shadowing across the main-repo `.venv` + worktree edits can mask the real handler closure"
  - "Pattern B — Worktree test execution with PYTHONPATH override is INSUFFICIENT when tests use `from src.artiscrapper.main import ...` (root-relative imports); CWD must be the worktree root + `.env` symlinked so pydantic-settings can read `LLM_ROUTER_BEARER_TOKEN`"

requirements-completed:
  - TECHDEBT-02

# Metrics
duration: ~15min
completed: 2026-06-06
---

# Phase 0.2.5 Plan 02: Remove deprecated ORJSONResponse from FastAPI app (TECHDEBT-02)

**Removed `default_response_class=ORJSONResponse` + the orphan `from fastapi.responses import ORJSONResponse` import from `src/artiscrapper/main.py`; FastAPI now resolves `POST /search` via Pydantic-Rust fast path (PR #14964) and the 3 dict-returning endpoints via framework-default JSONResponse — zero FastAPIDeprecationWarning emissions remain.**

## Performance

- **Duration:** ~15 min (1 task, 2 line-deletions + 1 isort tidy)
- **Started:** 2026-06-06T12:55Z (approx, branch base 8e448a5)
- **Completed:** 2026-06-06T13:04Z
- **Tasks:** 1/1
- **Files modified:** 1 (`src/artiscrapper/main.py`)

## Accomplishments

- `src/artiscrapper/main.py` line 28 (`from fastapi.responses import ORJSONResponse`) deleted.
- `src/artiscrapper/main.py` line 245 (`default_response_class=ORJSONResponse,`) deleted from the `FastAPI(...)` constructor.
- Runtime introspection confirms `app.router.default_response_class.value` resolves to `starlette.responses.JSONResponse` (the framework default Pydantic-Rust path placeholder), not `fastapi.responses.ORJSONResponse`.
- Full quick suite holds at 124 passed / 2 skipped (baseline regression invariant), and the `FastAPIDeprecationWarning: ORJSONResponse is deprecated` emission is gone from `tests/test_health.py` (was 2/run) and from `tests/integration/test_search_paginated.py` (was 3/run); total cleared = 5 warning emissions per quick-suite run.

## Task Commits

1. **Task 1: Remove ORJSONResponse import + default_response_class kwarg from main.py** — `5cf7693` (feat)

_Note: SUMMARY metadata commit will be added by this same plan at write-time (see end of file)._

## Files Created/Modified

- `src/artiscrapper/main.py` — Two deletions inside the FastAPI app construction block: orphan `ORJSONResponse` import (line 28) and `default_response_class=ORJSONResponse,` kwarg (line 245). Plus a 2-line isort swap of `from pathlib`/`from contextlib` (adjacent lines, Rule 3 deviation — see below). Post-edit `FastAPI(...)` constructor reads exactly `lifespan=lifespan, title="artiscrapper", version=settings.VERSION` (3 kwargs, no default_response_class).

## Decisions Made

- **Full removal vs. JSONResponse-swap** — Followed plan + RESEARCH Open Question 3: full removal. Rationale documented in frontmatter `key-decisions` (declaring `default_response_class=JSONResponse` explicitly would BREAK the Pydantic-Rust fast path because the fast-path check is `isinstance(response_class, DefaultPlaceholder)`; an explicit class breaks the DefaultPlaceholder wrapper).
- **Same-file isort fix accepted as in-scope** — The `pathlib` → `contextlib` ordering was a pre-existing latent I001 on HEAD (verified by running `ruff check` on `git show HEAD:src/artiscrapper/main.py`). It only began failing the lint gate after my deletion changed surrounding lines. Fixed inline (1 line swap, immediately adjacent to the lines I edited) because the plan's acceptance criteria mandates `ruff check src/artiscrapper/main.py exits 0` — see Rule 3 deviation below.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Pre-existing I001 isort violation surfaced by line deletion**
- **Found during:** Task 1, gate verification step (`ruff check src/artiscrapper/main.py`)
- **Issue:** Lines 18-19 originally read `from pathlib import Path` / `from contextlib import asynccontextmanager` — wrong alphabetical order for isort. Ruff did NOT flag this on baseline HEAD; the violation only surfaces after deleting line 28 (the orphan `ORJSONResponse` import) because ruff re-evaluates the entire import block. Confirmed pre-existing by running `ruff check` on `git show HEAD:src/artiscrapper/main.py` (clean diff except the I001).
- **Fix:** Swapped order to `from contextlib import asynccontextmanager` / `from pathlib import Path` (single-line swap, immediately adjacent to the lines I deleted). This is the deterministic fix `ruff check --fix --diff` would apply.
- **Files modified:** `src/artiscrapper/main.py` (1 line swap)
- **Verification:** `ruff check src/artiscrapper/main.py` → "All checks passed!" (exit 0).
- **Committed in:** `5cf7693` (Task 1 commit — bundled because the fix is in the same file + same import block + adjacent to the deletion).

**2. [Process note, NOT a code deviation] Worktree test execution required `.env` symlink + worktree-CWD invocation**
- **Found during:** Initial verification attempt
- **Issue:** First verification attempt used `cd /home/luis/proyectos/artiscrapper && PYTHONPATH=<worktree>/src .venv/bin/pytest`. Tests still showed the FastAPIDeprecationWarning because `tests/test_health.py` line 46 imports `from src.artiscrapper.main import app` (note the `src.` prefix — root-relative, NOT package import), which loads the MAIN repo's source tree (sys.path[0] = main repo). PYTHONPATH override only affects `artiscrapper.` lookups, not `src.artiscrapper.`.
- **Fix:** Symlinked `/home/luis/proyectos/artiscrapper/.env` into the worktree root and ran pytest from worktree CWD. This made `src/artiscrapper/main.py` resolve to the worktree's (edited) file. Symlink is git-ignored (`.gitignore:2 = .env`) — no commit pollution.
- **Verification:** Post-fix runs show `FastAPIDeprecationWarning` count = 0 in all three target suites (health, integration, full quick suite).
- **Committed in:** N/A (process change, not a code change). Symlink target is the main-repo `.env`; symlink itself is ignored.

**3. [Process incident — recovered] Accidental `git stash` of in-progress edits**
- **Found during:** Mid-task, while drafting the ruff lint comment in a Bash heredoc
- **Issue:** A `git stash` command executed (intended to be a dry-run echo). Per the `<destructive_git_prohibition>` rule, stash is forbidden in worktree mode because `refs/stash` is shared with the main repo and sibling worktrees. The stash succeeded silently, restoring `main.py` to baseline and putting my 2 edits into `stash@{0}`.
- **Fix:** Discarded the stash via `git stash drop stash@{0}` (does NOT touch the working tree) and manually re-applied the two original Edit operations. Net diff identical to first attempt. No cross-worktree contamination occurred because (a) only this agent's edits were in the stash, (b) it was the top-of-stack entry, and (c) it was dropped within seconds without `pop`/`apply` ever running. Verified via `git stash list` (empty) and `git status --short` (only the redo edits).
- **Verification:** Working tree post-recovery is byte-identical to the intended post-edit state; SHA of final committed file matches what the first-attempt edit would have produced.
- **Committed in:** N/A (process incident, fully recovered; no incorrect state ever committed).

---

**Total deviations:** 1 code deviation (Rule 3 isort), 2 process notes (no functional impact).
**Impact on plan:** Code deviation #1 is essential to satisfy the plan's `ruff check` acceptance gate and is strictly in-scope (same file, same import block, adjacent lines). No scope creep. Process notes documented for the next executor working in a worktree against `src.artiscrapper.*` import paths.

## Issues Encountered

- **FastAPI deprecation warning persisted after source removal** — Initial pytest run still emitted `FastAPIDeprecationWarning: ORJSONResponse is deprecated` despite zero live source references. Root-caused via runtime introspection: `tests/test_health.py` line 46 uses `from src.artiscrapper.main import app` (root-relative, sys.path-dependent), so the MAIN-repo source tree was loaded regardless of PYTHONPATH override. Fixed by switching pytest CWD to the worktree root (where `.` = worktree). Documented as Process Deviation #2 above; lesson captured in patterns-established Pattern B.

## User Setup Required

None — no external service configuration introduced or changed. Only an internal FastAPI configuration cleanup.

## Self-Check: PASSED

- File `src/artiscrapper/main.py` exists at worktree path: FOUND.
- Commit `5cf7693`: FOUND (`git log --oneline -1` shows `5cf7693 feat(0.2.5-02): remove deprecated ORJSONResponse from FastAPI app (TECHDEBT-02)`).
- Acceptance gates re-verified at SUMMARY-write time:
  - `grep -vE '^[[:space:]]*#' src/artiscrapper/main.py | grep -c 'ORJSONResponse'` → 0 ✓
  - `grep -vE '^[[:space:]]*#' src/artiscrapper/main.py | grep -c 'default_response_class'` → 0 ✓
  - `ruff check src/artiscrapper/main.py` → All checks passed (exit 0) ✓
  - `pytest tests/test_health.py tests/integration/test_search_paginated.py -q` → 9 passed, 0 FastAPIDeprecationWarning ✓
  - `pytest tests/ -q -m "not slow"` → 124 passed, 2 skipped ✓ (regression invariant ≥124 satisfied)
  - Runtime: `app.router.default_response_class.value == starlette.responses.JSONResponse` ✓
- Threat surface scan: no new network endpoints, auth paths, file access patterns, or schema changes introduced. T-0.2.5-03 (mitigate) verified by `tests/integration/test_search_paginated.py` (still green, response shape preserved). T-0.2.5-04 (accept) verified by `tests/test_health.py` (still green, dict shape preserved).

## Next Phase Readiness

- TECHDEBT-02 closed. ROADMAP success criterion 2 satisfied with decision = **full removal** documented above.
- No follow-ups needed for ORJSONResponse cleanup itself. The pre-existing `StarletteDeprecationWarning: httpx`/`httpx2` (testclient.py:1) and the `tldextract` `registered_domain` DeprecationWarning are unrelated — out of scope for this plan; could become standalone TECHDEBT items in a future phase if desired.
- Plan 03 of this phase can proceed independently (no shared files).

---
*Phase: 0.2.5-carried-tech-debt-v0-1*
*Completed: 2026-06-06*
