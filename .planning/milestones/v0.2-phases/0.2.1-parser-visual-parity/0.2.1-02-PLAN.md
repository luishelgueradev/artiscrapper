---
phase: 0.2.1-parser-visual-parity
plan: 02
type: execute
wave: 2
depends_on: ["0.2.1-01"]
files_modified:
  - tests/fixtures/serp/pla_unit_robotech.html
  - tests/fixtures/serp/pla_unit_zapatillas.html
  - tests/test_parser.py
  - tests/integration/test_serp_pla_unit_replay.py
  - src/artiscrapper/main.py
autonomous: true
requirements: []
closes_context_decisions: [D-12, D-13, D-14, D-15, D-16, D-17]

must_haves:
  truths:
    - "tests/fixtures/serp/pla_unit_robotech.html exists, size <500KB, contiene >= 4 div.pla-unit con URLs MELI canonical"
    - "tests/fixtures/serp/pla_unit_zapatillas.html exists, size <800KB, contiene >= 30 div.pla-unit con URLs reales no-Google"
    - "tests/test_parser.py contiene >= 9 nuevos tests (4 pla-unit + 5 regex precio)"
    - "tests/integration/test_serp_pla_unit_replay.py existe y cubre el fixture-replay end-to-end de pla-unit"
    - "src/artiscrapper/main.py expone endpoint GET /admin/parity/{query} autenticado con X-API-Key"
    - ".venv/bin/pytest tests/test_parser.py tests/integration/test_serp_pla_unit_replay.py -q exits 0 (all new tests pass)"
  artifacts:
    - path: "tests/fixtures/serp/pla_unit_robotech.html"
      provides: "Wave-0 fixture para test_pla_unit_extracts_4_products_from_robotech_fixture"
      contains: "div class=\"mnr-c c3mZkd j2G0Uc pla-unit\""
    - path: "tests/fixtures/serp/pla_unit_zapatillas.html"
      provides: "Wave-0 fixture para test_pla_unit_extracts_30_products_from_zapatillas_fixture"
      contains: "div class=\"mnr-c c3mZkd j2G0Uc pla-unit\""
    - path: "tests/test_parser.py"
      provides: "9 nuevos tests cubriendo pla-unit + regex precio"
      contains: "def test_pla_unit"
    - path: "tests/integration/test_serp_pla_unit_replay.py"
      provides: "Fixture-replay integration test"
      contains: "def test_robotech_returns_pla_unit_products"
    - path: "src/artiscrapper/main.py"
      provides: "Endpoint GET /admin/parity/{query} para auditoría manual del drift"
      contains: "@app.get(\"/admin/parity/"
  key_links:
    - from: "tests/test_parser.py"
      to: "src/artiscrapper/search.py"
      via: "Imports _extract_pla_unit + _PRICE_RE + PLA_SELECTORS y los testea contra fixtures"
      pattern: "from artiscrapper.search import"
---

<objective>
Cementar Wave 1 con tests + fixtures + harness mínimo de auditoría. Esta wave NO toca código del parser — solo agrega cobertura y el endpoint `/admin/parity/<q>` para que cualquier futura regresión (rotación de clases Google, cambios upstream) se detecte rápido.

**Purpose:** sin estos tests, los patches de Wave 1 podrían regresar silenciosamente cuando Google rota `VbBaOe`, `UsGWMe`, o cambia el contenedor `pla-unit`. Los fixtures congelan el HTML al estado 2026-06-05 (la "ground truth visual" del reporte). El endpoint `/admin/parity/<q>` da una forma manual de chequear cualquier query nueva sin esperar al harness completo (Phase 0.2.2).

**Output:**
- 2 fixtures HTML en `tests/fixtures/serp/` (robotech + zapatillas), filtradas para size <800KB con base64 placeholdered
- 9 tests nuevos en `tests/test_parser.py`: 4 pla-unit (counts + URLs + flags + no aclk) + 5 regex precio (10 casos del reporte, en 5 tests)
- 1 test integration en `tests/integration/test_serp_pla_unit_replay.py` que corre `parse_serp` end-to-end sobre los 2 fixtures
- Endpoint `GET /admin/parity/{query}` en `main.py` con auth `X-API-Key` y métrica Prometheus mínima
- Smoke validation: `pytest tests/test_parser.py tests/integration/test_serp_pla_unit_replay.py` pasa con 100% de los tests nuevos verdes
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/PARSER-VISUAL-PARITY-2026-06-05.md
@.planning/drafts/0.2.1-parser-visual-parity/0.2.1-CONTEXT.md
@.planning/drafts/0.2.1-parser-visual-parity/0.2.1-RESEARCH.md
@.planning/drafts/0.2.1-parser-visual-parity/0.2.1-01-PLAN.md
@src/artiscrapper/search.py
@src/artiscrapper/main.py
@tests/test_parser.py
</context>

<tasks>

<task id="2.1" wave="2" parallel_safe="yes" autonomous="yes">
<title>Generar fixture pla_unit_robotech.html</title>

<read_first>
- /tmp/toy-robotech.html (HTML source completo, ~2.2 MB)
- tests/fixtures/serp/ (verificar convención existente de fixtures Phase 1)
- .planning/PARSER-VISUAL-PARITY-2026-06-05.md (§3.4 estructura de pla-unit)
</read_first>

<action>
Crear `tests/fixtures/serp/pla_unit_robotech.html` a partir de `/tmp/toy-robotech.html` con dos transformaciones para reducir tamaño:

1. **Strip base64 images**: reemplazar todo match de `data:image/[^"]*` por `data:image/png;base64,PLACEHOLDER`. Esto reduce el HTML de ~2.2MB a ~400KB sin tocar la estructura del DOM.

2. **Preserve completo**: NO cortar bloques. Mantener todos los `div.pla-unit`, `div.Ez5pwe`, `div.MjjYud`, scripts inline, todo el `<head>`. Solo se reemplazan los blobs base64.

Script reusable (correr en host con `.venv/bin/python`):

```python
import re
from pathlib import Path
src = Path("/tmp/toy-robotech.html").read_text()
out = re.sub(r'data:image/[^"\']*', 'data:image/png;base64,PLACEHOLDER', src)
Path("tests/fixtures/serp/pla_unit_robotech.html").write_text(out)
print(f"size: {len(out)} bytes")
```

NO commitear el `/tmp/toy-robotech.html` mismo — solo el fixture filtrado.
</action>

<acceptance_criteria>
- `test -f tests/fixtures/serp/pla_unit_robotech.html` exits 0
- `stat -c%s tests/fixtures/serp/pla_unit_robotech.html` retorna un valor entre 200000 y 800000 (200KB-800KB)
- `grep -c 'div class="mnr-c c3mZkd j2G0Uc pla-unit"' tests/fixtures/serp/pla_unit_robotech.html` >= 4
- `grep -c 'data:image/png;base64,PLACEHOLDER' tests/fixtures/serp/pla_unit_robotech.html` >= 10 (confirma strip aplicado)
- `grep -c 'data:image/webp;base64,UklG' tests/fixtures/serp/pla_unit_robotech.html` == 0 (no quedan blobs reales)
- `grep -c 'mercadolibre.com.ar' tests/fixtures/serp/pla_unit_robotech.html` >= 4
</acceptance_criteria>

</task>

<task id="2.2" wave="2" parallel_safe="yes" autonomous="yes">
<title>Generar fixture pla_unit_zapatillas.html</title>

<read_first>
- /tmp/ropa-zapatilla.html (HTML source completo, ~2.4 MB)
- La estrategia es idéntica a la del task 2.1 — copiar el approach
</read_first>

<action>
Crear `tests/fixtures/serp/pla_unit_zapatillas.html` con el mismo script del task 2.1 pero source `/tmp/ropa-zapatilla.html` y destination `tests/fixtures/serp/pla_unit_zapatillas.html`.

Esta fixture cubre el caso de stores no-MELI (sporting.com.ar, nike.com.ar, stockcenter.com.ar, etc.) para garantizar que el extractor pla-unit no esté hardcodeado a MELI patterns.
</action>

<acceptance_criteria>
- `test -f tests/fixtures/serp/pla_unit_zapatillas.html` exits 0
- `stat -c%s tests/fixtures/serp/pla_unit_zapatillas.html` retorna un valor entre 200000 y 900000
- `grep -c 'div class="mnr-c c3mZkd j2G0Uc pla-unit"' tests/fixtures/serp/pla_unit_zapatillas.html` >= 30
- `grep -c 'sporting.com.ar\|nike.com.ar\|stockcenter.com.ar' tests/fixtures/serp/pla_unit_zapatillas.html` >= 5 (al menos 5 stores distintos)
- `grep -c 'data:image/png;base64,PLACEHOLDER' tests/fixtures/serp/pla_unit_zapatillas.html` >= 10
</acceptance_criteria>

</task>

<task id="2.3" wave="3" parallel_safe="no" autonomous="yes">
<title>Tests unit pla-unit en tests/test_parser.py</title>

<read_first>
- tests/test_parser.py (convención existente: imports, helpers, formato test)
- tests/fixtures/serp/pla_unit_robotech.html (output del task 2.1)
- tests/fixtures/serp/pla_unit_zapatillas.html (output del task 2.2)
- src/artiscrapper/search.py (después de Wave 1: imports disponibles `_extract_pla_unit`, `PLA_SELECTORS`)
</read_first>

<action>
En `tests/test_parser.py` agregar (al final del archivo) los siguientes 4 tests:

```python
# ── pla-unit extractor tests (Phase 0.2.1) ──
from pathlib import Path
from selectolax.parser import HTMLParser
from artiscrapper.search import _extract_pla_unit, PLA_SELECTORS, parse_serp

FIXTURE_DIR = Path(__file__).parent / "fixtures" / "serp"


def _load_fixture(name: str) -> str:
    return (FIXTURE_DIR / name).read_text()


def test_pla_unit_extracts_4_products_from_robotech_fixture():
    """Wave-0: robotech fixture has 4 pla-unit cards with MELI canonical URLs."""
    html = _load_fixture("pla_unit_robotech.html")
    tree = HTMLParser(html)
    extracted = [_extract_pla_unit(n) for n in tree.css("div.pla-unit")]
    extracted = [c for c in extracted if c]
    assert len(extracted) >= 4, f"Expected >=4 pla-units, got {len(extracted)}"
    assert all("mercadolibre.com.ar" in c["url"] for c in extracted)
    assert all(c["has_price"] for c in extracted)


def test_pla_unit_extracts_30_products_from_zapatillas_fixture():
    """Wave-0: zapatillas fixture has 30 pla-unit cards with diverse real-store URLs."""
    html = _load_fixture("pla_unit_zapatillas.html")
    tree = HTMLParser(html)
    extracted = [_extract_pla_unit(n) for n in tree.css("div.pla-unit")]
    extracted = [c for c in extracted if c]
    assert len(extracted) >= 30, f"Expected >=30 pla-units, got {len(extracted)}"
    # Stores diversos: not all MELI
    stores = {c["store_hint"] for c in extracted if c.get("store_hint")}
    assert len(stores) >= 3, f"Expected diverse stores, got {stores}"


def test_pla_unit_no_aclk_urls():
    """The /aclk? redirect <a> is a display:none tracking pixel — extractor must skip it."""
    html = _load_fixture("pla_unit_robotech.html")
    tree = HTMLParser(html)
    extracted = [_extract_pla_unit(n) for n in tree.css("div.pla-unit")]
    extracted = [c for c in extracted if c]
    for c in extracted:
        assert "/aclk?" not in c["url"], f"pla-unit returned aclk URL: {c['url']}"
        assert c["url"].startswith("http"), f"pla-unit URL not http: {c['url']}"


def test_pla_unit_sponsored_flag():
    """All pla-units carry 'sponsored' + 'pla_unit' flags for downstream policy."""
    html = _load_fixture("pla_unit_robotech.html")
    tree = HTMLParser(html)
    extracted = [_extract_pla_unit(n) for n in tree.css("div.pla-unit")]
    extracted = [c for c in extracted if c]
    for c in extracted:
        assert "pla_unit" in c["flags"]
        assert "sponsored" in c["flags"]
```
</action>

<acceptance_criteria>
- `grep -n 'def test_pla_unit_extracts_4_products_from_robotech_fixture' tests/test_parser.py` encuentra 1 línea
- `grep -n 'def test_pla_unit_extracts_30_products_from_zapatillas_fixture' tests/test_parser.py` encuentra 1 línea
- `grep -n 'def test_pla_unit_no_aclk_urls' tests/test_parser.py` encuentra 1 línea
- `grep -n 'def test_pla_unit_sponsored_flag' tests/test_parser.py` encuentra 1 línea
- `.venv/bin/pytest tests/test_parser.py -k "pla_unit" -v` exits 0 con los 4 tests pasando
</acceptance_criteria>

</task>

<task id="2.4" wave="3" parallel_safe="yes" autonomous="yes">
<title>Tests unit regex precio v2 en tests/test_parser.py</title>

<read_first>
- tests/test_parser.py (output del task 2.3 — agrega tests al final del archivo)
- .planning/PARSER-VISUAL-PARITY-2026-06-05.md (§7 Patch 2 — 10 casos validados)
- src/artiscrapper/search.py:_PRICE_RE (después de Wave 1 task 1.2)
</read_first>

<action>
Agregar al final de `tests/test_parser.py` (después de los pla-unit tests del task 2.3):

```python
# ── _PRICE_RE v2 tests (Phase 0.2.1) ──
import pytest
from artiscrapper.search import _PRICE_RE, _extract_commercial_signals


@pytest.mark.parametrize("text,expected", [
    ("Threezero figura $410.420,311001hobbies.es", "$410.420,31"),
    ("Yr-052F $\xa01.397.000,00 ahora", "$\xa01.397.000,00"),
    ("$45.885,00x 6Tigre.com.ar", "$45.885,00"),
    ("Precio: $5.000,00 ahora", "$5.000,00"),
    ("$ 12.345,67/mes x 6 cuotas", "$ 12.345,67"),
    ("Total $920.000,00 fin", "$920.000,00"),
])
def test_price_regex_matches_normal_AR_format(text, expected):
    m = _PRICE_RE.search(text)
    assert m is not None, f"No match in {text!r}"
    assert m.group(0) == expected, f"Got {m.group(0)!r}, expected {expected!r}"


def test_price_regex_no_bleed_into_store_digits():
    """Bug regression: text '$410.420,311001hobbies.es' must NOT match '$410.420,311001'.
    Real-world source: carousel item 'threezero Figura ... Robo-Dou$ 410.420,311001hobbies.es'.
    """
    text = "threezero Figura a escala 1:6 de Robo-Dou$\xa0410.420,311001hobbies.esy más5,0(2)"
    m = _PRICE_RE.search(text)
    assert m is not None
    assert m.group(0).rstrip() == "$\xa0410.420,31", f"Bleeding bug back: {m.group(0)!r}"


def test_price_regex_handles_ars_integer():
    """ARS without decimals (legacy format)."""
    m = _PRICE_RE.search("ARS 5000")
    assert m is not None
    assert m.group(0) == "ARS 5000"


def test_price_regex_handles_us_dollar_integer():
    """U$S without decimals (legacy format)."""
    m = _PRICE_RE.search("U$S 5000")
    assert m is not None
    assert m.group(0) == "U$S 5000"


def test_price_regex_filtered_by_short_digit_count():
    """'$5' is too short — _extract_commercial_signals must filter by digits >= 3.
    Regex may match prefix but downstream guard rejects.
    """
    sigs = _extract_commercial_signals("hola $5 chau")
    assert "price_in_card" not in sigs, f"Short price not filtered: {sigs}"
```
</action>

<acceptance_criteria>
- `grep -n 'def test_price_regex_matches_normal_AR_format' tests/test_parser.py` encuentra 1 línea
- `grep -n 'def test_price_regex_no_bleed_into_store_digits' tests/test_parser.py` encuentra 1 línea
- `grep -n 'def test_price_regex_handles_ars_integer' tests/test_parser.py` encuentra 1 línea
- `grep -n 'def test_price_regex_handles_us_dollar_integer' tests/test_parser.py` encuentra 1 línea
- `grep -n 'def test_price_regex_filtered_by_short_digit_count' tests/test_parser.py` encuentra 1 línea
- `.venv/bin/pytest tests/test_parser.py -k "price_regex" -v` exits 0
- `.venv/bin/pytest tests/test_parser.py -v` total >= 9 tests nuevos (4 pla + 5 regex)
</acceptance_criteria>

</task>

<task id="2.5" wave="3" parallel_safe="yes" autonomous="yes">
<title>Integration test fixture-replay end-to-end</title>

<read_first>
- tests/integration/ (convención de tests existentes — `test_challenge_backoff.py` como referencia)
- tests/fixtures/serp/pla_unit_robotech.html (output task 2.1)
- tests/fixtures/serp/pla_unit_zapatillas.html (output task 2.2)
- src/artiscrapper/search.py:parse_serp (después de Wave 1)
</read_first>

<action>
Crear nuevo archivo `tests/integration/test_serp_pla_unit_replay.py`:

```python
"""Integration test: parse_serp() over Phase 0.2.1 pla-unit fixtures.

Verifies end-to-end that the pla-unit pass integrates cleanly with the organic
cascade + carousel loop, AND that the regex precio v2 doesn't regress any
existing carousel coverage.

Source fixtures captured 2026-06-05 (see .planning/PARSER-VISUAL-PARITY-2026-06-05.md).
"""
from pathlib import Path
import pytest
from artiscrapper.search import parse_serp

FIXTURE_DIR = Path(__file__).parent.parent / "fixtures" / "serp"


def _load(name: str) -> str:
    return (FIXTURE_DIR / name).read_text()


def test_robotech_returns_pla_unit_products():
    """parse_serp on robotech fixture yields 4+ pla-units with MELI canonical URLs.
    Also: carousel coverage >=25 (no regression from Wave 1)."""
    cands = parse_serp(_load("pla_unit_robotech.html"))
    pla = [c for c in cands if "pla_unit" in (c.get("flags") or [])]
    carousel = [c for c in cands if "carousel" in (c.get("flags") or [])]
    assert len(pla) >= 4, f"Expected >=4 pla, got {len(pla)}"
    assert all("mercadolibre.com.ar" in c["url"] for c in pla)
    assert all(c["has_price"] for c in pla)
    assert len(carousel) >= 25, f"Carousel regression: {len(carousel)}"


def test_zapatillas_returns_pla_unit_products():
    """parse_serp on zapatillas fixture yields 30+ pla-units across diverse stores."""
    cands = parse_serp(_load("pla_unit_zapatillas.html"))
    pla = [c for c in cands if "pla_unit" in (c.get("flags") or [])]
    real_url = [c for c in pla if c.get("url") and "google.com" not in c["url"]]
    assert len(real_url) >= 30, f"Expected >=30 real-URL pla, got {len(real_url)}"
    # Stores diversos (sponsoreados de retail AR)
    stores = {c["store_hint"] for c in pla if c.get("store_hint")}
    assert len(stores) >= 3, f"Store diversity check: {stores}"


def test_robotech_no_duplicate_urls_within_pla_block():
    """Internal dedupe: si Google duplica una URL en panel + carousel paralelo,
    el pass de pla-unit dedupea internamente. Verifica que NO hay duplicate URLs
    entre los candidates con flag pla_unit."""
    cands = parse_serp(_load("pla_unit_robotech.html"))
    pla = [c for c in cands if "pla_unit" in (c.get("flags") or [])]
    urls = [c["url"] for c in pla]
    assert len(urls) == len(set(urls)), f"Duplicate URLs within pla block: {urls}"


def test_price_regex_no_dirty_prices():
    """No more $410.420,311001 in any candidate. All prices must end in ,DD or be integer."""
    import re
    cands = parse_serp(_load("pla_unit_robotech.html"))
    valid = re.compile(r"^\$\s?(?:\xa0)?[1-9]\d{0,2}(?:\.\d{3})*,\d{2}$|^(?:ARS|U\$S)\s\d+$")
    for c in cands:
        p = c.get("price_in_card")
        if p:
            assert valid.match(p), f"Dirty price: {p!r}"
```
</action>

<acceptance_criteria>
- `test -f tests/integration/test_serp_pla_unit_replay.py` exits 0
- `grep -n 'def test_robotech_returns_pla_unit_products' tests/integration/test_serp_pla_unit_replay.py` encuentra 1 línea
- `grep -n 'def test_zapatillas_returns_pla_unit_products' tests/integration/test_serp_pla_unit_replay.py` encuentra 1 línea
- `grep -n 'def test_robotech_no_duplicate_urls_within_pla_block' tests/integration/test_serp_pla_unit_replay.py` encuentra 1 línea
- `grep -n 'def test_price_regex_no_dirty_prices' tests/integration/test_serp_pla_unit_replay.py` encuentra 1 línea
- `.venv/bin/pytest tests/integration/test_serp_pla_unit_replay.py -v` exits 0 (los 4 tests verdes)
</acceptance_criteria>

</task>

<task id="2.6" wave="4" parallel_safe="no" autonomous="yes">
<title>Endpoint GET /admin/parity/{query} en main.py</title>

<read_first>
- src/artiscrapper/main.py (entender lifespan + auth + endpoint patterns existentes)
- src/artiscrapper/auth.py (verify_api_key dependency)
- src/artiscrapper/metrics.py (cómo se declara una métrica Prometheus)
- src/artiscrapper/search.py (parse_serp + heuristic_pre_classify para reusar)
- src/artiscrapper/browser.py (fetch_serp para obtener HTML)
</read_first>

<action>
Agregar en `src/artiscrapper/main.py`:

1. **Métrica Prometheus** (después de otras métricas existentes en `metrics.py` o en `main.py`):
```python
from prometheus_client import Gauge
parity_pla_units_in_html = Gauge(
    "artiscrapper_parity_pla_units_in_html",
    "Count of div.pla-unit found in the HTML for the audited query",
    ["query"],
)
parity_pla_units_extracted = Gauge(
    "artiscrapper_parity_pla_units_extracted",
    "Count of pla-unit candidates extracted by the parser for the audited query",
    ["query"],
)
```

2. **Endpoint** `GET /admin/parity/{query}` (después de los endpoints existentes, antes de cualquier wildcard):
```python
@app.get("/admin/parity/{query:path}", dependencies=[Depends(verify_api_key)])
async def admin_parity(query: str, request: Request) -> dict:
    """
    Parity audit endpoint — fetch Google SERP for {query}, parse with current parser,
    compare to what the HTML actually contains. Phase 0.2.1 minimal harness.

    For continuous nightly audits with 12-query dataset, see Phase 0.2.2.
    """
    from selectolax.parser import HTMLParser
    import re

    browser = request.app.state.browser
    rate_limiter = request.app.state.rate_limiter
    url = build_serp_url(query, meli=False)
    html, block_reason = await fetch_serp(browser, url, rate_limiter)
    if block_reason:
        return {"query": query, "blocked": block_reason}

    tree = HTMLParser(html)
    pla_nodes = tree.css("div.pla-unit")
    cands = parse_serp(html)
    pla_extracted = [c for c in cands if "pla_unit" in (c.get("flags") or [])]
    prices_in_html = set(re.findall(
        r"\$(?:&nbsp;|\xa0|\s)?[1-9]\d{0,2}(?:\.\d{3})*,\d{2}", html
    ))
    canon_urls = set()
    for u in re.findall(
        r"https?://[^\"'\s<>&\\]+(?:/(?:p/MLA|up/MLAU)\d+|/itm/\d+|/dp/[A-Z0-9]{10})", html
    ):
        canon_urls.add(u.split("?")[0])

    parity_pla_units_in_html.labels(query=query[:80]).set(len(pla_nodes))
    parity_pla_units_extracted.labels(query=query[:80]).set(len(pla_extracted))

    return {
        "query": query,
        "blocked": False,
        "html_metrics": {
            "size_bytes": len(html),
            "prices_unique": len(prices_in_html),
            "canonical_urls_in_html": len(canon_urls),
            "pla_units_in_html": len(pla_nodes),
        },
        "parser_metrics": {
            "candidates_total": len(cands),
            "candidates_with_price": sum(1 for c in cands if c.get("has_price")),
            "candidates_real_url": sum(
                1 for c in cands
                if c.get("url") and "google.com/search" not in (c.get("url") or "")
            ),
            "pla_unit_extracted": len(pla_extracted),
            "carousel_extracted": sum(
                1 for c in cands if "carousel" in (c.get("flags") or [])
            ),
        },
        "drift": (
            ["pla_unit_in_html_not_extracted"]
            if len(pla_nodes) > 0 and len(pla_extracted) == 0
            else []
        ),
    }
```

3. **Verificar que el import de `Depends`, `Request`, `fetch_serp`, `parse_serp`, `build_serp_url`, `verify_api_key` ya existen** o agregarlos al header del archivo.
</action>

<acceptance_criteria>
- `grep -n '@app.get(.*/admin/parity/' src/artiscrapper/main.py` encuentra 1 línea
- `grep -n 'async def admin_parity' src/artiscrapper/main.py` encuentra 1 línea
- `grep -n 'parity_pla_units_in_html' src/artiscrapper/main.py` encuentra al menos 2 matches (gauge + uso)
- `grep -n 'parity_pla_units_extracted' src/artiscrapper/main.py` encuentra al menos 2 matches
- `grep -n 'Depends(verify_api_key)' src/artiscrapper/main.py` encuentra al menos 2 matches (1 era pre-existente para /search, 1 nuevo)
- `.venv/bin/python -c "from artiscrapper.main import app; print('OK')"` exits 0
- Iniciar app (vía `compose up --force-recreate`) — el container hace healthcheck ok y nuevo endpoint responde 403 sin API-key, 200 con header válido (test manual diferido — D-18 smoke)
</acceptance_criteria>

</task>

</tasks>

<verification_criteria>
1. **2 fixtures HTML** creadas en `tests/fixtures/serp/`, ambas <800KB, ambas contienen el marker `div class="mnr-c c3mZkd j2G0Uc pla-unit"` el número correcto de veces (4 y 30+).
2. **9 tests nuevos** en `tests/test_parser.py` (4 pla-unit + 5 regex precio) — todos pasan en `pytest -v`.
3. **1 integration test file** `tests/integration/test_serp_pla_unit_replay.py` con 4 tests fixture-replay — todos pasan.
4. **Endpoint nuevo** `GET /admin/parity/{query}` registrado en `main.py`, autenticado con `X-API-Key`, retorna JSON con `html_metrics` + `parser_metrics` + `drift`.
5. **Métricas Prometheus** `artiscrapper_parity_pla_units_in_html` y `artiscrapper_parity_pla_units_extracted` registradas y visibles en `/metrics`.
6. **No regression**: `.venv/bin/pytest tests/ -q` sigue verde después de los cambios (incluye tests pre-existentes).
7. **Smoke runtime (D-18, NO autonomous)**: el operador corre `docker compose build && docker compose up --force-recreate -d`, luego ejercita el endpoint con `curl -H "X-API-Key: dev-key-1" http://localhost:8001/admin/parity/figura+coleccion+robotech` y confirma que `pla_unit_extracted >= 4` en la respuesta JSON.
</verification_criteria>
