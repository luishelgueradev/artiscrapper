---
phase: 03-robustness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/artiscrapper/config.py
  - src/artiscrapper/metrics.py
  - src/artiscrapper/logging_setup.py
  - src/artiscrapper/llm.py
  - src/artiscrapper/auth.py
  - src/artiscrapper/main.py
  - tests/test_auth.py
  - tests/test_sentry_init.py
  - tests/test_footguns.py
  - tests/integration/__init__.py
  - tests/integration/conftest.py
  - tests/integration/test_metrics_endpoint.py
  - tests/integration/test_rate_limit.py
autonomous: true
requirements:
  - OBS-07
requirements_addressed:
  - OBS-07
  - hardens-OBS-01..06
  - hardens-NF-01..04

must_haves:
  truths:
    - "GET /metrics returns 200 with text/plain Prometheus exposition containing ≥6 distinct artiscrapper_* metric families (OBS-07, D-13, ROADMAP-1)"
    - "Uncaught exception in /search produces a Sentry event with tags.correlation_id when SENTRY_DSN is set; nothing reaches Sentry when SENTRY_DSN is empty (D-14, D-15, D-16, D-17, ROADMAP-2)"
    - "POST /search without X-API-Key returns 401 WWW-Authenticate=ApiKey; with unknown key returns 401; with valid key passes through to existing pipeline (D-01, D-03)"
    - "POST /search with the SAME valid X-API-Key burst of 61 requests in <60s gets 429 with Retry-After header on the 61st request (D-02, D-04, ROADMAP-3)"
    - "lifespan emits three log lines (api_keys_loaded, sentry_init_skipped|sentry_init_done, rate_limit_init) declaring the loaded values so D-19 grep gate works"
    - "test_footguns.py invariants extended: sentry-sdk does NOT pull uvloop into uv.lock; /metrics is mounted via make_asgi_app() (no auth decorator) (D-10, D-06-related)"
    - "Phase 2 inline Metrics dataclass remains intact; D-11 bridge dual-writes to both dataclass and prometheus Counter (no rename, no removal)"
    - "Three Histograms wired via Histogram.time() context managers on the hot paths: artiscrapper_search_elapsed_seconds (in /search route), artiscrapper_llm_elapsed_seconds (around curate_candidates), artiscrapper_visit_elapsed_seconds{stage} with stage labels fetch|extract|classify in visit_candidates (D-12, ROADMAP-1)"
  artifacts:
    - path: "src/artiscrapper/auth.py"
      provides: "verify_api_key FastAPI dependency + get_api_key slowapi key_func"
      contains: "def verify_api_key(request: Request)"
      contains2: "def get_api_key(request: Request)"
    - path: "src/artiscrapper/metrics.py"
      provides: "prometheus_client Counter+Histogram bridges on top of legacy Metrics dataclass"
      contains: "llm_fallback_counter"
      contains2: "search_elapsed"
      contains3: "visit_elapsed"
      contains4: "inc_llm_fallback"
    - path: "src/artiscrapper/logging_setup.py"
      provides: "_init_sentry() at module-import + set_tag('correlation_id', cid) in add_correlation_id processor"
      contains: "_init_sentry"
      contains2: "set_tag"
    - path: "src/artiscrapper/main.py"
      provides: "Limiter wiring + /metrics mount + verify_api_key on /search + lifespan init log lines"
      contains: "make_asgi_app"
      contains2: "@limiter.limit"
      contains3: "Depends(verify_api_key)"
      contains4: "api_keys_loaded"
    - path: "src/artiscrapper/config.py"
      provides: "Phase 3 Settings fields"
      contains: "API_KEYS"
      contains2: "API_RATE_PER_MINUTE"
      contains3: "API_RATE_PER_DAY"
      contains4: "SENTRY_DSN"
    - path: "tests/test_auth.py"
      provides: "D-01/D-03 unit tests (missing/invalid/valid X-API-Key)"
      contains: "test_missing_key_returns_401"
    - path: "tests/test_sentry_init.py"
      provides: "D-14/D-15/D-16 unit tests + D-19 lifespan-log-matches-SDK-state integration test"
      contains: "test_no_init_when_dsn_empty"
      contains2: "test_sample_rates"
      contains3: "test_correlation_id_tag"
      contains4: "test_lifespan_log_matches_sdk_state"
    - path: "tests/integration/test_metrics_endpoint.py"
      provides: "OBS-07/D-13 ≥6 families assertion + D-10 no-auth assertion"
      contains: "test_metrics_returns_six_families"
      contains2: "test_metrics_endpoint_has_no_auth"
    - path: "tests/integration/test_rate_limit.py"
      provides: "D-02 stacked 60/min + 10000/day enforcement test"
      contains: "test_rate_limit_per_minute_returns_429"
  key_links:
    - from: "src/artiscrapper/main.py @app.post('/search')"
      to: "src/artiscrapper/auth.py::verify_api_key"
      via: "Depends(verify_api_key)"
      pattern: "Depends\\(verify_api_key\\)"
    - from: "src/artiscrapper/main.py limiter"
      to: "src/artiscrapper/auth.py::get_api_key"
      via: "Limiter(key_func=get_api_key)"
      pattern: "key_func=get_api_key"
    - from: "src/artiscrapper/main.py"
      to: "prometheus_client.make_asgi_app()"
      via: "app.mount('/metrics', make_asgi_app())"
      pattern: "app\\.mount\\(.{1,8}/metrics.{1,8}, *make_asgi_app"
    - from: "src/artiscrapper/llm.py increment sites"
      to: "src/artiscrapper/metrics.py::inc_llm_fallback"
      via: "wrapper bridges dataclass + prometheus Counter"
      pattern: "inc_llm_fallback\\("
    - from: "src/artiscrapper/logging_setup.py::add_correlation_id"
      to: "sentry_sdk.get_current_scope().set_tag"
      via: "tag injection inside structlog processor"
      pattern: "set_tag\\(.{1,30}correlation_id"
---

## Coordination Notes

This plan touches `src/artiscrapper/main.py`, which is ALSO modified by Plan 03-02. The two plans are
serialized: 03-01 ships in Wave 1 and 03-02 ships in Wave 2 (Plan 03-02 carries `depends_on: ["01"]`).
The merge contract below documents what 03-01 leaves in main.py so 03-02's Task 3 has unambiguous
insertion anchors.

**Plan 03-01 (this plan, lands first) leaves main.py in this shape:**
- Phase 3 imports added at top: `from fastapi import Depends`, `from slowapi import Limiter,
  _rate_limit_exceeded_handler`, `from slowapi.errors import RateLimitExceeded`,
  `from prometheus_client import make_asgi_app`, `from .auth import verify_api_key, get_api_key,
  _parse_api_keys`, `from .metrics import search_elapsed, llm_elapsed, visit_elapsed, inc_block_detected`.
- Lifespan emits THREE log lines BEFORE `boot_done` in this order: `api_keys_loaded`,
  `sentry_init_done|sentry_init_skipped`, `rate_limit_init`. The 4th line `challenge_backoff_init`
  is OWNED by Plan 03-02 Task 3 and is INSERTED BETWEEN `rate_limit_init` and `boot_done` when 03-02 lands.
- `app = FastAPI(...)` construction is followed by slowapi wiring (`limiter = Limiter(...)`,
  `app.state.limiter = limiter`, `app.add_exception_handler(RateLimitExceeded, ...)`,
  `app.mount("/metrics", make_asgi_app())`) BEFORE the existing `app.add_middleware(CorrelationIdMiddleware)` line.
- `/search` route signature carries `_api_key: str = Depends(verify_api_key)` as the last parameter;
  decorators stack as `@app.post("/search", ...)` then `@limiter.limit("60/minute")` then `@limiter.limit("10000/day")`.
- `/search` body opens with `with search_elapsed.time():` as the first body statement; the existing
  10-step pipeline is indented one level deeper inside that block.
- The block-detection branch (~line 338) calls `inc_block_detected(block_reason)` instead of the
  direct dataclass increment.

**Plan 03-02 (lands second) inserts into main.py at the following anchored sites — documented here
for forward-traceability, but the actual edits live in 03-02 Task 3:**
- **Lifespan log line:** `log.info("challenge_backoff_init", ...)` BETWEEN the `rate_limit_init` log
  line and the `boot_done` line.
- **/search gate (step [1.5]):** `await check_gate(...)` AFTER the cache-hit early-return AND INSIDE
  the `with search_elapsed.time():` block opened by 03-01 (so 503 responses STILL record latency on
  the search_elapsed Histogram) AND AFTER the `verify_api_key` Depends has resolved.
- **/search block-detected branch:** `await record_block(...)` at the existing `_detect_block()`
  callsite (~line 338), BEFORE the existing 503 return.
- **/search success path:** `await record_success(...)` AFTER both `_detect_block` calls return None
  but BEFORE the parser cascade step.

This plan (03-01) MUST NOT add the `challenge_backoff_init` log line, the `check_gate` call, or any
challenge-backoff imports — those are 03-02 Task 3 scope. Keeping ownership clean prevents
double-edits and merge ambiguity.

<objective>
Wire three production-grade observability + access-control libraries on top of the Phase 2 MVP: `prometheus-client` (D-10..D-13), `sentry-sdk` (D-14..D-17), and `slowapi` with X-API-Key auth (D-01..D-04). All three integrate at distinct injection points in `main.py` lifespan + middleware + `/search` route, so they ship together in a single plan to keep that file's edit blast-radius coherent.

Purpose: Close ROADMAP §Phase 3 success criteria 1, 2, 3. Address requirement OBS-07. Harden OBS-01..06 (Sentry adds crash visibility on top of structlog), NF-01..04 (consumer rate-limit protects the 1/min Google rate-limiter from runaway clients).

Output: Three new dependencies in `pyproject.toml`, four new Settings fields, one new `auth.py` module, extensions to `metrics.py`/`logging_setup.py`/`llm.py`/`main.py`, four new test files, two test_footguns invariants, integration test scaffolding (`tests/integration/__init__.py` + `conftest.py`). 14 total Phase-2-pattern-anchored file touches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-robustness/03-CONTEXT.md
@.planning/phases/03-robustness/03-RESEARCH.md
@.planning/phases/03-robustness/03-PATTERNS.md
@.planning/phases/03-robustness/03-VALIDATION.md
@.planning/phases/02-mvp/02-VERIFICATION.md
@src/artiscrapper/main.py
@src/artiscrapper/metrics.py
@src/artiscrapper/llm.py
@src/artiscrapper/logging_setup.py
@src/artiscrapper/config.py
@src/artiscrapper/rate_limit.py
@tests/conftest.py
@tests/test_health.py
@tests/test_footguns.py
@tests/test_llm.py

<interfaces>
<!-- Extracted from current codebase to remove scavenger-hunt cost for the executor. -->

From `src/artiscrapper/metrics.py` (Phase 2 — D-11 must BRIDGE, not REPLACE):
- Public symbol: `metrics: Metrics` (process-singleton dataclass)
- `Metrics.llm_fallback_total: dict[str, int]` — incremented in llm.py at 6 sites
- `Metrics.visit_failed_total: dict[str, int]` — incremented in visit.py
- `Metrics.block_detected_total: dict[str, int]` — incremented in main.py:338
- `Metrics.browser_recycles_total: int` — counter

From `src/artiscrapper/llm.py` increment sites that MUST switch to `inc_llm_fallback(reason)`:
- llm.py:196 `metrics.llm_fallback_total["timeout"] += 1`
- llm.py:214 `metrics.llm_fallback_total["overload"] += 1`
- llm.py:232 `metrics.llm_fallback_total[f"cold_load_{code}"] += 1`
- llm.py:234 `metrics.llm_fallback_total[f"http_{code}"] += 1`
- llm.py:237 `metrics.llm_fallback_total["malformed"] += 1`
- llm.py:240 `metrics.llm_fallback_total["conn_error"] += 1`

From `src/artiscrapper/main.py` (Phase 2 lifespan + /search):
- `lifespan(app)` async context manager — Phase 3 adds 3 `log.info()` lines AFTER the existing `boot_done` line is built up, BEFORE `yield`
- `app.add_middleware(CorrelationIdMiddleware)` at line ~183 — Phase 3 limiter + exception handler are registered BEFORE this line
- `@app.post("/search", response_model=SearchResponse) async def search(request: Request, body: SearchRequest)` — Phase 3 stacks `@limiter.limit("60/minute")` + `@limiter.limit("10000/day")` decorators + adds `_api_key: str = Depends(verify_api_key)` parameter

From `src/artiscrapper/logging_setup.py`:
- `add_correlation_id(_logger, _method, event_dict)` — Phase 2 processor at lines 16-35; Phase 3 EXTENDS by adding sentry tag injection inside the `if cid:` branch
- `configure_logging(json_logs, level)` — Phase 2 init function called from lifespan
- `SAFE_CANDIDATE_FIELDS` allow-list (lines 65-67) — OBS-05 anchor; Phase 3 does NOT add api-key/dsn fields here

From `src/artiscrapper/config.py`:
- `class Settings(BaseSettings)` with `model_config = {"env_file": ".env", "env_file_encoding": "utf-8", "extra": "ignore"}`
- Existing fields: `VERSION`, `CACHE_DB_PATH`, `HEADLESS`, `GOOGLE_MIN_INTERVAL_S`, `BROWSER_RECYCLE_AFTER`, `LLM_ROUTER_URL`, `LLM_ROUTER_BEARER_TOKEN`, `LLM_CONCURRENCY`, `LOG_JSON`, `LOG_LEVEL`
- Module-level singleton: `settings = Settings()` at bottom

From `src/artiscrapper/rate_limit.py` (analog for auth.py module-level cache pattern):
- `class GoogleRateLimiter` with `__init__(self, min_interval_s: float = 60.0)` storing in-process state
- This is the existing per-instance throttle; Phase 3 leaves it UNCHANGED and layers slowapi on top

From `tests/test_health.py:23-63` (TestClient fixture pattern):
- Sets `os.environ.setdefault("LLM_ROUTER_BEARER_TOKEN", "test-token-health")` BEFORE `from src.artiscrapper.main import app`
- Sets `os.environ["CACHE_DB_PATH"]` to a tempfile path
- Monkeypatches `src.artiscrapper.main.launch_async` to return a mock browser
- Yields `TestClient(app) as client`

From `tests/test_footguns.py:13-49` (subprocess + grep invariant pattern):
- `subprocess.run(["grep", "-rE", "<pattern>", "<path>"], capture_output=True, text=True)`
- Assertion: `result.returncode != 0` means pattern NOT found (grep convention)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0): pyproject.toml deps + integration scaffolding + Settings fields + test stubs</name>
  <files>pyproject.toml, src/artiscrapper/config.py, tests/integration/__init__.py, tests/integration/conftest.py, tests/test_auth.py, tests/test_sentry_init.py, tests/integration/test_metrics_endpoint.py, tests/integration/test_rate_limit.py</files>
  <read_first>
    - pyproject.toml (current [project].dependencies, [dependency-groups].dev, [tool.pytest.ini_options])
    - src/artiscrapper/config.py (current Settings class shape, env_file config)
    - tests/conftest.py (existing mock_app_state and tmp_db fixtures — lines 14-45)
    - tests/test_health.py (TestClient + tempfile-based CACHE_DB_PATH + monkeypatch.launch_async pattern, lines 23-63)
    - tests/test_footguns.py (subprocess-grep invariant style, lines 13-49)
    - .planning/phases/03-robustness/03-PATTERNS.md §"src/artiscrapper/config.py" and §"tests/integration/conftest.py" (verbatim Phase 2 analog cites)
    - .planning/phases/03-robustness/03-VALIDATION.md §"Wave 0 Requirements"
  </read_first>
  <behavior>
    - Failing test 1 (tests/test_auth.py::test_missing_key_returns_401): POST /search without X-API-Key → 401 with header WWW-Authenticate=ApiKey
    - Failing test 2 (tests/test_auth.py::test_xapikey_header_required): POST /search with unknown key → 401
    - Failing test 3 (tests/test_auth.py::test_valid_key_passes_auth): POST /search with valid key from API_KEYS env → reaches mocked /search body (not 401, not 429)
    - Failing test 4 (tests/test_sentry_init.py::test_no_init_when_dsn_empty): SENTRY_DSN="" → sentry_sdk.get_client().is_active() is False
    - Failing test 5 (tests/test_sentry_init.py::test_sample_rates): SENTRY_DSN set → sentry_sdk.init called with traces_sample_rate=0.1, profiles_sample_rate=0.0, send_default_pii=False
    - Failing test 6 (tests/test_sentry_init.py::test_correlation_id_tag): after add_correlation_id processor runs with a cid, sentry_sdk.get_current_scope().get('tags') contains 'correlation_id'
    - Failing test 7 (tests/test_sentry_init.py::test_lifespan_log_matches_sdk_state): TestClient boot with SENTRY_DSN set → captured structlog lifespan log contains `sentry_init_done` (NOT `sentry_init_skipped`) AND `sentry_sdk.get_client().is_active()` returns True; with SENTRY_DSN empty → captured log contains `sentry_init_skipped` AND `is_active()` returns False (WRN-04: log + SDK state cannot diverge)
    - Failing test 8 (tests/integration/test_metrics_endpoint.py::test_metrics_returns_six_families): GET /metrics → 200, content-type starts with "text/plain", body has ≥6 distinct lines matching `^artiscrapper_\w+`
    - Failing test 9 (tests/integration/test_metrics_endpoint.py::test_metrics_endpoint_has_no_auth): GET /metrics WITHOUT X-API-Key → 200 (not 401)
    - Failing test 10 (tests/integration/test_rate_limit.py::test_rate_limit_per_minute_returns_429): 61 sequential POST /search with same X-API-Key within <60s → request #61 returns 429 with Retry-After header
    - All tests use TestClient + monkeypatched launch_async per S5 pattern; env vars (LLM_ROUTER_BEARER_TOKEN, CACHE_DB_PATH, API_KEYS="test-key-1,test-key-2") set before app import
  </behavior>
  <action>
    Step 1 — Update `pyproject.toml` (per D-11 / D-14 / D-02 / D-04 — Wave 0 deps must land before production wiring):
    - Add to `[project].dependencies`: `prometheus-client==0.25.0`, `sentry-sdk==2.61.1`, `slowapi==0.1.9` (versions from 03-RESEARCH.md Standard Stack — all verified on PyPI 2026-06-03, slopcheck [OK] all three).
    - Extend `[tool.pytest.ini_options].markers` array to include `"integration: fixture-replay integration tests (run by default; selectable via -m integration)"` alongside the existing `"e2e: ..."` marker.
    - Do NOT modify `asyncio_mode`, `testpaths`, or `pythonpath`.

    Step 2 — Extend `src/artiscrapper/config.py` Settings class with four new fields per D-01/D-02/D-14 (place under a `# ── Phase 3 — Robustness ──` comment, immediately above `model_config`):
    - `API_KEYS: str = ""` — comma-separated keys (per D-01). Default empty so dev container can boot without setting it; auth.py logs a warning at module load if empty.
    - `API_RATE_PER_MINUTE: int = 60` — per D-02 (informational only; slowapi decorator uses string literal "60/minute" so the constant exists for logging/D-19 gate verification, NOT for runtime quota enforcement).
    - `API_RATE_PER_DAY: int = 10000` — per D-02 (same caveat as above).
    - `SENTRY_DSN: str = ""` — per D-14 (empty → init skipped).
    - Do NOT add `CHALLENGE_BACKOFF_*` fields — those are owned by Plan 03-02 and per D-06 are ROADMAP-locked hardcoded constants (NOT thread through settings — Pitfall 6 in 03-RESEARCH.md).

    Step 3 — Run `uv sync --locked` so `uv.lock` picks up the three new deps. Do NOT run `docker compose up -d --build` (per Phase 2 memory `feedback_compose_build_recreate`; rebuild happens at end of Task 5 with explicit `compose build` + `compose up --force-recreate`).

    Step 4 — Create integration test scaffolding:
    - `tests/integration/__init__.py` — empty file (pytest needs the package marker).
    - `tests/integration/conftest.py` — mirror PATTERNS.md §"tests/integration/conftest.py" analog. Implement: `load_labelled_jsonl` fixture (reads `tests/fixtures/llm/labelled.jsonl`, returns list of dicts), `reset_challenge_backoff_state` autouse fixture (sets `src.artiscrapper.challenge_backoff._STATE = None` before/after each test — guards against Plan 03-02's module-singleton bleeding across tests when both plans run in the same pytest invocation; wrap import in try/except ImportError so this conftest works while 03-02 hasn't shipped challenge_backoff.py yet). Re-export nothing else — parent `tests/conftest.py` auto-discovered fixtures (`tmp_db`, `mock_app_state`) flow through.

    Step 5 — Write failing test files (RED stage of TDD):
    - `tests/test_auth.py` — copy the env-var-then-app-import + TestClient + `_make_mock_browser` + `_fake_launch_async` skeleton verbatim from `tests/test_health.py:23-63`. Add `os.environ["API_KEYS"] = "test-key-1,test-key-2"` BEFORE `from src.artiscrapper.main import app`. Implement `test_missing_key_returns_401`, `test_xapikey_header_required` (unknown key), `test_valid_key_passes_auth` (valid key reaches mocked Google fetch which monkeypatched to return synthetic 0-result SERP — test asserts response status != 401 and != 429; do NOT assert 200 because the mocked pipeline may return 503 from challenge_backoff which is fine for auth purposes). Each assertion includes the WWW-Authenticate=ApiKey header check on 401.
    - `tests/test_sentry_init.py` — use `monkeypatch.setenv` + `importlib.reload(logging_setup)` per PATTERNS.md §"tests/test_sentry_init.py" verbatim. FOUR tests: `test_no_init_when_dsn_empty`, `test_sample_rates`, `test_correlation_id_tag`, `test_lifespan_log_matches_sdk_state`. For `test_correlation_id_tag`, set DSN, reload logging_setup, set `correlation_id.set("test-cid-xyz")`, call `add_correlation_id(None, None, {})`, then assert `sentry_sdk.get_current_scope()._tags["correlation_id"] == "test-cid-xyz"` (scope tag access via `_tags` dict — the public `set_tag` writes here; reading verifies the writer was called). For `test_lifespan_log_matches_sdk_state` (WRN-04 D-19 gate), capture structlog output using `structlog.testing.capture_logs()` while booting the TestClient under both `SENTRY_DSN=""` and `SENTRY_DSN="https://test@sentry.invalid/1"` configurations; for each configuration, locate the lifespan event with key `event` matching `sentry_init_done` or `sentry_init_skipped`; assert that the log event name matches the actual SDK state — `sentry_init_done` ↔ `sentry_sdk.get_client().is_active() is True`, `sentry_init_skipped` ↔ `is_active() is False`. This catches the divergence class where the log line is emitted before `_init_sentry()` actually ran (or vice versa).
    - `tests/integration/test_metrics_endpoint.py` — TestClient fixture inherited from parent conftest pattern. `test_metrics_returns_six_families` asserts `re.findall(r"^(artiscrapper_\w+)", body, re.MULTILINE)` returns a set of size ≥6. `test_metrics_endpoint_has_no_auth` sends GET /metrics with NO X-API-Key header → asserts 200 (D-10).
    - `tests/integration/test_rate_limit.py` — TestClient fixture, valid X-API-Key from `API_KEYS="test-key-1"`. Loop 60 POSTs to /search (mocked pipeline can return any status that is NOT 401/429), then assert request #61 returns 429 AND `response.headers["Retry-After"]` is a digit-string. D-04 reminder: in-memory storage means test isolation requires NOT sharing the limiter across tests — use a fresh TestClient per test (don't reuse the module-scope `test_client` fixture for this).

    Step 6 — All 10 tests MUST fail at this point (RED). Verify by running `pytest tests/test_auth.py tests/test_sentry_init.py tests/integration/test_metrics_endpoint.py tests/integration/test_rate_limit.py -x -v 2>&1 | tail -40`. Expected: ImportError / ModuleNotFoundError on `src.artiscrapper.auth`, or AssertionError on auth/sentry checks (because auth.py doesn't exist yet and Sentry init isn't wired). DO NOT proceed to Task 2 until all 10 tests are confirmed RED.
  </action>
  <verify>
    <automated>
      cd /home/luis/proyectos/artiscrapper &&
      uv sync --locked &&
      grep -E '(prometheus-client|sentry-sdk|slowapi)' pyproject.toml | grep -v '^#' | wc -l | grep -q '^[3-9]\|[1-9][0-9]' &&
      grep -E 'API_KEYS|API_RATE_PER_MINUTE|API_RATE_PER_DAY|SENTRY_DSN' src/artiscrapper/config.py | grep -v '^#' | wc -l | grep -q '^[4-9]\|[1-9][0-9]' &&
      test -f tests/integration/__init__.py &&
      test -f tests/integration/conftest.py &&
      test -f tests/test_auth.py &&
      test -f tests/test_sentry_init.py &&
      grep -q "test_lifespan_log_matches_sdk_state" tests/test_sentry_init.py &&
      test -f tests/integration/test_metrics_endpoint.py &&
      test -f tests/integration/test_rate_limit.py &&
      ! uv run pytest tests/test_auth.py tests/test_sentry_init.py tests/integration/test_metrics_endpoint.py tests/integration/test_rate_limit.py -x -q 2>&1 | tail -5 | grep -q '^10 passed'
    </automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` contains all three Phase 3 deps with exact pinned versions: `prometheus-client==0.25.0`, `sentry-sdk==2.61.1`, `slowapi==0.1.9`. Verify with: `grep -cE '(prometheus-client==0.25.0|sentry-sdk==2.61.1|slowapi==0.1.9)' pyproject.toml` returns `3`.
    - `pyproject.toml` `[tool.pytest.ini_options].markers` array contains BOTH `"e2e: ..."` AND `"integration: ..."` entries.
    - `uv.lock` exists, was regenerated by `uv sync --locked`, and contains entries for `prometheus-client`, `sentry-sdk`, `slowapi`. Verify with: `grep -cE 'name = "(prometheus-client|sentry-sdk|slowapi)"' uv.lock` returns `3`.
    - `src/artiscrapper/config.py` defines all four new Settings fields (`API_KEYS: str = ""`, `API_RATE_PER_MINUTE: int = 60`, `API_RATE_PER_DAY: int = 10000`, `SENTRY_DSN: str = ""`). Verify with: `grep -cE '^\s+(API_KEYS|API_RATE_PER_MINUTE|API_RATE_PER_DAY|SENTRY_DSN):' src/artiscrapper/config.py` returns `4`.
    - `tests/integration/__init__.py` exists (empty file is fine).
    - `tests/integration/conftest.py` defines `load_labelled_jsonl` fixture and `reset_challenge_backoff_state` autouse fixture (the latter wrapped in try/except ImportError so it tolerates challenge_backoff.py not existing yet).
    - `tests/test_sentry_init.py` declares all four tests including `test_lifespan_log_matches_sdk_state`. Verify: `grep -cE '^def test_(no_init_when_dsn_empty|sample_rates|correlation_id_tag|lifespan_log_matches_sdk_state)' tests/test_sentry_init.py` returns `4`.
    - All 10 RED tests fail when run: `uv run pytest tests/test_auth.py tests/test_sentry_init.py tests/integration/test_metrics_endpoint.py tests/integration/test_rate_limit.py -x -q` exits non-zero with at least 1 failure or error.
    - No production source file in `src/artiscrapper/` was edited in this task (Task 1 is pure scaffolding).
  </acceptance_criteria>
  <done>
    Wave 0 deps + Settings fields + 10 RED tests landed; `pytest tests/test_auth.py tests/test_sentry_init.py tests/integration/test_metrics_endpoint.py tests/integration/test_rate_limit.py` fails as expected; `uv.lock` includes the three new packages.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Wave 1): auth.py + metrics.py prometheus bridge + logging_setup.py Sentry init (pure modules, no FastAPI wiring yet)</name>
  <files>src/artiscrapper/auth.py, src/artiscrapper/metrics.py, src/artiscrapper/logging_setup.py, src/artiscrapper/llm.py, tests/test_footguns.py</files>
  <read_first>
    - src/artiscrapper/metrics.py (current 27-line Phase 2 dataclass — D-11 bridge target)
    - src/artiscrapper/llm.py lines 190-245 (the 6 increment sites that switch to inc_llm_fallback wrapper)
    - src/artiscrapper/logging_setup.py (current add_correlation_id processor lines 16-35; _init_sentry placement at module bottom)
    - src/artiscrapper/rate_limit.py (GoogleRateLimiter — analog for auth.py's module-level cached parsing pattern)
    - src/artiscrapper/config.py (settings.API_KEYS / settings.SENTRY_DSN reachable after Task 1)
    - tests/test_footguns.py (existing subprocess+grep invariant style — Task 2 extends with 2 new invariants)
    - .planning/phases/03-robustness/03-PATTERNS.md §"src/artiscrapper/metrics.py" (verbatim Counter/Histogram declarations, label pre-seeding, bridge wrapper)
    - .planning/phases/03-robustness/03-PATTERNS.md §"src/artiscrapper/logging_setup.py" (sentry set_tag extension inside add_correlation_id, _init_sentry placement)
    - .planning/phases/03-robustness/03-PATTERNS.md §"src/artiscrapper/auth.py" (verify_api_key + get_api_key code shape)
    - .planning/phases/03-robustness/03-RESEARCH.md §A2 (default collector unregistration), §A5 (Histogram.time() context-manager rule for async — never decorator), §A6 (tldextract host normalization for visit_failed cardinality)
  </read_first>
  <behavior>
    - tests/test_auth.py 3 tests turn GREEN once auth.py exists and main.py wires it (main.py wiring lands in Task 3 — Task 2 only ships the pure module).
    - tests/test_sentry_init.py 4 tests turn GREEN (logging_setup.py module-import init is testable in isolation; uses importlib.reload, no main.py dependency for tests 1-3. test_lifespan_log_matches_sdk_state requires main.py lifespan log lines from Task 3 — mark this one with `pytest.mark.xfail(strict=False, reason="lifespan log lines wired in Task 3")` here, then Task 3 removes the marker).
    - tests/test_footguns.py new tests `test_sentry_does_not_pull_uvloop` and `test_metrics_endpoint_unprotected_by_design` turn GREEN (the first is a uv.lock grep; the second checks main.py contains "make_asgi_app()" which will be present after Task 3 — so this test should be added but skipped/xfail until Task 3, OR added in Task 3 instead. Recommendation: add the grep assertion now but write the test to ALLOW main.py not yet containing "make_asgi_app()" via an `xfail(strict=False, reason="wired in Task 3")` marker, then remove the marker as part of Task 3).
    - The legacy `Metrics` dataclass continues to increment (Phase 2 tests in test_llm.py that read it must still pass).
  </behavior>
  <action>
    Step 1 — Create `src/artiscrapper/auth.py` per D-01 / D-03 / OBS-05 alignment. Mirror PATTERNS.md §"src/artiscrapper/auth.py" verbatim (do NOT include code blocks here per instruction — see PATTERNS.md analog). Required symbols and behavior:
    - `_parse_api_keys() -> set[str]` — module-level helper that splits `settings.API_KEYS` on commas, strips whitespace, drops empty entries (per D-01).
    - `API_KEYS = _parse_api_keys()` — module-load cache (D-01: rotation requires container restart, so no per-request re-parsing).
    - `verify_api_key(request: Request) -> str` — FastAPI dependency; reads `request.headers.get("X-API-Key")`; raises `HTTPException(status_code=401, detail="Missing X-API-Key header", headers={"WWW-Authenticate": "ApiKey"})` for missing key, raises 401 with `detail="Invalid API key"` for unknown key, returns the key string on success. OBS-05: never log the key value.
    - `get_api_key(request: Request) -> str` — slowapi `key_func`; returns `request.headers.get("X-API-Key", "anonymous")`. MUST NOT raise (per Pitfall 3 in 03-RESEARCH.md — slowapi treats key_func exceptions as 500).
    - At module bottom, emit a structlog warning if `API_KEYS` set is empty (so the lifespan log line `api_keys_loaded` with count=0 still appears, but auth.py also flags the dev-mode misconfiguration).

    Step 2 — Extend `src/artiscrapper/metrics.py` per D-11 / D-12 / D-13. Mirror PATTERNS.md §"src/artiscrapper/metrics.py" verbatim. Required additions (KEEP the existing dataclass + `metrics` singleton intact — D-11 says BRIDGE, NOT REPLACE):
    - Imports: `from prometheus_client import Counter, Histogram, REGISTRY, GC_COLLECTOR, PLATFORM_COLLECTOR, PROCESS_COLLECTOR` and `import tldextract` (already a Phase 2 dep per pyproject.toml).
    - Module-top default-collector unregistration loop (per 03-RESEARCH.md §A2) — wrap each `REGISTRY.unregister(...)` in `try/except KeyError: pass` to tolerate test re-import.
    - Three Counter declarations with exact names per D-11: `llm_fallback_counter = Counter("artiscrapper_llm_fallback_total", "LLM curator fallback verdicts emitted, by reason", ["reason"])`; `visit_failed_counter = Counter("artiscrapper_visit_failed_total", "Visit-pass failures, by host", ["host"])`; `block_detected_counter = Counter("artiscrapper_block_detected_total", "Google block-detection events, by marker reason", ["reason"])`. These three families are canonical from Phase 2 — DO NOT rename, DO NOT add label dimensions beyond the ones listed.
    - Three Histogram declarations with the bucket constants from 03-RESEARCH.md §A4: `search_elapsed` (no labels, SEARCH_BUCKETS), `llm_elapsed` (no labels, LLM_BUCKETS), `visit_elapsed` (with `["stage"]` label, VISIT_BUCKETS).
    - Three wrapper helpers that dual-write to dataclass + prometheus: `inc_llm_fallback(reason: str)`, `inc_visit_failed(url_or_host: str)` (the latter calls `_host_for_metric(url_or_host)` to truncate to TLD+1 via tldextract per 03-RESEARCH.md §A6), `inc_block_detected(reason: str)`. Each wrapper increments BOTH the legacy dataclass counter AND the prometheus Counter (D-11).
    - Pre-seed known label values for bounded enums (per 03-RESEARCH.md §A6 / Pitfall 10): at module load, call `llm_fallback_counter.labels(reason=r)` for r in `("timeout", "overload", "malformed", "conn_error", "cold_load_502", "cold_load_504", "http_400", "http_500")`; same shape for `block_detected_counter.labels(reason=r)` for r in `("consent_interstitial", "sorry", "recaptcha", "unknown")`. Do NOT seed `visit_failed_counter` host labels (unbounded — depends on actual hosts encountered).

    Step 3 — Refactor 6 call sites in `src/artiscrapper/llm.py` (lines 196, 214, 232, 234, 237, 240 per PATTERNS.md File Classification table). Replace each `metrics.llm_fallback_total["<reason>"] += 1` with `inc_llm_fallback("<reason>")`, importing `inc_llm_fallback` from `.metrics`. Do NOT remove the `metrics` import — other paths still reference the dataclass. Verify via `grep -c "metrics\.llm_fallback_total\[" src/artiscrapper/llm.py` — must return `0` after refactor. The dataclass increment STILL happens inside `inc_llm_fallback` — so behavior is preserved; only the call shape changes.

    Step 4 — Extend `src/artiscrapper/logging_setup.py` per D-14 / D-15 / D-16 / D-17. Two surgical changes:
    (a) Inside `add_correlation_id` (lines 16-35), after the `if cid := correlation_id.get():` block sets `event_dict["correlation_id"] = cid`, add a try/except that calls `sentry_sdk.get_current_scope().set_tag("correlation_id", cid)`. The set_tag is a no-op when DSN was empty (sentry init was skipped), so it's safe to always invoke. Wrap in `try/except Exception: pass` so observability code can never break a request (per PATTERNS.md §S3 + 03-RESEARCH.md §B4).
    (b) Append `_init_sentry()` function and its module-import-time call at the BOTTOM of the module (after `log_candidate_safe`). Behavior per 03-RESEARCH.md §B2: read `settings.SENTRY_DSN`; if empty, `return` (D-14); else call `sentry_sdk.init(dsn=dsn, traces_sample_rate=0.1, profiles_sample_rate=0.0, send_default_pii=False)` (D-15 sample rates; D-16/D-17 alignment via the set_tag in (a) and the auto-detected FastAPI integration). Call `_init_sentry()` at module level so `from .logging_setup import configure_logging` triggers Sentry init before lifespan (D-17).

    Step 5 — Extend `tests/test_footguns.py` with two new invariants per PATTERNS.md §S8:
    - `test_sentry_does_not_pull_uvloop` — subprocess grep on `uv.lock` for `uvloop`; assert returncode != 0 (per 03-RESEARCH.md §G3). Phase 2 already has `test_no_uvloop_installed` and `test_uvloop_absent_from_lock` — this new test functions as a Phase 3 re-assertion tagged with the intent (preserves test traceability when uv.lock changes due to sentry-sdk's transitive deps).
    - `test_metrics_endpoint_unprotected_by_design` — subprocess grep on `src/artiscrapper/main.py` for `/metrics`; assert `make_asgi_app()` appears in stdout (D-10: must be mounted as ASGI sub-app, NOT a FastAPI route — otherwise CorrelationIdMiddleware + slowapi will wrap it). Mark this test `pytest.mark.xfail(strict=False, reason="main.py wiring lands in Task 3")` for now — Task 3 removes the xfail marker once main.py contains the mount line.

    Step 6 — Run the test files from Task 1 in GREEN check mode: `uv run pytest tests/test_auth.py tests/test_sentry_init.py tests/test_footguns.py -x -v 2>&1 | tail -30`. Expected: tests/test_auth.py 3 tests STILL FAIL (main.py wiring is Task 3); tests/test_sentry_init.py tests 1-3 now PASS; `test_lifespan_log_matches_sdk_state` is XFAIL until Task 3 removes the marker; tests/test_footguns.py new tests pass (the metrics one is xfail). Acceptable failure mode at this checkpoint: auth tests + integration tests still red.
  </action>
  <verify>
    <automated>
      cd /home/luis/proyectos/artiscrapper &&
      test -f src/artiscrapper/auth.py &&
      grep -q "def verify_api_key" src/artiscrapper/auth.py &&
      grep -q "def get_api_key" src/artiscrapper/auth.py &&
      grep -q "WWW-Authenticate" src/artiscrapper/auth.py &&
      grep -q "llm_fallback_counter" src/artiscrapper/metrics.py &&
      grep -q "search_elapsed" src/artiscrapper/metrics.py &&
      grep -q "visit_elapsed" src/artiscrapper/metrics.py &&
      grep -q "inc_llm_fallback" src/artiscrapper/metrics.py &&
      grep -q "REGISTRY.unregister" src/artiscrapper/metrics.py &&
      grep -q "_init_sentry" src/artiscrapper/logging_setup.py &&
      grep -q "set_tag" src/artiscrapper/logging_setup.py &&
      test "$(grep -c 'metrics\.llm_fallback_total\[' src/artiscrapper/llm.py)" -eq 0 &&
      grep -q "test_sentry_does_not_pull_uvloop" tests/test_footguns.py &&
      uv run pytest tests/test_sentry_init.py tests/test_footguns.py -x -q 2>&1 | tail -3 | grep -qE '(passed|xfailed)'
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/artiscrapper/auth.py` exists and exports `verify_api_key`, `get_api_key`, `_parse_api_keys`, `API_KEYS`. Verify: `grep -cE '^(def verify_api_key|def get_api_key|def _parse_api_keys|API_KEYS = )' src/artiscrapper/auth.py` returns `4`.
    - auth.py never references `settings.LLM_ROUTER_BEARER_TOKEN`, `settings.SENTRY_DSN`, or any other secret (OBS-05). Verify: `grep -cE '(LLM_ROUTER_BEARER_TOKEN|SENTRY_DSN)' src/artiscrapper/auth.py` returns `0`.
    - auth.py 401 response includes header `WWW-Authenticate: ApiKey`. Verify: `grep -c 'ApiKey' src/artiscrapper/auth.py` returns at least `1`.
    - `src/artiscrapper/metrics.py` declares all three canonical Counter families with exact names from D-11. Verify: `grep -cE '"artiscrapper_(llm_fallback_total|visit_failed_total|block_detected_total)"' src/artiscrapper/metrics.py` returns `3`.
    - metrics.py declares all three Histogram families per D-12. Verify: `grep -cE '"artiscrapper_(search|llm|visit)_elapsed_seconds"' src/artiscrapper/metrics.py` returns `3`.
    - metrics.py unregisters all three default collectors. Verify: `grep -c 'REGISTRY.unregister' src/artiscrapper/metrics.py` returns `3` (or `1` if loop form).
    - llm.py no longer directly increments `metrics.llm_fallback_total[...]`. Verify: `grep -v '^#' src/artiscrapper/llm.py | grep -c 'metrics\.llm_fallback_total\['` returns `0`. All increments now go through `inc_llm_fallback(...)`. Verify: `grep -c 'inc_llm_fallback(' src/artiscrapper/llm.py` returns ≥6.
    - logging_setup.py adds `_init_sentry` at module bottom and calls it at import time. Verify: `grep -c '_init_sentry' src/artiscrapper/logging_setup.py` returns ≥2 (definition + call).
    - logging_setup.py `add_correlation_id` processor now sets the Sentry tag inside the `if cid` branch. Verify: structural inspection — `sentry_sdk` import + `set_tag.*correlation_id` token both present.
    - `tests/test_footguns.py` includes `test_sentry_does_not_pull_uvloop` and `test_metrics_endpoint_unprotected_by_design`.
    - `pytest tests/test_sentry_init.py tests/test_footguns.py -x -q` exits 0 (all green or xfailed-as-expected).
    - Phase 2 regression check: `pytest tests/test_llm.py -x -q` continues to exit 0 — the dataclass bridge means existing tests that read `metrics.llm_fallback_total["timeout"]` still see the increments (per D-11 dual-write semantics).
  </acceptance_criteria>
  <done>
    Pure-module wiring done: auth.py, metrics.py prometheus bridge, llm.py call-site refactor, logging_setup.py Sentry init+tag injection, footguns invariants. Sentry tests pass; auth tests still red (waiting for main.py wiring in Task 3); Phase 2 llm tests still green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (Wave 2): main.py wiring (Limiter + /metrics mount + verify_api_key on /search + lifespan init log lines + Histogram.time() brackets)</name>
  <files>src/artiscrapper/main.py, src/artiscrapper/llm.py, src/artiscrapper/visit.py, tests/test_footguns.py</files>
  <read_first>
    - src/artiscrapper/main.py (current lifespan structure lines 132-169, FastAPI construction + CorrelationIdMiddleware add lines 176-183, /search route signature line ~265, block-detection branch lines ~334-348)
    - src/artiscrapper/llm.py::curate_candidates (Histogram.time() wrapping target around line 276)
    - src/artiscrapper/visit.py::visit_candidates and inner visit-one function (Histogram.time() wrapping targets — stages: fetch / extract / classify)
    - src/artiscrapper/auth.py (Task 2 output — verify_api_key + get_api_key)
    - src/artiscrapper/metrics.py (Task 2 output — search_elapsed, llm_elapsed, visit_elapsed Histograms)
    - .planning/phases/03-robustness/03-PATTERNS.md §"src/artiscrapper/main.py" (verbatim lifespan additions, decorator stack, middleware order)
    - .planning/phases/03-robustness/03-RESEARCH.md §A5 (Histogram.time() async pitfall — context-manager form ONLY, never `@histogram.time()` on async def — Pitfall 1)
    - .planning/phases/03-robustness/03-RESEARCH.md §C5 + §C6 (slowapi middleware ordering, no SlowAPIMiddleware needed)
    - .planning/phases/03-robustness/03-RESEARCH.md §G1 (the D-19 log-line gate — three exact log_name strings: `api_keys_loaded`, `sentry_init_skipped`|`sentry_init_done`, `rate_limit_init`. Note: PATTERNS.md mentions `challenge_backoff_init` — that one is owned by Plan 03-02, NOT this task.)
  </read_first>
  <behavior>
    - tests/test_auth.py 3 tests turn GREEN (auth dependency now wired on /search).
    - tests/integration/test_metrics_endpoint.py 2 tests turn GREEN (/metrics endpoint exists at the mount point, exposes ≥6 artiscrapper_* families, no auth).
    - tests/integration/test_rate_limit.py 1 test turns GREEN (60/min stacked decorator triggers 429 on the 61st request).
    - tests/test_sentry_init.py::test_lifespan_log_matches_sdk_state xfail marker REMOVED — lifespan now emits the matching log line synchronously with SDK init state (WRN-04 D-19 alignment).
    - tests/test_footguns.py `test_metrics_endpoint_unprotected_by_design` xfail marker can be REMOVED — main.py now contains `make_asgi_app()`.
    - Lifespan emits exactly three D-19 log lines BEFORE `boot_done`: `api_keys_loaded`, `sentry_init_skipped`|`sentry_init_done`, `rate_limit_init`. The `sentry_init_*` log line is emitted AFTER `_init_sentry()` has returned (so the log name and the SDK state cannot diverge — WRN-04).
    - Phase 2 regression: `tests/test_health.py`, `tests/test_e2e.py`, `tests/test_parser.py`, `tests/test_llm.py`, `tests/test_cache.py`, `tests/test_visit.py` all continue green.
  </behavior>
  <action>
    Step 1 — Add Phase 3 imports at the top of `src/artiscrapper/main.py` (alongside existing Phase 2 imports):
    - `from fastapi import Depends`
    - `from slowapi import Limiter, _rate_limit_exceeded_handler`
    - `from slowapi.errors import RateLimitExceeded`
    - `from prometheus_client import make_asgi_app`
    - `from .auth import verify_api_key, get_api_key, _parse_api_keys`
    - `from .metrics import search_elapsed, llm_elapsed, visit_elapsed, inc_block_detected` (search_elapsed used here; llm_elapsed/visit_elapsed used in llm.py + visit.py edits below; inc_block_detected replaces the direct dataclass increment at line 338)
    - `import sentry_sdk` (needed for the WRN-04 invariant: the lifespan log line for sentry MUST be emitted AFTER `_init_sentry()` runs, and the log statement reads `sentry_sdk.get_client().is_active()` as the authoritative source of truth instead of branching on `settings.SENTRY_DSN`).

    Step 2 — Insert D-19 lifespan log lines per PATTERNS.md §"src/artiscrapper/main.py" verbatim. Place them AFTER step 1 (cache init / init_schema) and BEFORE step 2 (browser launch), so the lines appear early in boot regardless of whether the browser is mocked in tests:
    - `log.info("api_keys_loaded", count=len(_parse_api_keys()), rate_per_min=settings.API_RATE_PER_MINUTE, rate_per_day=settings.API_RATE_PER_DAY)` — the count, NOT the keys themselves (OBS-05).
    - For the Sentry init log line (WRN-04 ALIGNMENT REQUIREMENT): `_init_sentry()` is already called at module-import time by `logging_setup.py` (Task 2 wired this), so by the time lifespan runs the SDK state is fully resolved. Emit the log line by reading the SDK state directly: `_sentry_active = sentry_sdk.get_client().is_active()` then `log.info("sentry_init_done" if _sentry_active else "sentry_init_skipped", traces_sample_rate=0.1 if _sentry_active else None)`. This guarantees the log event name and `is_active()` return value cannot diverge (the log line is derived from the SDK state, not from the env var). The DSN value is NEVER logged (OBS-05).
    - `log.info("rate_limit_init", per_min=settings.API_RATE_PER_MINUTE, per_day=settings.API_RATE_PER_DAY)` — independent of the api_keys_loaded line so D-19 grep can target it directly.
    - Do NOT add `challenge_backoff_init` here — that line is added by Plan 03-02 Task 3 to keep ownership clean.

    Step 3 — Wire slowapi + /metrics mount AFTER `app = FastAPI(...)` construction (line ~176) but BEFORE the `app.add_middleware(CorrelationIdMiddleware)` line (line ~183) per PATTERNS.md and 03-RESEARCH.md §C5:
    - `limiter = Limiter(key_func=get_api_key, headers_enabled=True)` — `headers_enabled=True` makes 429 responses include X-RateLimit-* + Retry-After headers (per 03-RESEARCH.md §C4).
    - `app.state.limiter = limiter` — slowapi convention; `_rate_limit_exceeded_handler` reads `app.state.limiter`.
    - `app.add_exception_handler(RateLimitExceeded, _rate_limit_exceeded_handler)` — registers the canonical 429 handler.
    - `app.mount("/metrics", make_asgi_app())` — D-10: mounts as ASGI sub-app bypassing FastAPI middleware (no auth, no rate-limit, no CorrelationIdMiddleware wrapping — by design per 03-RESEARCH.md §A1).
    - The existing `app.add_middleware(CorrelationIdMiddleware)` line stays UNCHANGED (already last add_middleware call = first to execute = outermost — guarantees 429 responses also carry X-Request-ID per 03-RESEARCH.md §C5).

    Step 4 — Decorate the `/search` route per D-01 / D-02 / D-03. The existing signature is `async def search(request: Request, body: SearchRequest) -> SearchResponse`. Apply per PATTERNS.md §"src/artiscrapper/main.py":
    - Add decorators ABOVE the existing `@app.post("/search", response_model=SearchResponse)` line, in this exact order (slowapi requires its decorators between `@app.post` and the function definition; FastAPI `Depends` is added as a function parameter, NOT a decorator):
        `@app.post("/search", response_model=SearchResponse)`
        `@limiter.limit("60/minute")`
        `@limiter.limit("10000/day")`
    - Add `_api_key: str = Depends(verify_api_key)` as the LAST parameter of the search function signature (after `body: SearchRequest`). The variable name starts with `_` so linters don't flag it as unused (the dependency runs purely for the side effect of raising 401 when invalid).
    - `request: Request` MUST remain the FIRST positional argument (slowapi requirement per 03-RESEARCH.md §C2).

    Step 5 — Wrap the `/search` route body with `with search_elapsed.time():` per D-12. CRITICAL — per 03-RESEARCH.md §A5 / Pitfall 1: NEVER use `@search_elapsed.time()` as a decorator on an async def — it measures coroutine creation, not awaited completion. Use the context-manager form INSIDE the async function body: open `async def search(request, body, _api_key=...) -> SearchResponse`, then add `with search_elapsed.time():` as the first body statement (immediately inside the function body, before any other statement); indent the existing 10-step pipeline body (cache lookup → block-detected branch → etc.) ONE level deeper inside that `with` block. The existing `return` statements remain unchanged in position but are now nested one indentation level deeper. Pattern S7 (PATTERNS.md llm.py:178 analog — same shape with the metric wrapper); the analog there wraps `await client.post(...)` with `with llm_elapsed.time():` so the entire awaited body is timed.

    Step 6 — Wrap `curate_candidates` in `src/artiscrapper/llm.py` and `visit_candidates` in `src/artiscrapper/visit.py` with the corresponding Histograms per D-12:
    - In `llm.py::curate_candidates` (the async function around line 276 that gathers Semaphore-bounded classification calls), wrap the body with `with llm_elapsed.time():` (single bracket around the whole batched call — captures wall-clock of the entire curator pass, including any retries and the semaphore wait).
    - In `visit.py`, inside the per-candidate visit coroutine: wrap the fetch step with `with visit_elapsed.labels(stage="fetch").time():`, wrap the extract step (call to `extract_product`) with `with visit_elapsed.labels(stage="extract").time():`, wrap the live-vs-dead classify step with `with visit_elapsed.labels(stage="classify").time():`. Three separate brackets per candidate so the `stage` label captures the per-stage distribution.
    - Imports needed in llm.py: `from .metrics import llm_elapsed`. In visit.py: `from .metrics import visit_elapsed`.

    Step 7 — Update `src/artiscrapper/main.py` line ~338 where `metrics.block_detected_total[block_reason] += 1` was incremented in Phase 2. Replace with `inc_block_detected(block_reason)` (imported from `.metrics` in Step 1). This dual-writes to dataclass + prometheus Counter per D-11.

    Step 8 — Remove the `xfail` marker from `tests/test_footguns.py::test_metrics_endpoint_unprotected_by_design` (Task 2 added it as a placeholder; Task 3 now makes the assertion pass).

    Step 9 — Remove the `xfail` marker from `tests/test_sentry_init.py::test_lifespan_log_matches_sdk_state` (Task 1 added it; Task 3 now satisfies the D-19 invariant because the lifespan emits the log line by reading `sentry_sdk.get_client().is_active()` after `_init_sentry()` has already run at module-import time).

    Step 10 — Empirical retest gate per D-19 + Phase 2 memory `feedback_empirical_retest_after_default_changes` + `feedback_compose_build_recreate`. Rebuild and verify the lifespan log lines actually reach the hot path:
    - `docker compose build` (separate step — NEVER use `up -d --build` per Phase 2 memory, can take 25min and skip recreate).
    - `docker compose up -d --force-recreate` (separate step, with `--force-recreate` to guarantee a fresh container).
    - Wait up to 30s for boot, then: `docker compose logs artiscrapper 2>&1 | grep -E "(api_keys_loaded|sentry_init_(done|skipped)|rate_limit_init)"` — must return at least 3 matching lines.
    - WRN-04 alignment check: the line emitted MUST be exactly one of `sentry_init_done` / `sentry_init_skipped` per container start (not both); cross-check by querying the container's actual SDK state via `docker compose exec artiscrapper python -c "import sentry_sdk; print(sentry_sdk.get_client().is_active())"` — the boolean returned MUST match the log event name (`done`↔True, `skipped`↔False).
    - Document the grep output in the task's summary so subsequent /gsd:verify-work can replay it.
  </action>
  <verify>
    <automated>
      cd /home/luis/proyectos/artiscrapper &&
      grep -q "from slowapi import Limiter" src/artiscrapper/main.py &&
      grep -q "from prometheus_client import make_asgi_app" src/artiscrapper/main.py &&
      grep -q 'app.mount("/metrics"' src/artiscrapper/main.py &&
      grep -q "@limiter.limit" src/artiscrapper/main.py &&
      grep -q "Depends(verify_api_key)" src/artiscrapper/main.py &&
      grep -q "with search_elapsed.time()" src/artiscrapper/main.py &&
      grep -q "with llm_elapsed.time()" src/artiscrapper/llm.py &&
      grep -q 'with visit_elapsed.labels(stage=' src/artiscrapper/visit.py &&
      grep -q "api_keys_loaded" src/artiscrapper/main.py &&
      grep -q "sentry_init_" src/artiscrapper/main.py &&
      grep -q "rate_limit_init" src/artiscrapper/main.py &&
      grep -q "inc_block_detected" src/artiscrapper/main.py &&
      test "$(grep -c 'metrics\.block_detected_total\[' src/artiscrapper/main.py)" -eq 0 &&
      uv run pytest tests/test_auth.py tests/test_sentry_init.py tests/test_footguns.py tests/integration/test_metrics_endpoint.py tests/integration/test_rate_limit.py -x -q 2>&1 | tail -3 | grep -qE '(passed)' &&
      uv run pytest tests/ -x -q --ignore=tests/integration 2>&1 | tail -3 | grep -qE '(passed)'
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/artiscrapper/main.py` imports `Limiter`, `_rate_limit_exceeded_handler`, `RateLimitExceeded`, `make_asgi_app`, `verify_api_key`, `get_api_key`, `Depends`, `sentry_sdk`. Verify: `grep -cE 'from (slowapi|prometheus_client|.auth) import' src/artiscrapper/main.py` returns ≥3, and `grep -c '^import sentry_sdk$' src/artiscrapper/main.py` returns ≥1.
    - `/metrics` is mounted via `app.mount("/metrics", make_asgi_app())`. Verify literal: `grep -cF 'app.mount("/metrics", make_asgi_app())' src/artiscrapper/main.py` returns `1`.
    - `/search` route stacks both rate-limit decorators in correct order. Verify: between `@app.post("/search"` and `async def search`, there appear exactly two `@limiter.limit(` lines, one with `"60/minute"` and one with `"10000/day"`.
    - `/search` route signature includes `_api_key: str = Depends(verify_api_key)` AFTER `body: SearchRequest`. Verify: `grep -A 4 'async def search' src/artiscrapper/main.py | grep -c 'Depends(verify_api_key)'` returns `1`.
    - `/search` route body is wrapped in `with search_elapsed.time():`. Verify: `grep -c 'with search_elapsed.time()' src/artiscrapper/main.py` returns `1`. CRITICAL CHECK: `grep -c '@search_elapsed.time' src/artiscrapper/main.py` MUST return `0` (Pitfall 1 — decorator form is forbidden on async def).
    - llm.py wraps `curate_candidates` body in `with llm_elapsed.time():`. Verify: `grep -c 'with llm_elapsed.time()' src/artiscrapper/llm.py` returns ≥1. `grep -c '@llm_elapsed.time' src/artiscrapper/llm.py` MUST return `0`.
    - visit.py wraps three stages: fetch/extract/classify. Verify: `grep -cE 'with visit_elapsed.labels\(stage=.{1,4}(fetch|extract|classify)' src/artiscrapper/visit.py` returns `3`. `grep -c '@visit_elapsed' src/artiscrapper/visit.py` MUST return `0`.
    - Lifespan emits the three D-19 log lines BEFORE `boot_done`. Verify: `grep -cE 'log.info\("(api_keys_loaded|sentry_init_(done|skipped)|rate_limit_init)"' src/artiscrapper/main.py` returns ≥3 (one for each of the three log_name keys; sentry has a conditional so it's 1 occurrence with both variants).
    - **WRN-04 LIFESPAN-LOG-MATCHES-SDK-STATE INVARIANT:** The lifespan `sentry_init_done`|`sentry_init_skipped` log line is emitted AFTER `_init_sentry()` has returned (which happens at logging_setup module-import time, completed before lifespan executes). If the log says `sentry_init_done`, `sentry_sdk.get_client().is_active()` returns True; if `sentry_init_skipped`, `is_active()` returns False. Verified TWO ways:
        1. **Unit test:** `uv run pytest tests/test_sentry_init.py::test_lifespan_log_matches_sdk_state -x -v` exits 0 with NO xfail marker (Task 3 Step 9 removed the marker).
        2. **Empirical:** `docker compose logs artiscrapper 2>&1 | grep -cE 'sentry_init_(done|skipped)'` returns exactly `1` per process start; cross-checked via `docker compose exec artiscrapper python -c "import sentry_sdk; print(sentry_sdk.get_client().is_active())"` returning the boolean that matches the log event name (`done`↔True, `skipped`↔False).
    - main.py line that used to read `metrics.block_detected_total[block_reason] += 1` now calls `inc_block_detected(block_reason)`. Verify: `grep -v '^#' src/artiscrapper/main.py | grep -c 'metrics\.block_detected_total\['` returns `0` AND `grep -c 'inc_block_detected(' src/artiscrapper/main.py` returns ≥1.
    - Empirical retest (D-19 + Phase 2 `feedback_empirical_retest_after_default_changes` + `feedback_compose_build_recreate`): the two-step `docker compose build` followed by `docker compose up -d --force-recreate` succeeds AND `docker compose logs artiscrapper 2>&1 | grep -cE "(api_keys_loaded|sentry_init_(done|skipped)|rate_limit_init)"` returns ≥3. NEVER use `docker compose up -d --build` (combined form is the foot-gun).
    - All 10 previously-RED tests now GREEN: `uv run pytest tests/test_auth.py tests/test_sentry_init.py tests/integration/test_metrics_endpoint.py tests/integration/test_rate_limit.py -x -q` exits 0.
    - test_footguns invariants intact: `uv run pytest tests/test_footguns.py -x -q` exits 0; the xfail marker on `test_metrics_endpoint_unprotected_by_design` was removed and the test now genuinely passes.
    - Phase 2 regression check: `uv run pytest tests/test_health.py tests/test_e2e.py tests/test_parser.py tests/test_llm.py tests/test_cache.py tests/test_visit.py -x -q` exits 0.
    - `curl -s http://localhost:8001/metrics | grep -c '^artiscrapper_' | awk '{print ($1 >= 6) ? "ok" : "fail"}'` prints `ok` (D-13 smoke test against the live recreated container; port 8001 is the dev compose mapping per 02-RESEARCH.md — adjust if compose.yml uses a different port).
  </acceptance_criteria>
  <done>
    /metrics endpoint live with ≥6 families; /search gated by X-API-Key (401) and rate-limited (60/min, 10000/day → 429+Retry-After); Sentry init wired via logging_setup module import; lifespan emits 3 D-19 log lines verified empirically in container logs; WRN-04 log-matches-SDK-state invariant enforced (log derived from `sentry_sdk.get_client().is_active()` post-init). All Phase 3 unit + integration tests for this plan green; Phase 2 tests still green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer → /search | External HTTP traffic crosses authenticated boundary; X-API-Key header is the first gate |
| Prometheus scraper → /metrics | Internal docker-network only by design (D-10 — no public exposure unless reverse-proxy enforces IP allowlist) |
| dev/test container → Sentry SaaS | SENTRY_DSN empty in dev (D-14) — no boundary crossed; in prod, sentry-sdk crosses public internet to ingest.sentry.io |
| sentry events → external Sentry org | scrubbed via `send_default_pii=False` (D-15) — no request bodies, no headers, no PII; correlation_id is the only joinable tag |
| structlog event_dict → stdout (json mode) | OBS-05 allow-list applies; never log api-key value, never log DSN, never log scraped content |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01-01 | Spoofing | /search route (auth bypass) | HIGH | mitigate | `verify_api_key` FastAPI dependency raises 401 with WWW-Authenticate=ApiKey on missing/unknown X-API-Key (per D-01/D-03); API_KEYS env-var parsed once at module load (D-04 in-memory; rotation requires restart per D-01). Maps to T-auth-missing-key from 03-VALIDATION.md. Acceptance ref: Task 1 `test_missing_key_returns_401` + Task 3 `Depends(verify_api_key)` structural grep. |
| T-03-01-02 | Tampering / DoS | /search route (DoS via rate-limit miss) | HIGH | mitigate | Stacked `@limiter.limit("60/minute") + @limiter.limit("10000/day")` (slowapi 0.1.9) returns 429 + Retry-After (per D-02). headers_enabled=True so X-RateLimit-Limit/Remaining/Reset are exposed. In-memory MemoryStorage (per D-04) — restart resets counters; acceptable for single-container deploy. Maps to T-quota-overflow. Brute-force on the `"anonymous"` bucket (missing key) limited to 60/min from any source (per 03-RESEARCH.md §C3). Acceptance ref: Task 1 `test_rate_limit_per_minute_returns_429` + Task 3 stacked-decorator structural grep. |
| T-03-01-03 | Information Disclosure | /metrics endpoint (metrics exposure) | MEDIUM | accept | No auth on /metrics by design (D-10) — exposed on the same :8000 port as /search. Mounted via `make_asgi_app()` so CorrelationIdMiddleware + slowapi do NOT wrap it. Acceptable because the container runs on an internal docker network in dev/prod; any future public exposure requires reverse-proxy IP allowlist (deferred to Phase 4 in CONTEXT.md). Maps to T-metrics-exposure. Default Python-VM collectors (PROCESS/GC/PLATFORM) unregistered to minimize attack surface (per 03-RESEARCH.md §A2). |
| T-03-01-04 | Information Disclosure | Sentry SDK (Sentry PII leak / dev data leak) | HIGH | mitigate | `send_default_pii=False` (per D-15) — no request bodies/headers/cookies. SENTRY_DSN empty in dev (D-14) means SDK is not initialized at all. correlation_id added as tag (D-16) via add_correlation_id processor — useful for joining Sentry events to structlog logs without leaking scraped content. OBS-05 allow-list preserved (no scraped titles/snippets/URLs in log events that might also reach Sentry). Maps to T-dev-data-leak. Acceptance ref: Task 1 `test_no_init_when_dsn_empty` + `test_sample_rates`. |
| T-03-01-05 | Information Disclosure | logging_setup.add_correlation_id (logging-setup safety) | MEDIUM | mitigate | set_tag wrapped in try/except Exception: pass — observability code can NEVER break a request even if sentry_sdk module is malformed. correlation_id value is a UUID4 from asgi-correlation-id middleware (NOT user-controlled), so no XSS/injection risk in Sentry UI rendering. |
| T-03-01-06 | Tampering | Counter cardinality (visit_failed_total{host}) (counter cardinality DoS) | MEDIUM | mitigate | `host` label normalized via tldextract to TLD+1 (per 03-RESEARCH.md §A6) — caps cardinality at ~50 hosts even if SERPs contain subdomain variance. Without this, an attacker pushing arbitrary URLs through /search could blow up Prometheus memory. Pre-seeding for `reason` labels avoids absence-alerting brittleness (Pitfall 10). |
| T-03-01-07 | Tampering | pyproject.toml package installs (supply-chain) | MEDIUM | mitigate | All three deps verified on PyPI 2026-06-03 (prometheus-client==0.25.0 ~100M/wk downloads since 2015; sentry-sdk==2.61.1 ~50M/wk since 2018; slowapi==0.1.9 ~500k/wk since 2020). slopcheck [OK] for all three (`-client`/`-sdk` naming flagged but confirmed established). No `[ASSUMED]` or `[SUS]` packages → no human-verify checkpoint required. Foot-gun test `test_sentry_does_not_pull_uvloop` pins that sentry-sdk transitive deps NEVER pull uvloop into uv.lock (D-6 invariant). Maps to T-{phase}-SC. |
| T-03-01-08 | Information Disclosure / Repudiation | Sentry scope correlation_id tag (cross-request correlation gap) | MEDIUM | mitigate | `T-cross-request-correlation` (D-16): Sentry events emitted without `tags.correlation_id` cannot be joined to the structlog event stream, breaking the post-mortem forensics path (find the structlog request that crashed → query Sentry by correlation_id). Mitigation: the `add_correlation_id` processor in logging_setup.py extends to call `sentry_sdk.get_current_scope().set_tag("correlation_id", cid)` inside the `if cid:` branch (per D-16), so every Sentry event captured during an active request automatically carries the joining tag. The set_tag call is wrapped in try/except so a malformed sentry_sdk import never breaks the request path (T-03-01-05 overlap). Acceptance ref: Task 1 `test_correlation_id_tag` (asserts `sentry_sdk.get_current_scope()._tags["correlation_id"]` is set after `add_correlation_id` runs) from VALIDATION.md mapping for D-16. |
| T-03-01-09 | Repudiation | /search audit trail | LOW | accept | Per-request correlation_id propagates through structlog (OBS-03/04) and Sentry tag (D-16, T-03-01-08). api_key value NOT logged per OBS-05 — only `api_keys_loaded.count` aggregate. Acceptable for single-client deploy; per-key billing telemetry deferred to Phase 5 (MULTI-03). |
| T-03-01-10 | Elevation of Privilege | verify_api_key dependency vs slowapi key_func ordering | LOW | accept | slowapi 429 fires BEFORE verify_api_key 401 (per 03-RESEARCH.md §C3 Resolution order step 3 vs step 4) — unauthenticated brute-force requests consume the `"anonymous"` bucket's 60/min quota. Acceptable per CONTEXT.md "Claude's Discretion" — alternative (auth-before-rate-limit) requires custom middleware that breaks slowapi's contract. Information-leakage-free: failed attempts see 401, not 429, until quota is hit. |
| T-03-01-11 | Information Disclosure | container logs | MEDIUM | mitigate | OBS-05 allow-list extended to Phase 3 secrets: api_keys_loaded logs `count` not values; sentry_init_done logs sample_rate not DSN; rate_limit_init logs quotas not key values. SAFE_CANDIDATE_FIELDS in logging_setup.py:65-67 unchanged — no auth fields added. |
</threat_model>

<verification>
**End-to-end Plan 03-01 verification:**

1. `pytest tests/ -x -q` exits 0 (full suite green; <90s per 03-VALIDATION.md sampling rate).
2. `curl -s http://localhost:8001/metrics | grep -c '^artiscrapper_'` returns ≥6 (D-13).
3. `curl -X POST http://localhost:8001/search -H 'Content-Type: application/json' -d '{"query":"test"}'` returns 401 (no X-API-Key — D-03).
4. `curl -X POST http://localhost:8001/search -H 'X-API-Key: <valid>' -H 'Content-Type: application/json' -d '{"query":"test"}'` returns a non-401 response (200/503/other, depending on container state).
5. 61 sequential curl POSTs to /search with same valid X-API-Key in <60s → request #61 returns 429 with `Retry-After` header (D-02).
6. `docker compose logs artiscrapper 2>&1 | grep -cE "(api_keys_loaded|sentry_init_(done|skipped)|rate_limit_init)"` returns ≥3 (D-19 empirical retest gate).
7. With `SENTRY_DSN=""` in `.env`, container boot logs `sentry_init_skipped` AND `sentry_sdk.get_client().is_active()` returns False (D-14, WRN-04). With a valid DSN, the same source emits `sentry_init_done` AND `is_active()` returns True. The two MUST never diverge.
</verification>

<success_criteria>
- ROADMAP §Phase 3 success criterion 1 PROVEN: `curl /metrics | grep -c '^artiscrapper_'` returns ≥6 distinct families (OBS-07).
- ROADMAP §Phase 3 success criterion 2 PROVEN: SENTRY_DSN empty → no Sentry init (verified by `test_no_init_when_dsn_empty`); SENTRY_DSN set + uncaught exception → event with `tags.correlation_id` (D-16 verified by `test_correlation_id_tag` + T-03-01-08 register row). Lifespan log line matches SDK state (verified by `test_lifespan_log_matches_sdk_state`, WRN-04).
- ROADMAP §Phase 3 success criterion 3 PROVEN: X-API-Key missing → 401 (D-03); valid key burst of 61 in <60s → 429 + Retry-After (D-02).
- OBS-07 requirement closed in `.planning/REQUIREMENTS.md` traceability table after `/gsd:verify-work`.
- All 10 Phase 3 unit + integration tests for this plan are GREEN.
- Phase 2 regression suite (test_health, test_e2e, test_parser, test_llm, test_cache, test_visit, test_footguns) still GREEN — no D-11 bridge break, no D-6 uvloop pull from sentry-sdk.
- Phase 2 memory landmines respected: `docker compose build` + `docker compose up -d --force-recreate` (never combined); `api_keys_loaded` / `sentry_init_*` / `rate_limit_init` log lines empirically verified in container logs (D-19); sentry log line never diverges from SDK state (WRN-04).
</success_criteria>

<output>
Create `.planning/phases/03-robustness/03-01-SUMMARY.md` when done. Include:
- Tasks completed with the per-task automated verify outputs
- The 3 D-19 log lines as captured from `docker compose logs` (paste the exact strings)
- `pytest tests/ -x -q` total time + pass count
- `curl /metrics | grep -c '^artiscrapper_'` count
- Final list of artiscrapper_* metric families exposed (run `curl -s :8001/metrics | grep -oE '^artiscrapper_\w+' | sort -u`)
- Confirmation that uv.lock does NOT contain uvloop (`grep -c uvloop uv.lock` returns 0)
- WRN-04 empirical verification: paste the output of `docker compose exec artiscrapper python -c "import sentry_sdk; print(sentry_sdk.get_client().is_active())"` and confirm it matches the captured `sentry_init_*` log line.
</output>
