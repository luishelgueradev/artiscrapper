---
phase: 03-robustness
plan: 02
type: execute
wave: 2
depends_on: ["01"]
cross_plan_test_infrastructure:
  - tests/integration/__init__.py
  - tests/integration/conftest.py
files_modified:
  - src/artiscrapper/cache.py
  - src/artiscrapper/challenge_backoff.py
  - src/artiscrapper/main.py
  - tests/test_challenge_backoff.py
  - tests/fixtures/serp/sorry.html
  - tests/integration/test_challenge_backoff.py
  - tests/integration/test_degraded_mode.py
  - tests/integration/test_catalog_extraction.py
autonomous: true
requirements: []
requirements_addressed:
  - hardens-BROWSER-05
  - hardens-LLM-06
  - hardens-D12-acceptance
  - hardens-NF-02
  - hardens-NF-03

must_haves:
  truths:
    - "src/artiscrapper/challenge_backoff.py exists as a module-singleton state machine that computes exponential backoff `min(60 * 2^(retries-1), 3600)` (D-06) with reset trigger ≥1h after last_block_at (D-07) (ROADMAP-4)"
    - "ChallengeBackoff scope is GLOBAL — a single backoff state covers all *.google.com fetches (D-05); per-URL tracking (e.g., `query` vs `query mercadolibre`) is rejected because Google blocks are IP-level per Phase 1 SPIKE.md §Browser. State machine uses a single sqlite row, not a per-key table."
    - "challenge_state sqlite table is created in init_schema with CHECK(id=1) single-row constraint and seeded via INSERT OR IGNORE; state survives container restart (D-08)"
    - "/search returns 503 + structured metadata.block_detected=true + Retry-After header when check_gate() denies the request; consumer never gets a silent timeout (D-09)"
    - "/search calls challenge_backoff.record_block(reason) inside the existing block-detection branch and challenge_backoff.record_success() on the success path (D-07 reset)"
    - "tests/fixtures/serp/sorry.html exists as a synthetic /sorry/ stand-in (Phase 1 captured none — D-20) and triggers _detect_block() in a pinned test"
    - "tests/integration/test_catalog_extraction.py asserts ≥85% catalog price extraction across the Phase 1 catalog fixture set (ROADMAP-6, validates D12 hand-roll)"
    - "tests/integration/test_degraded_mode.py asserts the LLM-down heuristic-only path retains ≥5 useful results from Phase 1's labelled.jsonl (ROADMAP-5, hardens LLM-06)"
    - "tests/integration/test_challenge_backoff.py asserts /search → 503 + Retry-After when sorry.html is injected via monkeypatched fetch_serp (D-09 end-to-end)"
    - "lifespan emits a fourth D-19 log line `challenge_backoff_init` declaring base_s/cap_s/reset_after_s values (D-18 + D-19 + Phase 2 memory feedback_empirical_retest_after_default_changes)"
  artifacts:
    - path: "src/artiscrapper/challenge_backoff.py"
      provides: "module-singleton state machine mirroring llm.py::resolve_model pattern + sqlite persistence per cache.py::set_cached pattern"
      contains: "_BASE_S = 60"
      contains2: "_BACKOFF_CAP_S = 3600"
      contains3: "_RESET_AFTER_S = 3600"
      contains4: "check_gate"
      contains5: "record_block"
      contains6: "record_success"
    - path: "src/artiscrapper/cache.py"
      provides: "extended _DDL with challenge_state CREATE TABLE + INSERT OR IGNORE seed"
      contains: "CREATE TABLE IF NOT EXISTS challenge_state"
      contains2: "CHECK (id = 1)"
    - path: "src/artiscrapper/main.py"
      provides: "/search route check_gate gate before fetch + record_block/record_success after fetch + challenge_backoff_init lifespan log line"
      contains: "check_gate"
      contains2: "record_block"
      contains3: "record_success"
      contains4: "challenge_backoff_init"
      contains5: "Retry-After"
    - path: "tests/fixtures/serp/sorry.html"
      provides: "synthetic /sorry/ stand-in for D-20 (Phase 1 captured none per SPIKE.md §Browser)"
      contains: "sorry/index"
      contains2: "detected unusual traffic"
    - path: "tests/test_challenge_backoff.py"
      provides: "unit tests for D-06 backoff curve math, D-07 reset trigger, module-singleton lock semantics"
      contains: "test_exponential_curve"
      contains2: "test_reset_after_one_hour"
    - path: "tests/integration/test_challenge_backoff.py"
      provides: "end-to-end /search → 503 + Retry-After test using sorry.html fixture + monkeypatched fetch_serp + sqlite persistence assertion"
      contains: "test_503_with_retry_after"
      contains2: "test_state_survives_restart"
    - path: "tests/integration/test_degraded_mode.py"
      provides: "respx-mocked /healthz failure → heuristic path retains ≥5 useful results from Phase 1 labelled.jsonl (50 records)"
      contains: "test_llm_down_keeps_useful_results"
    - path: "tests/integration/test_catalog_extraction.py"
      provides: "in-process extract_product() over Phase 1 catalog fixtures → ≥85% price extraction rate"
      contains: "test_catalog_price_extraction_rate_above_85_percent"
  key_links:
    - from: "src/artiscrapper/main.py /search route step [1.5]"
      to: "src/artiscrapper/challenge_backoff.py::check_gate"
      via: "await check_gate(request.app.state.cache) before Google fetch"
      pattern: "await check_gate\\("
    - from: "src/artiscrapper/main.py block-detected branch (line ~338)"
      to: "src/artiscrapper/challenge_backoff.py::record_block"
      via: "await record_block(...) inside existing _detect_block consumer"
      pattern: "await record_block\\("
    - from: "src/artiscrapper/main.py success path"
      to: "src/artiscrapper/challenge_backoff.py::record_success"
      via: "await record_success(...) after parse_serp returns"
      pattern: "await record_success\\("
    - from: "tests/integration/test_challenge_backoff.py"
      to: "tests/fixtures/serp/sorry.html"
      via: "monkeypatched fetch_serp returns this fixture content"
      pattern: "sorry\\.html"
    - from: "src/artiscrapper/cache.py::init_schema"
      to: "challenge_state table seed row"
      via: "INSERT OR IGNORE INTO challenge_state (id=1, ...)"
      pattern: "INSERT OR IGNORE INTO challenge_state"
---

## Coordination Notes

This plan touches `src/artiscrapper/main.py`, which is ALSO modified by Plan 03-01. The two plans are
serialized via `depends_on: ["01"]` (this plan ships in Wave 2, after 03-01 lands in Wave 1) so the
executor never has to reconcile a parallel merge. The contract below documents the expected end-state
of main.py after 03-01 has landed so this plan's Task 3 can locate its insertion sites unambiguously.

**Plan 03-01 (lands first) leaves main.py in this shape:**
- Phase 3 imports added at top (slowapi Limiter + RateLimitExceeded + _rate_limit_exceeded_handler,
  prometheus_client make_asgi_app, FastAPI Depends, auth.verify_api_key + get_api_key + _parse_api_keys,
  metrics.search_elapsed + llm_elapsed + visit_elapsed + inc_block_detected).
- Lifespan emits THREE log lines BEFORE `boot_done` in this order: `api_keys_loaded`,
  `sentry_init_done|sentry_init_skipped`, `rate_limit_init`.
- `app = FastAPI(...)` construction is followed by slowapi wiring (`limiter = Limiter(...)`,
  `app.state.limiter = limiter`, `app.add_exception_handler(RateLimitExceeded, ...)`,
  `app.mount("/metrics", make_asgi_app())`) BEFORE the existing `app.add_middleware(CorrelationIdMiddleware)` line.
- `/search` route signature carries `_api_key: str = Depends(verify_api_key)` as the last parameter;
  decorators stack as `@app.post("/search", ...)` then `@limiter.limit("60/minute")` then `@limiter.limit("10000/day")`.
- `/search` body opens with `with search_elapsed.time():` as the first body statement; the existing
  10-step pipeline is indented one level deeper inside that block.
- The block-detection branch (~line 338) calls `inc_block_detected(block_reason)` instead of the
  direct dataclass increment.

**Plan 03-02 (lands second — this plan) inserts into main.py at the following anchored sites:**
- **Lifespan log line:** insert `log.info("challenge_backoff_init", base_s=..., cap_s=..., reset_after_s=...)`
  BETWEEN the existing `rate_limit_init` log line (from 03-01) and the `boot_done` line. It is the
  4th D-19 log line in declared order.
- **/search gate (step [1.5]):** insert `allowed, retry_after = await check_gate(...)` AFTER the
  03-01 `verify_api_key` Depends has resolved (which means inside the function body, after the
  cache-hit early-return block ~lines 287-299) AND AFTER the `with search_elapsed.time():` context
  manager has opened (so 503 responses STILL record latency on the search_elapsed Histogram). The
  insertion point is the first non-cache-hit, non-fetch line inside the `with search_elapsed.time():`
  block.
- **/search block-detected branch:** `await record_block(request.app.state.cache)` lands at the
  existing `_detect_block()` callsite (~line 338, after 03-01 changed the increment to
  `inc_block_detected(...)`); placed BEFORE the existing 503 return so the state is persisted before
  the response is built.
- **/search success path:** `await record_success(request.app.state.cache)` lands AFTER both
  `_detect_block` calls return None (no block) but BEFORE the parser cascade step.

If main.py drifts from this contract during 03-01 execution (e.g., the executor reorders the lifespan
log lines or moves the `with search_elapsed.time():` bracket), 03-02 Task 3 must update its insertion
anchors to match the new shape before applying its changes. The grep gates in `<verify>` blocks
catch the most likely drift modes.

<objective>
Ship the Google challenge-detection backoff state machine + the fixture-replay integration suite that proves the LLM-down degraded path keeps serving and that the Phase 1 D12 ≥85% catalog price extraction holds in production. Both work-streams touch separate modules from Plan 03-01 (challenge_backoff.py + cache.py DDL extension + /search-route hooks vs. metrics+sentry+slowapi), but the shared main.py touch points are serialized by `depends_on: ["01"]` — this plan ships in Wave 2 after 03-01 lands.

Purpose: Close ROADMAP §Phase 3 success criteria 4, 5, 6. Harden BROWSER-05 (block-detect now drives a backoff scheduler, not just a flag), LLM-06 (degraded mode empirically validated against 50-record labelled set), D12 (in-process extract_product() pinned at ≥85% on Phase 1 catalog fixtures), NF-02/NF-03 (integration suite + fixture-driven tests with per-fixture pinned assertions per D-18).

Output: One new `challenge_backoff.py` module (~100 LOC mirroring llm.py::resolve_model + cache.py::set_cached patterns verbatim), one DDL extension in cache.py, surgical /search route additions in main.py (gate + record_block + record_success + one lifespan log line), one synthetic sorry.html fixture, two new unit test files, three new integration test files. 8 file touches total.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-robustness/03-CONTEXT.md
@.planning/phases/03-robustness/03-RESEARCH.md
@.planning/phases/03-robustness/03-PATTERNS.md
@.planning/phases/03-robustness/03-VALIDATION.md
@.planning/phases/02-mvp/02-VERIFICATION.md
@.planning/phases/02-mvp/02-HUMAN-UAT.md
@src/artiscrapper/main.py
@src/artiscrapper/cache.py
@src/artiscrapper/llm.py
@src/artiscrapper/browser.py
@src/artiscrapper/search.py
@src/artiscrapper/visit.py
@tests/conftest.py
@tests/test_cache.py
@tests/test_llm.py
@tests/test_parser.py
@tests/test_health.py
@tests/fixtures/llm/labelled.jsonl

<interfaces>
<!-- Extracted from current codebase to remove scavenger-hunt cost for the executor. -->

From `src/artiscrapper/llm.py:93-141` (Pattern S1 — module-singleton with double-checked-lock — VERBATIM analog for challenge_backoff.py):
- Module-level cache var: `_RESOLVED_MODEL: str | None = None`
- Module-level lock: `_RESOLVE_LOCK = asyncio.Lock()`
- Public coroutine: `async def resolve_model(client, router_url, bearer_token) -> str:`
- Double-checked-lock idiom: check outside lock → acquire → re-check inside → mutate → return

From `src/artiscrapper/cache.py:87-116` (Pattern S2 — sqlite INSERT OR REPLACE with parameterized placeholders — VERBATIM analog for challenge_backoff._persist):
- Function signature: `async def set_cached(cache: aiosqlite.Connection, cache_key: str, ..., ttl_s: int) -> None`
- SQL shape: `"INSERT OR REPLACE INTO query_cache (...) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)"`
- Discipline: `await cache.execute(...)` then `await cache.commit()` immediately

From `src/artiscrapper/cache.py:29-50` (_DDL string + init_schema — Phase 3 extends both):
- `_DDL = """CREATE TABLE IF NOT EXISTS query_cache (..., bytes_total INTEGER NOT NULL DEFAULT 0); CREATE INDEX IF NOT EXISTS ix_cache_expires_at ON query_cache(expires_at); CREATE INDEX IF NOT EXISTS ix_cache_created_at ON query_cache(created_at);"""`
- `async def init_schema(cache): await cache.executescript(_DDL); await cache.commit()` — Phase 3 leaves the function body unchanged (executescript handles multi-statement DDL including the appended challenge_state DDL).

From `src/artiscrapper/main.py /search route` (Phase 2 pipeline steps to integrate into):
- Step [1] cache lookup is at lines ~287-299 (early return on cache hit)
- Step [1.5] NEW Phase 3 challenge-backoff gate inserts HERE — between cache hit return and Google fetch (per 03-PATTERNS.md §"src/artiscrapper/main.py")
- Step [2] Google fetch via `fetch_serp(app.state.browser, urls, ...)` — returns SERP HTML for both queries
- Step [3] block detection branch at lines ~334-348 — calls `_detect_block(html_a)` / `_detect_block(html_b)`, on truthy returns 503 + sets `metadata.block_detected=True`. Phase 3 INJECTS `await record_block(...)` here BEFORE the existing 503 return.
- Success-path injection point: after step [3] passes (no block detected) but before step [4] (parser cascade) — `await record_success(...)` (D-07 reset trigger fires only if last_block_at was ≥1h ago).

From `src/artiscrapper/browser.py` and `src/artiscrapper/search.py`:
- `_detect_block(html: str) -> str | None` returns one of `"consent_interstitial"`, `"sorry"`, `"recaptcha"`, `"unknown"`, or `None` if no block detected (per CONTEXT.md §code_context).
- `fetch_serp(browser, urls, ...)` is the unit test monkeypatch target — return synthetic HTML to drive the integration tests.

From `tests/conftest.py:32-45` (tmp_db fixture — used by test_cache.py::test_lazy_ttl):
- `@pytest.fixture async def tmp_db(tmp_path):` — creates aiosqlite connection at `tmp_path/cache.db`, applies PRAGMAS, calls `init_schema(db)`, yields db, closes on teardown.
- After Plan 03-02 Task 1 extends `_DDL` to include `challenge_state`, the tmp_db fixture automatically provides the new table — no fixture changes required.

From `tests/test_cache.py:85-109` (tmp_db + time-based assertion — analog for test_challenge_backoff.py):
- `await set_cached(tmp_db, ..., ttl_s=1)` then `await asyncio.sleep(2)` then `await get_cached(tmp_db, key) is None` — the same time-sleep + state-assertion shape applies to backoff curve tests (but with `time.time()` monkeypatch instead of real sleep to avoid 1h waits in tests).

From `tests/test_llm.py:132-151` (respx mock + fallback assertion — analog for test_degraded_mode.py):
- `with respx.mock: respx.post(...).mock(side_effect=httpx.TimeoutException("timeout"))` — wraps the test body and intercepts httpx calls.
- For Plan 03-02: `respx.head(...)` MUST be used for `/healthz` (NOT `respx.get`) because `router_health_check` in llm.py uses `client.head()`.

From `tests/fixtures/llm/labelled.jsonl` (50 records, Phase 1 spike):
- Each line is `{"id": "serpNN-cardNN", "candidate": {"title": ..., "url": ..., "snippet": ..., "price_in_card": ...}, "expected_is_product": bool, ...}`
- The `.candidate` dict shape matches what `should_keep()` and the degraded-mode branch consume (per 03-RESEARCH.md §E2).

From `tests/fixtures/catalog/`:
- 10 host subdirectories (one per AR e-commerce host from Phase 1 spike), each containing one HTML PDP fixture.
- `src/artiscrapper/visit.py::extract_product(html: str) -> dict | None` is a pure function (selectolax-based) — Phase 3 integration test calls it directly, no HTTP layer.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0): challenge_state DDL + sorry.html fixture + all 5 RED test files (test_challenge_backoff unit + 3 integration tests + the catalog fixture sweep)</name>
  <files>src/artiscrapper/cache.py, tests/fixtures/serp/sorry.html, tests/test_challenge_backoff.py, tests/integration/test_challenge_backoff.py, tests/integration/test_degraded_mode.py, tests/integration/test_catalog_extraction.py</files>
  <read_first>
    - src/artiscrapper/cache.py (current _DDL string + init_schema function — lines 29-50)
    - tests/conftest.py (tmp_db fixture lines 32-45 — auto-applies the extended _DDL)
    - tests/test_cache.py::test_lazy_ttl (lines 85-109 — tmp_db usage + time-assertion pattern)
    - tests/test_llm.py::test_router_timeout_returns_fallback (lines 132-151 — respx mock + fallback assertion pattern)
    - tests/test_parser.py::test_carousel_extracts_prices_from_fixtures (lines 101-138 — Phase 2 commit 160c133 per-fixture pinned counts; this is the D-18 anchor pattern)
    - tests/test_health.py:23-63 (TestClient + monkeypatched launch_async + mock browser pattern — analog for test_challenge_backoff integration)
    - tests/fixtures/llm/labelled.jsonl (50 records, verify line count: `wc -l tests/fixtures/llm/labelled.jsonl`)
    - tests/fixtures/catalog/ (10 host subdirectories — verify with `find tests/fixtures/catalog -type d | wc -l`)
    - .planning/phases/03-robustness/03-PATTERNS.md §"src/artiscrapper/cache.py" (verbatim DDL extension)
    - .planning/phases/03-robustness/03-PATTERNS.md §"tests/integration/test_catalog_extraction.py" (per-fixture pinned shape — D-18)
    - .planning/phases/03-robustness/03-PATTERNS.md §"tests/integration/test_degraded_mode.py" (respx.head vs respx.get — Pattern S6)
    - .planning/phases/03-robustness/03-RESEARCH.md §G4 (sorry.html content + _detect_block markers)
    - .planning/phases/03-robustness/03-RESEARCH.md §F3 (catalog extraction in-process pattern — pure extract_product call, no HTTP)
    - .planning/phases/03-robustness/03-RESEARCH.md §E3 + §E4 (degraded-mode test outline — respx.head on /healthz is the trigger)
  </read_first>
  <behavior>
    - Failing test 1 (tests/test_challenge_backoff.py::test_exponential_curve): record_block 5 times in succession, assert check_gate returns retry_after values that match `min(60 * 2^(n-1), 3600)` curve (D-06)
    - Failing test 2 (tests/test_challenge_backoff.py::test_reset_after_one_hour): set last_block_at to 2h ago, call record_success, assert _STATE.retry_count returns to 0 and next_allowed_at to 0 (D-07)
    - Failing test 3 (tests/test_challenge_backoff.py::test_state_persists_to_sqlite): call record_block, query `SELECT * FROM challenge_state WHERE id=1`, assert retry_count incremented + next_allowed_at > now (D-08)
    - Failing test 4 (tests/integration/test_challenge_backoff.py::test_503_with_retry_after): TestClient + monkeypatched fetch_serp returns sorry.html → POST /search returns 503 + Retry-After header is digit-string + body JSON has metadata.block_detected=true (D-09)
    - Failing test 5 (tests/integration/test_challenge_backoff.py::test_state_survives_restart): trip a block via /search, close+reopen sqlite connection (simulates container restart), assert challenge_state row persists (D-08)
    - Failing test 6 (tests/integration/test_degraded_mode.py::test_llm_down_keeps_useful_results): respx.head /healthz → 503, respx.post /v1/chat/completions → 503; load labelled.jsonl 50 records; assert router_health_check returns False; assert len([c for c in candidates if c.get("price_in_card") or c.get("price") or c.get("has_price")]) >= 5 (ROADMAP-5, hardens LLM-06)
    - Failing test 7 (tests/integration/test_catalog_extraction.py::test_catalog_price_extraction_rate_above_85_percent): iterate all .html files in tests/fixtures/catalog/*/, call extract_product(html), count how many yield a non-empty price; assert rate ≥ 0.85 (ROADMAP-6, validates D12)
    - sorry.html fixture: assert _detect_block(open("sorry.html").read()) is not None — pin the fixture as block-triggering (D-20)
    - Phase 2 regression: tests/test_cache.py still passes (DDL extension is additive)
  </behavior>
  <action>
    Step 1 — Extend `src/artiscrapper/cache.py::_DDL` per PATTERNS.md §"src/artiscrapper/cache.py" verbatim (D-08). Append to the existing _DDL multi-line string (after the `CREATE INDEX IF NOT EXISTS ix_cache_created_at ON query_cache(created_at);` line) two new statements:
    - `CREATE TABLE IF NOT EXISTS challenge_state (id INTEGER PRIMARY KEY CHECK (id = 1), last_block_at INTEGER, retry_count INTEGER NOT NULL DEFAULT 0, next_allowed_at INTEGER NOT NULL DEFAULT 0, updated_at INTEGER NOT NULL);` — single-row constraint via CHECK(id=1) per D-08.
    - `INSERT OR IGNORE INTO challenge_state (id, last_block_at, retry_count, next_allowed_at, updated_at) VALUES (1, NULL, 0, 0, strftime('%s', 'now'));` — idempotent seed (the `INSERT OR IGNORE` is the canonical pattern from cache.py).
    - Do NOT modify the `async def init_schema(cache)` function body — `executescript` already handles multi-statement DDL (verified Phase 2).
    - Do NOT add a new index on challenge_state — single-row table, primary-key lookup is O(1).

    Step 2 — Create `tests/fixtures/serp/sorry.html` per D-20 + 03-RESEARCH.md §G4. Phase 1 SPIKE.md §Browser confirmed NO real /sorry/ fixture was captured, so this is a synthetic stand-in. Minimum content (must trigger _detect_block markers: title contains "sorry/index", body contains "detected unusual traffic"–adjacent phrasing, optionally a recaptcha div):
    - `<!doctype html><html><head><title>https://www.google.com/sorry/index</title></head><body><div id="recaptcha"></div><p>We're sorry... but your computer or network may be sending automated queries.</p></body></html>`
    - File MUST be small (<1KB) and committed under tests/fixtures/serp/ alongside the Phase 1 fixtures.

    Step 3 — Write `tests/test_challenge_backoff.py` (RED — challenge_backoff.py doesn't exist yet) per PATTERNS.md §"tests/test_challenge_backoff.py". Required tests:
    - `test_exponential_curve`: uses the `tmp_db` fixture from parent conftest. Calls `record_block(tmp_db)` five times in sequence; after each call, queries `check_gate(tmp_db)` and asserts retry_after falls in the expected window per D-06 curve (60s ±2, 120s ±2, 240s ±2, 480s ±2, 960s ±2; further calls saturate to 3600s ±2). The ±2s tolerance accommodates `int(time.time())` quantization.
    - `test_reset_after_one_hour`: monkeypatch `time.time` (or the `time.time()` call inside record_success) to return a fixed offset 3700s into the future. Call `record_block(tmp_db)` (sets last_block_at to "now"), then advance time, then call `record_success(tmp_db)`, then query the table; assert retry_count == 0 AND next_allowed_at == 0 (D-07).
    - `test_state_persists_to_sqlite`: call `record_block(tmp_db)`, then directly query `SELECT last_block_at, retry_count, next_allowed_at FROM challenge_state WHERE id=1`; assert retry_count == 1, last_block_at within last 5s of current epoch, next_allowed_at > last_block_at (D-08).
    - Each test MUST reset the module-level `_STATE` via the `reset_challenge_backoff_state` autouse fixture from Plan 03-01's tests/integration/conftest.py (or add a local autouse fixture in this file — `from src.artiscrapper import challenge_backoff` followed by `cb._STATE = None` in setup). The Plan 03-01 conftest version already handles this gracefully via try/except ImportError; this Plan 03-02 task makes the import succeed, so the fixture becomes active automatically.

    Step 4 — Write `tests/integration/test_challenge_backoff.py` (RED) per PATTERNS.md §"tests/integration/test_challenge_backoff.py" + 03-RESEARCH.md §D3. Required tests:
    - `test_503_with_retry_after`: TestClient + `_make_mock_browser()` + `_fake_launch_async` per Pattern S5. Monkeypatch `src.artiscrapper.browser.fetch_serp` to return `(open("tests/fixtures/serp/sorry.html").read(), <similar sorry fixture for URL B>)`. POST /search with valid X-API-Key (after Plan 03-01 lands `verify_api_key` — for now use the dependency_overrides hook to bypass auth in this integration test: `app.dependency_overrides[verify_api_key] = lambda: "test-key"` IF auth.py exists, else skip the override). Assert: response.status_code == 503; `response.headers["Retry-After"].isdigit()`; `response.json()["metadata"]["block_detected"] is True` (D-09).
    - `test_state_survives_restart`: open `tmp_db`, call `record_block(tmp_db)`, close db; reopen the same path with a fresh aiosqlite.connect(); call `check_gate(<new_db>)`; assert allowed is False AND retry_after > 0 (D-08).
    - Also include the fixture-pin test `test_sorry_fixture_triggers_detect_block`: `from src.artiscrapper.browser import _detect_block`; `html = open("tests/fixtures/serp/sorry.html").read()`; `assert _detect_block(html) is not None`. This pins the fixture as block-triggering (D-20) — if Google changes its /sorry/ HTML and someone updates the fixture to no longer match, this test catches it.

    Step 5 — Write `tests/integration/test_degraded_mode.py` (RED) per PATTERNS.md §"tests/integration/test_degraded_mode.py" + 03-RESEARCH.md §E3-E4. Required test:
    - `test_llm_down_keeps_useful_results`: load Phase 1 labelled.jsonl (50 records). Inside `with respx.mock:` block, register `respx.head("http://127.0.0.1:3210/healthz").mock(return_value=httpx.Response(503))` AND `respx.post("http://127.0.0.1:3210/v1/chat/completions").mock(return_value=httpx.Response(503))`. CRITICAL — must use `respx.head` not `respx.get` because `router_health_check` in llm.py:267 calls `client.head()`. Assert: `await router_health_check("http://127.0.0.1:3210", "test-bearer")` returns False (this is the degraded-mode trigger per main.py:366-370). Then build `survivors = [c for c in candidates if c.get("price_in_card") or c.get("price") or c.get("has_price")]` and assert `len(survivors) >= 5` with a detailed error message listing the count and total (D-18: assertion includes specific count, not just `> 0`). This validates LLM-06 hardening (ROADMAP-5) without needing to spin up the full /search route — the heuristic-only filter is testable in isolation.

    Step 6 — Write `tests/integration/test_catalog_extraction.py` (RED) per PATTERNS.md §"tests/integration/test_catalog_extraction.py" + 03-RESEARCH.md §F3. Required test:
    - `test_catalog_price_extraction_rate_above_85_percent`: glob `tests/fixtures/catalog/*/*.html`. Assert `len(fixture_files) >= 10` (Phase 1 captured 10 — guards against accidental fixture deletion). For each fixture, read HTML, call `extract_product(html)`, check whether result is non-None AND `result.get("price")` is truthy. Build a `details` list of `(filename, has_price, price_value)` tuples for forensic logging. Compute `rate = with_price / len(fixture_files)`. Assert `rate >= 0.85` with a detailed error message listing `details` (D-18: per-fixture data, not just aggregate count). This is the D12 hand-roll production gate (ROADMAP-6).
    - Mark with `@pytest.mark.integration` so it can be selectively run.

    Step 7 — Informational baseline check ONLY (NOT a GREEN gate). Run `uv run pytest tests/test_challenge_backoff.py tests/integration/test_challenge_backoff.py tests/integration/test_degraded_mode.py tests/integration/test_catalog_extraction.py -x -v 2>&1 | tail -40` to confirm the RED state. Expected failure modes: `ModuleNotFoundError: src.artiscrapper.challenge_backoff` on the first 5 tests. NOTE: `test_catalog_price_extraction_rate_above_85_percent` may pass at this checkpoint if Phase 2 visit.py::extract_product already meets the bar, and `test_llm_down_keeps_useful_results` may pass standalone (the heuristic path doesn't actually depend on challenge_backoff.py). These standalone-passing tests are INFORMATIONAL ONLY — they do NOT satisfy the GREEN gate for this plan. The authoritative GREEN gate runs at Task 3 acceptance criteria AFTER all wiring lands. Executor MUST NOT declare "already green" and skip the empirical retest gate. Per Phase 2 memory `feedback_empirical_retest_after_default_changes`: tests passing in one configuration do NOT prove the value reaches the hot path in the wired configuration.

    Step 8 — Phase 2 regression check: `uv run pytest tests/test_cache.py -x -q` must STILL exit 0 — the DDL extension is additive (challenge_state table created on init_schema; query_cache table unchanged). If this fails, the appended DDL has a syntax error.
  </action>
  <verify>
    <automated>
      cd /home/luis/proyectos/artiscrapper &&
      grep -q "CREATE TABLE IF NOT EXISTS challenge_state" src/artiscrapper/cache.py &&
      grep -q "CHECK (id = 1)" src/artiscrapper/cache.py &&
      grep -q "INSERT OR IGNORE INTO challenge_state" src/artiscrapper/cache.py &&
      test -f tests/fixtures/serp/sorry.html &&
      grep -q "sorry/index" tests/fixtures/serp/sorry.html &&
      grep -q "detected unusual traffic\|automated queries" tests/fixtures/serp/sorry.html &&
      test -f tests/test_challenge_backoff.py &&
      grep -q "test_exponential_curve" tests/test_challenge_backoff.py &&
      grep -q "test_reset_after_one_hour" tests/test_challenge_backoff.py &&
      test -f tests/integration/test_challenge_backoff.py &&
      grep -q "test_503_with_retry_after" tests/integration/test_challenge_backoff.py &&
      test -f tests/integration/test_degraded_mode.py &&
      grep -q "respx.head" tests/integration/test_degraded_mode.py &&
      test -f tests/integration/test_catalog_extraction.py &&
      grep -q "test_catalog_price_extraction_rate_above_85_percent" tests/integration/test_catalog_extraction.py &&
      uv run pytest tests/test_cache.py -x -q 2>&1 | tail -3 | grep -qE 'passed' &&
      ! uv run pytest tests/test_challenge_backoff.py tests/integration/test_challenge_backoff.py -x -q 2>&1 | tail -3 | grep -qE '^[0-9]+ passed'
    </automated>
  </verify>
  <acceptance_criteria>
    - cache.py _DDL extended with both the CREATE TABLE statement and the INSERT OR IGNORE seed for `challenge_state`. Verify: `grep -cE '(CREATE TABLE IF NOT EXISTS challenge_state|INSERT OR IGNORE INTO challenge_state)' src/artiscrapper/cache.py` returns `2`.
    - challenge_state table has the single-row constraint. Verify: `grep -cE 'CHECK \(id = 1\)' src/artiscrapper/cache.py` returns `1`.
    - init_schema function body is NOT modified (still `executescript(_DDL)` + commit — the DDL is multi-statement so executescript handles the new table). Verify by reading: function body has exactly the two existing lines.
    - tests/fixtures/serp/sorry.html exists, is <1KB, and contains both the `sorry/index` token (in title) AND a `detected unusual traffic`/`automated queries` token (in body). Verify: `wc -c tests/fixtures/serp/sorry.html` shows <1024.
    - All 4 new test files exist at their declared paths.
    - Failing test count: `uv run pytest tests/test_challenge_backoff.py tests/integration/test_challenge_backoff.py tests/integration/test_degraded_mode.py -x -q` exits non-zero with at least 5 failures/errors (the 5 tests that depend on challenge_backoff.py). test_catalog_extraction may pass if Phase 2 extract_product already meets ≥85%; test_llm_down_keeps_useful_results may pass standalone. STANDALONE PASSES AT THIS CHECKPOINT ARE INFORMATIONAL ONLY — they do NOT close the GREEN gate. The authoritative GREEN gate is enforced at Task 3 acceptance criteria.
    - Phase 2 regression: `uv run pytest tests/test_cache.py -x -q` exits 0 (DDL extension is additive). Verify also that the `tmp_db` fixture in `tests/conftest.py` now creates a challenge_state table on init_schema — query the in-memory db inside a quick `pytest --collect-only` is sufficient (or run the new test_state_persists_to_sqlite as a quick sanity check after task 2 lands the module).
    - No production source file in `src/artiscrapper/` besides cache.py was edited (Task 1 scope is DDL + tests + fixture only).
  </acceptance_criteria>
  <done>
    cache.py _DDL extended with challenge_state table + seed; sorry.html fixture committed (per D-20); 4 new test files (1 unit + 3 integration) ship in RED state; Phase 2 cache tests still GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Wave 1): challenge_backoff.py module (pure module — module-singleton + sqlite UPSERT, no FastAPI wiring yet)</name>
  <files>src/artiscrapper/challenge_backoff.py</files>
  <read_first>
    - src/artiscrapper/llm.py lines 93-141 (Pattern S1 verbatim source — _RESOLVED_MODEL + _RESOLVE_LOCK + double-checked-lock idiom in resolve_model)
    - src/artiscrapper/cache.py lines 87-116 (Pattern S2 verbatim source — INSERT OR REPLACE + parameterized placeholders + immediate commit in set_cached)
    - src/artiscrapper/cache.py _DDL (Task 1 output — challenge_state schema)
    - tests/test_challenge_backoff.py (Task 1 RED tests — drives the GREEN implementation)
    - .planning/phases/03-robustness/03-PATTERNS.md §"src/artiscrapper/challenge_backoff.py" (verbatim shape; the planner-authoritative module skeleton)
    - .planning/phases/03-robustness/03-RESEARCH.md §D2 (full code listing for challenge_backoff.py)
  </read_first>
  <behavior>
    - tests/test_challenge_backoff.py 3 unit tests turn GREEN: test_exponential_curve, test_reset_after_one_hour, test_state_persists_to_sqlite
    - The module is pure (no FastAPI, no app.state) — entry points are `check_gate(cache)`, `record_block(cache)`, `record_success(cache)`, each taking an aiosqlite.Connection as the only argument.
    - Module is import-safe in tests: `from src.artiscrapper.challenge_backoff import check_gate, record_block, record_success, _STATE, _BASE_S, _BACKOFF_CAP_S, _RESET_AFTER_S` succeeds.
  </behavior>
  <action>
    Create `src/artiscrapper/challenge_backoff.py` per PATTERNS.md §"src/artiscrapper/challenge_backoff.py" and 03-RESEARCH.md §D2 verbatim. Required exports and behavior (per D-05/D-06/D-07/D-08; do NOT thread constants through settings per D-06 ROADMAP-lock + Pitfall 6 — they are hardcoded module constants):

    - `import asyncio`, `import time`, `import aiosqlite`, `from dataclasses import dataclass`.
    - Module-level constants per D-06 + D-07: `_BASE_S = 60`, `_BACKOFF_CAP_S = 3600`, `_RESET_AFTER_S = 3600`. Underscore-prefixed so they're treated as private but introspectable for the lifespan log line `challenge_backoff_init` (Task 3).
    - `@dataclass class _State` with three fields: `last_block_at: int | None`, `retry_count: int`, `next_allowed_at: int`. NOT exported — internal to the module.
    - Module-level singleton state: `_STATE: _State | None = None` (None before first _load). `_LOCK = asyncio.Lock()`. These are the Pattern S1 analog of `_RESOLVED_MODEL` + `_RESOLVE_LOCK` in llm.py.
    - `async def _load(cache: aiosqlite.Connection) -> _State`: double-checked-lock loader per llm.py:117-141. Outside lock check returns cached `_STATE` if set; inside lock re-checks (DCL guards against race in concurrent first calls); on miss, runs `SELECT last_block_at, retry_count, next_allowed_at FROM challenge_state WHERE id=1`; if no row (shouldn't happen because Task 1 seeds via INSERT OR IGNORE), initializes a fresh `_State(last_block_at=None, retry_count=0, next_allowed_at=0)`. Sets global `_STATE` and returns.
    - `async def _persist(cache: aiosqlite.Connection) -> None`: caller MUST hold `_LOCK`. Asserts `_STATE is not None`. Runs `INSERT OR REPLACE INTO challenge_state (id, last_block_at, retry_count, next_allowed_at, updated_at) VALUES (1, ?, ?, ?, ?)` with `(_STATE.last_block_at, _STATE.retry_count, _STATE.next_allowed_at, int(time.time()))` parameters (Pattern S2 — NEVER string interpolation). Immediately `await cache.commit()`.
    - `async def check_gate(cache: aiosqlite.Connection) -> tuple[bool, int]`: public entry. Calls `_load(cache)` (no lock acquired — read-only). Reads `state.next_allowed_at`; returns `(False, state.next_allowed_at - now)` if gate is closed (retry_after is a positive int), else `(True, 0)`. Called BEFORE every Google fetch (Task 3 wires it into /search).
    - `async def record_block(cache: aiosqlite.Connection) -> None`: public entry. Per D-06 curve. Acquires `_LOCK`. Loads state. Increments `state.retry_count`. Computes `wait_s = min(_BASE_S * (2 ** (state.retry_count - 1)), _BACKOFF_CAP_S)`. Sets `state.last_block_at = now` and `state.next_allowed_at = now + wait_s`. Calls `_persist(cache)`.
    - `async def record_success(cache: aiosqlite.Connection) -> None`: public entry. Per D-07. Acquires `_LOCK`. Loads state. If `state.last_block_at is None`: return (no-op — never blocked). Else if `now - state.last_block_at >= _RESET_AFTER_S` (3600s): reset `state.retry_count = 0`, `state.next_allowed_at = 0`, `state.last_block_at = None`, then `_persist(cache)`. Otherwise no-op (recent success doesn't clear an active backoff).
    - No top-level side effects — module import does NOT touch sqlite (state is lazy-loaded on first call).
    - All callable signatures use `aiosqlite.Connection` type (NOT `app.state.cache` — the caller in main.py passes `request.app.state.cache`, but the module is testable in isolation via the `tmp_db` fixture per Pattern S5).

    Run `uv run pytest tests/test_challenge_backoff.py -x -v` and confirm all 3 RED tests from Task 1 are now GREEN. If `test_reset_after_one_hour` fails, the most likely cause is the `time.time` monkeypatch not reaching the module — check that the test patches `src.artiscrapper.challenge_backoff.time.time` (not `time.time` globally) per Phase 2 monkeypatch idiom.
  </action>
  <verify>
    <automated>
      cd /home/luis/proyectos/artiscrapper &&
      test -f src/artiscrapper/challenge_backoff.py &&
      grep -q "^_BASE_S = 60$" src/artiscrapper/challenge_backoff.py &&
      grep -q "^_BACKOFF_CAP_S = 3600$" src/artiscrapper/challenge_backoff.py &&
      grep -q "^_RESET_AFTER_S = 3600$" src/artiscrapper/challenge_backoff.py &&
      grep -q "async def check_gate" src/artiscrapper/challenge_backoff.py &&
      grep -q "async def record_block" src/artiscrapper/challenge_backoff.py &&
      grep -q "async def record_success" src/artiscrapper/challenge_backoff.py &&
      grep -q "INSERT OR REPLACE INTO challenge_state" src/artiscrapper/challenge_backoff.py &&
      grep -q "min(_BASE_S \* (2 \*\*" src/artiscrapper/challenge_backoff.py &&
      grep -q "_LOCK = asyncio.Lock()" src/artiscrapper/challenge_backoff.py &&
      uv run pytest tests/test_challenge_backoff.py -x -q 2>&1 | tail -3 | grep -qE '^[3-9] passed' &&
      uv run pytest tests/test_cache.py tests/test_llm.py -x -q 2>&1 | tail -3 | grep -qE 'passed'
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/artiscrapper/challenge_backoff.py` exists.
    - Module exports `check_gate`, `record_block`, `record_success`, `_STATE`, `_LOCK`, `_BASE_S`, `_BACKOFF_CAP_S`, `_RESET_AFTER_S`. Verify: `python3 -c "from src.artiscrapper.challenge_backoff import check_gate, record_block, record_success, _STATE, _LOCK, _BASE_S, _BACKOFF_CAP_S, _RESET_AFTER_S; print('ok')"` prints `ok`.
    - Constants are exactly per D-06 + D-07. Verify literal: `grep -cE '^_BASE_S = 60$|^_BACKOFF_CAP_S = 3600$|^_RESET_AFTER_S = 3600$' src/artiscrapper/challenge_backoff.py` returns `3`.
    - Backoff curve formula present. Verify: `grep -cE 'min\(_BASE_S \* \(2 \*\*' src/artiscrapper/challenge_backoff.py` returns `1`.
    - sqlite UPSERT uses parameterized placeholders (NOT string interpolation — Phase 2 lesson T-02-01-02). Verify: `grep -E 'INSERT OR REPLACE INTO challenge_state' src/artiscrapper/challenge_backoff.py | grep -c 'VALUES (1, \?, \?, \?, \?)'` returns `1`. And: `grep -E 'INSERT OR REPLACE INTO challenge_state' src/artiscrapper/challenge_backoff.py | grep -cE 'f"|%s|\.format'` returns `0`.
    - Double-checked-lock pattern mirrors llm.py. Verify: `grep -cE 'if _STATE is not None' src/artiscrapper/challenge_backoff.py` returns ≥2 (one outside lock, one inside).
    - All 3 unit tests from Task 1 GREEN: `uv run pytest tests/test_challenge_backoff.py -x -q` exits 0 with at least 3 passed.
    - Phase 2 regression: `uv run pytest tests/test_cache.py tests/test_llm.py -x -q` still exits 0.
    - Module is import-safe with no side effects: `python3 -c "import src.artiscrapper.challenge_backoff; print(src.artiscrapper.challenge_backoff._STATE)"` prints `None` (lazy load — not eagerly populated at import).
  </acceptance_criteria>
  <done>
    challenge_backoff.py module shipping — 3 RED unit tests now GREEN; module is pure (no FastAPI dependency); integration tests still RED (Task 3 wires them).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (Wave 2): main.py /search wiring (check_gate before fetch + record_block/record_success after fetch + challenge_backoff_init lifespan log line) + integration tests GREEN</name>
  <files>src/artiscrapper/main.py</files>
  <read_first>
    - src/artiscrapper/main.py (current /search pipeline — note line numbers for cache lookup ~287-299, block-detection branch ~334-348, success path after step [3] passes)
    - src/artiscrapper/challenge_backoff.py (Task 2 output — check_gate, record_block, record_success signatures)
    - tests/integration/test_challenge_backoff.py (Task 1 RED — drives this task to GREEN)
    - tests/integration/test_degraded_mode.py (Task 1 — may already be GREEN since it doesn't need main.py changes; verify)
    - tests/integration/test_catalog_extraction.py (Task 1 — may already be GREEN since Phase 2 extract_product likely meets ≥85%; verify)
    - .planning/phases/03-robustness/03-PATTERNS.md §"src/artiscrapper/main.py" (Phase 3 lifespan + /search route additions — verbatim placement guidance)
    - .planning/phases/03-robustness/03-RESEARCH.md §D3 (challenge gate wiring + 503 + Retry-After header semantics)
    - .planning/phases/03-robustness/03-RESEARCH.md §G1 (D-19 log-line gate — the 4th line `challenge_backoff_init` lands here)
  </read_first>
  <behavior>
    - tests/integration/test_challenge_backoff.py 2 tests turn GREEN: test_503_with_retry_after, test_state_survives_restart, test_sorry_fixture_triggers_detect_block
    - tests/integration/test_degraded_mode.py turns GREEN (or was already GREEN if no main.py dep)
    - tests/integration/test_catalog_extraction.py turns GREEN (or was already GREEN)
    - Lifespan emits the 4th D-19 log line `challenge_backoff_init` with base_s/cap_s/reset_after_s values
    - Full suite GREEN: `pytest tests/ -x -q` exits 0
    - Empirical retest gate: container logs show all 4 D-19 lines (api_keys_loaded from 03-01 + sentry_init_* from 03-01 + rate_limit_init from 03-01 + challenge_backoff_init from this task)
  </behavior>
  <action>
    Step 1 — Add Phase 3 challenge_backoff import + lifespan log line in `src/artiscrapper/main.py`:
    - Add import alongside existing Phase 3 imports: `from . import challenge_backoff` (module import — needed to read _BASE_S etc. for the log line) AND `from .challenge_backoff import check_gate, record_block, record_success` (named imports for the /search route).
    - In the lifespan function, after the three Plan 03-01 log lines (`api_keys_loaded`, `sentry_init_*`, `rate_limit_init`) but BEFORE `boot_done`, add:
      `log.info("challenge_backoff_init", base_s=challenge_backoff._BASE_S, cap_s=challenge_backoff._BACKOFF_CAP_S, reset_after_s=challenge_backoff._RESET_AFTER_S)` per D-18/D-19 + 03-RESEARCH.md §G1. This makes the loaded constants empirically verifiable in `docker compose logs` after recreate — the foot-gun from Phase 2 memory `feedback_empirical_retest_after_default_changes`.

    Step 2 — Wire the challenge-backoff gate in `/search` per D-09 + PATTERNS.md §"src/artiscrapper/main.py":
    - INSERT a new step [1.5] AFTER the cache-hit early-return (lines ~287-299) AND AFTER the `with search_elapsed.time():` context manager opened by Plan 03-01 (so 503 responses STILL record latency on the search_elapsed Histogram) AND AFTER the `verify_api_key` Depends from Plan 03-01 has resolved (which happens implicitly because the function body only executes after FastAPI runs the dependency). The check_gate call is the first non-cache-hit, non-fetch line inside the `with search_elapsed.time():` block. The structure:
      `allowed, retry_after = await check_gate(request.app.state.cache)` — read-only check on the in-memory `_STATE` (sqlite query only on first call per request lifetime, cached in module-level `_STATE` thereafter).
      `if not allowed:` → log a structured warning `log.warning("challenge_backoff_active", retry_after=retry_after)` (OBS-05: no sorry-page content in the log), build the same SearchResponse shape that the existing block-detected branch returns (query, empty results list, Metadata with `cache_hit=False`, `block_detected=True`, `elapsed_ms` computed from `t_start`), then return a FastAPI `Response(status_code=503, content=response.model_dump_json(), media_type="application/json", headers={"Retry-After": str(retry_after)})`. CRITICAL — use `Response` not `HTTPException`, because HTTPException's headers parameter doesn't preserve the response_model JSON shape (per 03-RESEARCH.md §D3).

    Step 3 — Wire `record_block` and `record_success` in the existing block-detection branch (line ~334-348):
    - In the branch where `block_a = _detect_block(html_a)` or `block_b = _detect_block(html_b)` returns a non-None reason: BEFORE the existing 503 return (which sets metadata.block_detected=True), add `await record_block(request.app.state.cache)`. This persists the block to challenge_state and computes the next backoff window (D-08).
    - On the success path — AFTER both `_detect_block` calls return None (no block) but BEFORE the parser cascade (step [4]) — add `await record_success(request.app.state.cache)`. D-07: this is a no-op unless ≥1h has passed since last_block_at, in which case it resets retry_count to 0.

    Step 4 — Empirical retest per Phase 2 memory `feedback_empirical_retest_after_default_changes` + `feedback_compose_build_recreate`:
    - `docker compose build` (separate step).
    - `docker compose up -d --force-recreate` (separate step — NEVER use the combined `up -d --build`).
    - Wait ≤30s for boot, then run `docker compose logs artiscrapper 2>&1 | grep -cE "(api_keys_loaded|sentry_init_(done|skipped)|rate_limit_init|challenge_backoff_init)"` — must return ≥4 (all four D-19 lines now visible: 3 from Plan 03-01 + 1 from this task). If only 3 match, the new log line didn't reach the hot path — debug with `docker compose logs artiscrapper 2>&1 | grep challenge_backoff` to see whether the line was emitted at all.

    Step 5 — Trigger an end-to-end backoff cycle smoke test (D-08 manual UAT — automate per Luis's preference per MEMORY `feedback_agent_as_uat_operator`):
    - With container running, fire a curl POST /search with a valid X-API-Key and a synthetic query that you can intercept. Better: monkeypatch isn't accessible at runtime, so instead use the integration test as the gate (test_state_survives_restart already verifies the persistence behavior at the unit level — that's sufficient).
    - Run `docker compose exec artiscrapper sqlite3 /app/cache.db 'SELECT * FROM challenge_state;'` to confirm the seed row exists (id=1, retry_count=0, next_allowed_at=0). This empirically verifies Task 1's DDL extension reached the running container (D-19 spirit applied to DDL).

    Step 6 — Re-run the standalone-passing tests from Task 1 baseline (empirical retest gate). Per Phase 2 memory `feedback_empirical_retest_after_default_changes`: tests passing in one configuration do NOT prove the value reaches the hot path in the wired configuration. Run BOTH `test_llm_down_keeps_useful_results` and `test_catalog_price_extraction_rate_above_85_percent` AFTER all Task 3 main.py wiring has landed, regardless of whether they passed at Task 1 baseline.

    Step 7 — Run the full suite: `uv run pytest tests/ -x -q` MUST exit 0. If any Phase 2 test regresses, the most likely cause is the new /search step [1.5] returning 503 in a test environment where the test expected the existing block-detection branch — check that the test_health.py mock_browser pathway doesn't trip the gate.

    Step 8 — Run integration suite explicitly: `uv run pytest tests/integration/ -v 2>&1 | tail -20`. All 6+ integration tests across both plans (3 from this plan + 2 from Plan 03-01's test_metrics_endpoint and 1 from test_rate_limit) must be GREEN.
  </action>
  <verify>
    <automated>
      cd /home/luis/proyectos/artiscrapper &&
      grep -q "from . import challenge_backoff" src/artiscrapper/main.py &&
      grep -q "from .challenge_backoff import" src/artiscrapper/main.py &&
      grep -q "await check_gate(" src/artiscrapper/main.py &&
      grep -q "await record_block(" src/artiscrapper/main.py &&
      grep -q "await record_success(" src/artiscrapper/main.py &&
      grep -q "challenge_backoff_init" src/artiscrapper/main.py &&
      grep -q 'Retry-After' src/artiscrapper/main.py &&
      grep -q "challenge_backoff_active" src/artiscrapper/main.py &&
      uv run pytest tests/integration/test_degraded_mode.py tests/integration/test_catalog_extraction.py -x -q 2>&1 | tail -3 | grep -qE 'passed' &&
      uv run pytest tests/ -x -q 2>&1 | tail -3 | grep -qE 'passed' &&
      uv run pytest tests/integration/ -v 2>&1 | tail -3 | grep -qE 'passed'
    </automated>
  </verify>
  <acceptance_criteria>
    - main.py imports challenge_backoff symbols. Verify: `grep -cE 'from \.challenge_backoff import|from . import challenge_backoff' src/artiscrapper/main.py` returns ≥1 (combined import) or 2 (separate imports). Both forms acceptable.
    - main.py calls `await check_gate(...)` BEFORE the Google fetch dispatch. Verify by structural inspection: locate the cache-hit early-return block; the `check_gate` call appears AFTER it but BEFORE any `fetch_serp` / `app.state.browser` call. `grep -c "await check_gate(" src/artiscrapper/main.py` returns ≥1.
    - main.py calls `await record_block(...)` in the existing block-detection branch (line ~334-348 region). Verify: `grep -c "await record_block(" src/artiscrapper/main.py` returns ≥1. The call must be in the SAME branch that sets `metadata.block_detected=True` (verify by reading 5 lines of context around the grep match).
    - main.py calls `await record_success(...)` on the success path. Verify: `grep -c "await record_success(" src/artiscrapper/main.py` returns ≥1.
    - main.py 503 response carries Retry-After header. Verify: `grep -E 'Retry-After' src/artiscrapper/main.py | grep -cE 'str\(retry_after\)|str\(.*retry_after\)'` returns ≥1.
    - Lifespan emits `challenge_backoff_init` log line. Verify: `grep -E 'log\.info\("challenge_backoff_init"' src/artiscrapper/main.py | grep -cE '(base_s|cap_s|reset_after_s)'` returns ≥1.
    - Empirical retest (D-19): `docker compose build` succeeds, `docker compose up -d --force-recreate` succeeds, and `docker compose logs artiscrapper 2>&1 | grep -cE "(api_keys_loaded|sentry_init_(done|skipped)|rate_limit_init|challenge_backoff_init)"` returns ≥4. NEVER use `docker compose up -d --build` (Phase 2 memory `feedback_compose_build_recreate`).
    - challenge_state seed row exists in the running container: `docker compose exec artiscrapper sqlite3 /app/cache.db 'SELECT COUNT(*) FROM challenge_state WHERE id=1;'` returns `1`.
    - **EMPIRICAL RETEST GATE (Phase 2 memory `feedback_empirical_retest_after_default_changes`):** Both `test_llm_down_keeps_useful_results` and `test_catalog_price_extraction_rate_above_85_percent` run GREEN AFTER Task 3 wiring lands (NOT just at Task 1 baseline). Executor MUST re-run these post-wiring even if they passed standalone — the empirical retest gate proves the wired configuration matches the baseline configuration. Verify: `uv run pytest tests/integration/test_degraded_mode.py tests/integration/test_catalog_extraction.py -x -q 2>&1 | tail -3 | grep -qE 'passed'` exits 0 AFTER all Task 3 main.py edits have landed.
    - All Phase 3 integration tests GREEN: `uv run pytest tests/integration/ -v -q 2>&1 | tail -3 | grep -qE '^[5-9] passed|^[1-9][0-9] passed'` succeeds.
    - Full suite GREEN: `uv run pytest tests/ -x -q 2>&1 | tail -3 | grep -qE 'passed'` succeeds. No skips, no failures, no errors.
    - test_503_with_retry_after specifically verifies the end-to-end wiring. Verify by running it alone: `uv run pytest tests/integration/test_challenge_backoff.py::test_503_with_retry_after -x -v` exits 0.
  </acceptance_criteria>
  <done>
    /search route gated by check_gate before fetch; record_block on the existing block branch; record_success on success path; 4th D-19 log line `challenge_backoff_init` empirically verified in container logs. All Phase 3 integration tests GREEN; full suite GREEN; sqlite challenge_state row verified in container; empirical retest gate on test_llm_down_keeps_useful_results + test_catalog_price_extraction_rate_above_85_percent run AFTER wiring per Phase 2 memory.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google SERP fetch ←→ Cloak browser | Untrusted HTML payload crosses into the container; _detect_block + ChallengeBackoff consume it to drive backoff scheduler |
| /search route → consumer (503 + Retry-After) | Structured signal (NOT silent timeout) crosses outbound boundary; consumer learns to back off |
| sqlite challenge_state row ←→ container restart | Persistent state survives `docker compose up --force-recreate` (D-08) — restart cannot accidentally reset and re-trigger a block |
| Phase 1 labelled.jsonl / catalog fixtures → test runner | Trusted fixture data; sweep is in-process (no HTTP) — extract_product is a pure function |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02-01 | Denial of Service | Google SERP block detection (block amplification) | HIGH | mitigate | ChallengeBackoff state machine implements exponential backoff `min(60 * 2^(retries-1), 3600)` per D-06; module-level `_STATE` + `asyncio.Lock` ensure concurrent /search requests see linearizable updates; D-07 reset trigger (≥1h after last_block_at) lets the system recover without manual intervention. Maps to T-google-block-amplification from 03-VALIDATION.md. Prevents retry storms that would worsen the block. Acceptance ref: Task 1 `test_exponential_curve` + Task 3 `test_503_with_retry_after`. |
| T-03-02-02 | Tampering / Loss of Integrity | challenge_state across container restart (state loss on restart) | MEDIUM | mitigate | sqlite single-row table with `CHECK(id=1)` constraint per D-08; persisted via `INSERT OR REPLACE` with parameterized placeholders (Pattern S2 from cache.py:106-116 — Phase 2 lesson T-02-01-02 forbids string interpolation). `INSERT OR IGNORE` seed at init_schema makes the row idempotent across restarts. Maps to T-state-loss-on-restart. Acceptance ref: Task 1 `test_state_persists_to_sqlite` + Task 3 `test_state_survives_restart` + Task 3 `docker compose exec sqlite3 challenge_state` verify. |
| T-03-02-03 | Denial of Service (silent) | /search timeout under block (silent timeout / silent degradation) | MEDIUM | mitigate | When in backoff, /search returns 503 + `metadata.block_detected=true` + `Retry-After: <seconds>` header (D-09) — NOT a silent timeout. Consumer learns the wait interval and can respect it. Maps to T-silent-degradation. Acceptance ref: Task 3 `test_503_with_retry_after` + structural grep on `Retry-After` header. |
| T-03-02-04 | Denial of Service | LLM router outage cascades to /search (LLM-down cascade) | HIGH | mitigate | `router_health_check` HEAD /healthz failure flips /search to the degraded path: heuristic blocklist + price-in-card filter (LLM-06 from Phase 2). Plan 03-02 integration test `test_llm_down_keeps_useful_results` PINS that ≥5 useful results from Phase 1's 50-record labelled set survive without LLM. Maps to T-llm-router-outage. Hardens LLM-06 acceptance from Phase 2. Acceptance ref: Task 3 acceptance criterion enforces post-wiring re-run of `test_llm_down_keeps_useful_results` (empirical retest gate). |
| T-03-02-05 | Tampering | extract_product regression (Google HTML rotation) (extractor regression) | MEDIUM | mitigate | Integration test `test_catalog_price_extraction_rate_above_85_percent` runs in-process over all 10 Phase 1 catalog fixtures; asserts ≥85% yield a non-empty price (validates D12 hand-roll decision). Per-fixture `(filename, has_price, price_value)` details list surfaces regressions to the specific host that broke (D-18 — per-fixture assertion, not just aggregate count). Maps to T-extractor-regression. Acceptance ref: Task 3 acceptance criterion enforces post-wiring re-run of `test_catalog_price_extraction_rate_above_85_percent` (empirical retest gate). |
| T-03-02-06 | Information Disclosure | sorry-page content in logs | MEDIUM | mitigate | `challenge_backoff_active` log line carries only `retry_after` (a numeric seconds value); sorry-page HTML content NEVER appears in any log event (OBS-05 alignment). The synthetic `tests/fixtures/serp/sorry.html` is checked in but is a Claude-authored stand-in (Phase 1 captured no real /sorry/ per SPIKE.md §Browser) — no real Google content, no PII, no risk. |
| T-03-02-07 | Spoofing | Test fixture authenticity | LOW | accept | `tests/fixtures/serp/sorry.html` is a synthetic stand-in (D-20 says: real /sorry/ if Phase 1 captured one, else minimal stand-in). The test `test_sorry_fixture_triggers_detect_block` pins that _detect_block(fixture) is non-None — if Google rotates the /sorry/ HTML so badly that the stand-in stops matching but real Google pages do, the integration test will keep passing while production stops backing off. Mitigated by Phase 4's planned telemetry: `artiscrapper_block_detected_total{reason}` time series (from Plan 03-01) will show the actual marker reasons hitting production. |
| T-03-02-08 | Default Shadowing | _BASE_S/_BACKOFF_CAP_S/_RESET_AFTER_S not reaching the hot path (default shadowing) | MEDIUM | mitigate | Per D-06 ROADMAP-lock + Pitfall 6, these are hardcoded module-level constants (NOT threaded through settings — eliminates the settings/contract/route shadowing class of bugs that bit Phase 2 LLM_MODEL). The lifespan `challenge_backoff_init` log line declares the loaded values empirically (D-19) — `docker compose logs | grep challenge_backoff_init` verifies the running container's values match expectations. Maps to T-default-shadowing. |
| T-03-02-09 | Race Condition | concurrent /search requests trip block simultaneously | MEDIUM | mitigate | record_block acquires `_LOCK` (asyncio.Lock from Pattern S1) before mutating _STATE; subsequent waiters see the post-mutation state via double-checked-load in _load. retry_count increments are linearizable — no double-increment under concurrent block detection. uvicorn `--workers 1 --loop asyncio` (D6 invariant) ensures single-process single-event-loop — no GIL/threading concerns. |
| T-03-02-10 | Repudiation | block-detection audit trail | LOW | accept | `block_detected_total{reason}` Prometheus counter (from Plan 03-01) records every block event by marker reason. `challenge_state.updated_at` records the latest persistence timestamp for forensics. correlation_id propagates into the 503 response and Sentry tags (Plan 03-01) — joinable to structlog logs. Sufficient for single-client deploy; per-key block attribution deferred to Phase 5 (MULTI-03). |
</threat_model>

<verification>
**End-to-end Plan 03-02 verification:**

1. `pytest tests/ -x -q` exits 0 (full suite green; includes both 03-01 and 03-02 tests if 03-01 has landed; standalone for 03-02 if not).
2. `pytest tests/integration/ -v` exits 0 with all integration tests passing (test_metrics_endpoint × 2, test_rate_limit, test_challenge_backoff × 3, test_degraded_mode, test_catalog_extraction = 8 tests minimum).
3. `docker compose exec artiscrapper sqlite3 /app/cache.db 'SELECT * FROM challenge_state;'` shows seed row `(1, NULL, 0, 0, <epoch>)` after fresh recreate.
4. `docker compose logs artiscrapper 2>&1 | grep challenge_backoff_init` shows the line with base_s=60, cap_s=3600, reset_after_s=3600 — empirical retest gate (D-19).
5. Manual UAT step (Luis-as-operator, automated by Claude per MEMORY `feedback_agent_as_uat_operator`): inject sorry.html via monkeypatched fetch_serp through a debug code path or rely on `test_503_with_retry_after`; verify 503 response includes `Retry-After` numeric header. Then `docker compose down && docker compose up -d --force-recreate`; verify `challenge_state` row preserves last_block_at (manual UAT entry in 03-HUMAN-UAT.md after phase verify).
6. Phase 1 D12 production gate: `pytest tests/integration/test_catalog_extraction.py::test_catalog_price_extraction_rate_above_85_percent -x -v` exits 0 — confirms ≥85% catalog price extraction continues to hold.
7. LLM-06 hardening pinned: `pytest tests/integration/test_degraded_mode.py -x -v` exits 0 with ≥5 useful survivors from the 50-record labelled set.
</verification>

<success_criteria>
- ROADMAP §Phase 3 success criterion 4 PROVEN: backoff curve unit test pins `min(60 * 2^retries, 3600)`; integration test fires 503 + Retry-After when sorry.html is injected; sqlite state survives restart.
- ROADMAP §Phase 3 success criterion 5 PROVEN: respx-mocked router /healthz failure + ≥5 useful results from Phase 1 labelled.jsonl heuristic-only path (LLM-06 hardening).
- ROADMAP §Phase 3 success criterion 6 PROVEN: ≥85% of Phase 1 catalog fixtures yield a non-empty price via in-process extract_product (D12 validation).
- Phase 2 memory landmines respected: `docker compose build` + `docker compose up -d --force-recreate` (never combined); the 4th D-19 log line `challenge_backoff_init` empirically verified in container logs alongside Plan 03-01's three lines.
- D-18 fixture-driven assertions: integration tests pin per-fixture data (catalog details list, labelled.jsonl survivor count) — never `len(x) > 0` shape checks.
- D-20 synthetic /sorry/ fixture committed and pinned by `test_sorry_fixture_triggers_detect_block`.
- Phase 2 regression: test_cache.py, test_llm.py, test_parser.py, test_visit.py, test_health.py, test_e2e.py all still GREEN.
- challenge_backoff.py is import-safe + side-effect-free; module-level state lazy-loaded on first call.
</success_criteria>

<output>
Create `.planning/phases/03-robustness/03-02-SUMMARY.md` when done. Include:
- Tasks completed with the per-task automated verify outputs
- The 4 D-19 log lines as captured from `docker compose logs` (paste exact strings — confirms both Plan 03-01 and 03-02 landed correctly)
- `pytest tests/ -x -q` total time + pass count
- `pytest tests/integration/ -v` per-test outcomes (must show 8+ integration tests all green)
- Output of `docker compose exec artiscrapper sqlite3 /app/cache.db 'SELECT * FROM challenge_state;'` confirming seed row
- The catalog extraction rate measured by test_catalog_price_extraction_rate_above_85_percent — paste the per-fixture details list from the test's error message (or the success log if the test prints it on pass)
- The survivor count from test_llm_down_keeps_useful_results (e.g., "27/50 survivors with price_in_card from Phase 1 labelled set")
- Phase 2 WARNING items from 02-REVIEW.md status: note explicitly whether WR-01/WR-02/WR-04 were touched (these were [opportunistic] per CONTEXT.md §deferred — most likely NOT touched by this plan; document the decision)
</output>
