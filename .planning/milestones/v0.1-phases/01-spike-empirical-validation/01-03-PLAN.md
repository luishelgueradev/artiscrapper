---
phase: 01
plan: 01-03
type: execute
wave: 3
depends_on: ['01-01', '01-02']
files_modified:
  - scripts/spike/06_capture_catalog.py
  - scripts/spike/07_falabella_403.py
  - scripts/spike/08_extruct_classifier.py
  - tests/fixtures/catalog/MANIFEST.md
  - .planning/SPIKE.md
autonomous: true
requirements: []
gates_decisions: [D11, D12]
ac_covered: [AC-4, AC-6]
user_setup: []

must_haves:
  truths:
    - "D8 amended: router is OpenAI-compatible (POST /v1/chat/completions with Authorization: Bearer ...), NOT raw Ollama /api/chat. Brief 02-llm-curator.md is superseded on this point by 01-RESEARCH.md §Pattern 4."
    - "Phase 1 produces NO application code. Only scripts/spike/, tests/fixtures/, and .planning/SPIKE.md are written. Phase 2 will scaffold src/ and pyproject.toml."
    - "Bearer token + router URL live in .env.spike (gitignored). NEVER commit a real bearer token. If .env.spike is missing, the spike script must exit 2 with a hint pointing at scripts/spike/README.md."
    - "Each sub-spike plan's terminal task appends its SPIKE.md §<section> block from the captured artifacts — not from prose. The fill-in points are listed in 01-RESEARCH.md §SPIKE.md Template."
    - "10 raw catalog PDP HTML fixtures captured from 10 distinct AR hosts (Mercadolibre EXPLICITLY excluded per VISIT-08) under tests/fixtures/catalog/{host_slug}/product-NN.html with per-fixture MANIFEST entry (host, URL, capture date, byte count, classification)."
    - "Each fixture classified into exactly one of {jsonld-sufficient, og-only, microdata-only, regex-fallback, blob-JS-only} via the hand-rolled selectolax extractor lifted verbatim from RESEARCH §Pattern 5."
    - "D12 decision rule applied: ≥8/10 classified as (jsonld-sufficient OR og-only) → HAND-ROLL stays (D12 lock holds); else → NEEDS-PIVOT to extruct (D12 flips, Phase 2 plan 02-02 must add extruct==0.18.0 dependency)."
    - "Falabella + Mayorista Frog + Romero each probed with N≥10 httpx GETs using the verbatim DEFAULT_HEADERS (Sec-Fetch-Site: cross-site + Referer: https://www.google.com/ — D11 invariant); per-host outcome ratio recorded as (200/403/5xx/timeout)/N."
    - "Throttle discipline: ≥30s between attempts to the same host (per RESEARCH §Pitfall 5, lines 765-769); aggregate cap of 40 visits across the plan to avoid IP-block escalation."
    - "Cleanup invariant: no Cloak/Chromium process leaks after capture; aggregate visit count to each host stays under 15 per spike day."
    - "SPIKE.md §Visit AND §D12 Decision sections are appended from artifact values (per-fixture classification table, 403-rate ratios, decision rationale), not from prose; each ends with 'Status: GO | NO-GO | NEEDS-PIVOT'."
  artifacts:
    - path: scripts/spike/06_capture_catalog.py
      provides: "Drives Cloak (headed) to N=10 AR catalog hosts, captures raw HTML to tests/fixtures/catalog/<host_slug>/product-NN.html, appends MANIFEST.md row per fixture"
    - path: scripts/spike/07_falabella_403.py
      provides: "httpx batch loop with DEFAULT_HEADERS + 30s spacing against Falabella + Frog + Romero; outputs per-host ratio table"
    - path: scripts/spike/08_extruct_classifier.py
      provides: "Loads each catalog fixture, runs hand-rolled JSON-LD + OG + microdata + regex extractor (verbatim from RESEARCH §Pattern 5), classifies into 5 tiers, prints decision-table row per fixture"
    - path: tests/fixtures/catalog/MANIFEST.md
      provides: "Per-fixture row (host, URL, capture_date, byte_count, classification, extracted_price, extracted_name) + per-host outcome aggregate"
    - path: .planning/SPIKE.md
      provides: "§Visit + §D12 Decision sections filled with per-fixture classifications, 403-rate ratios, decision rationale, Status lines"
      contains: "## Visit"
  key_links:
    - from: scripts/spike/06_capture_catalog.py
      to: "tests/fixtures/catalog/<host_slug>/product-NN.html"
      via: "page.content() -> Path(...).write_text(encoding='utf-8')"
      pattern: "tests/fixtures/catalog"
    - from: scripts/spike/07_falabella_403.py
      to: "10 PDP URLs per host (sourced from 01-01 SERPs OR scripts/spike/falabella_urls.txt etc.)"
      via: "httpx.AsyncClient(http2=True, headers=DEFAULT_HEADERS, follow_redirects=True)"
      pattern: "DEFAULT_HEADERS"
    - from: scripts/spike/08_extruct_classifier.py
      to: "tests/fixtures/catalog/**/*.html"
      via: "selectolax.HTMLParser + JSON-LD/OG/microdata/regex cascade"
      pattern: "classify"
    - from: ".planning/SPIKE.md §Visit + §D12 Decision"
      to: "artifacts/spike/catalog_*.txt + tests/fixtures/catalog/MANIFEST.md"
      via: "terminal task appends fenced blocks from captured outputs"
      pattern: "^## (Visit|D12 Decision)$"
  outputs:
    - "AC-4: test \"$(find tests/fixtures/catalog -name '*.html' | wc -l)\" -ge 10 (passes)"
    - "AC-6 partial: .planning/SPIKE.md §Visit AND §D12 Decision Status lines are concrete (GO | NO-GO | NEEDS-PIVOT, not scaffold)"
---

<objective>
Capture 10 raw catalog PDP HTML fixtures from distinct AR hosts (Mercadolibre EXCLUDED per VISIT-08), classify each via the hand-rolled JSON-LD/OG/microdata/regex extractor (RESEARCH §Pattern 5 verbatim), measure Falabella/Frog/Romero httpx 403 rates under realistic Chromium-146 headers (D11 invariant), and make the D12 decision (hand-roll stays or pivot to extruct).

Purpose: Phase 2 plan 02-02 (VISIT-06 + visit pass) cannot lock the extractor strategy without the fixture-sweep classification. If ≥8/10 fixtures are jsonld-sufficient or og-only → D12 hand-roll stays; else Phase 2 must add `extruct==0.18.0` to its pyproject and update VISIT-06 acceptance. The 403-rate measurements also tell Phase 2 whether `curl-cffi` deferral to Phase 3 holds.

Output: 10+ `tests/fixtures/catalog/<host>/product-NN.html` files; `tests/fixtures/catalog/MANIFEST.md` with per-fixture rows; `.planning/SPIKE.md §Visit` filled with the per-host outcome table; `.planning/SPIKE.md §D12 Decision` filled with HAND-ROLL / EXTRUCT verdict + 1-2 sentence rationale.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/research/03-visit-extract.md
@.planning/phases/01-spike-empirical-validation/01-RESEARCH.md
@.planning/phases/01-spike-empirical-validation/01-PATTERNS.md
@.planning/phases/01-spike-empirical-validation/01-VALIDATION.md

<interfaces>
<!-- All interfaces lifted verbatim from 01-RESEARCH.md. -->

Target host list (RESEARCH §Pattern 5 lines 519-528 — Mercadolibre EXCLUDED per VISIT-08):
| # | Host                           | Platform                    | Expected classification        |
|---|--------------------------------|-----------------------------|--------------------------------|
| 1 | mayoristafrog.com.ar           | custom / PrestaShop?        | unknown — fixture decides      |
| 2 | casasusy.com.ar                | Tiendanube (likely)         | jsonld-sufficient              |
| 3 | falabella.com.ar               | VTEX/proprietary            | jsonld-sufficient (403 risk on httpx) |
| 4 | romero-jugueteria.com.ar       | likely Tiendanube           | jsonld-sufficient              |
| 5 | one *.mitiendanube.com store   | Tiendanube                  | jsonld-sufficient              |
| 6 | musimundo.com OR garbarino.com | VTEX                        | jsonld-sufficient              |
| 7 | walmart.com.ar                 | proprietary                 | jsonld-sufficient              |
| 8 | one *.myshopify.com store      | Shopify                     | jsonld-sufficient (89% globally) |
| 9 | Distribuidora Romero           | discover via Google         | unknown                        |
| 10 | small custom catalog from SERPs| varies (long-tail test)    | unknown                        |

PDP URL discovery (RESEARCH §Open Questions Q4 lines 913-916): operator hand-curates URLs into `scripts/spike/catalog_urls.txt` (one URL per line, in the order matching the table above) using the SERPs captured in 01-01 OR direct Google search on the spike day.

DEFAULT_HEADERS (D11 invariant — verbatim from RESEARCH lines 623-638):
```
DEFAULT_HEADERS = {
    "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/146.0.0.0 Safari/537.36",
    "Accept": "text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,*/*;q=0.8",
    "Accept-Language": "es-AR,es;q=0.9,en;q=0.6",
    "Accept-Encoding": "gzip, deflate, br",
    "Sec-Ch-Ua": '"Chromium";v="146", "Not_A Brand";v="24"',
    "Sec-Ch-Ua-Mobile": "?0",
    "Sec-Ch-Ua-Platform": '"Windows"',
    "Sec-Fetch-Dest": "document",
    "Sec-Fetch-Mode": "navigate",
    "Sec-Fetch-Site": "cross-site",
    "Sec-Fetch-User": "?1",
    "Upgrade-Insecure-Requests": "1",
    "Referer": "https://www.google.com/",
}
```

Hand-rolled extractor (RESEARCH §Pattern 5 lines 532-604 — VERBATIM, do not modify):
- `extract_jsonld_product(tree)` — iterates `script[type="application/ld+json"]`, json.loads, flattens `@graph`, returns first dict with `@type == "Product"` (or list containing "Product").
- `extract_og_product(tree)` — reads `meta[property]`, returns dict if `product:price:amount` exists.
- `extract_microdata_product(tree)` — finds `[itemtype$='/Product']`, reads `[itemprop]` children.
- `classify(html)` returns tuple `(tier, data | None)` where tier ∈ {`jsonld-sufficient`, `og-only`, `microdata-only`, `regex-fallback`, `blob-JS-only`}.
- AR price regex (verbatim line 595): `r'\$\s?(\d{1,3}(?:\.\d{3})*(?:,\d{2})?)'`

D12 decision rule (RESEARCH lines 614-617 — verbatim threshold):
- ≥8/10 fixtures classify as `jsonld-sufficient` OR `og-only` → **HAND-ROLL** (D12 stays locked).
- 3+ fixtures classify as `microdata-only` OR `blob-JS-only` → **NEEDS-PIVOT to extruct** (D12 flips).
- Borderline (4-7/10 sufficient) → **NEEDS-PIVOT** + flag in Risks for more samples in Phase 2.

Cloak browser launch + ephemeral context (same as 01-01 task 2 — verbatim from RESEARCH lines 297-322):
- `pw.chromium.launch(headless=True, args=["--no-sandbox","--disable-dev-shm-usage"])`
- `browser.new_context(locale="es-AR", timezone_id="America/Argentina/Buenos_Aires", viewport={"width":1366,"height":768})`
- NEVER `launch_persistent_context`.

D6 invariant: every Python spike script runs `asyncio.run(...)`. NEVER `uvloop`.

VISIT-08 invariant: NEVER capture or visit `*.mercadolibre.*` URLs. The script's pre-flight asserts every URL in `catalog_urls.txt` does NOT match `re.search(r'mercadolibre\.', url)`; abort with exit 1 if any do.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Catalog fixture capture — 10 hosts, headed Cloak, raw HTML to tests/fixtures/catalog/ (AC-4)</name>
  <files>
    scripts/spike/06_capture_catalog.py,
    scripts/spike/catalog_urls.txt,
    tests/fixtures/catalog/MANIFEST.md,
    tests/fixtures/catalog/<host_slug>/product-01.html (x10),
    artifacts/spike/catalog_capture.log
  </files>
  <read_first>
    - .planning/phases/01-spike-empirical-validation/01-RESEARCH.md §"Pattern 5: Catalog fixture sweep + extraction probe" intro (lines 511-516); per-host target table (lines 519-528); §"Open Questions" Q4 (lines 913-916) — URL discovery is operator-driven on spike day.
    - .planning/phases/01-spike-empirical-validation/01-RESEARCH.md §"Pattern 1" (lines 267-322) — Cloak launch+context boilerplate (PATTERNS.md row for 06_capture_catalog.py at lines 65-67 says: "no verbatim code block exists for the capture loop itself — planner emits from scratch using §Pattern 1 (browser pattern) as the skeleton, swapping QUERIES for PDP URLs").
    - .planning/phases/01-spike-empirical-validation/01-RESEARCH.md §"Anti-Patterns to Avoid" (lines 711-714) — throttle, single-host-rate, IP-block protection.
    - .planning/phases/01-spike-empirical-validation/01-PATTERNS.md row for `scripts/spike/06_capture_catalog.py` (lines 65-67).
    - .planning/phases/01-spike-empirical-validation/01-VALIDATION.md row AC-4 (line 48).
    - .planning/research/03-visit-extract.md (extended per-host context — already loaded above in this plan).
    - scripts/spike/02_cloak_smoke.py (from 01-01 task 2) — for the Cloak import-discovery pattern + cleanup approach. DO NOT import; copy the launch idiom.
    - tests/fixtures/serp/*.html (from 01-01) — source for hand-curating URLs into catalog_urls.txt (look for product links from the 10 target hosts).
  </read_first>
  <action>
**Sub-step A — Seed `scripts/spike/catalog_urls.txt`:**

Operator (or Claude per memory `feedback_agent_as_uat_operator`) hand-curates one PDP URL per host into a plain-text file, one URL per line, in the order matching the RESEARCH host table:
```
# scripts/spike/catalog_urls.txt
# Lines starting with # are comments. Blank lines ignored. One URL per host.
# Order: mayoristafrog, casasusy, falabella, romero-jugueteria, tiendanube-sample,
#        vtex-sample (musimundo/garbarino), walmart-ar, shopify-sample,
#        distribuidora-romero, long-tail
https://www.mayoristafrog.com.ar/...
https://www.casasusy.com.ar/...
https://www.falabella.com.ar/falabella-ar/product/...
https://www.romero-jugueteria.com.ar/...
https://...mitiendanube.com/...
https://www.musimundo.com/...   (or garbarino)
https://www.walmart.com.ar/...
https://...myshopify.com/products/...
https://...   (Distribuidora Romero — discover via Google)
https://...   (long-tail — pick from 01-01 SERP)
```

Per memory `feedback_agent_as_uat_operator`: Claude drives the Google searches itself (using the queries from 01-01) and seeds the file; Luis reviews before commit. If a host genuinely cannot be located (e.g. Distribuidora Romero is not indexable), substitute another AR catalog host with a 1-line `# substituted because:` comment.

**Sub-step B — Create `scripts/spike/06_capture_catalog.py`:**

Self-contained script following the Cloak idiom from 01-01:
- Header + import-discovery (try cloakbrowser, fallback to playwright.async_api post-`import cloakbrowser`).
- `set_default_context()` factory builds an ephemeral context with the standard locale/timezone/viewport.
- Read `scripts/spike/catalog_urls.txt`, strip comments and blank lines.
- **Pre-flight VISIT-08 assertion**: for each URL, `assert "mercadolibre." not in url.lower()`; abort with exit 1 if any URL matches (the host is forbidden per VISIT-08 and the planner's `must_haves` invariant).
- For each URL:
  - Parse hostname → `host_slug` (e.g. `falabella.com.ar` → `falabella_ar`).
  - Create `tests/fixtures/catalog/{host_slug}/` directory.
  - Fresh ephemeral `new_context()`.
  - `page.goto(url, wait_until="domcontentloaded", timeout=20_000)`.
  - `html = await page.content()`.
  - Path `tests/fixtures/catalog/{host_slug}/product-01.html` (or `product-NN.html` if multiple URLs per host).
  - Append a row to `tests/fixtures/catalog/MANIFEST.md` (create file with header if absent):
    ```
    | host_slug | url | capture_date | byte_count | classification | extracted_price | extracted_name |
    |---|---|---|---|---|---|---|
    | falabella_ar | https://... | 2026-06-01 | 514823 | TBD | TBD | TBD |
    ```
    (`classification`, `extracted_price`, `extracted_name` columns are filled by task 3 — leave as `TBD` here.)
  - Close context.
  - **Throttle:** `await asyncio.sleep(30)` between hosts (per RESEARCH §Anti-Patterns line 712-714 — ≥30s spacing to avoid IP-block).
  - **Per-host cap:** never visit the same host more than once in this script.

- Cleanup in `finally`: `await browser.close()`; no zombie processes.
- Exit 0 if `find tests/fixtures/catalog -name '*.html' | wc -l` ≥ 10 after the run; exit 1 otherwise (allows operator to top up with a second invocation pointing at a different URL list).

**Sub-step C — Run capture:**
```bash
test -f scripts/spike/catalog_urls.txt || { echo "ERROR: catalog_urls.txt missing — see plan task 1 sub-step A"; exit 2; }
[ "$(grep -cE '^https?://' scripts/spike/catalog_urls.txt)" -ge 10 ] || { echo "ERROR: need ≥10 URLs in catalog_urls.txt"; exit 2; }
! grep -qE 'mercadolibre\.' scripts/spike/catalog_urls.txt || { echo "ERROR: VISIT-08 violation — mercadolibre URL detected"; exit 1; }

source .venv/bin/activate 2>/dev/null || true
python scripts/spike/06_capture_catalog.py 2>&1 | tee artifacts/spike/catalog_capture.log
```

If any host fails to load (timeout, 403 from the browser, redirect to login), record `byte_count=0, classification=load_failed` in MANIFEST.md and continue. Aim for 10 captured fixtures total; if only 8 succeed, top up by substituting in two more long-tail hosts.

D11 invariant carry-over (anticipates task 2): the captured fixtures are produced via Cloak (stealth-patched Chromium with full TLS fingerprint), so they represent the "best case" — task 2 measures the httpx degradation per host.
  </action>
  <verify>
    <automated>test -f scripts/spike/catalog_urls.txt && [ "$(grep -cE '^https?://' scripts/spike/catalog_urls.txt)" -ge 10 ] && ! grep -qE 'mercadolibre\.' scripts/spike/catalog_urls.txt && test -f scripts/spike/06_capture_catalog.py && ! grep -q 'launch_persistent_context' scripts/spike/06_capture_catalog.py && ! grep -q '^[^#]*uvloop' scripts/spike/06_capture_catalog.py && python scripts/spike/06_capture_catalog.py && [ "$(find tests/fixtures/catalog -name '*.html' | wc -l)" -ge 10 ] && test -f tests/fixtures/catalog/MANIFEST.md && [ "$(grep -cE '^\| [a-z0-9_]+ \|' tests/fixtures/catalog/MANIFEST.md)" -ge 10 ] && ! find tests/fixtures/catalog -name '*.html' | xargs -I{} dirname {} | grep -qE 'mercadolibre'</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/spike/catalog_urls.txt` exists with ≥10 lines matching `^https?://` (verified by `grep -cE '^https?://'`).
    - `scripts/spike/catalog_urls.txt` contains NO `mercadolibre.` substring (VISIT-08 invariant — verified by `! grep -qE 'mercadolibre\.' scripts/spike/catalog_urls.txt`).
    - `scripts/spike/06_capture_catalog.py` exists and exits 0.
    - `find tests/fixtures/catalog -name '*.html' | wc -l` ≥ 10 (AC-4 satisfied).
    - `tests/fixtures/catalog/MANIFEST.md` exists with ≥10 data rows (verified by `grep -cE '^\| [a-z0-9_]+ \|' tests/fixtures/catalog/MANIFEST.md` ≥ 10 — count actual host_slug rows, not the header/divider).
    - No fixture is under `tests/fixtures/catalog/mercadolibre*/` (VISIT-08 invariant carried into capture — verified by `! find tests/fixtures/catalog -name '*.html' | grep -qE '/mercadolibre'`).
    - The literal substring `launch_persistent_context` does NOT appear in `scripts/spike/06_capture_catalog.py`.
    - The literal substring `uvloop` does NOT appear in `scripts/spike/06_capture_catalog.py` (outside comments — verified by `! grep -q '^[^#]*uvloop' scripts/spike/06_capture_catalog.py`).
    - No leaked Cloak/Chromium processes (verified by `! pgrep -f 'cloakbrowser|chromium.*catalog'`).
    - Mean byte count of successful fixtures ≥50 KB (sanity floor — anything smaller is a login wall or 403 stub; recorded but flagged).
  </acceptance_criteria>
  <done>
AC-4 satisfied: 10+ raw catalog PDP HTML fixtures under tests/fixtures/catalog/, per-fixture rows in MANIFEST.md, VISIT-08 invariant honored (no MELI host touched). Fixtures classified `TBD` — task 3 fills the classification, price, name columns.
  </done>
</task>

<task type="auto">
  <name>Task 2: Falabella + Frog + Romero httpx 403-rate probe (D11 gate)</name>
  <files>
    scripts/spike/07_falabella_403.py,
    scripts/spike/falabella_urls.txt,
    scripts/spike/frog_urls.txt,
    scripts/spike/romero_urls.txt,
    artifacts/spike/falabella_403.txt,
    artifacts/spike/frog_403.txt,
    artifacts/spike/romero_403.txt,
    artifacts/spike/visit_403_summary.txt
  </files>
  <read_first>
    - .planning/phases/01-spike-empirical-validation/01-RESEARCH.md §"Pattern 6: Falabella 403 rate" (lines 619-670) — VERBATIM DEFAULT_HEADERS at lines 623-638, async loop at lines 644-662.
    - .planning/phases/01-spike-empirical-validation/01-RESEARCH.md §"Pitfall 5: Falabella 403 misread as 'always blocks httpx'" (lines 765-769) — N≥10 over ≥60s, ratio not binary.
    - .planning/phases/01-spike-empirical-validation/01-PATTERNS.md row for `scripts/spike/08_falabella_403_rate.py` (lines 74-77) — PATTERNS.md uses different filename; this plan's filename `07_falabella_403.py` is authoritative per ROADMAP plan layout.
    - .planning/research/03-visit-extract.md §Q6 (per-host strategy table) — context for why Falabella is the highest-risk host.
    - scripts/spike/catalog_urls.txt (from this plan's task 1) — for the canonical Falabella/Frog/Romero PDP URLs (use the same URLs + 9 more per host harvested from 01-01 SERPs OR Google search on spike day).
  </read_first>
  <action>
**Sub-step A — Seed per-host URL lists** (one URL per line, ≥10 URLs each — operator or Claude harvests from 01-01 SERPs):
- `scripts/spike/falabella_urls.txt` — 10 falabella.com.ar PDP URLs.
- `scripts/spike/frog_urls.txt` — 10 mayoristafrog.com.ar PDP URLs.
- `scripts/spike/romero_urls.txt` — 10 romero-jugueteria.com.ar PDP URLs.

Each file: header comment + ≥10 `https://` lines.

**Sub-step B — Create `scripts/spike/07_falabella_403.py`:**

Self-contained `asyncio` script (no uvloop):
- DEFAULT_HEADERS dict pasted VERBATIM from RESEARCH lines 623-638 (Chromium-146 UA + Sec-Fetch-Site cross-site + Referer https://www.google.com/ — D11 invariant).
- Read URL list from argv[1] (default: `scripts/spike/falabella_urls.txt`).
- For each URL:
  - `httpx.AsyncClient(http2=True, headers=DEFAULT_HEADERS, follow_redirects=True, timeout=10.0)` — VERBATIM from RESEARCH line 651.
  - `await client.get(url)`.
  - Record: `(idx, status_code, elapsed_seconds, final_url, byte_count)` OR `(idx, ERR, error_type, error_message)` on exception.
  - **Throttle: `await asyncio.sleep(30)` between attempts** (per RESEARCH §Pitfall 5 line 768 — same host requires ≥30s spacing).
- After all N attempts, write to `artifacts/spike/{host_slug}_403.txt`:
  ```
  | N=10 | host | 200 | 403 | 5xx | timeout | other |
  |------|------|-----|-----|-----|---------|-------|
  | 10   | falabella.com.ar | 3 | 7 | 0 | 0 | 0 |
  conclusion: visit_failed rate ≈ 70% under bare httpx + Chromium-146 headers
  ```
- Per-attempt rows also written to the file (for forensics).
- The script accepts a `--host <slug>` arg that defaults to `falabella`; can be invoked as `07_falabella_403.py falabella` / `frog` / `romero`. Reads `scripts/spike/{slug}_urls.txt`.

**Sub-step C — Run for all 3 hosts:**
```bash
for host in falabella frog romero; do
  python scripts/spike/07_falabella_403.py --host=$host 2>&1 | tee artifacts/spike/${host}_403_run.log
done

# Aggregate into one summary file
{
  echo "## Visit pass 403-rate summary (N=10 per host, 30s spacing, httpx + DEFAULT_HEADERS)"
  echo
  for host in falabella frog romero; do
    echo "### $host"
    cat artifacts/spike/${host}_403.txt
    echo
  done
} > artifacts/spike/visit_403_summary.txt
```

**Sub-step D — Interpretation rules** (encoded in the script's stdout):
- After each host: print `D11_VERDICT_{host_slug}: realistic-headers-sufficient` if `200/N ≥ 0.7` (≥7/10 succeed).
- Print `D11_VERDICT_{host_slug}: realistic-headers-insufficient` if `200/N < 0.5` (≥5/10 blocked).
- Print `D11_VERDICT_{host_slug}: borderline` if 0.5 ≤ 200/N < 0.7.
- **Phase 2 implication carried into §Visit Status line in task 3:**
  - All 3 hosts ≥0.7 success → D11 holds; Phase 2 `curl-cffi` deferral to Phase 3 confirmed.
  - Falabella specifically <0.5 success → expected per brief 03 (Akamai). Note in §Risks but not a NO-GO; the `visit_failed` flag is the honest signal at MVP.
  - Frog OR Romero <0.5 success → BIGGER surprise; flag for Phase 3 priority.

**Cap protection:** total aggregate across hosts = 30 attempts, spread over ≥30s × 30 = 15min wall-clock. This is well below any reasonable site rate-limit. The script enforces the cap by reading `--host` and never looping internally across hosts.
  </action>
  <verify>
    <automated>test -f scripts/spike/falabella_urls.txt && test -f scripts/spike/frog_urls.txt && test -f scripts/spike/romero_urls.txt && [ "$(grep -cE '^https?://' scripts/spike/falabella_urls.txt)" -ge 10 ] && test -f scripts/spike/07_falabella_403.py && ! grep -q '^[^#]*uvloop' scripts/spike/07_falabella_403.py && grep -q 'Sec-Fetch-Site' scripts/spike/07_falabella_403.py && grep -q 'Referer' scripts/spike/07_falabella_403.py && python scripts/spike/07_falabella_403.py --host=falabella && python scripts/spike/07_falabella_403.py --host=frog && python scripts/spike/07_falabella_403.py --host=romero && test -f artifacts/spike/visit_403_summary.txt && grep -qE '^D11_VERDICT_falabella:' artifacts/spike/falabella_403.txt && grep -qE '^D11_VERDICT_frog:' artifacts/spike/frog_403.txt && grep -qE '^D11_VERDICT_romero:' artifacts/spike/romero_403.txt</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/spike/{falabella,frog,romero}_urls.txt` each exist with ≥10 `https?://` lines.
    - `scripts/spike/07_falabella_403.py` exists with the literal substring `Sec-Fetch-Site` AND the literal substring `Referer` (D11 invariant — Chromium-146 headers actually present).
    - Script exits 0 for all three hosts when invoked with `--host=<slug>`.
    - `artifacts/spike/{falabella,frog,romero}_403.txt` each exist and contain:
      - A line matching `^| 10` (or `^N=10`) with the per-host total.
      - A line matching `^D11_VERDICT_{host}: (realistic-headers-sufficient|realistic-headers-insufficient|borderline)$`.
    - `artifacts/spike/visit_403_summary.txt` aggregates all 3 host outputs.
    - The literal substring `uvloop` does NOT appear in `scripts/spike/07_falabella_403.py` (outside comments).
    - Per-attempt rows total to 30 (10 per host); no host received >12 attempts (the cap protection holds — verified by `grep -c '^| [0-9]' artifacts/spike/falabella_403.txt` ≤ 12).
    - No IP-block escalation: if any host returns 3 consecutive 503s mid-run, the script should ABORT that host (record `aborted_after_3_consecutive_5xx: YES` in the artifact) rather than continue hammering.
  </acceptance_criteria>
  <done>
D11 empirically gated per-host. Falabella expected to be poor (brief 03 prediction); Frog + Romero expected to be acceptable. Numbers feed §Visit Status decision in task 3. Phase 2 plan 02-02 inherits the per-host outcome table for the visit_failed-flag-rate dashboard design (or the explicit `curl-cffi` defer to Phase 3 if Falabella is the only failing host).
  </done>
</task>

<task type="auto">
  <name>Task 3: Hand-rolled extractor + classify each fixture + D12 decision + SPIKE.md §Visit + §D12 finalization (AC-6 finalization)</name>
  <files>
    scripts/spike/08_extruct_classifier.py,
    tests/fixtures/catalog/MANIFEST.md,
    artifacts/spike/classifier_results.txt,
    artifacts/spike/d12_decision.txt,
    .planning/SPIKE.md
  </files>
  <read_first>
    - .planning/phases/01-spike-empirical-validation/01-RESEARCH.md §"Pattern 5 → Hand-rolled extractor probe" (lines 532-604) — the entire script body to lift VERBATIM (4 functions: extract_jsonld_product, extract_og_product, extract_microdata_product, classify; plus AR price regex at line 595).
    - .planning/phases/01-spike-empirical-validation/01-RESEARCH.md D12 decision rule (lines 614-617) — VERBATIM thresholds.
    - .planning/phases/01-spike-empirical-validation/01-RESEARCH.md §"SPIKE.md Template" §Visit + §D12 Decision blocks (lines 1032-1059) — output target.
    - .planning/phases/01-spike-empirical-validation/01-PATTERNS.md row for `scripts/spike/07_extract_fixture.py` (lines 69-72) — PATTERNS.md uses different filename; this plan's filename `08_extruct_classifier.py` is authoritative per ROADMAP plan layout.
    - .planning/phases/01-spike-empirical-validation/01-VALIDATION.md row AC-6 (line 50).
    - tests/fixtures/catalog/**/*.html (from this plan's task 1).
    - tests/fixtures/catalog/MANIFEST.md (from this plan's task 1) — has TBD columns this task fills.
    - artifacts/spike/visit_403_summary.txt (from this plan's task 2) — input to §Visit Status line.
    - .planning/SPIKE.md (post-01-01 + 01-02 state) — §Browser and §LLM are filled; this task finalizes §Visit, §D12 Decision; §Risks-going-forward stays for phase wrap-up.
  </read_first>
  <action>
**Sub-step A — Create `scripts/spike/08_extruct_classifier.py`** (despite the filename — the script is the HAND-ROLLED classifier; the name "extruct_classifier" reflects what it DECIDES about extruct, not what it uses):
- Lift the 4 functions verbatim from RESEARCH §Pattern 5 (lines 540-597): `extract_jsonld_product`, `extract_og_product`, `extract_microdata_product`, `classify`. Include the AR price regex at line 595 exactly.
- CLI: accept either a single fixture path OR a directory glob.
- Default invocation: walk `tests/fixtures/catalog/**/*.html`; for each fixture print one row to stdout AND to `artifacts/spike/classifier_results.txt`:
  ```
  host_slug          | fixture_filename     | classification     | extracted_price  | extracted_name
  ---                | ---                  | ---                | ---              | ---
  falabella_ar       | product-01.html      | jsonld-sufficient  | $ 18.999         | Filtro de aceite Mahle
  mayoristafrog      | product-01.html      | regex-fallback     | $ 1.200          | (none)
  ...
  ```
- After all fixtures, write the aggregate:
  ```
  ## Aggregate
  total_fixtures: 10
  jsonld-sufficient: 7
  og-only: 1
  microdata-only: 1
  regex-fallback: 1
  blob-JS-only: 0
  sufficient_count (jsonld+og): 8
  ```
- Apply D12 decision rule (RESEARCH lines 614-617) and write to `artifacts/spike/d12_decision.txt`:
  - if `sufficient_count >= 8` → `D12_DECISION: HAND-ROLL (D12 stays locked)` + 1-sentence rationale.
  - if `microdata-only + blob-JS-only >= 3` → `D12_DECISION: EXTRUCT (D12 flips — Phase 2 plan 02-02 must add extruct==0.18.0)` + rationale.
  - else (borderline 4-7 sufficient) → `D12_DECISION: NEEDS-PIVOT-MORE-SAMPLES (D12 flips conservatively; Phase 2 plan 02-02 adds extruct as a hedge AND a task to revisit with 10 more fixtures post-MVP)` + rationale.

**Sub-step B — Update `tests/fixtures/catalog/MANIFEST.md`:**
- For each fixture row whose `classification` column is `TBD`, replace `TBD` with the value from `classifier_results.txt`. Likewise `extracted_price` and `extracted_name`.
- Append the `## Aggregate` block from `classifier_results.txt` to MANIFEST.md.

**Sub-step C — Finalize `.planning/SPIKE.md §Visit`:**
- Replace placeholders with values from MANIFEST.md (per-host classification rows) and `artifacts/spike/visit_403_summary.txt` (3 Falabella/Frog/Romero N=10 rows).
- Set §Visit Status:
  - **GO** if: 10 fixtures captured (AC-4); all 3 host 403-probes completed; no surprises (Falabella high 403 expected, Frog/Romero acceptable).
  - **NEEDS-PIVOT** if: Frog or Romero shows >50% 403 (unexpected — Phase 2 needs DEFAULT_HEADERS audit OR earlier `curl-cffi` adoption).
  - **NO-GO** if: <10 fixtures captured OR all 3 hosts >70% 403 (D11 invariant broken — realistic headers do NOT pass any AR catalog).

**Sub-step D — Finalize `.planning/SPIKE.md §D12 Decision`:**
- Transcribe the verdict line from `artifacts/spike/d12_decision.txt`.
- Add the aggregate counts (X jsonld-sufficient + Y og-only + Z other) line.
- Add 1-2 sentence rationale.
- Set §D12 Decision Status:
  - **GO** if `D12_DECISION: HAND-ROLL` (D12 lock holds; Phase 2 plan 02-02 ships hand-rolled VISIT-06 unchanged).
  - **NEEDS-PIVOT** if `D12_DECISION: EXTRUCT` (D12 flips; Phase 2 plan 02-02 adds extruct==0.18.0 and updates VISIT-06 acceptance — surfaced in §Risks).
  - **NEEDS-PIVOT** also if `D12_DECISION: NEEDS-PIVOT-MORE-SAMPLES` (Phase 2 hedges with extruct + adds revisit task).
  - **NO-GO** only if `sufficient_count == 0` (catastrophic — no fixture yielded any structured data; needs re-research).

**Sub-step E — Defer §Risks-going-forward** to phase-wrap-up (per orchestrator brief: "Luis (or Claude as UAT operator) sets the overall Status: line at phase wrap-up" — this task does NOT touch the top-level Overall Status line nor §Risks).

All grep gates use line-anchored patterns to avoid header-prose self-invalidation (per planner critical rules).
  </action>
  <verify>
    <automated>test -f scripts/spike/08_extruct_classifier.py && grep -q 'def classify' scripts/spike/08_extruct_classifier.py && grep -q 'extract_jsonld_product' scripts/spike/08_extruct_classifier.py && grep -q 'extract_og_product' scripts/spike/08_extruct_classifier.py && grep -q 'extract_microdata_product' scripts/spike/08_extruct_classifier.py && ! grep -q '^[^#]*import extruct' scripts/spike/08_extruct_classifier.py && python scripts/spike/08_extruct_classifier.py && test -f artifacts/spike/classifier_results.txt && test -f artifacts/spike/d12_decision.txt && grep -qE '^D12_DECISION: (HAND-ROLL|EXTRUCT|NEEDS-PIVOT-MORE-SAMPLES)' artifacts/spike/d12_decision.txt && [ "$(awk '/^## Visit$/,/^## D12 Decision$/' .planning/SPIKE.md | grep -c '__')" -eq 0 ] && [ "$(awk '/^## D12 Decision$/,/^## Risks$/' .planning/SPIKE.md | grep -c '__')" -eq 0 ] && [ "$(awk '/^## Visit$/,/^## D12 Decision$/' .planning/SPIKE.md | grep -cE '^\*\*Status: (GO|NO-GO|NEEDS-PIVOT)\*\*$')" -eq 1 ] && [ "$(awk '/^## D12 Decision$/,/^## Risks$/' .planning/SPIKE.md | grep -cE '^\*\*Status: (GO|NO-GO|NEEDS-PIVOT)\*\*$')" -eq 1 ]</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/spike/08_extruct_classifier.py` exists and contains all 4 function definitions (`extract_jsonld_product`, `extract_og_product`, `extract_microdata_product`, `classify`).
    - The script does NOT import the `extruct` library (verified by `! grep -q '^[^#]*import extruct' scripts/spike/08_extruct_classifier.py`) — the name is intentional misdirection; the script's whole point is to AVOID extruct.
    - The AR price regex literal `\$\s?(\d{1,3}(?:\.\d{3})*(?:,\d{2})?)` appears in the script (verbatim from RESEARCH line 595).
    - `python scripts/spike/08_extruct_classifier.py` exits 0 after walking `tests/fixtures/catalog/**/*.html`.
    - `artifacts/spike/classifier_results.txt` contains a row per fixture (verified by line count ≥ 10 + a header).
    - `artifacts/spike/classifier_results.txt` ends with a `## Aggregate` block containing concrete counts (`total_fixtures`, 5 classification tiers, `sufficient_count`).
    - `artifacts/spike/d12_decision.txt` contains exactly one line matching `^D12_DECISION: (HAND-ROLL|EXTRUCT|NEEDS-PIVOT-MORE-SAMPLES)$` (line-anchored — no header prose triggers a false match).
    - `tests/fixtures/catalog/MANIFEST.md` no longer has `TBD` in the `classification` column (verified by `! grep -E '^\| [a-z0-9_]+ \|.*\| TBD \|' tests/fixtures/catalog/MANIFEST.md`).
    - `.planning/SPIKE.md §Visit` has zero `__` placeholders (verified by `[ "$(awk '/^## Visit$/,/^## D12 Decision$/' .planning/SPIKE.md | grep -c '__')" -eq 0 ]`) AND exactly one concrete Status line.
    - `.planning/SPIKE.md §D12 Decision` has zero `__` placeholders AND exactly one concrete Status line.
    - Phase 2 dependency hint: if §D12 Status is NEEDS-PIVOT and `d12_decision.txt` says EXTRUCT, an extra line `Phase 2 dependency: add extruct==0.18.0 to pyproject.toml` appears in §D12 (verified manually post-run; not a grep gate because the line may not be needed if HAND-ROLL wins).
  </acceptance_criteria>
  <done>
AC-4 satisfied (≥10 catalog fixtures). AC-6 satisfied for §Visit + §D12 Decision (Status lines concrete; placeholders gone). D12 decision empirically gated — Phase 2 plan 02-02 either ships hand-rolled VISIT-06 unchanged OR adds `extruct==0.18.0` per the spike's verdict. D11 per-host 403-rate documented; `curl-cffi` Phase 3 deferral confirmed (or escalated if a non-Falabella host fails).

Note: §Risks-going-forward and the top-level Overall Status remain placeholder — Luis (or Claude per memory `feedback_agent_as_uat_operator`) sets them at phase wrap-up by reading all three §sections plus the 403 + classifier + death-mode + concurrency + KV-cache + TTFT artifacts end-to-end. THIS plan does not touch §Risks or §Overall Status.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike scripts → 10 AR catalog hosts (`*.falabella.com.ar`, `*.mayoristafrog.com.ar`, ...) | Outbound HTTPS from VPS IP; subject to per-host WAF (Akamai, Imperva, custom) |
| spike scripts → local chromium subprocess | Inherited from 01-01 |
| `tests/fixtures/catalog/**/*.html` ← AR catalog hosts | External content; parsed but never executed |
| `scripts/spike/{falabella,frog,romero}_urls.txt` ← operator-curated | Operator may include URLs with tracking params — stripped during analysis but NOT before commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Denial of Service (self-DoS, IP-block escalation) | Falabella + Frog + Romero hammered with N=10 requests each | mitigate | 30s spacing between same-host requests (RESEARCH §Pitfall 5 line 768); hard cap of 12 requests per host enforced by script (`--host=<slug>` reads ≤12 URLs); abort-on-3-consecutive-5xx (per acceptance criterion in task 2). Aggregate visit count across plan ≤30 (cap protection). |
| T-01-03-02 | Information Disclosure | captured catalog HTML may contain seller PII (customer reviews, store address, embedded JS payloads with API keys) | mitigate | Cold ephemeral context (D8 carried from 01-01); fixtures reviewed by Luis (or Claude per memory `feedback_agent_as_uat_operator`) before commit; visit-pass parses fixtures via selectolax (text extraction, no JS) per RESEARCH §V4. |
| T-01-03-03 | Tampering (process state) | task 1 captures via Cloak — if interrupted mid-run, chromium subprocess may leak | mitigate | Task 1 acceptance: `! pgrep -f 'cloakbrowser|chromium.*catalog'`. Script uses `try/finally browser.close()`. If signal-killed, `pkill -9 -f cloakbrowser` is the recovery (documented in scripts/spike/README.md). |
| T-01-03-04 | Tampering (supply chain) | `selectolax==0.4.10` already audited in 01-01; this plan introduces no new deps | accept | No new package installs in this plan. All deps cleared slopcheck on 2026-06-01 (RESEARCH lines 148-171). |
| T-01-03-05 | Repudiation | D12 decision flows into Phase 2 plan 02-02; if wrong, Phase 2 ships the wrong extractor strategy | mitigate | `artifacts/spike/classifier_results.txt` records per-fixture (host, classification, extracted_price, extracted_name) AND the aggregate counts. Phase 2 can re-run the classifier against the same fixtures to verify. The decision rule (8/10 threshold) is documented in script source AND SPIKE.md §D12 Decision rationale. |
| T-01-03-06 | Tampering | VISIT-08 violation — accidentally fetching a `*.mercadolibre.*` URL | mitigate | Task 1 pre-flight assertion (`assert "mercadolibre." not in url.lower()` for every URL in catalog_urls.txt); script aborts with exit 1 if any violation detected. Task 1 acceptance criterion `! grep -qE 'mercadolibre\.' scripts/spike/catalog_urls.txt`. Carried into task 2: `falabella_urls.txt`, `frog_urls.txt`, `romero_urls.txt` likewise audited. |
| T-01-03-07 | Information Disclosure | DEFAULT_HEADERS include a `Referer: https://www.google.com/` — may leak our routing pattern to host log analyzers | accept | This IS the realistic-headers strategy (D11); the leak is intentional and matches what a real browser session would emit. No PII in headers (no cookies, no Auth). |
| T-01-03-SC | Tampering | npm/pip/cargo installs | mitigate | All spike packages audited in 01-01 (slopcheck OK 15/15). No new installs in this plan. |
</threat_model>

<verification>
Phase-level checks for this plan (executed by `/gsd:execute-phase` after all 3 tasks):

```bash
# 1. AC-4: ≥10 catalog fixtures, no MELI hosts
[ "$(find tests/fixtures/catalog -name '*.html' | wc -l)" -ge 10 ] || { echo "FAIL: AC-4 not satisfied"; exit 1; }
! find tests/fixtures/catalog -name '*.html' | grep -qE '/mercadolibre' || { echo "FAIL: VISIT-08 violation"; exit 1; }
test -f tests/fixtures/catalog/MANIFEST.md || { echo "FAIL: MANIFEST.md missing"; exit 1; }
! grep -E '^\| [a-z0-9_]+ \|.*\| TBD \|' tests/fixtures/catalog/MANIFEST.md || { echo "FAIL: TBD classification rows remain"; exit 1; }

# 2. D11 gate: 3 host outcome files + verdict lines
for host in falabella frog romero; do
  test -f artifacts/spike/${host}_403.txt || { echo "FAIL: ${host}_403.txt missing"; exit 1; }
  grep -qE "^D11_VERDICT_${host}: (realistic-headers-sufficient|realistic-headers-insufficient|borderline)$" artifacts/spike/${host}_403.txt || { echo "FAIL: ${host} verdict missing"; exit 1; }
done

# 3. D12 gate: decision file + concrete verdict
test -f artifacts/spike/d12_decision.txt || { echo "FAIL: d12_decision.txt missing"; exit 1; }
grep -qE '^D12_DECISION: (HAND-ROLL|EXTRUCT|NEEDS-PIVOT-MORE-SAMPLES)$' artifacts/spike/d12_decision.txt || { echo "FAIL: D12 decision missing"; exit 1; }

# 4. AC-6 partial: §Visit + §D12 Decision finalized in SPIKE.md
[ "$(awk '/^## Visit$/,/^## D12 Decision$/' .planning/SPIKE.md | grep -c '__')" -eq 0 ] || { echo "FAIL: §Visit has __ placeholders"; exit 1; }
[ "$(awk '/^## Visit$/,/^## D12 Decision$/' .planning/SPIKE.md | grep -cE '^\*\*Status: (GO|NO-GO|NEEDS-PIVOT)\*\*$')" -eq 1 ] || { echo "FAIL: §Visit Status not concrete"; exit 1; }
[ "$(awk '/^## D12 Decision$/,/^## Risks$/' .planning/SPIKE.md | grep -c '__')" -eq 0 ] || { echo "FAIL: §D12 Decision has __ placeholders"; exit 1; }
[ "$(awk '/^## D12 Decision$/,/^## Risks$/' .planning/SPIKE.md | grep -cE '^\*\*Status: (GO|NO-GO|NEEDS-PIVOT)\*\*$')" -eq 1 ] || { echo "FAIL: §D12 Status not concrete"; exit 1; }

# 5. D6 / D8 / VISIT-08 invariants (defensive grep)
! grep -Rq '^[^#]*uvloop' scripts/spike/ || { echo "FAIL: uvloop reference outside comments"; exit 1; }
! grep -Rq 'launch_persistent_context' scripts/spike/ || { echo "FAIL: D8 foot-gun"; exit 1; }
! grep -RqE 'mercadolibre\.' scripts/spike/catalog_urls.txt scripts/spike/falabella_urls.txt scripts/spike/frog_urls.txt scripts/spike/romero_urls.txt || { echo "FAIL: VISIT-08 violation in URL lists"; exit 1; }

# 6. Hand-roll discipline: 08_extruct_classifier.py does NOT import extruct
! grep -q '^[^#]*import extruct' scripts/spike/08_extruct_classifier.py || { echo "FAIL: classifier should NOT use extruct (its whole job is to decide whether to adopt it)"; exit 1; }

# 7. No leaked processes
! pgrep -f 'cloakbrowser|chromium.*catalog' >/dev/null || { echo "FAIL: leaked process"; exit 1; }

echo "OK: 01-03 verification passed"
```
</verification>

<success_criteria>
This plan is COMPLETE when:

1. **AC-4 satisfied:** ≥10 raw catalog PDP HTML files under `tests/fixtures/catalog/{host}/product-NN.html`, each ≥50 KB (sanity floor), VISIT-08 honored (no `mercadolibre.` anywhere in URL lists or fixture paths).
2. **D11 empirically gated per-host:** Falabella + Frog + Romero each have an N=10 outcome file with concrete `D11_VERDICT_{host}: ...` line. Phase 2 plan 02-02 knows the per-host `visit_failed` baseline rate.
3. **D12 empirically gated:** `artifacts/spike/d12_decision.txt` contains exactly one of `D12_DECISION: HAND-ROLL` / `EXTRUCT` / `NEEDS-PIVOT-MORE-SAMPLES`. Phase 2 plan 02-02 dependencies update accordingly.
4. **§Visit Status concrete** (GO / NO-GO / NEEDS-PIVOT, not scaffold) with rationale citing the per-host 403 mix + fixture count.
5. **§D12 Decision Status concrete** with rationale citing the aggregate classification counts (X jsonld-sufficient + Y og-only + Z other).
6. **Tests/fixtures/catalog/MANIFEST.md** has zero TBD classification rows; each fixture row has concrete `classification`, `extracted_price` (or empty), `extracted_name` (or empty).
7. **No D6/D8/VISIT-08 foot-guns** in spike scripts: grep confirms.
8. **No leaked Cloak/Chromium processes** after task 1 completes.
9. **Phase 2 hand-off ready:** Phase 2 plan 02-02 inherits (a) the fixture set for visit-pass regression tests, (b) the per-host 403 baseline for the visit_failed-rate dashboard, (c) the D12 verdict for the VISIT-06 extractor strategy.
10. **§Risks-going-forward + Overall Status remain placeholder** — those are set by Luis (or Claude per memory) at phase wrap-up, NOT by this plan.
</success_criteria>

<output>
Create `.planning/phases/01-spike-empirical-validation/01-03-SUMMARY.md` when done, documenting:
- Number of catalog fixtures captured per host.
- Classification breakdown (X jsonld-sufficient + Y og-only + Z microdata-only + W regex-fallback + V blob-JS-only).
- D12 decision verdict + 1-sentence rationale.
- Per-host 403-rate ratios (Falabella / Frog / Romero — `200/N` fractions).
- D11 per-host verdicts (sufficient / insufficient / borderline).
- §Visit + §D12 Decision Status lines set in SPIKE.md.
- Phase 2 dependency implications:
  - If D12 = HAND-ROLL: Phase 2 plan 02-02 VISIT-06 ships hand-rolled unchanged.
  - If D12 = EXTRUCT: Phase 2 plan 02-02 must add `extruct==0.18.0` to pyproject.toml AND update VISIT-06 acceptance.
  - If D11 verdict shows Frog or Romero >50% 403: Phase 2 plan 02-02 should consider `curl-cffi` earlier than Phase 3 (escalate; this was supposed to be Phase 3 territory).
- Pointer for Luis / Claude-as-operator: phase-wrap-up step is to read SPIKE.md end-to-end + set §Risks-going-forward (free-text from the 3 sections' findings) + set Overall Status.
</output>
