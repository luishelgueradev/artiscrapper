---
phase: 01
plan: 01-01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - pyproject.toml.spike
  - scripts/spike/README.md
  - scripts/spike/run_all.sh
  - scripts/spike/01_verify_cloak_tag.sh
  - scripts/spike/02_cloak_smoke.py
  - scripts/spike/03_pws_consent_probe.py
  - scripts/spike/04_death_modes.sh
  - tests/fixtures/serp/.gitkeep
  - tests/fixtures/serp/README.md
  - tests/fixtures/catalog/.gitkeep
  - tests/fixtures/llm/.gitkeep
  - .planning/SPIKE.md
autonomous: true
requirements: []
gates_decisions: [D1, D3, D4, D8, D11]
ac_covered: [AC-1, AC-3, AC-6]
user_setup: []

must_haves:
  truths:
    - "Wave 0 scaffolding exists: scripts/spike/ + tests/fixtures/{serp,catalog,llm}/ + pyproject.toml.spike + .gitignore entry for .env.spike + .planning/SPIKE.md skeleton with 5 sections + Overall Status line."
    - "Docker tag cloakhq/cloakbrowser:0.3.31 verified present on Hub (or D1 pin revised in writing in SPIKE.md §Browser)."
    - "Cold-context fetch to google.com/search?q=...&pws=0&hl=es&gl=ar from a fresh ephemeral Cloak context is captured; presence/absence of consent interstitial is recorded against the documented INTERSTITIAL_MARKERS list."
    - "Browser.is_connected() flips to false on SIGKILL of the Chromium subprocess. SIGSTOP and (where applicable) network-drop / OOM behavior is documented with detection lag in seconds."
    - "Two consecutive ephemeral new_context() calls produce different context identifiers and a marker cookie set in ctx1 is NOT present in ctx2's outgoing Cookie header."
    - "5-10 raw Google SERP HTML files exist under tests/fixtures/serp/ with sizes in the 200-800 KB range (sanity-check that they are not 30 KB consent pages)."
    - "SPIKE.md §Browser section is appended from captured artifact values (filenames, byte counts, marker hits, is_connected() table), not from prose; ends with 'Status: GO | NO-GO | NEEDS-PIVOT'."
    - "D8 amended: router is OpenAI-compatible (POST /v1/chat/completions with Authorization: Bearer ...), NOT raw Ollama /api/chat. Brief 02-llm-curator.md is superseded on this point by 01-RESEARCH.md §Pattern 4."
    - "Phase 1 produces NO application code. Only scripts/spike/, tests/fixtures/, and .planning/SPIKE.md are written. Phase 2 will scaffold src/ and pyproject.toml."
    - "Bearer token + router URL live in .env.spike (gitignored). NEVER commit a real bearer token. If .env.spike is missing, spike scripts must exit 2 with a hint pointing at scripts/spike/README.md."
    - "Each sub-spike plan's terminal task appends its SPIKE.md section block from captured artifacts, not from prose. Fill-in points are listed in 01-RESEARCH.md §SPIKE.md Template (lines 985-1076)."
  artifacts:
    - path: .gitignore
      provides: "Excludes .env.spike before any token-extraction step runs"
      contains: ".env.spike"
    - path: pyproject.toml.spike
      provides: "Throwaway pin list for spike-only deps (cloakbrowser==0.3.31, httpx[http2]==0.28.1, httpx-sse>=0.4.0, selectolax==0.4.10, pydantic>=2.0)"
      contains: "cloakbrowser==0.3.31"
    - path: scripts/spike/README.md
      provides: "Env vars (LLM_ROUTER_URL default http://127.0.0.1:3210, ROUTER_BEARER_TOKEN source path), run order, exit-code semantics, throttle rules"
    - path: scripts/spike/run_all.sh
      provides: "Sequential orchestrator (NOT parallel, per RESEARCH §Anti-Patterns line 712)"
    - path: scripts/spike/01_verify_cloak_tag.sh
      provides: "Hub-API check for cloakhq/cloakbrowser:0.3.31 + GitHub release check for chromium-v146.0.7680.177.5"
    - path: scripts/spike/02_cloak_smoke.py
      provides: "Cold-boot smoke + cookie isolation between ephemeral new_context() calls"
    - path: scripts/spike/03_pws_consent_probe.py
      provides: "Drives 5-10 queries against Google with pws=0&safe=off, captures raw HTML to tests/fixtures/serp/, scans for INTERSTITIAL_MARKERS"
    - path: scripts/spike/04_death_modes.sh
      provides: "SIGKILL / SIGSTOP / network-drop death-mode driver; records is_connected() flip + lag"
    - path: .planning/SPIKE.md
      provides: "Empty 5-section skeleton (Browser / LLM / Visit / D12 Decision / Risks) + Overall Status, appended by terminal tasks of each sub-spike plan"
      contains: "## Browser"
    - path: tests/fixtures/serp/
      provides: "Output dir for 5-10 raw SERP HTML files; README.md documents which query produced which fixture and capture date"
  key_links:
    - from: scripts/spike/02_cloak_smoke.py
      to: "cloakbrowser 0.3.31 import surface"
      via: "import cloakbrowser; from cloakbrowser import async_playwright (with documented playwright.async_api fallback)"
      pattern: "from cloakbrowser import"
    - from: scripts/spike/03_pws_consent_probe.py
      to: "tests/fixtures/serp/*.html"
      via: "page.content() -> Path(...).write_text(encoding='utf-8')"
      pattern: "tests/fixtures/serp"
    - from: scripts/spike/04_death_modes.sh
      to: "Chromium subprocess pid"
      via: "ps --ppid + kill -KILL/-STOP/-CONT"
      pattern: "is_connected"
    - from: ".planning/SPIKE.md §Browser"
      to: "captured artifact values"
      via: "terminal task appends a fenced block from grep/wc output, not prose"
      pattern: "^## Browser$"
  outputs:
    - "AC-1: bash scripts/spike/01_verify_cloak_tag.sh exits 0 with stdout containing 'TAG_EXISTS' and 'chromium-v146.0.7680.177.5'"
    - "AC-3: test \"$(ls tests/fixtures/serp/*.html 2>/dev/null | wc -l)\" -ge 5 (passes)"
    - "AC-6 partial: .planning/SPIKE.md contains '## Browser' section + 'Status: GO | NO-GO | NEEDS-PIVOT' line; other sections are scaffolded with placeholder Status lines that 01-02 and 01-03 will fill"
---

<objective>
Wave 0 scaffolding + Browser spike (01-01). Produces the empty SPIKE.md skeleton, the spike-only pyproject pin set, the throwaway env file plumbing, the 9-script run order documentation, then runs the 4 browser-specific probes: Cloak Docker tag verification (D1), cold-context cookie isolation (D8), pws=0 consent-interstitial detection (D3), and Browser.is_connected() death-mode coverage (D8/death modes). Captures 5-10 raw Google SERP HTML fixtures (AC-3).

Purpose: Without Wave 0 scaffolding the other two plans cannot run (they depend on .env.spike + SPIKE.md skeleton + fixture dirs). Without the browser spike, Phase 2 cannot lock D1 (pin verified), D3 (URL params safe — no surprise consent screen), D8 (singleton + ephemeral context isolation actually works, is_connected() is a reliable death signal).

Output: All Wave 0 scaffolding committed; SPIKE.md §Browser filled from captured values with a Status line; 5-10 SERP fixtures under tests/fixtures/serp/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/phases/01-spike-empirical-validation/01-RESEARCH.md
@.planning/phases/01-spike-empirical-validation/01-PATTERNS.md
@.planning/phases/01-spike-empirical-validation/01-VALIDATION.md

<interfaces>
<!-- Greenfield repo — no in-repo code exists. All "contracts" are lifted verbatim from 01-RESEARCH.md. -->

Cloak import shape (verify at task 1; documented fallback exists):
- Primary:  `from cloakbrowser import async_playwright`
- Fallback: `from playwright.async_api import async_playwright`  (after `import cloakbrowser`)
Reference: 01-RESEARCH.md §"Pitfall 1" (lines 741-745) and §"Pattern 1" (lines 297-318).

Ephemeral context defaults (apply to every browser.new_context() call):
- `locale="es-AR"`
- `timezone_id="America/Argentina/Buenos_Aires"`
- `viewport={"width": 1366, "height": 768}`
Reference: 01-RESEARCH.md §"Pattern 1" lines 304-307.

INTERSTITIAL_MARKERS (verbatim — do NOT paraphrase):
- `id="L2AGLb"`            (consent screen "I agree" button)
- `href="https://policies.google.com`
- `aria-label="Antes de continuar`
- `/sorry/index`
- `g-recaptcha`
Reference: 01-RESEARCH.md §"Pattern 1" lines 289-295.

QUERIES list (verbatim 10 entries — use first 5-10 for fixture capture):
"pelota playera quico", "filtro aceite ford focus", "amortiguador trasero peugeot 208",
"buja ngk bosch", "correa distribucion fiat cronos", "disco freno renault sandero",
"rotula direccion vw gol", "kit embrague chevrolet onix", "termostato corsa classic",
"balatas brembo toyota hilux"
Reference: 01-RESEARCH.md §"Pattern 1" lines 283-287.

Google URL template (D3 invariant — no num=, no tbm, no udm, no site:mercadolibre):
`https://www.google.com/search?q={q}&hl=es&gl=ar&pws=0&safe=off`
Reference: 01-RESEARCH.md line 281 and SUMMARY.md D3.

Browser launch args (verbatim, do not alter):
`pw.chromium.launch(headless=True, args=["--no-sandbox", "--disable-dev-shm-usage"])`
Reference: 01-RESEARCH.md line 301.

D8 invariant (foot-gun): **NEVER** call `browser.launch_persistent_context(...)`. Use `browser.new_context(...)` only.

D6 invariant (foot-gun): every Python spike script runs `asyncio.run(...)`. NEVER `uvloop.install()` or `uvloop.EventLoopPolicy()`.

SPIKE.md skeleton structure (5 required sections + Overall Status):
- `## Browser`        (filled by 01-01 terminal task)
- `## LLM`            (filled by 01-02 terminal task)
- `## Visit`          (filled by 01-03 terminal task)
- `## D12 Decision`   (filled by 01-03 terminal task)
- `## Risks`          (filled by Luis / Claude-as-operator at phase wrap-up)
- Each section ends with one line: `**Status: GO | NO-GO | NEEDS-PIVOT**`
- File ends with: `## Overall Status\n\n**Status: GO | NO-GO | NEEDS-PIVOT**`
Reference: 01-RESEARCH.md §"SPIKE.md Template" lines 985-1076.

Spike pin set (verbatim, pyproject.toml.spike):
- `cloakbrowser==0.3.31`
- `httpx[http2]==0.28.1`
- `httpx-sse>=0.4.0`
- `selectolax==0.4.10`
- `pydantic>=2.0`
- `requires-python = ">=3.12"`
Reference: 01-RESEARCH.md §"Standard Stack → Installation" lines 100-108.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 scaffolding — gitignore, pyproject.toml.spike, README, run_all.sh, SPIKE.md skeleton, fixture dirs</name>
  <files>
    .gitignore,
    pyproject.toml.spike,
    scripts/spike/README.md,
    scripts/spike/run_all.sh,
    tests/fixtures/serp/.gitkeep,
    tests/fixtures/serp/README.md,
    tests/fixtures/catalog/.gitkeep,
    tests/fixtures/llm/.gitkeep,
    .planning/SPIKE.md
  </files>
  <read_first>
    - .planning/phases/01-spike-empirical-validation/01-RESEARCH.md (read §"Standard Stack" lines 88-126 for pin set; §"Recommended Project Structure" lines 226-263 for directory layout; §"Anti-Patterns to Avoid" lines 709-717 for gitignore + throttle rules; §"SPIKE.md Template" lines 985-1076 for the SPIKE.md skeleton — copy structure verbatim with all values blanked out as `__`)
    - .planning/phases/01-spike-empirical-validation/01-PATTERNS.md (rows for `.gitignore`, `pyproject.toml.spike`, `scripts/spike/README.md`, `scripts/spike/run_all.sh`, `.planning/SPIKE.md`, `tests/fixtures/{serp,catalog,llm}/`)
    - .planning/phases/01-spike-empirical-validation/01-VALIDATION.md (§"Wave 0 Requirements" lines 57-65; §"Per-Task Verification Map" AC-6 line 50 for the grep gate this scaffolding must satisfy after later tasks fill it)
  </read_first>
  <action>
Create scaffolding so the remaining tasks (and 01-02, 01-03) can run. Do NOT install dependencies — that happens at task 2.

1. **.gitignore** — Create if absent; ensure `.env.spike` is on its own line. Per 01-RESEARCH.md line 713: this MUST land BEFORE any token-extraction (which happens in 01-02). Also gitignore `.venv/`, `__pycache__/`, `*.pyc`, `artifacts/` (for runtime stdout captures during spike).

2. **pyproject.toml.spike** — Write a minimal `[project]` table with `name = "artiscrapper-spike"`, `version = "0.0.0"`, `requires-python = ">=3.12"`, and `dependencies` listing the 5 pins from `<interfaces>` (cloakbrowser==0.3.31, httpx[http2]==0.28.1, httpx-sse>=0.4.0, selectolax==0.4.10, pydantic>=2.0). The `.spike` extension is deliberate so tooling does not auto-pick it up — Phase 2 writes the real `pyproject.toml`.

3. **scripts/spike/README.md** — Document:
   - Env vars: `LLM_ROUTER_URL` (default `http://127.0.0.1:3210`), `ROUTER_BEARER_TOKEN` (read from `.env.spike`, source = `/home/luis/proyectos/local-llms/.env` per 01-RESEARCH.md lines 407-411).
   - One-time token extraction one-liner (verbatim from 01-RESEARCH.md lines 407-411).
   - Setup: `uv venv .venv && source .venv/bin/activate && uv pip install -r <(python3 -c "import tomllib; ...")` — or simply: `uv pip install 'cloakbrowser==0.3.31' 'httpx[http2]==0.28.1' 'httpx-sse>=0.4.0' 'selectolax==0.4.10' 'pydantic>=2.0'` then `python -c "from cloakbrowser import install; install()"` (per 01-RESEARCH.md line 107; verify the install call signature in task 2).
   - Run order: 01 → 02 → 03 → 04 (this plan) → 05 → 09 (plan 01-02) → 06 → 07 → 08 (plan 01-03).
   - Exit-code semantics for every script: 0 = OK, 2 = missing env (e.g. .env.spike not found), other non-zero = empirical failure (record in SPIKE.md, do not retry blindly).
   - Throttle rules (per 01-RESEARCH.md lines 711-716): ≤1 SERP fetch/min to Google during 03; ≤1 router probe/2s during 05; ≥30s between same-host visits in 08.
   - Anti-uvloop reminder (D6).

4. **scripts/spike/run_all.sh** — Sequential bash driver. `set -euo pipefail`. Source `.env.spike` if present, else `echo "ERROR: .env.spike missing — see scripts/spike/README.md" >&2; exit 2`. Then run each script in order; trap each non-zero exit and continue (the spike's whole job is to record empirical failures, not abort). Echo a banner before each script. Per 01-PATTERNS.md row "run_all.sh" and 01-RESEARCH.md §Anti-Patterns line 712 — serialize, NEVER `&` background.

5. **tests/fixtures/serp/.gitkeep**, **tests/fixtures/catalog/.gitkeep**, **tests/fixtures/llm/.gitkeep** — empty marker files. The catalog dir also needs per-host subdirs created on first run by 06 (do NOT pre-create — let the capture script make them).

6. **tests/fixtures/serp/README.md** — One paragraph: "5-10 raw Google SERP HTML files captured by `scripts/spike/03_pws_consent_probe.py`. Filename pattern: `{NN}-{query_slug}.html`. Each fixture records the query + capture date in `tests/fixtures/serp/MANIFEST.md` (appended by the capture script)." (MANIFEST.md is created on first run.)

7. **.planning/SPIKE.md** — Empty skeleton transcribing the structure from 01-RESEARCH.md lines 985-1076, with all values replaced by `__`. Five `##` sections (Browser / LLM / Visit / D12 Decision / Risks), each ending with `**Status: GO | NO-GO | NEEDS-PIVOT**`. File ends with `## Overall Status\n\n**Status: GO | NO-GO | NEEDS-PIVOT**`. Header: `# SPIKE.md — Phase 1 Go/No-Go for artiscrapper v0\n\n**Date:** __  **Operator:** Luis (or Claude on his behalf, per memory feedback_agent_as_uat_operator.md)`.

Do NOT commit `.env.spike` itself — only the `.gitignore` entry. Do NOT run scripts in this task — just create files.
  </action>
  <verify>
    <automated>test -f .gitignore && grep -qx '.env.spike' .gitignore && test -f pyproject.toml.spike && grep -q 'cloakbrowser==0.3.31' pyproject.toml.spike && test -f scripts/spike/README.md && test -x scripts/spike/run_all.sh && test -f tests/fixtures/serp/.gitkeep && test -f tests/fixtures/catalog/.gitkeep && test -f tests/fixtures/llm/.gitkeep && test -f .planning/SPIKE.md && [ "$(grep -cE '^## (Browser|LLM|Visit|D12 Decision|Risks)$' .planning/SPIKE.md)" -eq 5 ] && [ "$(grep -cE '^\*\*Status: GO \| NO-GO \| NEEDS-PIVOT\*\*$' .planning/SPIKE.md)" -ge 6 ]</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` exists and contains exactly the line `.env.spike` (verified by `grep -qx '.env.spike' .gitignore` exit 0).
    - `pyproject.toml.spike` exists with `cloakbrowser==0.3.31` literal substring AND `requires-python = ">=3.12"` literal substring.
    - `scripts/spike/README.md` documents `LLM_ROUTER_URL`, `ROUTER_BEARER_TOKEN`, throttle rules, exit codes (verified by `grep -q 'LLM_ROUTER_URL' scripts/spike/README.md && grep -q 'ROUTER_BEARER_TOKEN' scripts/spike/README.md && grep -q '1 req/2s' scripts/spike/README.md`).
    - `scripts/spike/run_all.sh` exists, is executable (`test -x`), uses `set -euo pipefail`, and does NOT contain ` & ` or ` &$` (no background tasks).
    - `tests/fixtures/{serp,catalog,llm}/.gitkeep` all exist.
    - `.planning/SPIKE.md` contains exactly 5 top-level section headers (`## Browser`, `## LLM`, `## Visit`, `## D12 Decision`, `## Risks`) and AT LEAST 6 `**Status: GO | NO-GO | NEEDS-PIVOT**` lines (one per section + overall).
    - The grep gates above use line-anchored patterns (`grep -cE '^...$'`), avoiding the "header prose self-invalidating" foot-gun.
    - NO `.env.spike` file is committed (verified by `git status --porcelain | grep -v '^?? \.env\.spike$' || true; git ls-files | grep -v '\.env\.spike'`).
  </acceptance_criteria>
  <done>
Wave 0 scaffolding exists and is committable. `git status` shows all 9 new files staged-ready. `.env.spike` is gitignored but does not yet exist (task for the operator to create when starting the spike day, per scripts/spike/README.md instructions). SPIKE.md is an empty contract that subsequent tasks append to.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cloak Docker tag + chromium binary verification (AC-1, D1 gate) + browser smoke + cookie isolation</name>
  <files>
    scripts/spike/01_verify_cloak_tag.sh,
    scripts/spike/02_cloak_smoke.py,
    artifacts/spike/cloak_tag.json,
    artifacts/spike/cloak_smoke.log
  </files>
  <read_first>
    - .planning/phases/01-spike-empirical-validation/01-RESEARCH.md §"Code Examples → Verify Cloak Docker tag" (lines 791-805) — verbatim curl + python3 parse; §"Code Examples → Verify chromium binary tag" (lines 807-813) — verbatim GitHub API curl; §"Pattern 1: Cold-container browser smoke" (lines 267-322) — full script body; §"Pattern 3: Ephemeral context cookie isolation" (lines 368-400) — full block to merge into 02_cloak_smoke.py; §"Pitfall 1" (lines 741-745) — Cloak import-path fallback discipline; §"Code Examples → `is_connected()` check pattern" (lines 850-858); §"Pitfall 7" (lines 777-781) — prefer Hub HTTP API over `docker pull` for quota.
    - .planning/phases/01-spike-empirical-validation/01-PATTERNS.md rows for `scripts/spike/01_verify_cloak_tag.sh` (line 39-40) and `scripts/spike/02_cloak_smoke.py` (lines 42-45).
    - .planning/phases/01-spike-empirical-validation/01-VALIDATION.md row AC-1 (line 45).
    - scripts/spike/README.md (just written) — to make sure 01 and 02 honor the env-var contract documented there.
  </read_first>
  <action>
**Sub-step A — Install spike deps locally** (one-time, document the exact commands run in `artifacts/spike/setup.log`):
```bash
mkdir -p artifacts/spike
uv venv .venv  # OR python3 -m venv .venv if uv is not installed (per RESEARCH §Env Availability)
source .venv/bin/activate
uv pip install 'cloakbrowser==0.3.31' 'httpx[http2]==0.28.1' 'httpx-sse>=0.4.0' 'selectolax==0.4.10' 'pydantic>=2.0' 2>&1 | tee artifacts/spike/setup.log
# Cloak ships its own Chromium binary — confirm the install entry point first:
python -c "import cloakbrowser; print('module path:', cloakbrowser.__file__); print('exports:', sorted(n for n in dir(cloakbrowser) if not n.startswith('_')))" | tee -a artifacts/spike/setup.log
# Then run the Chromium installer in whatever form 0.3.31 exposes (try in order; record which one works):
python -c "from cloakbrowser import install; install()" 2>&1 | tee -a artifacts/spike/setup.log || \
python -m cloakbrowser install 2>&1 | tee -a artifacts/spike/setup.log || \
python -m playwright install chromium 2>&1 | tee -a artifacts/spike/setup.log
```
Record which install incantation actually worked — that's a Phase 2 hint.

**Sub-step B — Create `scripts/spike/01_verify_cloak_tag.sh`:**
- Header: `#!/usr/bin/env bash` + `set -euo pipefail`.
- Trap exit 2 if no internet (a single `curl -sf -o /dev/null https://hub.docker.com/v2/` precheck).
- Lift the curl + python3 parse from 01-RESEARCH.md lines 794-805 verbatim, but redirect stdout to BOTH `artifacts/spike/cloak_tag.json` (raw JSON) AND `artifacts/spike/cloak_tag.txt` (parsed summary: `TAG_EXISTS|TAG_MISSING\nupdated: ...\nsize_bytes: ...\narches: ...`).
- Lift the GitHub release curl from 01-RESEARCH.md lines 809-811 verbatim, append to `artifacts/spike/cloak_tag.txt` as `chromium_release_tag: chromium-v146.0.7680.177.5` (or whatever returns).
- Exit 0 ONLY if both `TAG_EXISTS` AND chromium tag starts with `chromium-v146.0.7680.177.` (do not over-pin patch suffix; D1 said `.5` but allow `.4..` if Hub shipped a newer minor refresh). Exit 1 otherwise.
- `chmod +x` the file.

**Sub-step C — Create `scripts/spike/02_cloak_smoke.py`:**
- Header line: `#!/usr/bin/env python3`. Top-of-file docstring referencing 01-RESEARCH.md §Pattern 1 + §Pattern 3.
- Import discovery first (per 01-RESEARCH.md §Pitfall 1, lines 741-745): `try: from cloakbrowser import async_playwright; CLOAK_IMPORT="cloakbrowser"\nexcept ImportError: import cloakbrowser; from playwright.async_api import async_playwright; CLOAK_IMPORT="playwright_async_api_post_cloak"`. Print which path won.
- Read QUERIES verbatim from 01-RESEARCH.md lines 283-287.
- Read INTERSTITIAL_MARKERS verbatim from 01-RESEARCH.md lines 289-295.
- Implement `async def smoke(out_dir)` from 01-RESEARCH.md lines 297-321 verbatim, but: write each fixture to `out_dir / f"{i:02d}-{slug}.html"`; also append a `MANIFEST.md` line per fixture: `| {filename} | "{query}" | {byte_count} | {marker_hits or "none"} | {YYYY-MM-DD HH:MM} |`.
- Implement `async def cookie_isolation_test()` from 01-RESEARCH.md lines 373-398 verbatim. Print PASS/FAIL based on the assertion at line 398.
- Add a verification check at the end: confirm `not browser.launch_persistent_context` was used anywhere (string search of own source for the literal substring — exit 1 if found; documents D8 invariant in the script itself).
- Main: parse `argv[1]` for out_dir (default `tests/fixtures/serp/`). Run smoke + cookie test sequentially. `asyncio.run(main())` — NEVER `uvloop`.
- Exit 0 if smoke captured ≥5 fixtures AND cookie isolation PASSED; non-zero otherwise.
- Throttle: `await asyncio.sleep(60)` between queries (per 01-RESEARCH.md line 712 — ≤1/min to Google during capture).

**Sub-step D — Run both scripts and append to `.planning/SPIKE.md §Browser`:**
```bash
bash scripts/spike/01_verify_cloak_tag.sh > artifacts/spike/cloak_tag.log 2>&1 || echo "TAG VERIFY FAILED — see log"
python scripts/spike/02_cloak_smoke.py tests/fixtures/serp/ > artifacts/spike/cloak_smoke.log 2>&1 || echo "SMOKE FAILED — see log"
# Then the operator (or Claude-as-operator per memory) edits .planning/SPIKE.md §Browser:
#   - Cloak Docker tag ...: YES/NO  (from cloak_tag.txt)
#   - Cloak Python import path: cloakbrowser | playwright_async_api_post_cloak  (from cloak_smoke.log)
#   - Cold-context fetch triggered interstitial: YES/NO  (from MANIFEST.md marker_hits column)
#   - is_connected() flips on SIGKILL/SIGSTOP/network-drop: filled by task 3
#   - Ephemeral new_context() cookie isolation: PASS/FAIL  (from cloak_smoke.log)
#   - SERP fixtures captured: N files
```
DO NOT yet write the §Browser Status line — task 4 (death modes) finishes the section and sets Status.

D11 reminder (cross-cutting): `headless=True` + `--no-sandbox --disable-dev-shm-usage` (verbatim from RESEARCH line 301).
  </action>
  <verify>
    <automated>bash scripts/spike/01_verify_cloak_tag.sh && test -f artifacts/spike/cloak_tag.json && grep -q '"name"' artifacts/spike/cloak_tag.json && python scripts/spike/02_cloak_smoke.py tests/fixtures/serp/ && test "$(ls tests/fixtures/serp/*.html 2>/dev/null | wc -l)" -ge 5 && grep -q 'PASS' artifacts/spike/cloak_smoke.log</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/spike/01_verify_cloak_tag.sh` exists, is executable, exits 0.
    - `artifacts/spike/cloak_tag.json` exists and contains the literal substring `"name": "0.3.31"` (Docker Hub tag exists).
    - `artifacts/spike/cloak_tag.txt` contains `TAG_EXISTS` and a `chromium_release_tag: chromium-v146.0.7680.177` line (any `.X` patch acceptable; D1 documents .5 but Hub-realized version wins).
    - `scripts/spike/02_cloak_smoke.py` exists, runs to completion against live Google, captures ≥5 `tests/fixtures/serp/*.html` files each in the 200-800 KB range (NOT 30 KB consent stub).
    - `artifacts/spike/cloak_smoke.log` contains the literal substring `Cloak Python import path: cloakbrowser` OR `Cloak Python import path: playwright_async_api_post_cloak` (one of the two; the script records which won).
    - `artifacts/spike/cloak_smoke.log` contains the literal `cookie isolation: PASS` line.
    - `tests/fixtures/serp/MANIFEST.md` exists with one row per captured fixture, each row showing the query + byte count + marker hits (empty list `none` if no interstitial).
    - Running `python scripts/spike/02_cloak_smoke.py` a second time produces DIFFERENT context IDs in stdout each iteration (proves `new_context()` isolation; logged via context-id print added by the script).
    - The literal substring `launch_persistent_context` does NOT appear anywhere in scripts/spike/02_cloak_smoke.py (verified by `! grep -q 'launch_persistent_context' scripts/spike/02_cloak_smoke.py`).
    - The literal substring `uvloop` does NOT appear in any spike script under scripts/spike/ (verified by `! grep -r 'uvloop' scripts/spike/`).
  </acceptance_criteria>
  <done>
D1 verified empirically (Hub API + GitHub releases) with artifacts pinned to disk. Cloak import surface known and recorded. Cookie isolation invariant verified (passes D8 prerequisite). 5+ raw SERP fixtures committed for Phase 2's parser regression set. SPIKE.md §Browser has values appended for the first 4 rows but Status line not yet set (task 4 finalizes).
  </done>
</task>

<task type="auto">
  <name>Task 3: pws=0 consent-interstitial sweep (D3 gate) — capture remaining SERP fixtures + record marker stats</name>
  <files>
    scripts/spike/03_pws_consent_probe.py,
    tests/fixtures/serp/MANIFEST.md,
    artifacts/spike/pws_consent_summary.txt
  </files>
  <read_first>
    - .planning/phases/01-spike-empirical-validation/01-RESEARCH.md §"Pattern 1" (lines 267-322 — same template as task 2 but configured for capture-mode rather than smoke-mode); §"Pitfall 2: pws=0 triggers the EU consent screen" (lines 747-751); §"Open Questions" Q3 (lines 908-911) — this script IS that answer.
    - .planning/phases/01-spike-empirical-validation/01-PATTERNS.md row for `scripts/spike/04_capture_serps.py` (lines 52-54) — note PATTERNS.md mentions `04_capture_serps.py`; we are folding capture into 03_pws_consent_probe.py because task 2 already ran the smoke variant.
    - tests/fixtures/serp/ (post-task-2 state — should already have ≥5 fixtures; this task tops up to 10 AND records aggregate marker stats).
    - scripts/spike/02_cloak_smoke.py (task 2) — to copy the launch/context/INTERSTITIAL_MARKERS scaffolding pattern; DO NOT import from it (each script is self-contained per 01-RESEARCH.md §Pitfall 6).
  </read_first>
  <action>
Top up SERP fixtures from 5 (task 2 minimum) to a target of 8-10, and aggregate the consent-marker findings across all captured fixtures into a single summary file.

1. **Create `scripts/spike/03_pws_consent_probe.py`** following the same launch + context pattern as task 2:
   - Read existing fixtures from `tests/fixtures/serp/*.html`; if count ≥ 10, skip capture and just analyze.
   - Else: capture additional fixtures using the next QUERIES entries (5..10) NOT yet on disk. Same throttle (≥60s between fetches).
   - For each fixture on disk (including those captured in task 2), reopen and scan for INTERSTITIAL_MARKERS (verbatim list from 01-RESEARCH.md lines 289-295). Aggregate:
     - `total_fixtures`, `with_any_marker`, `per_marker_hit_counts` (a dict), `mean_size_kb`, `min_size_kb`, `max_size_kb`.
   - Per 01-RESEARCH.md §Pitfall 2 (line 751): a sub-30KB fixture or absence of `<h3>` elements is a strong consent-screen indicator. Count those too: `h3_count = len(tree.css("h3"))`.
   - Write the summary to `artifacts/spike/pws_consent_summary.txt` as a plain table.
   - Also UPDATE `tests/fixtures/serp/MANIFEST.md` (task 2 created it as an append-log; this task adds a `## Aggregate` block at the bottom with the same numbers).

2. **Decision rule encoded in the script's exit code:**
   - Exit 0 if `with_any_marker == 0` AND all fixtures `>= 100 KB` AND each has `>= 5` h3 elements → "pws=0 is clean on this VPS" → D3 stays as-is for Phase 2.
   - Exit 1 (non-fatal — spike records, does not abort the day) if any fixture shows markers → Phase 2 plan 02-01 will need a cookie-banner-dismissal step (record this in §Risks at phase wrap-up).
   - The script's stdout MUST include one of these two literal lines at the end:
     - `D3_VERDICT: clean — no consent interstitial observed on N={count} cold-context fetches`
     - `D3_VERDICT: pivot — interstitial observed on {hit_count}/{count} fetches; Phase 2 must add cookie-banner-dismissal`

3. **Throttle reminder:** capture additional fixtures one-per-minute. If running on a different machine than the one that ran task 2, `tests/fixtures/serp/` may already be populated — the script must idempotently top up.

4. After running, append the §Browser block in `.planning/SPIKE.md` with the pws=0 verdict line. Do NOT yet finalize the §Browser Status — task 4 (death modes) does that.

**Anti-pattern guard:** the script MUST NOT call `launch_persistent_context`. The script MUST use `asyncio.run`. Both enforced by grep at acceptance time.
  </action>
  <verify>
    <automated>python scripts/spike/03_pws_consent_probe.py && test "$(ls tests/fixtures/serp/*.html 2>/dev/null | wc -l)" -ge 5 && test -f artifacts/spike/pws_consent_summary.txt && grep -qE '^D3_VERDICT: (clean|pivot)' artifacts/spike/pws_consent_summary.txt && ! grep -q 'launch_persistent_context' scripts/spike/03_pws_consent_probe.py && ! grep -q 'uvloop' scripts/spike/03_pws_consent_probe.py</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/spike/03_pws_consent_probe.py` exists and exits 0 OR 1 (both acceptable — 0 = clean, 1 = pivot needed; ≥2 means script crashed and is a bug).
    - `tests/fixtures/serp/` contains at least 5 `*.html` files (carries over from task 2; ideally 8-10 by end of this task).
    - `artifacts/spike/pws_consent_summary.txt` exists and contains exactly one line matching `^D3_VERDICT: (clean|pivot)` (anchored regex — no header prose).
    - `tests/fixtures/serp/MANIFEST.md` ends with a `## Aggregate` block listing `total_fixtures`, `with_any_marker`, `mean_size_kb` (verified by `grep -q '^## Aggregate$' tests/fixtures/serp/MANIFEST.md`).
    - The literal substring `launch_persistent_context` does NOT appear in `scripts/spike/03_pws_consent_probe.py`.
    - The literal substring `uvloop` does NOT appear in `scripts/spike/03_pws_consent_probe.py`.
    - Per 01-RESEARCH.md §Pitfall 2 warning sign: if any fixture is `< 35 KB` the script MUST flag it in stdout with `WARNING: fixture {name} only {N}KB — likely consent stub` (verified by manual inspection if mean size is low).
  </acceptance_criteria>
  <done>
D3 empirical verdict captured. Either "clean" (pws=0 works on this VPS, Phase 2 keeps URL as-is) or "pivot" (Phase 2 must add a consent-dismissal step to build_serp_url + first-fetch warmup). Aggregate marker stats land in SPIKE.md §Browser as a fenced block. AC-3 (≥5 fixtures) satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 4: Death-mode coverage + SPIKE.md §Browser finalization (D8 gate + AC-6 partial)</name>
  <files>
    scripts/spike/04_death_modes.sh,
    artifacts/spike/death_modes.txt,
    .planning/SPIKE.md
  </files>
  <read_first>
    - .planning/phases/01-spike-empirical-validation/01-RESEARCH.md §"Pattern 2: Death-mode provocation" (lines 330-356) — bash block to lift verbatim; §"Pattern 2 → table" (lines 359-365) — output schema; §"Code Examples → `is_connected()` check pattern" (lines 850-858); §"Assumptions Log" A7 (line 889) — SIGKILL flips, SIGSTOP may NOT flip is HIGH risk; §"SPIKE.md Template" §Browser block (lines 994-1006) — final output target schema.
    - .planning/phases/01-spike-empirical-validation/01-PATTERNS.md row for `scripts/spike/03_death_modes.sh` (lines 47-50) — PATTERNS calls it `03_` but our run order calls it `04_` (the filename in this plan is authoritative; PATTERNS.md numbering is a hint, not a contract).
    - artifacts/spike/cloak_smoke.log (task 2 output — provides the chromium pid scoping pattern).
    - .planning/SPIKE.md (post-tasks-1-2-3 state — §Browser has values for rows 1, 2, 3, 5, 6; this task fills row 4 and sets Status).
  </read_first>
  <action>
Provoke 3-4 death modes against a running Cloak browser and record whether `Browser.is_connected()` flips for each. This is the highest-risk row in SPIKE.md §Browser per RESEARCH.md A7 — if SIGSTOP does NOT flip, Phase 2's `_recycle_browser_loop` needs a secondary heartbeat (Phase 2 plan 02-01 inherits this finding).

1. **Create `scripts/spike/04_death_modes.sh`:**
   - `#!/usr/bin/env bash` + `set -uo pipefail` (NOT `-e` — script must continue after a death-mode is provoked, because the death IS the test).
   - Boot a Cloak session via a small Python helper script (inline `python3 -c` or call `scripts/spike/02_cloak_smoke.py --keep-alive` — task 2's smoke script may already support a "leave browser running" flag; if not, write a tiny inline asyncio launcher here that keeps `browser` alive in a separate process and writes its chromium child pid to `artifacts/spike/chromium.pid`).
   - Sequentially run each death mode from 01-RESEARCH.md lines 333-355:
     - **SIGKILL**: `kill -KILL $(cat artifacts/spike/chromium.pid)`. Poll `browser.is_connected()` from the launcher process at 1s intervals for up to 30s. Record the lag (time-to-flip) or `NEVER_FLIPPED`.
     - **SIGSTOP**: re-launch a fresh Cloak (chromium pid 2); `kill -STOP <pid>`. Poll for 30s. **Expect this MAY NOT flip** — record exactly what happens. Then `kill -CONT <pid>` to clean up.
     - **Network drop** (Docker-only — if running outside Docker, mark as `n/a: not_in_docker` per RESEARCH.md line 356).
     - **OOM** (Docker-only — same).
   - Output format (write to `artifacts/spike/death_modes.txt`, pipe-separated, one row per mode):
     ```
     | mode             | flipped | lag_seconds | notes |
     |------------------|---------|-------------|-------|
     | SIGKILL          | YES     | 2.3         | clean |
     | SIGSTOP          | NO      | NEVER       | needs heartbeat — Phase 2 plan 02-01 attention |
     | network_drop     | n/a     | n/a         | not_in_docker |
     | OOM              | n/a     | n/a         | not_in_docker |
     ```
   - **CRITICAL cleanup**: at the end (even on failure), `kill -9 $(cat artifacts/spike/chromium.pid) 2>/dev/null || true` AND any Cloak process spawned. Per threat-model: "death-mode tests use docker kill / SIGSTOP — risk: leaving Cloak container in bad state; mitigation: each death-mode test must end with cleanup step". If running locally (no Docker), the equivalent is `pkill -9 -f cloakbrowser; pkill -9 -f chromium 2>/dev/null || true`.

2. **Finalize `.planning/SPIKE.md §Browser`:**
   - Replace the `__` placeholders in the SIGKILL / SIGSTOP / network-drop rows with values from `artifacts/spike/death_modes.txt`.
   - Replace remaining `__` placeholders (Cloak Hub tag, import path, cold-context interstitial verdict, cookie isolation result, fixture count) with values from artifacts in tasks 2-3.
   - Set the §Browser Status line based on the cumulative evidence:
     - **GO** if: Hub tag exists, import path resolved, no interstitial OR pivot recorded, cookie isolation PASS, SIGKILL flips is_connected().
     - **NEEDS-PIVOT** if: SIGSTOP does not flip (Phase 2 needs heartbeat) OR pws=0 triggers interstitial (Phase 2 needs cookie-dismissal warmup).
     - **NO-GO** if: Hub tag missing, import path unresolvable, or cookie isolation FAILS (the D8 promise is broken).
   - Write a 1-sentence rationale beneath the Status line referring to the load-bearing finding.

3. **Append the row count of captured SERP fixtures** to §Browser ("SERP fixtures captured: N files under tests/fixtures/serp/").

The cumulative §Browser block, when grepped, must satisfy AC-6's structural test (5 sections, 6+ Status lines — but only §Browser's Status is GO/NO-GO/NEEDS-PIVOT in this plan; the other sections still hold their scaffold Status placeholders until 01-02 and 01-03 land).
  </action>
  <verify>
    <automated>bash scripts/spike/04_death_modes.sh && test -f artifacts/spike/death_modes.txt && grep -qE '^\| SIGKILL +\| (YES|NO|n/a) +\|' artifacts/spike/death_modes.txt && grep -qE '^## Browser$' .planning/SPIKE.md && grep -qE '^\*\*Status: (GO|NO-GO|NEEDS-PIVOT)\*\*$' .planning/SPIKE.md && ! pgrep -f 'cloakbrowser|chromium.*spike'</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/spike/04_death_modes.sh` exists, is executable, and runs to completion (exit 0 — note: the script itself ALWAYS exits 0 even if a death-mode failed to flip, because the FAILURE is the data, not the script error).
    - `artifacts/spike/death_modes.txt` exists with at LEAST one row matching `^\| SIGKILL\s+\| (YES|NO)\s+\| [0-9.]+\s+\|` (SIGKILL row has a concrete flip+lag value or NO+NEVER).
    - `artifacts/spike/death_modes.txt` has at LEAST one row matching `^\| SIGSTOP\s+\| (YES|NO)\s+\|` (SIGSTOP row recorded).
    - `.planning/SPIKE.md §Browser` placeholders are all replaced (no `__` substrings remain inside the `## Browser` section — verified by `awk '/^## Browser$/,/^## LLM$/' .planning/SPIKE.md | grep -c '__' | grep -q '^0$'`).
    - `.planning/SPIKE.md` contains exactly one `**Status: GO ...**` line BETWEEN `^## Browser$` and `^## LLM$` (the Browser section's Status).
    - **Cleanup**: after the script exits, `pgrep -f cloakbrowser` returns empty AND `pgrep -f 'chromium.*spike'` returns empty (no leaked processes).
    - The literal substring `launch_persistent_context` does NOT appear anywhere in `scripts/spike/04_death_modes.sh` or its Python helpers.
  </acceptance_criteria>
  <done>
SPIKE.md §Browser is finalized with empirical values and a Status line. AC-1 satisfied (Hub tag verified). AC-3 satisfied (≥5 SERP fixtures). AC-6 partially satisfied (§Browser Status line present; other sections still scaffold). D1 + D8 (death-mode coverage) + D11 (ephemeral context isolation) decisions empirically gated for Phase 2 lock-in. Death-mode table is the load-bearing artifact for Phase 2 plan 02-01's `_recycle_browser_loop` design — specifically whether a secondary heartbeat (page.evaluate("1") every N seconds) is needed alongside is_connected().
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike scripts → Docker Hub HTTP API | Unauthenticated; rate-limited per IP |
| spike scripts → GitHub API | Unauthenticated; rate-limited per IP |
| spike scripts → google.com | Outbound HTTPS from VPS IP; subject to IP-block on burst |
| spike scripts → local chromium subprocess | OS process boundary; signals cross it |
| `.env.spike` ← `/home/luis/proyectos/local-llms/.env` | File-system read of a secret; never logged, never committed |
| captured `tests/fixtures/serp/*.html` ← google.com | External content; parsed but never executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Information Disclosure | scripts/spike/*.py logging of `Authorization: Bearer …` header | mitigate | This plan touches no router calls (those are 01-02). N/A here, but the read_first contract reminds the operator that any future inline `print(headers)` in spike scripts MUST whitelist; structlog field whitelist (RESEARCH §V14) carries forward. |
| T-01-01-02 | Information Disclosure | `tests/fixtures/serp/*.html` may contain SERP cards with personal-data slivers (account info, browsing artifacts) | mitigate | Cold ephemeral `new_context()` per fetch (D8); locale="es-AR" + timezone forced; no user-account cookies; fixtures reviewed by Luis before commit per memory `feedback_agent_as_uat_operator`. Acceptance step: visual review by operator before `git add tests/fixtures/serp/`. |
| T-01-01-03 | Denial of Service (self-DoS) | google.com IP-block on burst | mitigate | Throttle ≥60s between fetches in task 2 + task 3; abort capture if 3 consecutive fetches return `/sorry/index` markers (recorded by 03_pws_consent_probe.py — script exits with `WARNING: 3 consecutive blocks — aborting capture`); aggregate cap of 15 SERP fetches across both tasks. |
| T-01-01-04 | Tampering (process state) | death-mode tests (SIGSTOP, SIGKILL) may leave Cloak/Chromium in zombie state | mitigate | Task 4 mandates `pkill -9 -f cloakbrowser; pkill -9 -f chromium 2>/dev/null \|\| true` in cleanup; acceptance criterion verifies `pgrep` returns empty after the script. |
| T-01-01-05 | Tampering (supply chain) | spike pin set introduces new packages — `cloakbrowser`, `httpx[http2]`, `httpx-sse`, `selectolax`, `pydantic` | accept | All 5 cleared slopcheck on 2026-06-01 (RESEARCH §"Package Legitimacy Audit" lines 148-171, verdict: 15/15 OK). No `[ASSUMED]` / `[SUS]` packages; no blocking-human checkpoint required. Pins are immutable for the spike duration. |
| T-01-01-06 | Information Disclosure | `.env.spike` accidentally committed | mitigate | `.gitignore` entry written in task 1 BEFORE any token-extraction step (which happens in 01-02, not this plan); acceptance criterion in task 1 verifies `git ls-files \| grep -v '.env.spike'` (no commit). Per memory `feedback_self_sufficient_installers`: scripts/spike/README.md documents the gitignore enforcement so operator sees it on first read. |
| T-01-01-07 | Tampering | captured fixtures contain unsanitized HTML; if rendered in a browser context could XSS-on-replay | accept | Fixtures parsed only via selectolax (text extraction, no JS) per RESEARCH §V4 row line 1100. No browser-render of fixtures during spike or Phase 2 testing. Documented in tests/fixtures/serp/README.md. |
| T-01-01-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck audit complete (RESEARCH lines 148-171); all 5 spike packages OK. No `[ASSUMED]`/`[SUS]` packages → no blocking-human checkpoint needed for this plan. |
</threat_model>

<verification>
Phase-level checks for this plan (executed by `/gsd:execute-phase` after task 4):

```bash
# 1. Wave 0 scaffolding present
test -f .gitignore && grep -qx '.env.spike' .gitignore || { echo "FAIL: .env.spike not gitignored"; exit 1; }
test -f pyproject.toml.spike && grep -q 'cloakbrowser==0.3.31' pyproject.toml.spike || { echo "FAIL: pyproject.toml.spike missing/incorrect"; exit 1; }
test -f scripts/spike/README.md || { echo "FAIL: README missing"; exit 1; }
test -x scripts/spike/run_all.sh || { echo "FAIL: run_all.sh missing/not executable"; exit 1; }
test -f tests/fixtures/serp/.gitkeep && test -f tests/fixtures/catalog/.gitkeep && test -f tests/fixtures/llm/.gitkeep || { echo "FAIL: fixture .gitkeep missing"; exit 1; }

# 2. SPIKE.md skeleton + Browser section finalized
test -f .planning/SPIKE.md || { echo "FAIL: SPIKE.md missing"; exit 1; }
[ "$(grep -cE '^## (Browser|LLM|Visit|D12 Decision|Risks)$' .planning/SPIKE.md)" -eq 5 ] || { echo "FAIL: SPIKE.md missing one of the 5 sections"; exit 1; }
[ "$(grep -cE '^\*\*Status: (GO \| NO-GO \| NEEDS-PIVOT|GO|NO-GO|NEEDS-PIVOT)\*\*$' .planning/SPIKE.md)" -ge 5 ] || { echo "FAIL: not enough Status lines"; exit 1; }
# §Browser must be filled (no __ placeholders left)
[ "$(awk '/^## Browser$/,/^## LLM$/' .planning/SPIKE.md | grep -c '__')" -eq 0 ] || { echo "FAIL: §Browser still has __ placeholders"; exit 1; }
# §Browser Status must be one of GO/NO-GO/NEEDS-PIVOT (concrete, not the scaffold)
awk '/^## Browser$/,/^## LLM$/' .planning/SPIKE.md | grep -qE '^\*\*Status: (GO|NO-GO|NEEDS-PIVOT)\*\*$' || { echo "FAIL: §Browser Status not concrete"; exit 1; }

# 3. AC-1: Cloak Docker tag verification
test -f artifacts/spike/cloak_tag.json && grep -q '"name": "0.3.31"' artifacts/spike/cloak_tag.json || { echo "FAIL: AC-1 not satisfied"; exit 1; }

# 4. AC-3: ≥5 SERP fixtures
[ "$(ls tests/fixtures/serp/*.html 2>/dev/null | wc -l)" -ge 5 ] || { echo "FAIL: AC-3 not satisfied"; exit 1; }

# 5. Anti-pattern grep gates (D8, D6)
! grep -Rq 'launch_persistent_context' scripts/spike/ || { echo "FAIL: D8 foot-gun present"; exit 1; }
! grep -Rq '^[^#]*uvloop' scripts/spike/ || { echo "FAIL: D6 foot-gun present (uvloop reference outside comments)"; exit 1; }

# 6. No leaked processes after death-mode tests
! pgrep -f 'cloakbrowser|chromium.*spike' >/dev/null || { echo "FAIL: leaked process"; exit 1; }

# 7. No secrets in git
! git ls-files | grep -qE '\.env\.spike$' || { echo "FAIL: .env.spike committed"; exit 1; }

echo "OK: 01-01 verification passed"
```

The grep gates use `^...$` anchors + `grep -v '^#'`-equivalent (the `^[^#]*uvloop` pattern) so header prose in SPIKE.md / README mentioning "uvloop banned" does not self-invalidate the check (per critical rules in the planner spec).
</verification>

<success_criteria>
This plan is COMPLETE when:

1. **Wave 0 scaffolding committed:** `.gitignore` excludes `.env.spike`; `pyproject.toml.spike` pins all 5 spike deps; `scripts/spike/{README.md, run_all.sh}` exist; `tests/fixtures/{serp,catalog,llm}/.gitkeep` exist; `.planning/SPIKE.md` skeleton has 5 sections + 6 Status lines.
2. **AC-1 satisfied (D1 gate):** `artifacts/spike/cloak_tag.json` shows tag `0.3.31` exists on Hub; `artifacts/spike/cloak_tag.txt` shows chromium tag `chromium-v146.0.7680.177.X`. If either fails: `SPIKE.md §Browser Status: NO-GO` AND a Risks note for Phase 2.
3. **AC-3 satisfied (parser regression set):** ≥5 raw Google SERP HTML files under `tests/fixtures/serp/`, each 200-800 KB, with `MANIFEST.md` documenting query + size + marker hits per file.
4. **D3 gate evaluated:** `artifacts/spike/pws_consent_summary.txt` contains a `D3_VERDICT: clean` or `D3_VERDICT: pivot` line. If `pivot`: §Risks-going-forward in SPIKE.md gets a note ("Phase 2 plan 02-01 must add cookie-banner-dismissal warmup").
5. **D8 gate evaluated (cookie isolation):** `artifacts/spike/cloak_smoke.log` contains `cookie isolation: PASS`. If FAIL: §Browser Status: NO-GO (D8 broken — Phase 2 needs a different cookie-clear strategy).
6. **D8 gate evaluated (death modes):** `artifacts/spike/death_modes.txt` documents SIGKILL flip behavior (and SIGSTOP). If SIGKILL does not flip `is_connected()` to false: §Browser Status: NO-GO. If SIGSTOP does not flip: §Browser Status: NEEDS-PIVOT + Phase 2 plan 02-01 attention note in §Risks.
7. **§Browser Status line is concrete** (GO / NO-GO / NEEDS-PIVOT, not the `GO | NO-GO | NEEDS-PIVOT` scaffold) and rationale references the load-bearing finding (e.g. "SIGSTOP did not flip is_connected() in 30s — Phase 2 needs heartbeat").
8. **No D6/D8 foot-guns present:** `grep` confirms `launch_persistent_context` and `uvloop` do not appear in any spike script (excluding comments).
9. **No leaked state:** No Cloak/Chromium processes left running after task 4; `.env.spike` not committed.
10. **All artifacts committed** (probe scripts + captured artifacts under `artifacts/spike/` + fixtures under `tests/fixtures/serp/` + the partially-filled `.planning/SPIKE.md`).
</success_criteria>

<output>
Create `.planning/phases/01-spike-empirical-validation/01-01-SUMMARY.md` when done, documenting:
- D1 verdict (Hub tag exists/missing; chromium tag observed).
- Cloak Python import path resolved (`cloakbrowser` vs `playwright_async_api_post_cloak`).
- pws=0 verdict (clean / pivot) — affects Phase 2 plan 02-01 if pivot.
- Cookie isolation verdict (PASS / FAIL) — affects D8 confidence.
- Death-mode table: SIGKILL flip lag, SIGSTOP flip lag (or NEVER), network/OOM (n/a or measured).
- Whether `_recycle_browser_loop` in Phase 2 needs a secondary heartbeat (SIGSTOP did not flip).
- §Browser Status line set in SPIKE.md.
- 01-02 and 01-03 prerequisites confirmed: `.env.spike` infrastructure exists, fixture dirs exist, SPIKE.md scaffold present for them to append into.
</output>
