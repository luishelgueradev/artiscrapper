---
phase: 03.1-v0-1-close-hygiene
plan: 02
type: execute
wave: 2
depends_on: [03.1-01]
files_modified:
  - tests/test_e2e.py
  - tests/integration/test_degraded_mode.py
  - src/artiscrapper/visit.py
  - tests/test_visit.py
  - tests/test_footguns.py
  - tests/test_health.py
autonomous: true
requirements: []
closes_audit_items:
  - "v0.1-MILESTONE-AUDIT tech_debt[2] — tests/test_e2e.py X-API-Key header"
  - "v0.1-MILESTONE-AUDIT tech_debt[3] — degraded-mode TestClient coverage"
  - "v0.1-MILESTONE-AUDIT tech_debt[5] — WR-01, WR-02 (verify-only), WR-03, WR-04 cleanup"
closes_context_decisions: [D-03, D-04, D-09]

must_haves:
  truths:
    - "tests/test_e2e.py POST calls carry X-API-Key header sourced from E2E_API_KEY env (D-03)"
    - "tests/integration/test_degraded_mode.py contains test_degraded_mode_via_search_endpoint that exercises POST /search through TestClient with real SERP fixture, asserts metadata.llm_degraded is True AND len(results) >= 3 (D-04)"
    - "src/artiscrapper/visit.py MELI guard uses tldextract.registered_domain match against a frozenset of MELI registered domains (WR-01)"
    - "src/artiscrapper/visit.py guard does NOT false-positive on notmercadolibre.com (WR-01 regression test)"
    - "src/artiscrapper/main.py cache-write task strong-ref pattern verified in place (WR-02 verify-only — NO source diff)"
    - "tests/test_footguns.py::test_recycle_triggers contains a real boundary invariant, not a tautology (WR-03)"
    - "tests/test_health.py uses tempfile.mkdtemp + atexit cleanup instead of NamedTemporaryFile(delete=False) (WR-04)"
    - "pytest tests/ -x -q -k 'not e2e' passes green after all changes"
  artifacts:
    - path: "tests/test_e2e.py"
      provides: "X-API-Key header on the two POST /search TestClient calls"
      contains: 'X-API-Key'
    - path: "tests/integration/test_degraded_mode.py"
      provides: "Full-route degraded-mode coverage via TestClient + real fixture"
      contains: "test_degraded_mode_via_search_endpoint"
    - path: "src/artiscrapper/visit.py"
      provides: "tldextract-based MELI guard (suffix-aware)"
      contains: "tldextract"
    - path: "tests/test_visit.py"
      provides: "Regression test pinning the WR-01 fix"
      contains: "test_meli_guard_no_false_positive_on_notmercadolibre"
    - path: "tests/test_footguns.py"
      provides: "Real BROWSER_RECYCLE_AFTER boundary invariant (no tautology)"
      contains: "settings.BROWSER_RECYCLE_AFTER == 200"
    - path: "tests/test_health.py"
      provides: "Leak-free tempdir for CACHE_DB_PATH with atexit cleanup"
      contains: "tempfile.mkdtemp"
  key_links:
    - from: "tests/integration/test_degraded_mode.py::test_degraded_mode_via_search_endpoint"
      to: "src/artiscrapper/main.py POST /search degraded-mode branch"
      via: "TestClient + monkeypatched fetch_serp + router_health_check (Pattern S5)"
      pattern: "monkeypatch.setattr.*router_health_check"
    - from: "src/artiscrapper/visit.py MELI guard"
      to: "tldextract.extract().registered_domain"
      via: "frozenset membership check on the registered domain (public-suffix-aware)"
      pattern: "tldextract.extract"
    - from: "tests/test_visit.py::test_meli_guard_no_false_positive_on_notmercadolibre"
      to: "src/artiscrapper/visit.py MELI guard"
      via: "fixture URL notmercadolibre.com — must NOT trigger meli_skip flag"
      pattern: "notmercadolibre"
---

<objective>
Close the test-coverage + code-hygiene audit items in a single wave that is fully independent of Plan 03.1-01 (no file overlap — runs in parallel-eligible Wave 2). All edits are tests + one small src/ change (WR-01 in visit.py) + one verify-only confirmation (WR-02 in main.py — NO diff to src/).

Purpose: D-03 (X-API-Key headers in e2e tests) unblocks the runnable e2e suite Phase 3 silently broke. D-04 (degraded-mode TestClient sibling) closes the audit-flagged coverage gap that the existing trigger-only test left open. The 4 WR items are opportunistic cleanup that ride along because they touch test files (low risk + sub-task scope). Crucially, WR-02 is **verify-only** per RESEARCH §5 / R-02 — the strong-ref pattern is ALREADY in place at src/artiscrapper/main.py:207 + main.py:668-670; re-fixing would produce a no-op diff and falsely claim a fix.

Output:
- 2 X-API-Key headers added to tests/test_e2e.py (D-03)
- 1 new integration test function in tests/integration/test_degraded_mode.py (D-04)
- 1 src/ change: tldextract-based MELI guard in src/artiscrapper/visit.py (WR-01)
- 1 new regression test in tests/test_visit.py (WR-01 false-positive guard)
- 1 verify-only commit message confirming WR-02 strong-ref pattern is in place (no source diff)
- 1 test rewrite in tests/test_footguns.py (WR-03 tautology removal)
- 1 test fixture rewrite in tests/test_health.py (WR-04 NamedTemporaryFile→mkdtemp+atexit)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v0.1-MILESTONE-AUDIT.md
@.planning/phases/03.1-v0-1-close-hygiene-requirements-md-traceability-flip-summary/03.1-CONTEXT.md
@.planning/phases/03.1-v0-1-close-hygiene-requirements-md-traceability-flip-summary/03.1-RESEARCH.md
@.planning/phases/03.1-v0-1-close-hygiene-requirements-md-traceability-flip-summary/03.1-VALIDATION.md
@.planning/phases/02-mvp/02-REVIEW.md
@src/artiscrapper/main.py
@src/artiscrapper/visit.py
@src/artiscrapper/freshness.py
@src/artiscrapper/auth.py
@src/artiscrapper/llm.py
@tests/test_e2e.py
@tests/integration/test_degraded_mode.py
@tests/integration/test_challenge_backoff.py
@tests/test_visit.py
@tests/test_footguns.py
@tests/test_health.py
@tests/integration/conftest.py
@pyproject.toml

<interfaces>
<!-- Current visit.py MELI guard (verified at lines 328-332): -->

# VISIT-08: FIRST CHECK — never visit *.mercadolibre.*
# This is a security control (architecture-level), not optional.
if "mercadolibre." in urlparse(url).netloc:
    candidate["flags"] = candidate.get("flags", []) + ["meli_skip"]
    return candidate

<!-- visit.py existing imports (line 11-19): asyncio, json, re, defaultdict, urlparse, httpx, structlog, HTMLParser. tldextract NOT yet imported in this module. -->
<!-- pyproject.toml line 19 confirms tldextract==5.3.1 is pinned. metrics.py:26 already imports it. -->

<!-- Current main.py strong-ref pattern (verified — DO NOT modify): -->
# main.py:207 (lifespan):  app.state.background_tasks = set()
# main.py:665-670 (in /search):
_cache_task = asyncio.create_task(_write_cache())
request.app.state.background_tasks.add(_cache_task)
_cache_task.add_done_callback(request.app.state.background_tasks.discard)

<!-- Current main.py health endpoints (verified — these will be touched in Plan 03.1-03, NOT here): -->
@app.get("/health")           # line 269
async def health(request: Request) -> dict: ...

@app.get("/health/deep")      # line 289
async def health_deep(request: Request, _api_key: str = Depends(verify_api_key)) -> dict: ...

<!-- tests/test_health.py current import sequence (verified lines 18-21): -->
_tmp_db = tempfile.NamedTemporaryFile(suffix=".db", delete=False)
_tmp_db.close()
os.environ.setdefault("LLM_ROUTER_BEARER_TOKEN", "test-token-health")
os.environ["CACHE_DB_PATH"] = _tmp_db.name   # ← UNCONDITIONAL OVERRIDE — bug per WR-04

<!-- tests/test_footguns.py current tautology (verified line 70-75): -->
BROWSER_RECYCLE_AFTER = 200  # locally bound, not imported
assert BROWSER_RECYCLE_AFTER >= BROWSER_RECYCLE_AFTER, (...)   # ← tautology

<!-- tests/integration/test_challenge_backoff.py Pattern S5 (lines ~33-76, ~157-258) is the verbatim mirror for D-04 -->
<!-- tests/integration/test_degraded_mode.py currently only covers TRIGGER + reimplemented _heuristic_only_survivors, NOT the full route -->

<!-- Existing fixture: tests/fixtures/serp/01-pelota_playera_quico.html (≥15 carousel + ≥18 with price per tests/test_parser.py:114) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add X-API-Key header to tests/test_e2e.py POST calls (D-03)</name>
  <files>tests/test_e2e.py</files>
  <read_first>
    - tests/test_e2e.py (full file — note `httpx.Client` calls at lines ~46 and ~87 lacking headers kwarg)
    - .planning/phases/03.1-v0-1-close-hygiene-requirements-md-traceability-flip-summary/03.1-CONTEXT.md (D-03)
    - .planning/phases/03.1-v0-1-close-hygiene-requirements-md-traceability-flip-summary/03.1-RESEARCH.md (User Constraints D-03)
    - src/artiscrapper/auth.py (to confirm header name is exactly "X-API-Key" — case-sensitive)
  </read_first>
  <action>
Add `headers={"X-API-Key": os.getenv("E2E_API_KEY", "test-key-1")}` as a kwarg to the two `client.post("/search", ...)` calls in tests/test_e2e.py (currently at line ~46 inside test_serp_pelota and line ~87 inside test_cache_hit per D-03). Both calls already exist; add the headers kwarg next to the existing `json={...}` argument.

The test file already imports `os` (verified line 8 of test_e2e.py reads `import os`). If `os` is NOT already imported, add `import os` to the top of the file alongside the existing stdlib imports.

Pattern (apply at both sites):
  resp = client.post(
      "/search",
      json={"query": "pelota playera quico"},
      headers={"X-API-Key": os.getenv("E2E_API_KEY", "test-key-1")},
  )

Per RESEARCH User Constraints D-03 — Phase 3 made API-key auth mandatory; without this header these e2e tests would fail 401 against the dev-box (the suite is gated by E2E=1 env so CI stays green, but the moment Luis runs them they 401).

DO NOT change the test bodies, assertions, fixtures, or `@pytest.mark.skipif` decorators. Only the two POST calls get the new kwarg.
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/artiscrapper &amp;&amp; H=$(grep -c "X-API-Key" tests/test_e2e.py); EAPI=$(grep -c "E2E_API_KEY" tests/test_e2e.py); test "$H" -ge 2 &amp;&amp; test "$EAPI" -ge 2 &amp;&amp; echo "D-03 PASS: X-API-Key=$H, E2E_API_KEY=$EAPI (both >=2)" || (echo "D-03 FAIL: X-API-Key=$H (want >=2), E2E_API_KEY=$EAPI (want >=2)"; exit 1)</automated>
  </verify>
  <done>tests/test_e2e.py has ≥2 occurrences of `X-API-Key` and ≥2 occurrences of `E2E_API_KEY`; no test bodies/assertions changed; the file still imports cleanly (`python -c "import ast; ast.parse(open('tests/test_e2e.py').read())"` succeeds).</done>
</task>

<task type="auto">
  <name>Task 2: Append test_degraded_mode_via_search_endpoint to tests/integration/test_degraded_mode.py (D-04)</name>
  <files>tests/integration/test_degraded_mode.py</files>
  <read_first>
    - tests/integration/test_degraded_mode.py (full file — note current test only covers TRIGGER + standalone _heuristic_only_survivors; the new test exercises the REAL /search route)
    - tests/integration/test_challenge_backoff.py (lines 33-76 = cross-test env-var contract; lines 157-258 = Pattern S5 mirror — copy verbatim per R-04)
    - .planning/phases/03.1-v0-1-close-hygiene-requirements-md-traceability-flip-summary/03.1-RESEARCH.md §6 (D-04 fixture choice + test skeleton — lines 759-944 — full reference implementation)
    - .planning/phases/03.1-v0-1-close-hygiene-requirements-md-traceability-flip-summary/03.1-CONTEXT.md (D-04 + discretion item on fixture choice)
    - tests/fixtures/serp/01-pelota_playera_quico.html (the chosen fixture — has ≥15 carousel + ≥18 with price per tests/test_parser.py:114)
    - src/artiscrapper/main.py (lines 354-475 = /search route shape; the degraded branch lives around line ~470-478)
    - src/artiscrapper/auth.py (cross-test API_KEYS merge contract per RESEARCH R-04)
  </read_first>
  <action>
APPEND (do not replace) a new test function `test_degraded_mode_via_search_endpoint(monkeypatch)` to tests/integration/test_degraded_mode.py, plus the cross-test env-var setup needed at the file top to make the FastAPI app importable. The reference implementation lives in RESEARCH §6 lines 791-939 — copy it verbatim, with one fixture pinned to `tests/fixtures/serp/01-pelota_playera_quico.html` per RESEARCH §6 fixture-choice rationale (PRD §10 canonical query; ≥15 carousel; well-trodden in test_parser.py:145,163).

Required changes:

1. **At the TOP of the file, BEFORE any `from src.artiscrapper...` import** — add the cross-test merge pattern per RESEARCH §6 lines 800-813 (mirrors tests/integration/test_challenge_backoff.py:33-76 exactly). This handles: tmp CACHE_DB_PATH, LLM_ROUTER_BEARER_TOKEN setdefault, API_KEYS merge with `test-key-dm`, SENTRY_DSN setdefault to empty string. Then import `fastapi.testclient.TestClient`, `auth as _auth_module`, `main.app` and refresh `_auth_module.API_KEYS` from the merged env.

2. **Add the SERP_FIXTURE pathlib reference** + `_make_mock_browser()` helper (mirrors tests/test_health.py::_make_mock_browser — small AsyncMock factory for the Cloak browser singleton).

3. **The new test function `test_degraded_mode_via_search_endpoint(monkeypatch)`** — RESEARCH §6 lines 854-938:
   - Loads serp_html from `01-pelota_playera_quico.html`
   - Defines `_fake_fetch_serp` returning `(serp_html, None)` matching production shape
   - Defines `_fake_router_health` returning `False` (the degraded trigger)
   - Monkeypatches `src.artiscrapper.main.launch_async`, `src.artiscrapper.main.fetch_serp`, `src.artiscrapper.main.router_health_check`
   - Resets `challenge_backoff._STATE = None` to prevent sibling-test pollution
   - Inside `with TestClient(app)`: resets the slowapi limiter (try/except for sibling-test bleed defense per the pattern in test_challenge_backoff.py:209), resets challenge_backoff state again, POSTs /search with `headers={"X-API-Key": "test-key-dm"}` and body `{"query": "pelota playera quico", "max_results": 10}`
   - Three pinned assertions (per CONTEXT D-04 + D-18 alignment):
     - `r.status_code == 200` (NOT 503 — degraded must still return 200)
     - `metadata.get("llm_degraded") is True`
     - `len(results) > 0` AND `len(results) >= 3` (D-18 alignment — never bare `>0` per RESEARCH §6 + 02-HUMAN-UAT G-01)

DO NOT shortcut the cross-test env merge pattern per R-04 — sibling test API_KEYS bleed is the exact bug Plan 03-02 had to fix in Phase 3 Task 3.

DO NOT introduce synthetic HTML or hand-rolled stub responses — D-04 mandates the real Phase 1 SERP fixture per `feedback_verify_volatile_data`.

DO NOT modify the existing test_llm_down_keeps_useful_results function — append the new test below it (the file already has the imports/fixtures it needs for the existing test; new test's needs are appended on top).
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/artiscrapper &amp;&amp; LLM_ROUTER_BEARER_TOKEN=test-token uv run pytest tests/integration/test_degraded_mode.py::test_degraded_mode_via_search_endpoint -x -q 2>&amp;1 | tee /tmp/d04.log; grep -qE "1 passed" /tmp/d04.log &amp;&amp; echo "D-04 PASS: degraded-mode TestClient test green" || (echo "D-04 FAIL: see /tmp/d04.log"; tail -30 /tmp/d04.log; exit 1)</automated>
  </verify>
  <done>`pytest tests/integration/test_degraded_mode.py::test_degraded_mode_via_search_endpoint -x` passes; the test asserts status_code==200, metadata.llm_degraded is True, and len(results) ≥ 3 against the real `01-pelota_playera_quico.html` fixture; the existing `test_llm_down_keeps_useful_results` test still passes.</done>
</task>

<task type="auto">
  <name>Task 3: WR-01 — replace MELI substring guard with tldextract registered_domain match + regression test</name>
  <files>src/artiscrapper/visit.py, tests/test_visit.py</files>
  <read_first>
    - src/artiscrapper/visit.py (lines 1-25 imports + lines 315-340 = MELI guard + visit_one entry)
    - src/artiscrapper/metrics.py (line 26 = existing tldextract import pattern; line 112 = `ext = tldextract.extract(url_or_host)` usage)
    - src/artiscrapper/freshness.py (lines 15-25 = canonical MELI_HOSTS frozenset — read for context, do NOT modify)
    - tests/test_visit.py (full file — note existing `test_meli_guard` and `test_meli_guard_no_http` tests for reference; new test appends at the end)
    - .planning/phases/03.1-v0-1-close-hygiene-requirements-md-traceability-flip-summary/03.1-RESEARCH.md §5 / WR-01 (lines 562-633 = full fix recipe + recommended frozenset)
    - .planning/phases/03.1-v0-1-close-hygiene-requirements-md-traceability-flip-summary/03.1-CONTEXT.md (D-09 / WR-01)
    - pyproject.toml line 19 (confirm tldextract==5.3.1 already pinned — no new dep)
  </read_first>
  <action>
**Two edits — one source change, one test addition. Both ship in this one task because the test is the regression pin for the source change.**

**Edit A — src/artiscrapper/visit.py:**

1. Add `import tldextract` to the import block (alphabetical position alongside other third-party imports — currently `import httpx`, `import structlog`, `from selectolax.parser import HTMLParser` at lines 17-19; insert `import tldextract` after `import structlog`).

2. Add a module-level frozenset `_MELI_REGISTERED_DOMAINS` near the top of the module (alongside `DEFAULT_HEADERS` constant) — verbatim from RESEARCH §5 / WR-01 lines 603-612:

   _MELI_REGISTERED_DOMAINS = frozenset({
       "mercadolibre.com.ar",
       "mercadolibre.com",
       "mercadolibre.com.mx",
       "mercadolibre.cl",
       "mercadolibre.com.uy",
       "mercadolibre.com.br",
       "mercadolibre.com.co",
       "mercadolibre.com.pe",
   })

   Include a comment block above the frozenset explaining WR-01: "substring `'mercadolibre.' in netloc` over-matches notmercadolibre.com / mercadoliberia.com; tldextract is public-suffix-aware and registered_domain comparison eliminates the false positive. Sister registry is freshness.MELI_HOSTS (full hostnames including listado./articulo. subdomains for the date-based freshness path); they intentionally serve different purposes (freshness wants HOSTS, the visit guard wants REGISTERED_DOMAIN)."

3. Replace lines 328-332 (the existing substring check) with:

   # VISIT-08 + WR-01 (Phase 3.1): suffix-aware MELI match
   # Uses tldextract (pinned at 5.3.1, already used in metrics.py) to compare
   # on registered_domain — public-suffix-aware. Eliminates false positives
   # on notmercadolibre.com, mercadoliberia.com, etc.
   ext = tldextract.extract(url)
   if ext.registered_domain.lower() in _MELI_REGISTERED_DOMAINS:
       candidate["flags"] = candidate.get("flags", []) + ["meli_skip"]
       return candidate

DO NOT introduce a parallel `MELI_HOSTS_SUFFIX` tuple. DO NOT modify freshness.MELI_HOSTS (it intentionally lists full hosts incl. `listado.mercadolibre.com.ar` for date-based freshness lookup, not registered domains — different purpose per RESEARCH §5 / WR-01 alternative-rejected paragraph).

**Edit B — tests/test_visit.py:**

Append a new test function at the end of the file (after existing tests):

   async def test_meli_guard_no_false_positive_on_notmercadolibre():
       """
       WR-01 regression: tldextract.registered_domain match must NOT fire on
       notmercadolibre.com. The pre-Phase-3.1 substring check `"mercadolibre."
       in netloc` would have falsely returned meli_skip for this URL.
       """
       cand = {"url": "https://notmercadolibre.com/product-123"}
       result = await visit_one(cand)
       assert "meli_skip" not in result.get("flags", []), (
           "WR-01: notmercadolibre.com is NOT a MELI domain — guard must not fire"
       )

The exact import surface needed and the visit_one symbol exposure in tests/test_visit.py — copy the import pattern from the existing test_meli_guard test in the same file (the visit_one helper is exposed via the same mechanism). If visit_one is nested inside visit_candidates (verified at src/artiscrapper/visit.py:325 — it IS nested), import the module-level wrapper that the existing test uses; do not refactor the source to expose visit_one publicly.

Note on R-10 (tldextract first-call latency): tldextract may fetch the Public Suffix List on first call. metrics.py already uses tldextract, so the dev-box cache is warm. If CI gets flaky, RESEARCH §5 has the offline fallback `tldextract.TLDExtract(suffix_list_urls=())` — DO NOT pre-apply this; only ship it if the regression test goes red on CI.
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/artiscrapper &amp;&amp; TLD=$(grep -c "tldextract" src/artiscrapper/visit.py); FROZ=$(grep -c "_MELI_REGISTERED_DOMAINS" src/artiscrapper/visit.py); SUBSTR=$(grep -c '"mercadolibre\." in' src/artiscrapper/visit.py); test "$TLD" -ge 2 &amp;&amp; test "$FROZ" -ge 2 &amp;&amp; test "$SUBSTR" -eq 0 &amp;&amp; LLM_ROUTER_BEARER_TOKEN=test-token uv run pytest tests/test_visit.py::test_meli_guard_no_false_positive_on_notmercadolibre tests/test_visit.py::test_meli_guard tests/test_visit.py::test_meli_guard_no_http -x -q 2>&amp;1 | tee /tmp/wr01.log &amp;&amp; grep -qE "3 passed" /tmp/wr01.log &amp;&amp; echo "WR-01 PASS: tldextract import + frozenset + regression test green + sibling tests green" || (echo "WR-01 FAIL: tldextract=$TLD (want >=2) frozenset=$FROZ (want >=2) substring=$SUBSTR (want 0); see /tmp/wr01.log"; tail -30 /tmp/wr01.log; exit 1)</automated>
  </verify>
  <done>visit.py imports tldextract + defines _MELI_REGISTERED_DOMAINS frozenset; the substring check `"mercadolibre." in` is removed; tests/test_visit.py contains test_meli_guard_no_false_positive_on_notmercadolibre + the existing test_meli_guard and test_meli_guard_no_http tests still pass (regression coverage intact for the true-positive MELI hosts).</done>
</task>

<task type="auto">
  <name>Task 4: WR-02 verify-only — confirm cache-write task strong-ref pattern already in place (NO source diff)</name>
  <files>src/artiscrapper/main.py</files>
  <read_first>
    - src/artiscrapper/main.py (lines 195-210 = lifespan background_tasks set init; lines 660-675 = /search cache-write task strong-ref pattern)
    - .planning/phases/03.1-v0-1-close-hygiene-requirements-md-traceability-flip-summary/03.1-RESEARCH.md §5 / WR-02 (lines 635-667 = "DISCOVERY: WR-02 is ALREADY FIXED" + the exact lines + verify-only recommendation)
    - .planning/phases/03.1-v0-1-close-hygiene-requirements-md-traceability-flip-summary/03.1-RESEARCH.md §8 / R-02 (lines 1086-1090 = do-not-re-fix mitigation)
    - .planning/phases/02-mvp/02-REVIEW.md WR-02 entry (the original audit text — note it predates Phase 3 line numbers)
  </read_first>
  <action>
**This task produces ZERO diff to src/. It is a documentation-and-verification task.** Per RESEARCH §5 / R-02, the strong-ref pattern for the cache-write asyncio.create_task is ALREADY in place. The audit text in v0.1-MILESTONE-AUDIT.md tech_debt[5] references pre-Phase-3 line numbers; the pattern was wired during Plan 03-01 lifespan setup.

Verify the pattern is present at the following exact locations and document the finding in the plan SUMMARY (not as a code edit, as a SUMMARY note):

1. **Lifespan init (main.py:207):**
     app.state.background_tasks = set()

2. **In POST /search route (main.py:665-670):**
     # WR-03: retain a strong reference to the task so Python doesn't
     # garbage-collect it mid-flight (asyncio.create_task keeps only a
     # weak ref). The done_callback removes it once the task finishes.
     _cache_task = asyncio.create_task(_write_cache())
     request.app.state.background_tasks.add(_cache_task)
     _cache_task.add_done_callback(request.app.state.background_tasks.discard)

(Note the in-source comment label says "WR-03" but the *pattern* is the strong-ref fix the audit tracks as "WR-02". This is a labeling drift between code comments and audit IDs; the pattern itself is correct.)

DO NOT modify src/artiscrapper/main.py in this task. The task's deliverable is:
- Run the two grep commands in <verify> below to confirm the pattern is in place
- In 03.1-02-SUMMARY.md, add a note under the WR cleanup section: "WR-02: verify-only — strong-ref pattern confirmed in place at main.py:207 (background_tasks init) + main.py:668-670 (cache-write task strong-ref + done_callback). Pattern was wired during Plan 03-01 lifespan setup; audit text referenced pre-Phase-3 line numbers. Mark WR-02 RESOLVED in any downstream audit-tracking artifact."
- Commit message for this task should read: `chore(03.1-02): verify-only — WR-02 strong-ref pattern already in place in main.py (no diff)`

Per R-02, if this verify-only step is skipped and a "fix" is emitted instead, the diff will be a no-op AND the SUMMARY will falsely claim a fix. Skipping this task is the failure mode the planner is preventing.
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/artiscrapper &amp;&amp; INIT=$(grep -c "app.state.background_tasks = set()" src/artiscrapper/main.py); ADD=$(grep -c "background_tasks.add" src/artiscrapper/main.py); CB=$(grep -c "background_tasks.discard" src/artiscrapper/main.py); CREATE=$(grep -c "_cache_task = asyncio.create_task" src/artiscrapper/main.py); test "$INIT" -ge 1 &amp;&amp; test "$ADD" -ge 1 &amp;&amp; test "$CB" -ge 1 &amp;&amp; test "$CREATE" -ge 1 &amp;&amp; echo "WR-02 PASS: strong-ref pattern verified — init=$INIT, add=$ADD, discard_callback=$CB, create_task=$CREATE (all >=1; no source diff required)" || (echo "WR-02 FAIL: init=$INIT add=$ADD discard=$CB create=$CREATE — pattern missing; investigate before claiming fix"; exit 1)</automated>
  </verify>
  <done>All four greps return ≥1 hit each confirming the strong-ref pattern is in place; `git diff src/artiscrapper/main.py` is empty for this task's commit (no source changes); the finding is documented in 03.1-02-SUMMARY.md.</done>
</task>

<task type="auto">
  <name>Task 5: WR-03 — replace tautological assertion in tests/test_footguns.py::test_recycle_triggers</name>
  <files>tests/test_footguns.py</files>
  <read_first>
    - tests/test_footguns.py (full file — note line 62-82 = test_recycle_triggers with tautology at lines 73-75)
    - src/artiscrapper/config.py (confirm `BROWSER_RECYCLE_AFTER: int = 200` default; ~line 42-47)
    - .planning/phases/03.1-v0-1-close-hygiene-requirements-md-traceability-flip-summary/03.1-RESEARCH.md §5 / WR-03 (lines 669-712 = full recipe + the env-var setup constraint per R-03)
    - .planning/phases/03.1-v0-1-close-hygiene-requirements-md-traceability-flip-summary/03.1-CONTEXT.md (D-09 / WR-03)
    - tests/test_health.py (lines 18-21 — reference for the LLM_ROUTER_BEARER_TOKEN setdefault pattern needed before importing settings per R-03)
  </read_first>
  <action>
Replace the tautological assertion in tests/test_footguns.py::test_recycle_triggers (currently at lines 70-75) with the real boundary invariant from RESEARCH §5 / WR-03 lines 685-708. The new test pins the default value (200) AND the upper cap (500 per Phase 1 SPIKE memory-drift cliff).

**Required changes:**

1. At the TOP of tests/test_footguns.py (before any `from src.artiscrapper...` import), add the env-var setdefault pattern that mirrors tests/test_health.py:20:

   import os
   os.environ.setdefault("LLM_ROUTER_BEARER_TOKEN", "test-token-footguns")

   This is required per R-03 — the WR-03 fix re-introduces `from src.artiscrapper.config import settings` which previously was deliberately avoided (existing test comment at line 69: "avoid importing settings which requires LLM token"). The setdefault pattern unlocks the import without breaking the test's stand-alone-ness.

2. Replace the body of `test_recycle_triggers` (lines 62-82 approximately) with the recipe from RESEARCH §5 / WR-03 lines 685-708 — verbatim:

   def test_recycle_triggers():
       """
       BROWSER-03: Recycle condition fires when browser_uses >= BROWSER_RECYCLE_AFTER.
       """
       from src.artiscrapper.config import settings

       # WR-03 fix: pin the DEFAULT against the source of truth + the cap.
       # If a future change bumps settings.BROWSER_RECYCLE_AFTER past 500 (the
       # recycle-too-late cliff documented in Phase 1 SPIKE), this test fails.
       assert settings.BROWSER_RECYCLE_AFTER == 200, (
           f"WR-03: BROWSER_RECYCLE_AFTER drifted from documented default 200; "
           f"got {settings.BROWSER_RECYCLE_AFTER}. Update test + SPIKE.md if intentional."
       )
       assert settings.BROWSER_RECYCLE_AFTER <= 500, (
           "BROWSER-03: recycle threshold above 500 risks Chromium memory drift "
           "(Phase 1 SPIKE.md §Browser; Playwright issue #15400)"
       )

       # Pin the actual recycle condition's boundary semantics.
       uses_over_threshold = settings.BROWSER_RECYCLE_AFTER + 1
       assert uses_over_threshold >= settings.BROWSER_RECYCLE_AFTER, (
           f"BROWSER-03: uses={uses_over_threshold} should trigger recycle "
           f"(BROWSER_RECYCLE_AFTER={settings.BROWSER_RECYCLE_AFTER})"
       )

       # Below-threshold case (regression pin):
       uses_below_threshold = settings.BROWSER_RECYCLE_AFTER - 1
       assert uses_below_threshold < settings.BROWSER_RECYCLE_AFTER, (
           f"BROWSER-03: uses={uses_below_threshold} should NOT trigger recycle"
       )

DO NOT introduce a local `BROWSER_RECYCLE_AFTER = 200` rebind (the old test did this; the fix uses `settings.BROWSER_RECYCLE_AFTER` everywhere). DO NOT remove the test entirely — the BROWSER-03 invariant matters; only the assertion shape changes.

DO NOT touch the other tests in this file (`test_no_uvloop`, `test_d2_confidence_cutoff`, etc.) — they have nothing to do with WR-03.
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/artiscrapper &amp;&amp; TAUT=$(grep -c "BROWSER_RECYCLE_AFTER >= BROWSER_RECYCLE_AFTER" tests/test_footguns.py); IMP=$(grep -c "from src.artiscrapper.config import settings" tests/test_footguns.py); PIN=$(grep -c "settings.BROWSER_RECYCLE_AFTER == 200" tests/test_footguns.py); CAP=$(grep -c "settings.BROWSER_RECYCLE_AFTER &lt;= 500" tests/test_footguns.py); test "$TAUT" -eq 0 &amp;&amp; test "$IMP" -ge 1 &amp;&amp; test "$PIN" -eq 1 &amp;&amp; test "$CAP" -eq 1 &amp;&amp; LLM_ROUTER_BEARER_TOKEN=test-token uv run pytest tests/test_footguns.py -x -q 2>&amp;1 | tee /tmp/wr03.log &amp;&amp; grep -qE "passed" /tmp/wr03.log &amp;&amp; ! grep -qE "failed|error" /tmp/wr03.log &amp;&amp; echo "WR-03 PASS: tautology=$TAUT (want 0), settings import=$IMP (want >=1), pin=$PIN (want 1), cap=$CAP (want 1), all footguns tests pass" || (echo "WR-03 FAIL: tautology=$TAUT (want 0), settings import=$IMP, pin=$PIN, cap=$CAP; see /tmp/wr03.log"; tail -30 /tmp/wr03.log; exit 1)</automated>
  </verify>
  <done>Tautological `>= BROWSER_RECYCLE_AFTER` assertion removed; `from src.artiscrapper.config import settings` imported with prerequisite `LLM_ROUTER_BEARER_TOKEN` setdefault; both pin (== 200) and cap (<= 500) assertions present; all tests in tests/test_footguns.py pass.</done>
</task>

<task type="auto">
  <name>Task 6: WR-04 — replace NamedTemporaryFile with mkdtemp + atexit cleanup in tests/test_health.py</name>
  <files>tests/test_health.py</files>
  <read_first>
    - tests/test_health.py (lines 1-40 — current NamedTemporaryFile + unconditional CACHE_DB_PATH override at lines 17-21)
    - .planning/phases/03.1-v0-1-close-hygiene-requirements-md-traceability-flip-summary/03.1-RESEARCH.md §5 / WR-04 (lines 714-755 = full fix recipe with the `setdefault` correction + verification script)
    - .planning/phases/03.1-v0-1-close-hygiene-requirements-md-traceability-flip-summary/03.1-CONTEXT.md (D-09 / WR-04)
    - tests/integration/test_challenge_backoff.py (lines 50-60 — reference for the cross-test setdefault pattern)
  </read_first>
  <action>
Replace lines 17-21 of tests/test_health.py with the WR-04 fix from RESEARCH §5 lines 730-744. Two distinct changes:

1. **`tempfile.mkdtemp` instead of `tempfile.NamedTemporaryFile(delete=False)`:** mkdtemp returns a path string with no open file handle, register an `atexit` callback to recursively remove the dir on interpreter exit. This eliminates the leak of orphan `/tmp/tmp*.db` files that the current pattern produces every test run.

2. **`os.environ.setdefault("CACHE_DB_PATH", _TMP_DB_PATH)` instead of `os.environ["CACHE_DB_PATH"] = _tmp_db.name`:** the unconditional override at line 21 silently clobbers cross-test state; align with the cross-test contract used in tests/integration/test_challenge_backoff.py:52 (first-importing test wins, siblings do not clobber).

**Required edits — replace lines 8-21 (imports + tmp setup) with:**

   import atexit
   import os
   import shutil
   import tempfile
   from unittest.mock import AsyncMock, MagicMock

   import httpx
   import pytest
   import respx

   # WR-04: tempfile.mkdtemp + atexit cleanup — eliminates the orphan /tmp/tmp*.db
   # leak that NamedTemporaryFile(delete=False) produced every test run.
   # The tmp DIR (not just the file) is rm-recursed at interpreter exit.
   _TMP_DB_DIR = tempfile.mkdtemp(prefix="artiscrapper-test-health-")
   _TMP_DB_PATH = os.path.join(_TMP_DB_DIR, "cache.db")
   atexit.register(lambda: shutil.rmtree(_TMP_DB_DIR, ignore_errors=True))

   os.environ.setdefault("LLM_ROUTER_BEARER_TOKEN", "test-token-health")
   os.environ.setdefault("CACHE_DB_PATH", _TMP_DB_PATH)
   # ↑ WR-04: setdefault (not bracket assignment) honors the cross-test
   # contract — first-importing test wins, siblings do not clobber.

Then KEEP the existing CR-03 API_KEYS merge block (lines 22-27 of the current file) and the existing FastAPI/auth import block (lines 29-39) UNCHANGED — those are correct and serve a different purpose (cross-test API_KEYS merge per Phase 3 contract).

DO NOT switch to pytest's `tmp_path` fixture per the RESEARCH §5 / WR-04 constraint — CACHE_DB_PATH MUST be set at module-import time (before `from src.artiscrapper.main import app`); `tmp_path` is function-scope and fires too late.

**Verification of the leak fix:** record `/tmp/artiscrapper-test-health-*` count before and after a test run; they MUST match.
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/artiscrapper &amp;&amp; OLD=$(grep -c "NamedTemporaryFile" tests/test_health.py); MKD=$(grep -c "tempfile.mkdtemp" tests/test_health.py); ATX=$(grep -c "atexit.register" tests/test_health.py); SETD=$(grep -c 'os.environ.setdefault("CACHE_DB_PATH"' tests/test_health.py); BEFORE=$(ls /tmp/artiscrapper-test-health-* 2>/dev/null | wc -l); LLM_ROUTER_BEARER_TOKEN=test-token uv run pytest tests/test_health.py -x -q 2>&amp;1 | tee /tmp/wr04.log; AFTER=$(ls /tmp/artiscrapper-test-health-* 2>/dev/null | wc -l); test "$OLD" -eq 0 &amp;&amp; test "$MKD" -ge 1 &amp;&amp; test "$ATX" -ge 1 &amp;&amp; test "$SETD" -ge 1 &amp;&amp; grep -qE "passed" /tmp/wr04.log &amp;&amp; ! grep -qE "failed|error" /tmp/wr04.log &amp;&amp; test "$AFTER" -le "$(($BEFORE + 1))" &amp;&amp; echo "WR-04 PASS: NamedTemporaryFile removed (=$OLD), mkdtemp=$MKD, atexit=$ATX, setdefault=$SETD; tests pass; /tmp leak before=$BEFORE after=$AFTER (delta <=1 — atexit cleans on interpreter exit)" || (echo "WR-04 FAIL: NamedTemp=$OLD (want 0), mkdtemp=$MKD, atexit=$ATX, setdefault=$SETD, /tmp before=$BEFORE after=$AFTER; see /tmp/wr04.log"; tail -30 /tmp/wr04.log; exit 1)</automated>
  </verify>
  <done>tests/test_health.py contains 0 `NamedTemporaryFile` references, ≥1 `tempfile.mkdtemp`, ≥1 `atexit.register`, ≥1 `os.environ.setdefault("CACHE_DB_PATH"`; `pytest tests/test_health.py -x -q` passes; orphan `/tmp/artiscrapper-test-health-*` directories do NOT accumulate (atexit handles cleanup at interpreter exit).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /search (API tier) | Pre-existing — X-API-Key auth at the API tier; D-03 adds the header to e2e tests that were silently failing 401 |
| (no new boundaries) | All other edits are test-side or one source-side bug fix that TIGHTENS an existing security control (WR-01 MELI guard) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-01 | (none new) | Test + visit.py | accept | Per RESEARCH §Security Domain — Phase 3.1 hygiene has no new attack surface. The WR-01 fix TIGHTENS a pre-existing security control (VISIT-08 architecture-level MELI block); no new I/O channels; no new inputs. |
| T-03.1-WR01-FP | Tampering (false-positive denial-of-service self-inflicted) | src/artiscrapper/visit.py MELI guard | mitigate | The pre-fix substring `"mercadolibre." in netloc` over-matches notmercadolibre.com and would silently drop legitimate results (visit_failed inflation). The tldextract.registered_domain match is mitigated by Task 3 + pinned by tests/test_visit.py::test_meli_guard_no_false_positive_on_notmercadolibre regression test. |
</threat_model>

<verification>
**Plan-level gate after all 6 tasks pass:**

```bash
# 1. D-03 — X-API-Key headers in e2e tests
grep -c "X-API-Key" tests/test_e2e.py            # Expect: >= 2

# 2. D-04 — new degraded-mode TestClient test
LLM_ROUTER_BEARER_TOKEN=test-token uv run pytest tests/integration/test_degraded_mode.py -x -q

# 3. WR-01 — tldextract guard + regression
grep -c "tldextract" src/artiscrapper/visit.py   # Expect: >= 2
grep -c '"mercadolibre\." in' src/artiscrapper/visit.py  # Expect: 0
LLM_ROUTER_BEARER_TOKEN=test-token uv run pytest tests/test_visit.py -x -q

# 4. WR-02 — strong-ref pattern (no diff, grep only)
grep -c "background_tasks.add" src/artiscrapper/main.py    # Expect: >= 1

# 5. WR-03 — tautology removed + footguns pass
grep -c "BROWSER_RECYCLE_AFTER >= BROWSER_RECYCLE_AFTER" tests/test_footguns.py  # Expect: 0
LLM_ROUTER_BEARER_TOKEN=test-token uv run pytest tests/test_footguns.py -x -q

# 6. WR-04 — no NamedTemporaryFile, no /tmp accumulation
grep -c "NamedTemporaryFile" tests/test_health.py    # Expect: 0
LLM_ROUTER_BEARER_TOKEN=test-token uv run pytest tests/test_health.py -x -q

# 7. Full quick suite green (sampling rate per VALIDATION.md)
LLM_ROUTER_BEARER_TOKEN=test-token uv run pytest tests/ -x -q -k "not e2e"
```
</verification>

<success_criteria>
- 2 X-API-Key headers added to tests/test_e2e.py (no test logic changed)
- test_degraded_mode_via_search_endpoint added to tests/integration/test_degraded_mode.py and passing
- src/artiscrapper/visit.py uses tldextract.registered_domain match + matching regression test in tests/test_visit.py
- src/artiscrapper/main.py untouched (WR-02 verify-only — strong-ref pattern already in place at lines 207 + 668-670)
- tests/test_footguns.py::test_recycle_triggers pins settings.BROWSER_RECYCLE_AFTER == 200 + cap <= 500 (no tautology)
- tests/test_health.py uses tempfile.mkdtemp + atexit cleanup + setdefault for CACHE_DB_PATH
- `pytest tests/ -x -q -k "not e2e"` green
- pyproject.toml / uv.lock unchanged (no new deps — tldextract is already pinned at 5.3.1)
</success_criteria>

<output>
Create `.planning/phases/03.1-v0-1-close-hygiene-requirements-md-traceability-flip-summary/03.1-02-SUMMARY.md` when done. The SUMMARY must:
- Record closed audit items (tech_debt[2], tech_debt[3], tech_debt[5] partial — WR-01/02/03/04)
- Record closed CONTEXT.md decisions (D-03, D-04, D-09)
- Note that WR-02 was verify-only with ZERO source diff (per R-02 mitigation)
- Note that tldextract is pre-existing pin (no new dep added)
- Confirm the cross-test env merge pattern in test_degraded_mode.py mirrors test_challenge_backoff.py:33-76 (per R-04)
- List 0 changes to pyproject.toml / uv.lock
</output>
