# Phase 3.1: v0.1 close hygiene - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-04
**Phase:** 03.1 — v0.1 close hygiene: REQUIREMENTS.md traceability flip + SUMMARY frontmatter population + tests/test_e2e.py X-API-Key header + degraded-mode TestClient coverage + slowapi rate-limit settings-source-of-truth + Nyquist wave-0 accept
**Areas discussed:** Slowapi fix approach, Nyquist por fase, Alcance WR-01..04, Mock SERP en degraded-mode

---

## Slowapi rate-limit fix approach

| Option | Description | Selected |
|--------|-------------|----------|
| (a) Single source-of-truth refactor | Wire `settings.API_RATE_PER_MINUTE` al decorator real via lazy string / callable. Resuelve la causa raíz. | ✓ |
| (b) Log decorator strings reales | Cambio mínimo: `rate_limit_init` loguea los limits efectivos. NO arregla, solo hace visible. Riesgo de seguir teniendo divergencia. | |
| (c) Híbrido (a + log) | Refactor settings→decorator AND log de los limits efectivos como verificación empirical. Doble defensa, más código. | |

**User's choice:** (a) Settings → decorator (refactor)
**Notes:** D-05/D-06 en CONTEXT.md. Memoria `feedback_empirical_retest_after_default_changes` apoya esto — fix de causa raíz, no telemetría que hace visible el shadowing. Verificación post-fix con `compose build && compose up --force-recreate` + log inspection (memoria `feedback_compose_build_recreate`).

---

## Nyquist wave-0 strategy for phases 1, 2, 3

| Option | Description | Selected |
|--------|-------------|----------|
| Accept en las 3 (uniforme) | Formal accept con justificación en VALIDATION.md frontmatter. Código verified+passed; wave_0 missing es bookkeeping. Mínimo trabajo. | ✓ |
| Phase 1 accept, 2+3 completar | Phase 1 (spike) accept; agregar wave_0 entries en Phase 2 y 3 (1-2h por fase). | |
| Decidir por fase tras leer VALIDATION.md | Leer las 3 VALIDATION.md primero; decidir per-fase. Más informado, más tiempo. | |

**User's choice:** Accept uniforme
**Notes:** D-07/D-08 en CONTEXT.md. Las 3 fases tienen VERIFICATION.md `status=passed` + 53/53 reqs satisfied. Phase 1 obvio (spike sin código), Phase 2 ya tiene `compliant=true` solo falta flippear `wave_0_complete`, Phase 3 tiene 10/10 UAT + 21/21 threats. Generar wave-0 retroactivo es no-incremental.

---

## WR-01..04 scope (Phase 2 review warnings, severidad low)

| Option | Description | Selected |
|--------|-------------|----------|
| Incluir los 4 (oportunístico) | Cerrar WR-01..04 dentro de 3.1. ~15 LOC, ~30min. Evita carry a v0.2. | ✓ |
| Diferir todos a v0.2 | Strict scope del título. WR-01..04 a Carried Debt en MILESTONES.md al cerrar v0.1. | |
| Solo WR-02 (GC risk real) | WR-02 único con riesgo runtime (task GC). WR-01/03/04 cosméticos → diferir. Compromiso. | |

**User's choice:** Incluir los 4 oportunísticos
**Notes:** D-09 en CONTEXT.md. 4 fixes triviales (~15 LOC total). Cierra v0.1 sin debt arrastrado. CONTEXT.md de Phase 3 ya anticipaba "Phase 3 may opportunistically close these".

---

## Mock SERP en sibling degraded-mode test

| Option | Description | Selected |
|--------|-------------|----------|
| (a) Fixture real de Phase 1 | Leer `tests/fixtures/serp/*.html` real. D-18 compliant, audit pide "realistic SERP HTML". | ✓ |
| (b) HTML hand-crafted mínimo | 3-5 candidatos sintéticos. Determinístico pero rompe D-18 (fixture-driven assertions). | |
| (c) Mirror exact de test_challenge_backoff | Copiar pattern S5 ya validado en Phase 3. Researcher decide qué fixture exacta. | |

**User's choice:** (a) Fixture real de Phase 1
**Notes:** D-04 en CONTEXT.md. D-18 + `feedback_verify_volatile_data` apoyan fixture real. Mirror del patrón S5 (`test_challenge_backoff.py:199`) pero con fixture SERP en vez de fixture /sorry/.

---

## Claude's Discretion

- Mecanismo exacto de wiring `settings → @limiter.limit(...)` decorator (D-05): slowapi soporta varias formas; el researcher pica la documentada en slowapi 0.1.9.
- Forma del YAML `requirements_completed` (flow vs block) — consistencia con el frontmatter existente del SUMMARY editado.
- Mapeo REQ-ID → SUMMARY (D-02): `02-VERIFICATION.md` coverage tables como source-of-truth.
- Fixture SERP específica para D-04: el planner pica una con ≥3 candidatos post-dedupe.
- Si `tldextract` no está en pyproject para WR-01: alternativa stdlib con suffix tuple (CONTEXT.md §specifics tiene snippet propuesto).
- Orden de plans: bookkeeping (safe) primero, tests + refactor después, WR al final como cleanup batch.

## Deferred Ideas

- **Audit item #7 (deferred-phases scope decision)** — informational severity, responsabilidad de `/gsd-complete-milestone v0.1` (NO de Phase 3.1).
- **Refactor mayor de la pipeline `/search`** — fuera de scope hygiene.
- **Tests E2E reales contra dev-box** — el header fix los habilita pero correrlos como parte de 3.1 expandiría el scope al UAT real.
- **Per-host ChallengeBackoff scope, admin endpoint, multi-environment Sentry, Bearer en /metrics, distributed rate-limit store, sliding-window quota** — ya deferred en `03-CONTEXT.md`.
