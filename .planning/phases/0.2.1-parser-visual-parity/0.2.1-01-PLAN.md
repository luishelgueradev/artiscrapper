---
phase: 0.2.1-parser-visual-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/artiscrapper/browser.py
  - src/artiscrapper/search.py
autonomous: true
requirements: []
closes_context_decisions: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11]

must_haves:
  truths:
    - "src/artiscrapper/browser.py uses wait_until=\"load\" (NOT \"domcontentloaded\")"
    - "src/artiscrapper/browser.py page.goto timeout is 15_000 (NOT 20_000)"
    - "src/artiscrapper/search.py _PRICE_RE matches '$410.420,31' (NOT '$410.420,311001') for text containing '$ 410.420,311001hobbies.es'"
    - "src/artiscrapper/search.py exports _extract_pla_unit function and PLA_SELECTORS list"
    - "src/artiscrapper/search.py parse_serp() includes a pla-unit pass after the carousel loop, additive (does not replace organic cascade)"
    - "parse_serp() applied to /tmp/toy-robotech.html returns >=4 candidates with 'pla_unit' in flags AND mercadolibre.com.ar/.../p/MLA or up/MLAU canonical URL"
    - "parse_serp() applied to /tmp/ropa-zapatilla.html returns >=30 candidates with 'pla_unit' in flags AND non-Google real store URL"
    - "No regression on toy-robotech existing carousel coverage: still 30 candidates with has_price=true (sample-stable from prior run)"
  artifacts:
    - path: "src/artiscrapper/browser.py"
      provides: "Cloak fetch with wait_until='load' so Shopping panel renders into HTML"
      contains: "wait_until=\"load\""
    - path: "src/artiscrapper/search.py"
      provides: "Parser cascade + pla-unit extractor + regex price v2"
      contains: "def _extract_pla_unit"
  key_links:
    - from: "src/artiscrapper/search.py"
      to: ".planning/PARSER-VISUAL-PARITY-2026-06-05.md"
      via: "Inline comment cites the empirical justification of wait_until + pla-unit"
      pattern: "PARITY"
---

<objective>
Aplicar los 3 patches quirúrgicos de Estrategia A (reporte `PARSER-VISUAL-PARITY-2026-06-05.md`) sobre 2 archivos source: `browser.py` (wait_until + timeout) y `search.py` (regex precio v2 + extractor pla-unit + nuevo pass en `parse_serp()`).

**Purpose:** los 3 patches son interdependientes — el extractor pla-unit es inútil si el HTML no trae el shopping panel; el regex fix es independiente pero se mete junto para evitar dos PRs. Ningún patch toca el LLM curator, freshness, ni cache. Cambios totalmente aislables al parser SERP.

**Output (ver §must_haves para los assertions exactos):**
- `browser.py` switched a `wait_until="load"` + timeout 15s
- `search.py` con regex precio v2 (fix bug `$410.420,311001`)
- `search.py` con `_extract_pla_unit()` + `PLA_SELECTORS` + nuevo loop en `parse_serp()` después del carousel
- parser corre sobre las 5 fixtures del reporte y produce las cantidades esperadas (4 pla-unit en robotech, 30 en zapatillas, etc.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/PARSER-VISUAL-PARITY-2026-06-05.md
@.planning/drafts/0.2.1-parser-visual-parity/0.2.1-CONTEXT.md
@.planning/drafts/0.2.1-parser-visual-parity/0.2.1-RESEARCH.md
@src/artiscrapper/browser.py
@src/artiscrapper/search.py
</context>

<tasks>

<task id="1.1" wave="1" parallel_safe="no" autonomous="yes">
<title>Switch wait_until to "load" en browser.py</title>

<read_first>
- src/artiscrapper/browser.py (línea 81 — actualmente `wait_until="domcontentloaded", timeout=20_000`)
- .planning/PARSER-VISUAL-PARITY-2026-06-05.md (§2 Exp 2 — justificación empírica)
</read_first>

<action>
Editar `src/artiscrapper/browser.py:81`. Cambiar la línea exacta `await page.goto(url, wait_until="domcontentloaded", timeout=20_000)` por:

```python
        # PARITY: wait_until="load" recupera Shopping panel (pla-unit) y JSON-inline
        # mappings que domcontentloaded corta antes de renderizar. Sin penalty p50
        # según Exp 2 del reporte. Timeout bajado a 15s para fail-fast.
        # Ver .planning/PARSER-VISUAL-PARITY-2026-06-05.md §2.
        await page.goto(url, wait_until="load", timeout=15_000)
```

Mantener TODO el resto de `fetch_serp` (rate limiter, new_context, finally, _detect_block) exactamente igual. NO tocar `_detect_block` ni `BLOCK_MARKERS`. NO tocar `create_browser`. NO tocar el resto del archivo.
</action>

<acceptance_criteria>
- `grep -n 'wait_until="load"' src/artiscrapper/browser.py` retorna línea 81
- `grep -n 'wait_until="domcontentloaded"' src/artiscrapper/browser.py` no encuentra nada
- `grep -n 'timeout=15_000' src/artiscrapper/browser.py` encuentra al menos 1 match
- `grep -c '# PARITY:' src/artiscrapper/browser.py` >= 1
- El resto de `fetch_serp` permanece intacto: `grep -n "rate_limiter.acquire()" src/artiscrapper/browser.py` retorna 1 línea, `grep -n "_detect_block" src/artiscrapper/browser.py` retorna 2 líneas (definition + call)
- `.venv/bin/python -c "from artiscrapper.browser import fetch_serp, _detect_block, BLOCK_MARKERS; print('OK')"` exits 0
</acceptance_criteria>

</task>

<task id="1.2" wave="1" parallel_safe="yes" autonomous="yes">
<title>Reemplazar _PRICE_RE por v2 en search.py</title>

<read_first>
- src/artiscrapper/search.py (líneas 61-66 — _PRICE_RE actual)
- src/artiscrapper/search.py (líneas 93-139 — _normalize_price_text + _extract_commercial_signals; NO se tocan)
- .planning/PARSER-VISUAL-PARITY-2026-06-05.md (§7 Patch 2 — código exacto + casos test)
</read_first>

<action>
Reemplazar `_PRICE_RE` en `src/artiscrapper/search.py:63-66` por:

```python
# AR price: $X.XXX,XX (con miles + 2 decimales OBLIGATORIO para evitar bleeding
# en texto concatenado como "$410.420,31" + "1001hobbies.es"). Alt acepta
# entero sin separadores (ARS 5000, U$S 5000). Bug fix: el regex anterior
# (?:\$|ARS|U\$S)\s?[\d](?:[\d.,]*\d)? era greedy y absorbía dígitos del store.
# Ver .planning/PARSER-VISUAL-PARITY-2026-06-05.md §7 Patch 2.
_PRICE_RE = re.compile(
    r"(?:\$|ARS|U\$S)\s?(?:&nbsp;)?\s?"
    r"(?:"
    r"[1-9]\d{0,2}(?:\.\d{3})*,\d{2}"
    r"|"
    r"[1-9]\d{2,7}(?![\d.,])"
    r")",
    re.IGNORECASE,
)
```

NO tocar `_INSTALLMENT_RE`, `_STOCK_AGOTADO_RE`, `_STOCK_OK_RE`, `_SHIPPING_FREE_RE`, `_RATING_RE`, `_STORE_HINT_RE`. NO tocar `_normalize_price_text` ni `_extract_commercial_signals` — estos consumen `_PRICE_RE` pero su lógica se mantiene.
</action>

<acceptance_criteria>
- `grep -n 'r"\[1-9\]\\\\d{0,2}(?:\\\\.\\\\d{3})\*,\\\\d{2}"' src/artiscrapper/search.py` encuentra el branch principal
- `grep -n 'r"\[1-9\]\\\\d{2,7}(?!\\\\\[\\\\d\\\\.,\\\\])"' src/artiscrapper/search.py` encuentra el branch alt entero
- `grep -n '\[\\\\d\]\(\?:\[\\\\d\\\\.,\]\*\\\\d\)\?' src/artiscrapper/search.py` no encuentra nada (regex viejo borrado)
- `.venv/bin/python -c "
from artiscrapper.search import _PRICE_RE
cases = {
    'Threezero \\$410.420,311001hobbies.es': '\\$410.420,31',
    '\\$1.397.000,00 Mercadolibre.com.ar': '\\$1.397.000,00',
    '\\$45.885,00x 6Tigre.com.ar': '\\$45.885,00',
    'ARS 5.000,00': 'ARS 5.000,00',
    'U\\$S 5000': 'U\\$S 5000',
}
for txt, expected in cases.items():
    m = _PRICE_RE.search(txt)
    got = m.group(0) if m else None
    assert got == expected, f'{txt!r} -> {got!r}, expected {expected!r}'
print('OK 5/5')
"` exits 0 con `OK 5/5`
- `.venv/bin/pytest tests/test_parser.py -x -q` ya existente NO regression (smoke check)
</acceptance_criteria>

</task>

<task id="1.3" wave="1" parallel_safe="yes" autonomous="yes">
<title>Agregar _extract_pla_unit + PLA_SELECTORS</title>

<read_first>
- src/artiscrapper/search.py (líneas 196-242 — `_extract_carousel` como referencia de estructura)
- src/artiscrapper/search.py (líneas 50-53 — `CAROUSEL_SELECTORS` como pattern)
- /tmp/parser_v2.py (prototipo validado de `extract_pla_unit`)
- .planning/PARSER-VISUAL-PARITY-2026-06-05.md (§7 Patch 3 — código exacto)
</read_first>

<action>
En `src/artiscrapper/search.py`, **después de `CAROUSEL_SELECTORS`** (~línea 54) agregar:

```python
PLA_SELECTORS = [
    "div.pla-unit",  # Google Shopping ads — sponsored, traen URL canonical real
]
```

**Después de `_extract_carousel` (~línea 242)** agregar la función `_extract_pla_unit`:

```python
def _extract_pla_unit(node) -> dict | None:
    """
    Extract a candidate dict from a Google Shopping pla-unit cell.

    pla-unit = sponsored ad slot. Tiene URL canonical real (no sintética como carousel),
    precio cierto (span.VbBaOe), store hint (div.UsGWMe), cuota mensual opcional
    (div.OkcyVb). El título vive en [role=heading], NO en aria-label del <a> que
    viene vacío. Hay dos <a>: uno con redirect /aclk?... (display:none, tracking pixel)
    y otro con URL real al producto — elegimos el segundo.

    Returns None si falta título o URL real (HTML incompleto, selector rotation).
    """
    title_el = node.css_first("[role=heading]")
    title = title_el.text(strip=True) if title_el else None

    href = None
    for a in node.css("a[href]"):
        h = a.attributes.get("href", "")
        if h.startswith("http") and "/aclk?" not in h:
            href = h.split("&amp;")[0]
            break
    if not href or not title or "google.com" in href:
        return None

    price_el = node.css_first("span.VbBaOe")
    price = price_el.text(strip=True).replace("\xa0", "") if price_el else None

    store_el = node.css_first("div.UsGWMe")
    store = store_el.text(strip=True) if store_el else None

    inst_el = node.css_first("div.OkcyVb")
    inst = inst_el.text(strip=True).replace("\xa0", "") if inst_el else None

    return {
        "url": href,
        "title": title,
        "snippet": None,
        "price_in_card": price,
        "has_price": bool(price),
        "store_hint": store,
        "installments": inst,
        "flags": ["pla_unit", "sponsored"],
    }
```
</action>

<acceptance_criteria>
- `grep -n 'PLA_SELECTORS = \\[' src/artiscrapper/search.py` encuentra 1 línea
- `grep -n 'def _extract_pla_unit' src/artiscrapper/search.py` encuentra 1 línea
- `.venv/bin/python -c "from artiscrapper.search import _extract_pla_unit, PLA_SELECTORS; print(PLA_SELECTORS)"` exits 0 e imprime `['div.pla-unit']`
- `.venv/bin/python -c "
from selectolax.parser import HTMLParser
from artiscrapper.search import _extract_pla_unit
import re
html = open('/tmp/toy-robotech.html').read()
tree = HTMLParser(html)
extracted = [_extract_pla_unit(n) for n in tree.css('div.pla-unit')]
extracted = [x for x in extracted if x]
assert len(extracted) >= 4, f'Expected >=4, got {len(extracted)}'
assert all(c['has_price'] for c in extracted), 'all must have price'
assert all('mercadolibre.com.ar' in c['url'] for c in extracted), 'all must be MELI URL'
assert all('pla_unit' in c['flags'] for c in extracted), 'all must have pla_unit flag'
print(f'OK {len(extracted)} pla-units extracted')
"` exits 0
</acceptance_criteria>

</task>

<task id="1.4" wave="2" parallel_safe="no" autonomous="yes">
<title>Conectar pla-unit pass en parse_serp() (additive, después del carousel)</title>

<read_first>
- src/artiscrapper/search.py (líneas 284-316 — `parse_serp` actual: organic cascade + carousel loop)
- .planning/PARSER-VISUAL-PARITY-2026-06-05.md (§7 Patch 3 — código exacto del nuevo pass)
- Las tasks 1.1, 1.2, 1.3 deben estar completas (depends_on implícito por edición del mismo archivo)
</read_first>

<action>
En `src/artiscrapper/search.py`, dentro de `parse_serp()`, **DESPUÉS del loop de carousel** (~línea 314, después de `break` del último for carousel) y **ANTES del `return candidates`** (~línea 316), insertar:

```python
    # ── NEW: pla-unit pass (Shopping panel, sponsored ads) ──
    # Aditivo al organic + carousel: pla-unit = ads con URL canonical real,
    # NO reemplaza organic cascade. Dedupe interno por URL para evitar duplicar
    # productos cuando el panel y carousel paralelo listan el mismo.
    # El dedupe global (dedupe()) sigue corriendo después y combina con organic.
    # Ver .planning/PARSER-VISUAL-PARITY-2026-06-05.md §7 Patch 3.
    pla_seen: set[str] = set()
    for sel in PLA_SELECTORS:
        for n in tree.css(sel):
            c = _extract_pla_unit(n)
            if c and c["url"] not in pla_seen:
                pla_seen.add(c["url"])
                candidates.append(c)
```

NO tocar la organic cascade ni el carousel loop. NO cambiar el `parse_cascade_exhausted` warning behavior (sigue dependiendo de organic-only). NO tocar `_extract_by_h3`.
</action>

<acceptance_criteria>
- `grep -n "PLA_SELECTORS" src/artiscrapper/search.py` encuentra al menos 2 matches (la definición + el uso en parse_serp)
- `grep -n "pla_seen" src/artiscrapper/search.py` encuentra al menos 2 matches
- El orden en parse_serp es: organic cascade → carousel → pla-unit → return. Verificable con `grep -n 'for sel in' src/artiscrapper/search.py` mostrando ORGANIC_SELECTORS antes que CAROUSEL_SELECTORS antes que PLA_SELECTORS
- `.venv/bin/python -c "
from artiscrapper.search import parse_serp
html = open('/tmp/toy-robotech.html').read()
cands = parse_serp(html)
pla = [c for c in cands if 'pla_unit' in (c.get('flags') or [])]
carousel = [c for c in cands if 'carousel' in (c.get('flags') or [])]
organic = [c for c in cands if not c.get('flags')]
assert len(pla) >= 4, f'Expected >=4 pla, got {len(pla)}'
assert len(carousel) >= 25, f'Expected >=25 carousel (no regression), got {len(carousel)}'
print(f'OK organic={len(organic)} carousel={len(carousel)} pla={len(pla)}')
"` exits 0
- `.venv/bin/python -c "
from artiscrapper.search import parse_serp
html = open('/tmp/ropa-zapatilla.html').read()
cands = parse_serp(html)
pla = [c for c in cands if 'pla_unit' in (c.get('flags') or [])]
real_url = [c for c in pla if c.get('url') and 'google.com' not in c['url']]
assert len(real_url) >= 30, f'Expected >=30 real-URL pla in zapatillas, got {len(real_url)}'
print(f'OK ropa-zapatilla pla real URLs: {len(real_url)}')
"` exits 0
- `.venv/bin/pytest tests/test_parser.py -x -q` (suite existente) pasa sin regression
</acceptance_criteria>

</task>

</tasks>

<verification_criteria>
1. **Compatibilidad backwards** — todos los tests existentes en `tests/test_parser.py` pasan sin tocar (Wave 1 no introduce regression).
2. **Cobertura nueva** — corriendo `parse_serp()` sobre `/tmp/toy-robotech.html` devuelve >=4 candidates con flag `pla_unit`; sobre `/tmp/ropa-zapatilla.html` devuelve >=30 candidates con flag `pla_unit` y URL real no-Google.
3. **Bug regex cerrado** — `_PRICE_RE.search("$410.420,311001hobbies.es")` retorna match.group(0) == `"$410.420,31"`.
4. **Setting Cloak nuevo activo** — `grep` en `browser.py` confirma `wait_until="load"` y `timeout=15_000`, sin restos del valor anterior.
5. **No D8 break** — `tests/test_footguns.py::test_no_persistent_context_in_codebase` sigue pasando (invariante de browser context ephemeral).
6. **Imports limpios** — `.venv/bin/python -c "from artiscrapper.search import parse_serp, _extract_pla_unit, PLA_SELECTORS, _PRICE_RE; print('OK')"` exits 0.
</verification_criteria>
