---
phase: 02-mvp
plan: "02"
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/artiscrapper/llm.py
  - src/artiscrapper/visit.py
  - src/artiscrapper/freshness.py
  - src/artiscrapper/main.py
  - tests/test_llm.py
  - tests/test_visit.py
autonomous: true
requirements:
  - LLM-01
  - LLM-02
  - LLM-03
  - LLM-04
  - LLM-05
  - LLM-06
  - LLM-07
  - LLM-08
  - VISIT-01
  - VISIT-02
  - VISIT-03
  - VISIT-04
  - VISIT-05
  - VISIT-06
  - VISIT-07
  - VISIT-08
  - FRESH-01
  - FRESH-02
  - FRESH-03
  - FRESH-04
  - CACHE-01
  - CACHE-02
  - CACHE-03
  - CACHE-04
  - CACHE-05

must_haves:
  truths:
    - "D2: LLMVerdict.fallback() returns confidence=0.3 which IS dropped at the <0.4 cut in should_keep() — test pins this behavior"
    - "LLM calls use OpenAI-compat shape: POST /v1/chat/completions, choices[0].message.content (NOT message.content)"
    - "asyncio.Semaphore(4) limits concurrent LLM calls; 5s timeout → fallback('timeout') verdict"
    - "Candidates with confidence<0.4 OR freshness_signal='blog' are dropped; metadata.llm_degraded=true when >50% fallback"
    - "LLM degraded mode (router HEAD fails): skip LLM step, use heuristic blocklist + price-in-card only"
    - "Bearer token from LLM_ROUTER_BEARER_TOKEN env var — NEVER logged (LLM-08, OBS-05)"
    - "Visit pass uses httpx.AsyncClient(http2=True) + global Semaphore(8) + per-host Semaphore(2)"
    - "Visit pass headers: Chromium-146 UA + Sec-Fetch-Site: cross-site + Referer: https://www.google.com/ (D11)"
    - "VISIT-08: any *.mercadolibre.* URL gets meli_skip flag immediately without HTTP call"
    - "VISIT-01: skip-if-you-can — no visit when price is not None OR freshness=live_marketplace"
    - "Visit pass uses real PDP URLs from SERP, never constructs catalog URLs from query (Phase 1 NEEDS-PIVOT)"
    - "Extractor cascade: JSON-LD Product → OG product:price:amount → microdata itemprop=price → AR-regex (D12 HAND-ROLL)"
    - "Freshness: MELI + 200 → fresh=true; datePublished <90d → fresh=true; no signal → fresh=unknown (not false)"
    - "POST /search end-to-end: cache miss → Google fetch → parse → blocklist → LLM filter → visit pass → freshness → rerank → cache write → response"
  artifacts:
    - path: "src/artiscrapper/llm.py"
      provides: "LLMVerdict + classify_candidate + should_keep + SYSTEM_PROMPT + FEW_SHOT_EXAMPLES + curate_candidates"
      exports: ["LLMVerdict", "classify_candidate", "should_keep", "curate_candidates"]
    - path: "src/artiscrapper/visit.py"
      provides: "visit_candidates + classify_response + extract_product + DEFAULT_HEADERS"
      exports: ["visit_candidates", "classify_response", "extract_product", "DEFAULT_HEADERS"]
    - path: "src/artiscrapper/freshness.py"
      provides: "assess_freshness(candidate, verdict, extracted)"
      exports: ["assess_freshness"]
    - path: "src/artiscrapper/main.py"
      provides: "Fully wired POST /search handler (all 10 pipeline steps)"
      contains: "curate_candidates"
  key_links:
    - from: "src/artiscrapper/main.py"
      to: "src/artiscrapper/llm.py"
      via: "POST /search calls curate_candidates(candidates, settings.LLM_ROUTER_URL, settings.LLM_ROUTER_BEARER_TOKEN)"
      pattern: "curate_candidates"
    - from: "src/artiscrapper/llm.py"
      to: "local-llms-router at 127.0.0.1:3210"
      via: "httpx POST /v1/chat/completions with Authorization: Bearer"
      pattern: "choices\\[0\\]\\.message\\.content"
    - from: "src/artiscrapper/main.py"
      to: "src/artiscrapper/visit.py"
      via: "POST /search calls visit_candidates() for skip-if-you-can survivors"
      pattern: "visit_candidates"
    - from: "src/artiscrapper/visit.py"
      to: "AR catalog hosts"
      via: "httpx.AsyncClient(http2=True) with DEFAULT_HEADERS; Semaphore(8)+Semaphore(2)"
      pattern: "global_sem.*host_sems"
    - from: "src/artiscrapper/main.py"
      to: "src/artiscrapper/cache.py"
      via: "POST /search: CACHE-05 — always check cache before Google fetch; write result after pipeline"
      pattern: "get_cached.*set_cached"
---

<objective>
Implement the LLM curator, visit pass, freshness assessment, and complete the POST /search handler
pipeline. This plan produces the three modules (llm.py, visit.py, freshness.py) that transform
raw SERP candidates into curated, price-enriched, freshness-assessed products, and wires them into
the full 10-step POST /search handler in main.py.

Purpose: After this plan, POST /search works end-to-end from cache check through Google fetch,
parse, LLM filter, visit pass, freshness, rerank, and cache write. The D2 foot-gun is pinned
by tests. The MELI host guard (VISIT-08) is the first check in visit_one().

Output: Full POST /search pipeline. `uv run pytest tests/test_llm.py tests/test_visit.py -x -q`
green. End-to-end request against dev-box returns real results (validated in 02-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/SPIKE.md
@.planning/phases/02-mvp/02-RESEARCH.md
@.planning/phases/02-mvp/02-PATTERNS.md
@.planning/phases/02-mvp/02-VALIDATION.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-mvp/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: llm.py — LLMVerdict + classify_candidate + should_keep + curate_candidates</name>
  <files>
    src/artiscrapper/llm.py,
    tests/test_llm.py
  </files>
  <read_first>
    - .planning/phases/02-mvp/02-RESEARCH.md — Pattern 5 in full (lines 774-937): LLMVerdict model, fallback() classmethod, SYSTEM_PROMPT, FEW_SHOT_EXAMPLES, USER_TEMPLATE, classify_candidate(), should_keep(), semaphore orchestration. Copy verbatim — this is authoritative.
    - .planning/phases/02-mvp/02-PATTERNS.md — §src/artiscrapper/llm.py: router call shape from scripts/spike/05_router_probe.py; bearer token pattern; response field path `choices[0].message.content`
    - .planning/SPIKE.md — §LLM: endpoint confirmed POST /v1/chat/completions; JSON mode response_format={type: "json_object"}; LLM_CONCURRENCY=4 empirically; NOT Ollama /api/chat
    - .planning/REQUIREMENTS.md — LLM-01 through LLM-08 definitions
    - tests/fixtures/llm/labelled.jsonl — 50 labelled records (use in test_verdict_valid and test_concurrency_semaphore)
    - .planning/phases/02-mvp/02-VALIDATION.md — Per-task verification map rows for LLM-01..LLM-08
  </read_first>
  <behavior>
    - LLMVerdict: Pydantic BaseModel per Pattern 5 exactly. Fields: is_product: bool, confidence: float ge=0.0 le=1.0, price_hint: float|None, store_hint: str|None max_length=80, freshness_signal: FreshnessSignal literal, reason: str max_length=140. fallback(cls, reason) classmethod: returns confidence=0.3, is_product=True, freshness_signal="unknown", reason=f"llm_fail:{reason}". D2 FOOT-GUN comment: "confidence=0.3 is BELOW the <0.4 cut — this verdict IS dropped in should_keep()".
    - SYSTEM_PROMPT: Spanish prompt per Pattern 5 lines 822-845 verbatim. FEW_SHOT_EXAMPLES: 2 examples per lines 847-857 (MELI card positive, blog negative). USER_TEMPLATE: "INPUT:\n{candidate_json}\nOUTPUT:".
    - classify_candidate(client, candidate, sem, router_url, bearer_token): async, acquires Semaphore before HTTP call. POST f"{router_url}/v1/chat/completions" with payload {model:"chat-local", messages:[system+user], response_format:{type:"json_object"}, temperature:0.0, max_tokens:128, stream:False}. Authorization header: f"Bearer {bearer_token}". Parse: `resp.json()["choices"][0]["message"]["content"]` — NOT `resp.json()["message"]["content"]` (Pitfall 5). On TimeoutException → fallback("timeout"). On HTTPStatusError 503 → sleep 1s + single retry → fallback("overload"). On ValidationError/JSONDecodeError/KeyError → fallback("malformed"). On any other Exception → fallback("conn_error"). NEVER log prompt content, response content, or bearer_token (LLM-08/OBS-05).
    - should_keep(verdict): if not verdict.is_product → False. if verdict.confidence < 0.4 → False (D2: 0.3 < 0.4). if verdict.freshness_signal == "blog" → False. return True.
    - curate_candidates(candidates, router_url, bearer_token, concurrency=4): accepts list of candidate dicts, creates asyncio.Semaphore(concurrency), opens httpx.AsyncClient(http2=True), runs asyncio.gather over classify_candidate calls. Tracks fallback count. If fallback_count > len(candidates) * 0.5 → set llm_degraded=True on return dict. Returns (kept_candidates, dropped_count, llm_degraded).
    - router_health_check(router_url, bearer_token): async HEAD to f"{router_url}/healthz" with bearer. Returns bool. Used by LLM-06 degraded mode. LLM health endpoint is /healthz (with z, per SPIKE.md).
    - Implement fully tests/test_llm.py (Wave 0 had stubs — now implement properly):
      - test_verdict_valid: LLMVerdict.model_validate_json of valid JSON → no exception, fields correct
      - test_fallback_shape: LLMVerdict.fallback("timeout") → confidence=0.3, is_product=True, reason="llm_fail:timeout"
      - test_d2_fallback_is_dropped: assert not should_keep(LLMVerdict.fallback("timeout")) — D2 pin
      - test_router_call_shape: respx mock for POST /v1/chat/completions returning choices[0].message.content shape; assert classify_candidate returns valid LLMVerdict
      - test_concurrency_semaphore: mock 6 concurrent classify_candidate calls with respx; assert all complete without error; Semaphore(4) doesn't deadlock
  </behavior>
  <action>
    llm.py is the most security-sensitive module. Two hard rules:
    1. LLM-08/OBS-05: Never emit any of these in log calls: prompt content, response content,
       candidate.title, candidate.url, candidate.snippet, bearer_token. Only url_hash, host,
       confidence, freshness_signal, is_product are allowed via log_candidate_safe().
    2. D2: The constant `0.4` in should_keep() is the ONLY enforcement point. It must NOT be
       configurable via env vars or settings — it is a hard-coded product invariant.

    For curate_candidates: the degraded mode check is `>50% of calls returned fallback verdicts`.
    Use a counter: each time classify_candidate returns LLMVerdict.fallback(*), increment. After
    all calls: if fallback_count > total * 0.5, set llm_degraded=True.

    SYSTEM_PROMPT must be the exact Spanish text from Pattern 5 — it is the regression-tested
    prompt that generated the 50 labelled records in tests/fixtures/llm/labelled.jsonl. Do not
    paraphrase or shorten it.

    For test_concurrency_semaphore: use respx.mock to intercept all POST calls; fire 6 concurrent
    tasks with Semaphore(4); assert all 6 complete and return valid LLMVerdict objects. The test
    does not assert timing — it asserts correctness under concurrency.
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/artiscrapper && uv run pytest tests/test_llm.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_llm.py -x -q` exits 0 with ≥5 passed (test_verdict_valid, test_fallback_shape, test_d2_fallback_is_dropped, test_router_call_shape, test_concurrency_semaphore)
    - `uv run pytest tests/test_llm.py::test_d2_fallback_is_dropped -x -q` exits 0 — D2 pinned
    - `grep -c "choices\[0\]\[.message.\]\[.content.\]" src/artiscrapper/llm.py` ≥1 OR `grep -c 'choices.*0.*message.*content' src/artiscrapper/llm.py` ≥1 (correct response path)
    - `grep -c "0\.4" src/artiscrapper/llm.py` ≥1 (hard-coded threshold present)
    - `grep -c "confidence=0\.3" src/artiscrapper/llm.py` ≥1 (fallback value present)
    - `grep -rn "bearer_token" src/artiscrapper/llm.py | grep -i "log\|print"` returns 0 (token never logged)
    - `grep -rn "SYSTEM_PROMPT\|FEW_SHOT" src/artiscrapper/llm.py` returns ≥2 (prompt constants defined)
    - `grep -c "Semaphore" src/artiscrapper/llm.py` ≥1 (concurrency control present)
  </acceptance_criteria>
  <done>LLM tests green. D2 invariant pinned by test. Router call shape confirmed by respx mock. curate_candidates wired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: visit.py + freshness.py + wire full POST /search in main.py</name>
  <files>
    src/artiscrapper/visit.py,
    src/artiscrapper/freshness.py,
    src/artiscrapper/main.py,
    tests/test_visit.py
  </files>
  <read_first>
    - .planning/phases/02-mvp/02-RESEARCH.md — Pattern 4 (visit orchestrator + DEFAULT_HEADERS + classify_response, lines 650-768); Pattern 6 (hand-rolled extractor cascade, lines 943-1063 — copy verbatim); §Freshness logic (FRESH-01..04 in §Phase Requirements table, lines 56-61); Pattern 1 §System Architecture Diagram step [6] visit pass + step [7] freshness (lines 217-230)
    - .planning/phases/02-mvp/02-PATTERNS.md — §src/artiscrapper/visit.py (analogs: scripts/spike/07_falabella_403.py + 08_extruct_classifier.py); §shared patterns D11 DEFAULT_HEADERS + VISIT-08 MELI guard
    - .planning/SPIKE.md — §Visit: DEFAULT_HEADERS sufficient for all 3 AR hosts (0 WAF 403s); real-URL-only constraint (never slug-guess); §D12 Decision: HAND-ROLL confirmed (9/10 jsonld-sufficient)
    - .planning/SPIKE.md — §Risks: real-URL-only discovery — visit pass MUST use URLs from SERP, never construct from query terms
    - tests/fixtures/catalog/ — 10 subdirs, each with product-01.html; MANIFEST.md for classification
    - scripts/spike/07_falabella_403.py — DEFAULT_HEADERS used in probe (same headers for production)
    - scripts/spike/08_extruct_classifier.py — extract_jsonld_product, extract_og_product, extract_microdata_product functions (source of truth for Pattern 6)
    - .planning/REQUIREMENTS.md — VISIT-01..08, FRESH-01..04 definitions
    - .planning/phases/02-mvp/02-VALIDATION.md — test rows for VISIT-05, VISIT-06, VISIT-08, CACHE-05
  </read_first>
  <behavior>
    - visit.py: DEFAULT_HEADERS dict per Pattern 4 lines 663-681 verbatim: User-Agent Chromium-146, Accept, Accept-Language "es-AR,es;q=0.9,en;q=0.6", Sec-Ch-Ua, Sec-Fetch-Site: "cross-site" (D11), Referer: "https://www.google.com/" (D11). visit_candidates(candidates, visit_timeout_s=10): global_sem=Semaphore(8), host_sems=defaultdict(lambda: Semaphore(2)). visit_one(candidate): FIRST CHECK is VISIT-08 MELI guard: `if "mercadolibre." in urlparse(url).netloc → candidate["flags"].append("meli_skip"); return`. SECOND CHECK is VISIT-01 skip-if-you-can: `if candidate.get("price") is not None or candidate.get("freshness_signal") == "live_marketplace" → return as-is`. Then async with global_sem, host_sems[host]: httpx.AsyncClient(http2=True, headers=DEFAULT_HEADERS, follow_redirects=True, timeout=Timeout(connect=3.0, read=visit_timeout_s, write=3.0, pool=2.0)). classify_response → extract_product if live. Exception → visit_failed=True. NO retry (VISIT-07).
    - classify_response(response): per Pattern 4 lines 741-767. DEAD_MARKERS tuple. SOFT_404_PATHS set. Check status ≥400 → "failed". Check final URL path in SOFT_404_PATHS → "dead". Check content-type "html" → "failed" if absent. Check body < 5KB → "failed". Check title/h1 for DEAD_MARKERS → "dead". Return "live".
    - extract_product(html): full cascade per Pattern 6 lines 1034-1063. AR_PRICE_PATTERN regex per lines 952-956. extract_jsonld_product: handles @graph wrapper (Tiendanube/VTEX pattern) per lines 958-979. extract_og_product: checks product:price:amount (NOT og:price). extract_microdata_product: itemprop="price". extract_price_regex: scopes to .price/.precio elements first, then full body.
    - freshness.py: assess_freshness(candidate, verdict, extracted): FRESH-01: if host in MELI_HOSTS and candidate visit returned 200 → fresh=True. FRESH-02: if extracted has date_modified or datePublished and parsed date is within 90 days → fresh=True. FRESH-03: if verdict.freshness_signal == "blog" → already dropped by LLM-05, no further assessment needed. FRESH-04: no signal → fresh=None (NOT False). Return fresh value.
    - main.py: Update POST /search handler to full 10-step pipeline: [1] cache lookup → [2] Google fetch (parallel A+B via asyncio.gather) → [3] _detect_block on both → [4] parse_serp on both → [5] merge + dedupe + is_junk filter → [6] router_health_check; if router healthy: curate_candidates; if router DOWN: degraded mode (blocklist + price-in-card only) → [7] visit_candidates on survivors → [8] assess_freshness per candidate → [9] rerank → [10] set_cached + return SearchResponse. Populate all Metadata fields: elapsed_ms (time.time() at start/end), google_fetches=2, candidates_total, llm_filtered_out, visited, visit_failed, cache_hit, llm_degraded.
    - Implement tests/test_visit.py fully (Wave 0 had stubs):
      - test_classify_soft404: httpx.Response with redirect to "/" → "dead"
      - test_classify_4xx: httpx.Response status=404 → "failed"
      - test_extractor_catalog_fixtures: load all 9 jsonld-sufficient catalog HTML fixtures; assert extract_jsonld_product returns dict with price set
      - test_ar_price_regex: AR_PRICE_PATTERN matches "ARS 24.900", "$18.032,30", "pesos 5000"
      - test_meli_guard: call visit_one with {"url": "https://www.mercadolibre.com.ar/MLA-12345", "flags": []}; assert "meli_skip" in flags; no HTTP call made (respx active but no request fired)
  </behavior>
  <action>
    Three critical invariants to enforce in visit.py, in order:

    1. VISIT-08 (MELI guard) is the FIRST check in visit_one() — before semaphore acquisition,
       before any network I/O. Pattern: `if "mercadolibre." in urlparse(url).netloc: ...return`.
       This is a security control (architecture-level SSRF mitigation), not optional.

    2. VISIT-01 (skip-if-you-can) is the SECOND check. Only then acquire semaphores and make HTTP.
       Skip condition: `candidate.get("price") is not None` OR
       `candidate.get("freshness_signal") == "live_marketplace"`.

    3. Real-URL-only (Phase 1 NEEDS-PIVOT): visit_candidates() receives URLs from SERP parser.
       It MUST NOT construct any URL from the query string. This is enforced structurally —
       the function only operates on candidate["url"] values.

    For the extractor cascade in Pattern 6: the @graph wrapper is critical for Tiendanube stores
    (casasusy, dphidraulica, lspalermo all emit @graph). Without this, 3/9 jsonld-sufficient
    fixtures would fail to extract. Copy Pattern 6 extract_jsonld_product() verbatim including
    the @graph expansion loop.

    For the fully wired POST /search in main.py: the handler must track start_time = time.time()
    at the very beginning and compute elapsed_ms = int((time.time() - start_time) * 1000) just
    before building the SearchResponse. The metadata.cache_hit field comes from get_cached()
    returning non-None. CACHE-05: get_cached() is ALWAYS called first — before any Google fetch.

    For freshness: MELI_HOSTS should be a frozenset containing known MELI domains
    ("mercadolibre.com.ar", "mercadolibre.com", "listado.mercadolibre.com.ar"). The FRESH-01
    path is for candidates whose URL is a MELI host AND the visit returned 200 (or is in the
    allowlist and wasn't visited due to VISIT-01 skip).

    For LLM degraded mode in main.py: if router_health_check() returns False, set llm_degraded=True
    in metadata and use heuristic-only filtering: keep candidates that have price_in_card set
    (from SERP parser extraction) or that pass the junk-domain blocklist. This is LLM-06.
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/artiscrapper && uv run pytest tests/test_visit.py tests/test_llm.py -x -q 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_visit.py -x -q` exits 0 with ≥5 passed (test_classify_soft404, test_classify_4xx, test_extractor_catalog_fixtures, test_ar_price_regex, test_meli_guard)
    - test_extractor_catalog_fixtures: all 9 jsonld-sufficient fixtures return a dict with non-None "price" (argautopartes, autodo, casasusy, dphidraulica, lspalermo, martinmorris, mayoristafrog, mipol, reps)
    - test_meli_guard: "meli_skip" in flags, and respx intercept shows 0 HTTP requests fired
    - `grep -c "mercadolibre\." src/artiscrapper/visit.py` ≥1 (MELI guard present)
    - `grep -c "meli_skip" src/artiscrapper/visit.py` ≥1 (MELI skip flag set)
    - `grep -c "Sec-Fetch-Site" src/artiscrapper/visit.py` ≥1 (D11 header present)
    - `grep -c "Referer.*google\.com" src/artiscrapper/visit.py` ≥1 (D11 Referer present)
    - `grep -c "@graph" src/artiscrapper/visit.py` ≥1 (Tiendanube @graph unwrap present)
    - `grep -c "AR_PRICE_PATTERN" src/artiscrapper/visit.py` ≥2 (defined and used)
    - main.py contains references to both `curate_candidates` and `visit_candidates` (pipeline wired)
    - `grep -c "get_cached" src/artiscrapper/main.py` ≥1 (CACHE-05: cache checked before Google fetch)
    - `grep -c "set_cached" src/artiscrapper/main.py` ≥1 (cache written after pipeline)
    - `uv run pytest tests/ -x -q -k "not e2e"` exits 0 (full suite including LLM + visit tests)
  </acceptance_criteria>
  <done>Visit pass tests green. Full POST /search pipeline wired end-to-end. All 10 pipeline steps execute in correct order. D2 and MELI guard invariants verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visit_one → AR catalog hosts | Untrusted HTML from external stores; may redirect or return dead content |
| main.py → local-llms-router | Internal HTTP; bearer token in Authorization header must never appear in logs |
| SERP → candidate URLs | Google SERP URLs are the only acceptable source for visit pass; no user-supplied URL reaches visit_one directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Information Disclosure | Bearer token in LLM prompt logging | mitigate | LLM-08: never log prompt/response content; bearer_token passed as parameter, never bound to structlog context; OBS-05 allow-list enforced in classify_candidate() |
| T-02-02-02 | Tampering | SSRF via candidate URLs in visit pass | mitigate | VISIT-08: MELI host guard is FIRST check in visit_one() — `"mercadolibre." in netloc` blocks any MELI URL; visit_candidates() only receives URLs from SERP parser output (controlled source, not user input); no user-supplied URL enters visit_one() |
| T-02-02-03 | Tampering | D2 confidence threshold soft-coded wrong | mitigate | should_keep() uses hard-coded `< 0.4` constant (not from settings/env); test_d2_fallback_is_dropped pins the invariant: LLMVerdict.fallback("timeout").confidence=0.3 must NOT pass should_keep() |
| T-02-02-04 | Tampering | SQL injection via cache key (from query normalization) | mitigate | make_cache_key() returns sha256 hex (alphanumeric only); all SQL uses parameterized `?` placeholders; no f-string SQL anywhere in cache.py |
| T-02-02-05 | Information Disclosure | Scraped title/snippet/URL in logs (OBS-05) | mitigate | log_candidate_safe() used exclusively; SAFE_CANDIDATE_FIELDS = {url_hash, host, has_price, freshness_signal, confidence}; direct logging of candidate fields is forbidden |
| T-02-02-06 | Tampering | slug-guessing in visit pass inflates visit_failed (Phase 1 NEEDS-PIVOT) | mitigate | visit_candidates() operates only on candidate["url"] from SERP parser — no URL construction from query terms; structural enforcement |
</threat_model>

<verification>
Full plan verification (after all tasks complete):

1. `uv run pytest tests/test_llm.py tests/test_visit.py -x -q` exits 0
2. `uv run pytest tests/ -x -q -k "not e2e"` exits 0 (all non-e2e tests pass)
3. `grep -c "0\.4" src/artiscrapper/llm.py` ≥1 (D2 threshold hard-coded)
4. `grep -c "mercadolibre\." src/artiscrapper/visit.py` ≥1 (VISIT-08 guard)
5. `grep -c "meli_skip" src/artiscrapper/visit.py` ≥1 (MELI skip flag)
6. `grep -c "Sec-Fetch-Site.*cross-site" src/artiscrapper/visit.py` ≥1 (D11 header)
7. `grep -c "@graph" src/artiscrapper/visit.py` ≥1 (Tiendanube unwrap)
8. `grep -c "choices\|message\|content" src/artiscrapper/llm.py` ≥3 (correct response path)
9. `grep -rn "bearer_token" src/artiscrapper/llm.py | grep -i "log\|print"` returns 0 (token never logged)
10. `grep -c "curate_candidates\|visit_candidates\|assess_freshness" src/artiscrapper/main.py` ≥3 (all 3 pipeline functions wired)
11. `grep -c "get_cached\|set_cached" src/artiscrapper/main.py` ≥2 (cache-first invariant)
</verification>

<success_criteria>
- llm.py implements LLMVerdict, fallback(), should_keep() with 0.3 confidence drop, curate_candidates() with Semaphore(4) and degraded-mode detection
- visit.py implements visit_candidates() with MELI guard first, skip-if-you-can second, DEFAULT_HEADERS with D11 Sec-Fetch-Site + Referer, dual Semaphore pattern, extract_product() cascade including @graph unwrap
- freshness.py implements assess_freshness() per FRESH-01..04
- main.py POST /search is fully wired (all 10 steps) including CACHE-05 cache-first invariant
- `uv run pytest tests/test_llm.py -x -q` green (≥5 tests)
- `uv run pytest tests/test_visit.py -x -q` green (≥5 tests)
- `uv run pytest tests/ -x -q -k "not e2e"` green (full suite)
- D2 pinned: test_d2_fallback_is_dropped passes
- MELI guard pinned: test_meli_guard passes with 0 HTTP calls fired
- 9/9 jsonld-sufficient catalog fixtures yield non-None price from extract_jsonld_product()
</success_criteria>

<output>
Create `.planning/phases/02-mvp/02-02-SUMMARY.md` when done.
</output>
