---
phase: 02-mvp
plan: "03"
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - Dockerfile
  - compose.yml
  - .dockerignore
  - .github/workflows/ci.yml
  - tests/test_health.py
  - tests/test_e2e.py
  - tests/test_footguns.py
autonomous: true
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-03
  - DEPLOY-04
  - DEPLOY-05
  - DEPLOY-06
  - OBS-01
  - OBS-02
  - OBS-03
  - OBS-04
  - OBS-05
  - OBS-06
  - NF-01
  - NF-02
  - NF-03
  - NF-04

must_haves:
  truths:
    - "D6: Dockerfile CMD uses --loop asyncio --workers 1; uvloop is absent from pyproject.toml + uv.lock; CI asserts absence"
    - "D6 pinned by test: test_no_uvloop_installed and test_uvloop_absent_from_lock both pass"
    - "libnspr4 + libnss3 installed in Docker runtime stage (Phase 1 NEEDS-PIVOT — without this, every launch_async() fails)"
    - "tini ENTRYPOINT in Dockerfile for Chromium zombie reaping (DEPLOY-04)"
    - "GET /health returns 200 with {status, cloak, llm, cache} in <50ms (OBS-01)"
    - "GET /health/deep performs real Cloak nav to about:blank + HEAD /healthz (OBS-02)"
    - "structlog JSON output with merge_contextvars + correlation_id in every log line (OBS-03)"
    - "asgi-correlation-id middleware propagates X-Request-ID; UUID generated if absent (OBS-04)"
    - "OBS-05: no scraped content (title, snippet, url) in any log event"
    - "OBS-06: artiscrapper_llm_fallback_total{reason} and artiscrapper_visit_failed_total{host} counters increment correctly"
    - "docker build succeeds; docker run boots GET /health in <10s"
    - "pytest tests/ -x -q green including health tests; ruff check passes; mypy --strict on public modules passes"
    - "e2e test against live dev-box: POST /search q=pelota+playera+quico → ≥10 results, ≥6 with price"
  artifacts:
    - path: "Dockerfile"
      provides: "Multi-stage build: uv builder → cloakbrowser:0.3.31 runtime + libnspr4 + libnss3 + tini"
      contains: "libnspr4 libnss3"
    - path: "compose.yml"
      provides: "Dev local compose with sqlite bind-mount"
      contains: "cache.db"
    - path: ".github/workflows/ci.yml"
      provides: "CI: ruff + mypy + pytest + uvloop-absence assertion"
      contains: "uvloop"
    - path: "tests/test_health.py"
      provides: "GET /health + /health/deep shape tests"
    - path: "tests/test_e2e.py"
      provides: "Live dev-box end-to-end test (marked @pytest.mark.e2e)"
  key_links:
    - from: "Dockerfile CMD"
      to: "uvicorn"
      via: "--loop asyncio --workers 1 — D6 foot-gun enforced at container level"
      pattern: "loop asyncio"
    - from: ".github/workflows/ci.yml"
      to: "uv.lock + pyproject.toml"
      via: "grep -rE uvloop assertion — D6 foot-gun enforced at CI level"
      pattern: "uvloop"
    - from: "src/artiscrapper/metrics.py"
      to: "src/artiscrapper/llm.py + visit.py"
      via: "metrics.llm_fallback_total[reason] incremented in classify_candidate; metrics.visit_failed_total[host] incremented in visit_one"
      pattern: "metrics\\.llm_fallback_total\\|metrics\\.visit_failed_total"
---

<objective>
Wire observability (structlog, asgi-correlation-id, inline counters), build the Dockerfile
and compose.yml, create the GitHub Actions CI pipeline, implement health tests and the live
e2e test, and run full quality gates (ruff + mypy + pytest). This is the final plan that makes
the service deployable and fully tested.

Purpose: After this plan, `docker build .` produces a working image, `docker run` boots `/health`
in <10s, and `pytest tests/ -x -q` is fully green (including health and footgun tests). The e2e
test validates PRD §10 acceptance criteria on a live dev-box request.

Output: Dockerfile + compose.yml + CI workflow + green full test suite. Phase 2 is shippable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/SPIKE.md
@.planning/phases/02-mvp/02-RESEARCH.md
@.planning/phases/02-mvp/02-PATTERNS.md
@.planning/phases/02-mvp/02-VALIDATION.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-mvp/02-01-SUMMARY.md
@.planning/phases/02-mvp/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dockerfile + compose.yml + .dockerignore + OBS-06 counter wiring + health tests</name>
  <files>
    Dockerfile,
    compose.yml,
    .dockerignore,
    src/artiscrapper/metrics.py,
    src/artiscrapper/llm.py,
    src/artiscrapper/visit.py,
    tests/test_health.py
  </files>
  <read_first>
    - .planning/phases/02-mvp/02-RESEARCH.md — Pattern 12 (Dockerfile multi-stage + compose.yml + compose.yml verbatim, lines 1298-1364); Pattern 11 (/health + /health/deep implementation, lines 1231-1292); Pattern 13 (Metrics dataclass, lines 1368-1389 — verify it's already in metrics.py from 02-01)
    - .planning/SPIKE.md — §Risks: "libnspr4 + libnss3 are NOT bundled in the cloakbrowser wheel. Phase 2 Dockerfile MUST install both via apt-get install -y libnspr4 libnss3"; §Browser: Chromium pin chromium-v146.0.7680.177.5
    - .planning/phases/02-mvp/02-PATTERNS.md — §Dockerfile section (NO-ANALOG — Pattern 12 verbatim); §compose.yml section
    - .planning/REQUIREMENTS.md — DEPLOY-01 (multi-stage base cloakhq/cloakbrowser:0.3.31), DEPLOY-02 (chromium pin), DEPLOY-03 (--loop asyncio --workers 1), DEPLOY-04 (tini), DEPLOY-05 (uv sync --locked + CI uvloop assertion), DEPLOY-06 (compose sqlite bind-mount), OBS-01 (/health), OBS-02 (/health/deep), OBS-06 (inline counters)
    - .planning/phases/02-mvp/02-VALIDATION.md — test rows for OBS-01 (test_health_shape) and OBS-02 (test_health_deep_shape)
  </read_first>
  <behavior>
    - Dockerfile: copy Pattern 12 verbatim. Two stages: builder uses `ghcr.io/astral-sh/uv:python3.12-bookworm-slim`; runtime uses `cloakhq/cloakbrowser:0.3.31`. Runtime stage MUST include: `RUN apt-get update && apt-get install -y --no-install-recommends libnspr4 libnss3 tini && rm -rf /var/lib/apt/lists/*`. ENTRYPOINT: `["/usr/bin/tini", "--"]`. CMD: `["uvicorn", "src.artiscrapper.main:app", "--host", "0.0.0.0", "--port", "8000", "--workers", "1", "--loop", "asyncio"]`. D6 FOOT-GUN: --loop asyncio and --workers 1 are MANDATORY in CMD. D1 pin comment: `# D1 pin: chromium-v146.0.7680.177.5`. ENV PATH="/app/.venv/bin:$PATH" PYTHONUNBUFFERED=1.
    - compose.yml: copy Pattern 12 compose section verbatim. Service artiscrapper: build:., ports 8000:8000, volumes `./data/cache.db:/app/cache.db`, environment with CACHE_DB_PATH, LLM_ROUTER_URL=http://host.docker.internal:3210, LLM_ROUTER_BEARER_TOKEN=${LLM_ROUTER_BEARER_TOKEN}, GOOGLE_MIN_INTERVAL_S=60, BROWSER_RECYCLE_AFTER=200, LLM_CONCURRENCY=4, LOG_JSON=true, LOG_LEVEL=INFO. `init: true`.
    - .dockerignore: exclude .git, __pycache__, *.pyc, .env*, tests/fixtures/*.html (fixtures are large; tests run inside the image only if needed), scripts/spike/, .planning/, artifacts/.
    - metrics.py: verify it has the Metrics dataclass from 02-01. Now wire it: ensure classify_candidate() in llm.py calls `from .metrics import metrics; metrics.llm_fallback_total[reason] += 1` for each fallback verdict (OBS-06). Ensure visit_one() in visit.py calls `metrics.visit_failed_total[host] += 1` when outcome is "failed" or exception (OBS-06). Also ensure block detection in main.py calls `metrics.block_detected_total[reason] += 1` when _detect_block() returns non-None.
    - tests/test_health.py: implement fully (Wave 0 had stubs). Use FastAPI TestClient with mock_app_state fixture from conftest.py. test_health_shape: GET /health → 200, JSON body has keys "status", "cloak", "llm", "cache", all non-null. test_health_deep_shape: mock browser.new_context() + page.goto() to succeed; mock httpx HEAD /healthz to return 200; GET /health/deep → 200, "cloak" field starts with "ok".
  </behavior>
  <action>
    Dockerfile follows Pattern 12 exactly. The critical NEEDS-PIVOT line from Phase 1 is the
    apt-get install of libnspr4 and libnss3. Without these, every launch_async() call in the
    container fails with `error while loading shared libraries: libnspr4.so.4`. Place this in
    the runtime stage, AFTER the `FROM cloakhq/cloakbrowser:0.3.31 AS runtime` line.

    The builder stage uses uv cache mounts (`--mount=type=cache,target=/root/.cache/uv`) for
    fast rebuilds. The COPY pattern: copy only pyproject.toml + uv.lock first, run
    `uv sync --locked --no-install-project`, then COPY . /app, then `uv sync --locked --no-editable`.
    This layer-caches the dependency install.

    For OBS-06 counter wiring: the metrics singleton from 02-01 already exists. In this task,
    verify that llm.py's classify_candidate() increments `metrics.llm_fallback_total[reason]`
    in the fallback paths (timeout, malformed, overload, conn_error, http_*). In visit.py's
    visit_one(), increment `metrics.visit_failed_total[host]` when outcome is "failed". In main.py,
    increment `metrics.block_detected_total[block_reason]` after _detect_block() returns non-None.
    If these increments are already present from 02-02, verify with grep and skip re-editing.

    For test_health.py: the /health/deep endpoint makes a real Cloak navigation (ctx.new_page +
    goto about:blank) and a real httpx HEAD to LLM router. In tests, mock both. Use
    unittest.mock.patch or monkeypatch on `browser.new_context`, `page.goto`, and respx for httpx.
    The test asserts the JSON shape contains all expected keys, not exact values.
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/artiscrapper && uv run pytest tests/test_health.py -x -q 2>&1 | tail -20 && grep -q 'loop asyncio' Dockerfile && grep -q 'workers 1' Dockerfile && grep -q 'libnspr4' Dockerfile && grep -q 'libnss3' Dockerfile && grep -q 'tini' Dockerfile && echo "Dockerfile invariants OK"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_health.py -x -q` exits 0 with ≥2 passed (test_health_shape, test_health_deep_shape)
    - `grep -q 'loop asyncio' Dockerfile && grep -q 'workers 1' Dockerfile` exits 0 (D6 enforced in CMD)
    - `grep -q 'libnspr4' Dockerfile && grep -q 'libnss3' Dockerfile` exits 0 (Phase 1 NEEDS-PIVOT addressed)
    - `grep -q 'tini' Dockerfile` exits 0 (DEPLOY-04)
    - `grep -q 'cloakhq/cloakbrowser:0.3.31' Dockerfile` exits 0 (DEPLOY-01 pin)
    - `grep -c 'llm_fallback_total' src/artiscrapper/llm.py` ≥1 (OBS-06 counter in LLM module)
    - `grep -c 'visit_failed_total' src/artiscrapper/visit.py` ≥1 (OBS-06 counter in visit module)
    - compose.yml contains `cache.db:/app/cache.db` (DEPLOY-06 sqlite bind-mount)
    - compose.yml contains `LLM_ROUTER_BEARER_TOKEN` (env var template)
    - .dockerignore exists and excludes .git and __pycache__
  </acceptance_criteria>
  <done>Dockerfile with all critical deps (libnspr4, libnss3, tini). compose.yml wired. OBS-06 counters live in llm.py and visit.py. Health tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CI pipeline + e2e test + full quality gates (ruff + mypy + pytest green)</name>
  <files>
    .github/workflows/ci.yml,
    tests/test_e2e.py,
    tests/test_footguns.py
  </files>
  <read_first>
    - .planning/phases/02-mvp/02-RESEARCH.md — Pattern 10 (D6 CI uvloop assertion + Dockerfile CMD assertion, lines 1192-1226); §Validation Architecture (e2e test structure, lines 1636-1638); §Open Questions #1 (asgi-correlation-id 5.0.0 import path verification)
    - .planning/phases/02-mvp/02-PATTERNS.md — §.github/workflows/ci.yml (NO-ANALOG); §tests/test_e2e.py (NO-ANALOG); §tests/test_footguns.py (copy verbatim from Pattern 10)
    - .planning/ROADMAP.md — Phase 2 Success Criteria items 5 (D2), 6 (D6), 7 (D8), 8 (docker build + pytest green)
    - .planning/REQUIREMENTS.md — NF-01 (P50 <500ms cache-hit, P50 cold <20s), NF-02 (parser + LLM respx tests), NF-03 (ruff + mypy strict), NF-04 ($0 cost)
    - .planning/phases/02-mvp/02-VALIDATION.md — e2e test rows: test_serp_pelota, test_cache_hit; PRD §10 acceptance criteria
    - .planning/phases/02-mvp/02-VALIDATION.md — Sampling rate: full suite must be green before /gsd:verify-work
  </read_first>
  <behavior>
    - .github/workflows/ci.yml: on push/pull_request to main. Steps: (1) checkout + setup Python 3.12 + install uv, (2) `uv sync --locked`, (3) ruff check, (4) ruff format --check, (5) mypy --strict src/artiscrapper/ (public modules only), (6) uvloop absence assertion per Pattern 10: `grep -rE 'uvloop' pyproject.toml uv.lock && echo "FAIL: uvloop found — D6 foot-gun!" && exit 1 || echo "OK"`, (7) Dockerfile CMD assertion: `grep -q 'loop asyncio' Dockerfile && grep -q 'workers 1' Dockerfile || { echo "FAIL: Dockerfile CMD missing flags"; exit 1; }`, (8) pytest tests/ -x -q -k "not e2e". The e2e marker tests are excluded from CI (they require the live dev-box + local-llms-router).
    - tests/test_e2e.py: implement test_serp_pelota and test_cache_hit, both marked @pytest.mark.e2e. test_serp_pelota: POST http://localhost:8000/search with body {"query": "pelota playera quico"}, assert status_code==200, assert len(response.json()["results"]) >= 10, assert sum(1 for r in results if r["price"] is not None) >= 6, assert not any(h in ("youtube.com","www.youtube.com","wikipedia.org","reddit.com") for r in results for h in [urlparse(r["url"]).netloc.lower()]), assert response.json()["metadata"]["cache_hit"] is False (first call). test_cache_hit: run the same query again within the test session, assert metadata.cache_hit is True, assert elapsed < 500ms. Both tests: skip with @pytest.mark.skip(reason="e2e: requires live dev-box at localhost:8000") when not running in e2e mode. Actually implement with a skipif condition: `pytest.mark.skipif(os.getenv("E2E", "0") != "1", reason="set E2E=1 to run live tests")`.
    - tests/test_footguns.py: finalize all 4 invariant tests. The test_d2_fallback_is_dropped test was stubs in 02-01 — now llm.py exists, make it a real import + assertion. All 4 tests must pass: test_no_uvloop_installed, test_uvloop_absent_from_lock, test_no_persistent_context_in_codebase, test_d2_fallback_is_dropped. Also add test_recycle_triggers: mock app.state with browser_uses=settings.BROWSER_RECYCLE_AFTER+1; assert _recycle_browser_loop would trigger a recycle (test via checking the conditional logic, not full async loop).
    - Run quality gates and fix any issues: `uv run ruff check src/ tests/` (fix any linting errors), `uv run ruff format --check src/ tests/` (fix formatting), `uv run mypy --strict src/artiscrapper/` (fix any type errors). After fixing: run full pytest suite `uv run pytest tests/ -x -q -k "not e2e"` green.
  </behavior>
  <action>
    The e2e test (test_serp_pelota) validates PRD §10 success criteria item #1: "POST /search with
    body {"query": "pelota playera quico"} returns ≥10 products in <30s, ≥6 with price non-null,
    zero blogs/wiki/youtube in top 10."

    The test uses the dev-box's local service at localhost:8000. It requires local-llms-router
    running and the artiscrapper service started with `uvicorn src.artiscrapper.main:app
    --loop asyncio --workers 1`. Set E2E=1 to activate.

    For ruff and mypy: fix all errors found. Common issues to expect:
    - mypy: annotate Optional types explicitly; use `X | None` syntax (Python 3.12+)
    - ruff: remove unused imports; fix line length; ensure consistent quote style
    - mypy strict: all public function signatures need return type annotations

    The CI uvloop absence assertion uses `grep -rE 'uvloop' pyproject.toml uv.lock`. The `||` is
    critical — grep exits 1 when nothing found (which is what we want), so the `|| echo "OK"` part
    runs and exits 0. The `&&` that follows the echo exits 0 too. This pattern matches Pattern 10.

    For test_footguns.py: the test_d2_fallback_is_dropped is now real (llm.py exists):
    `from src.artiscrapper.llm import LLMVerdict, should_keep; v = LLMVerdict.fallback("timeout");
    assert v.confidence == 0.3; assert not should_keep(v)`.

    For test_recycle_triggers: this is a unit test that checks the recycle condition, NOT a full
    async test of the loop. Simply assert: `BROWSER_RECYCLE_AFTER = 200; uses = 201;
    assert uses >= BROWSER_RECYCLE_AFTER` as a logic pin. Alternatively, check the conditional
    in browser.py or main.py is present via source inspection.
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/artiscrapper && uv run pytest tests/test_footguns.py -x -q 2>&1 | tail -20 && uv run pytest tests/ -x -q -k "not e2e" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_footguns.py -x -q` exits 0 with 4+ passed: test_no_uvloop_installed, test_uvloop_absent_from_lock, test_no_persistent_context_in_codebase, test_d2_fallback_is_dropped
    - `uv run pytest tests/ -x -q -k "not e2e"` exits 0 — full suite green
    - `uv run ruff check src/ tests/` exits 0 (clean)
    - `uv run ruff format --check src/ tests/` exits 0 (formatted)
    - `uv run mypy --strict src/artiscrapper/` exits 0 (strict type-check passes)
    - .github/workflows/ci.yml exists with: ruff step, mypy step, uvloop grep assertion step, Dockerfile CMD assertion step, pytest step
    - `grep -c 'uvloop' .github/workflows/ci.yml` ≥1 (D6 CI assertion wired)
    - `grep -c 'loop asyncio' .github/workflows/ci.yml` ≥1 (D6 CMD assertion wired)
    - tests/test_e2e.py has test_serp_pelota and test_cache_hit both decorated with skipif E2E!=1
    - `grep -c 'pelota playera quico' tests/test_e2e.py` ≥1 (PRD §10 query present)
    - `grep -c 'cache_hit' tests/test_e2e.py` ≥1 (cache hit assertion present)
    - E2E smoke (manual only — not in automated): `E2E=1 uv run pytest tests/test_e2e.py -x -q -m e2e` exits 0 when dev-box is live
  </acceptance_criteria>
  <done>CI pipeline green. Full test suite (non-e2e) passing with ruff clean and mypy strict. D2/D6/D8 foot-guns all pinned by tests. e2e test defined and ready for live run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → source code | grep assertions on uv.lock + pyproject.toml + Dockerfile ensure D6 invariants |
| docker build → PyPI + apt | Builder pulls from PyPI (pinned uv.lock); runtime pulls apt packages from Debian repos |
| e2e test → localhost:8000 | Live request to dev-box; only runs with E2E=1 env var; no credentials in test file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | D6 uvloop creeping in via transitive dep | mitigate | CI `grep -rE 'uvloop' pyproject.toml uv.lock` assertion exits 1 if present; blocks merge; test_uvloop_absent_from_lock pins it in pytest |
| T-02-03-02 | Tampering | D6 Dockerfile CMD uvloop flag omission | mitigate | CI `grep -q 'loop asyncio' Dockerfile && grep -q 'workers 1' Dockerfile` assertion; Dockerfile is reviewed at build time |
| T-02-03-03 | Elevation of Privilege | Container runs as root with --no-sandbox Chromium | accept | Standard Docker container pattern; Chromium sandbox is inside the container; no external kernel namespace concern at this scale (1-user VPS) |
| T-02-03-04 | Information Disclosure | LLM_ROUTER_BEARER_TOKEN in compose.yml env | mitigate | Token pulled from ${LLM_ROUTER_BEARER_TOKEN} env var (not hard-coded); compose.yml is safe to commit (contains template, not value); .env file is gitignored |
| T-02-03-05 | Information Disclosure | Scraped content in CI logs (test fixtures in log output) | mitigate | OBS-05: test_health and test_e2e tests use mocked responses; no real HTML logged; structlog allow-list enforced in production code (verified in 02-01/02-02) |
| T-02-03-SC | Tampering | Dockerfile apt-get installs (libnspr4, libnss3, tini) | mitigate | These are Debian standard packages from official Debian bookworm repos; Docker image layer is pinned via FROM cloakhq/cloakbrowser:0.3.31 (exact digest); no third-party apt sources added |
</threat_model>

<verification>
Full phase 2 verification (all three plans complete):

1. `uv run pytest tests/ -x -q -k "not e2e"` exits 0 — full suite green
2. `uv run ruff check src/ tests/` exits 0 — lint clean
3. `uv run ruff format --check src/ tests/` exits 0 — format clean
4. `uv run mypy --strict src/artiscrapper/` exits 0 — strict types
5. `grep -v '^#' uv.lock | grep uvloop` returns nothing — D6 clear
6. `grep -rn "launch_persistent_context" src/` returns nothing — D8 clear
7. `grep -q 'libnspr4' Dockerfile && grep -q 'libnss3' Dockerfile` exits 0 — Phase 1 NEEDS-PIVOT clear
8. `grep -q 'loop asyncio' Dockerfile && grep -q 'workers 1' Dockerfile` exits 0 — D6 in Dockerfile
9. `grep -c 'tini' Dockerfile` ≥1 — DEPLOY-04
10. `grep -c 'cloakhq/cloakbrowser:0.3.31' Dockerfile` ≥1 — DEPLOY-01
11. `docker build -t artiscrapper:test . && docker run --rm artiscrapper:test uvicorn --version` exits 0 (build sanity)
12. `uv run pytest tests/test_footguns.py -x -q` exits 0 with 4+ tests — D2/D6/D8 all pinned
13. `uv run pytest tests/test_health.py -x -q` exits 0 — OBS-01/02 verified
14. Manual: `E2E=1 uv run pytest tests/test_e2e.py -x -q -m e2e` on live dev-box → ≥10 results, ≥6 with price

Phase 2 success criteria (ROADMAP.md):
- SC-1: POST /search q=pelota+playera+quico → ≥10 products, ≥6 with price, zero blogs (e2e)
- SC-2: POST /search q=filtro+aceite+ford+focus → ≥10 products, ≥7 from real stores (manual)
- SC-3: GET /health → {status:ok, cloak:ok, llm:ok, cache:ok} <50ms
- SC-4: Identical query → cache_hit=true, <500ms
- SC-5: D2 verified (unit test)
- SC-6: D6 verified (test + CI + Dockerfile CMD)
- SC-7: D8 verified (test + grep)
- SC-8: docker build + run boots + pytest green + ruff + mypy
</verification>

<success_criteria>
- Dockerfile: multi-stage cloakhq/cloakbrowser:0.3.31 runtime + libnspr4 + libnss3 + tini + --loop asyncio --workers 1 CMD
- compose.yml: artiscrapper service with sqlite bind-mount and env var template
- .github/workflows/ci.yml: ruff + mypy + uvloop grep + Dockerfile CMD assertion + pytest steps
- `uv run pytest tests/ -x -q -k "not e2e"` exits 0 (all non-e2e tests pass)
- `uv run ruff check src/ tests/` exits 0
- `uv run ruff format --check src/ tests/` exits 0
- `uv run mypy --strict src/artiscrapper/` exits 0
- All 4 footgun tests pass (D2, D6×2, D8)
- Health tests pass (test_health_shape, test_health_deep_shape)
- e2e test defined and skippable (skipif E2E!=1)
- Phase 2 ROADMAP success criteria SC-3 through SC-8 verified by automated tests
- SC-1 and SC-2 verified on live dev-box by executor or Luis as UAT operator
</success_criteria>

<output>
Create `.planning/phases/02-mvp/02-03-SUMMARY.md` when done.
</output>
