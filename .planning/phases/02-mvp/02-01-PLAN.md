---
phase: 02-mvp
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/artiscrapper/__init__.py
  - src/artiscrapper/config.py
  - src/artiscrapper/models.py
  - src/artiscrapper/logging_setup.py
  - src/artiscrapper/metrics.py
  - src/artiscrapper/rate_limit.py
  - src/artiscrapper/browser.py
  - src/artiscrapper/search.py
  - src/artiscrapper/main.py
  - src/artiscrapper/cache.py
  - tests/conftest.py
  - tests/test_parser.py
  - tests/test_footguns.py
autonomous: true
requirements:
  - SEARCH-01
  - SEARCH-02
  - SEARCH-03
  - SEARCH-04
  - SEARCH-05
  - SEARCH-06
  - SEARCH-07
  - SEARCH-08
  - BROWSER-01
  - BROWSER-02
  - BROWSER-03
  - BROWSER-04
  - BROWSER-05

must_haves:
  truths:
    - "FastAPI app boots with lifespan: Cloak singleton, sqlite cache, and background tasks all start"
    - "Two parallel Google SERP fetches return HTML via build_serp_url() with pws=0&safe=off (D3)"
    - "Parser cascade extracts candidates from all 10 Phase 1 SERP fixtures; alert fires on cascade exhaustion (D4)"
    - "D8: launch_persistent_context is absent from src/; ephemeral new_context() per request"
    - "D6: uvloop is absent from pyproject.toml and uv.lock; --loop asyncio --workers 1 used"
    - "SIGSTOP heartbeat in _recycle_browser_loop catches a frozen Cloak process (Phase 1 NEEDS-PIVOT)"
    - "URL canonicalization strips utm_*/gclid/ved; dedupe eliminates duplicate candidates"
    - "Junk-domain blocklist drops youtube/fandom/wikipedia/reddit before LLM step (D9)"
    - "Re-rank by (has_price DESC, fresh DESC, llm_confidence DESC) works correctly (SEARCH-07)"
    - "POST /search returns 200 with SearchResponse shape (metadata fields present)"
  artifacts:
    - path: "src/artiscrapper/main.py"
      provides: "FastAPI app + lifespan + health routes + search handler stub"
      contains: "_recycle_browser_loop"
    - path: "src/artiscrapper/browser.py"
      provides: "fetch_serp + _detect_block + create_browser"
      exports: ["fetch_serp", "_detect_block", "create_browser"]
    - path: "src/artiscrapper/search.py"
      provides: "build_serp_url + parse_serp + canonicalize_url + dedupe + is_junk + rerank"
      exports: ["build_serp_url", "parse_serp", "canonicalize_url", "dedupe", "is_junk", "rerank"]
    - path: "src/artiscrapper/cache.py"
      provides: "DDL + PRAGMAS + get_cached + set_cached + prune_loop"
      contains: "query_cache"
    - path: "src/artiscrapper/config.py"
      provides: "Settings class with all env vars"
      contains: "LLM_ROUTER_BEARER_TOKEN"
    - path: "tests/conftest.py"
      provides: "mock_app_state + tmp_db fixtures"
    - path: "tests/test_parser.py"
      provides: "Unit tests against 10 SERP fixtures"
    - path: "tests/test_footguns.py"
      provides: "D2/D6/D8 invariant tests"
    - path: "pyproject.toml"
      provides: "All production + dev deps, pytest config, ruff config"
  key_links:
    - from: "src/artiscrapper/main.py"
      to: "src/artiscrapper/browser.py"
      via: "lifespan calls launch_async() then exposes as app.state.browser"
      pattern: "launch_async"
    - from: "src/artiscrapper/main.py"
      to: "src/artiscrapper/cache.py"
      via: "lifespan opens aiosqlite connection, calls init_schema()"
      pattern: "aiosqlite.connect"
    - from: "src/artiscrapper/browser.py"
      to: "src/artiscrapper/rate_limit.py"
      via: "fetch_serp calls rate_limiter.acquire() before every Google fetch"
      pattern: "rate_limiter.acquire"
    - from: "src/artiscrapper/search.py"
      to: "tests/fixtures/serp/*.html"
      via: "parse_serp() tested against 10 SERP fixtures"
      pattern: "parse_serp"
---

<objective>
Bootstrap the entire Python package and implement the core scraping pipeline: FastAPI scaffold
with lifespan (Cloak singleton + SIGSTOP heartbeat + sqlite cache init), Google SERP URL builder,
four-level parser cascade with alert, URL canonicalization, junk-domain blocklist, re-rank logic,
Pydantic models, and the Wave 0 test scaffold (conftest + test files with failing stubs ready
for Wave 2 implementation).

Purpose: Every subsequent plan (02-02 and 02-03) depends on this plan's outputs. This plan creates
the full package skeleton, wires the FastAPI lifespan, and provides tested core parsing logic.
The SIGSTOP heartbeat (Phase 1 NEEDS-PIVOT) must be implemented here — it cannot be deferred.

Output: A bootable (but not yet complete) FastAPI service with green parser tests and the Wave 0
test scaffold in place. `uv run pytest tests/test_parser.py tests/test_footguns.py -x -q` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/SPIKE.md
@.planning/phases/02-mvp/02-RESEARCH.md
@.planning/phases/02-mvp/02-PATTERNS.md
@.planning/phases/02-mvp/02-VALIDATION.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Package scaffold + pyproject.toml + config + models + logging + metrics</name>
  <files>
    pyproject.toml,
    uv.lock,
    src/artiscrapper/__init__.py,
    src/artiscrapper/config.py,
    src/artiscrapper/models.py,
    src/artiscrapper/logging_setup.py,
    src/artiscrapper/metrics.py
  </files>
  <read_first>
    - .planning/phases/02-mvp/02-RESEARCH.md — §Standard Stack (lines 114-174): exact package versions; §Arch Responsibility Map; Pattern 3 (configure_logging verbatim); Pattern 13 (Metrics dataclass verbatim)
    - .planning/phases/02-mvp/02-PATTERNS.md — §config.py Settings pattern; §models.py SearchRequest/Candidate/Metadata/SearchResponse patterns; §pyproject.toml section
    - .planning/REQUIREMENTS.md — SEARCH-01 (request/response shape), SEARCH-08 (metadata fields), DEPLOY-03 (no uvloop), DEPLOY-05 (uv.lock)
    - .planning/SPIKE.md — §LLM (LLM_CONCURRENCY=4 empirically confirmed, LLM_ROUTER_URL/PORT)
  </read_first>
  <behavior>
    - pyproject.toml: name="artiscrapper", version="0.1.0", requires-python=">=3.12". Production deps: fastapi==0.136.3, uvicorn==0.48.0, cloakbrowser==0.3.31, aiosqlite==0.22.1, selectolax==0.4.10, httpx[http2]==0.28.1, structlog==25.5.0, asgi-correlation-id==5.0.0, orjson==3.11.9, tldextract==5.3.1, pydantic>=2.0, pydantic-settings>=2.0. Dev deps: pytest>=9.0, pytest-asyncio==1.4.0, respx==0.23.1, ruff>=0.15, mypy>=2.0, pytest-cov. pytest config: asyncio_mode="auto", markers=["e2e: live network tests (skipped by default)"]. NEVER add uvloop.
    - config.py: pydantic-settings BaseSettings with VERSION="0.1.0", CACHE_DB_PATH="/app/cache.db", HEADLESS=True, GOOGLE_MIN_INTERVAL_S=60, BROWSER_RECYCLE_AFTER=200, LLM_ROUTER_URL="http://127.0.0.1:3210", LLM_ROUTER_BEARER_TOKEN (no default — startup fails without it), LLM_CONCURRENCY=4, LOG_JSON=True, LOG_LEVEL="INFO". Instantiate settings=Settings() at module level.
    - models.py: SearchRequest (q: str min_length=1 max_length=500, max_results: int default=15 ge=1 le=30, visit_timeout_s: int default=10 ge=3 le=30). Candidate (url, title, snippet, price, currency, has_price, fresh, llm_confidence, freshness_signal, flags). Metadata (cache_hit, llm_degraded, block_detected, elapsed_ms, google_fetches, candidates_total, llm_filtered_out, visited, visit_failed). SearchResponse (results: list[Candidate], metadata: Metadata).
    - logging_setup.py: configure_logging() per RESEARCH.md Pattern 3 verbatim. add_correlation_id() with try/except for asgi-correlation-id 5.0.0 vs 4.x import path. Verify 5.0.0 path: run `python -c "from asgi_correlation_id.context import correlation_id"` first — if ImportError, try `from asgi_correlation_id import correlation_id`. Use whichever works. log_candidate_safe() with SAFE_CANDIDATE_FIELDS allow-list (OBS-05: never url, title, snippet, reason in logs).
    - metrics.py: Metrics dataclass per RESEARCH.md Pattern 13 verbatim. metrics = Metrics() singleton.
    - Run `uv sync --locked` after pyproject.toml is written. Verify uv.lock has no uvloop line.
  </behavior>
  <action>
    Create the directory tree: src/artiscrapper/ with empty __init__.py. Create pyproject.toml with
    [build-system] hatchling, [project] metadata, and all versioned deps listed above. Run
    `uv sync --locked` to generate uv.lock. Verify `grep -v '^#' uv.lock | grep -c uvloop` returns 0.

    In config.py: import pydantic_settings.BaseSettings; define Settings with all fields named above;
    LLM_ROUTER_BEARER_TOKEN has no default — pydantic-settings will raise ValidationError at import
    if the env var is absent. The module-level `settings = Settings()` is the startup sentinel.

    In models.py: use pydantic v2 BaseModel. SearchRequest.q must have min_length=1 for SEARCH-01
    validation. Metadata must include all eight fields from SEARCH-08: elapsed_ms, google_fetches,
    candidates_total, llm_filtered_out, visited, visit_failed, cache_hit, llm_degraded.

    In logging_setup.py: implement configure_logging() exactly per Pattern 3 lines 569-613. Add
    SAFE_CANDIDATE_FIELDS frozenset and log_candidate_safe() per Pattern 3 lines 628-644.
    OBS-05: NEVER include title, snippet, url, or reason in any log call.

    In metrics.py: copy Pattern 13 verbatim. metrics singleton is safe with --workers 1.
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/artiscrapper && python -c "from src.artiscrapper.config import settings; print('config OK')" 2>&1 | head -5 || echo "SKIP: LLM_ROUTER_BEARER_TOKEN not set in env — expected ValidationError"; python -c "from src.artiscrapper.models import SearchRequest, SearchResponse, Candidate, Metadata; print('models OK')"; python -c "from src.artiscrapper.metrics import metrics; print('metrics OK')"; grep -v '^#' uv.lock | grep -c uvloop || true</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml has [project.dependencies] with all 11 production packages at pinned versions
    - `grep -v '^#' uv.lock | grep uvloop` returns no output (exit code 1) — D6 foot-gun clear
    - `python -c "from src.artiscrapper.models import SearchRequest"` exits 0
    - `python -c "from src.artiscrapper.metrics import metrics"` exits 0
    - `python -c "from src.artiscrapper.logging_setup import configure_logging"` exits 0
    - Metadata model has all 8 SEARCH-08 fields: elapsed_ms, google_fetches, candidates_total, llm_filtered_out, visited, visit_failed, cache_hit, llm_degraded
    - src/artiscrapper/__init__.py exists (can be empty)
  </acceptance_criteria>
  <done>Package boots, models import cleanly, uv.lock contains no uvloop reference.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 0 test scaffold (conftest + test stubs) + cache.py + rate_limit.py</name>
  <files>
    tests/conftest.py,
    tests/test_parser.py,
    tests/test_footguns.py,
    tests/test_llm.py,
    tests/test_visit.py,
    tests/test_cache.py,
    tests/test_health.py,
    tests/test_e2e.py,
    src/artiscrapper/cache.py,
    src/artiscrapper/rate_limit.py
  </files>
  <read_first>
    - .planning/phases/02-mvp/02-VALIDATION.md — full per-task verification map (27 entries); Wave 0 Requirements list
    - .planning/phases/02-mvp/02-RESEARCH.md — Pattern 2 (cache DDL + PRAGMAS + insert + get + prune verbatim, lines 427-553); Pattern 1 test override block (conftest, lines 404-420); §Validation Architecture (lines 1596-1656): exact test function names and commands
    - .planning/phases/02-mvp/02-PATTERNS.md — §tests/conftest.py section; §tests/test_footguns.py section (copy verbatim)
    - .planning/REQUIREMENTS.md — CACHE-01 (WAL schema), CACHE-02 (gzip BLOB), CACHE-03 (sha256 key), CACHE-04 (lazy TTL), CACHE-05 (cache-first invariant)
    - tests/fixtures/serp/ — confirm 10 .html files exist (from Phase 1)
    - tests/fixtures/catalog/ — confirm 10 subdirs with product-01.html (from Phase 1)
    - tests/fixtures/llm/labelled.jsonl — confirm 50 records exist (from Phase 1)
  </read_first>
  <behavior>
    - tests/conftest.py: mock_app_state fixture per RESEARCH.md Pattern 1 test override (lines 404-420). tmp_db fixture: use tmp_path + aiosqlite.connect(str(tmp_path/"cache.db")); run PRAGMAS + init_schema; yield db; close. mock_browser fixture: MagicMock with is_connected()=True. asyncio_mode="auto" is set in pyproject.toml so no need for @pytest.mark.asyncio.
    - tests/test_footguns.py: copy RESEARCH.md Pattern 10 verbatim (lines 1205-1083): test_no_uvloop_installed, test_uvloop_absent_from_lock, test_no_persistent_context_in_codebase, test_d2_fallback_is_dropped. NOTE: test_d2_fallback_is_dropped imports from src.artiscrapper.llm which doesn't exist yet — stub it to import-guard with try/except and xfail until Wave 2 ships llm.py.
    - tests/test_parser.py: define test function stubs for test_cascade_exhausted_alert, test_canonicalize, test_blocklist, test_parse_serp_fixtures (reads all 10 serp/*.html). Import parse_serp, canonicalize_url, is_junk from src.artiscrapper.search — these will be implemented in Task 3. Use @pytest.mark.xfail(strict=False) on stubs that import from not-yet-existing modules.
    - tests/test_llm.py: define stubs for test_verdict_valid, test_fallback_shape, test_d2_fallback_is_dropped, test_router_call_shape, test_concurrency_semaphore. All xfail until Wave 2 ships llm.py.
    - tests/test_visit.py: stubs for test_classify_soft404, test_classify_4xx, test_extractor_catalog_fixtures, test_ar_price_regex, test_meli_guard. xfail.
    - tests/test_cache.py: IMPLEMENT FULLY (cache.py ships in this task): test_schema (DDL creates query_cache with cache_key TEXT PK, raw_serp_html_a BLOB, raw_serp_html_b BLOB), test_gzip_roundtrip (set_cached gzips; decompress back = original), test_cache_key_deterministic (identical normalized queries → same sha256), test_lazy_ttl (expired row returns None from get_cached).
    - tests/test_health.py: stubs for test_health_shape, test_health_deep_shape. xfail until Wave 3.
    - tests/test_e2e.py: single stub test_serp_pelota and test_cache_hit, both decorated @pytest.mark.e2e and @pytest.mark.skip(reason="e2e: needs live dev-box").
    - cache.py: implement per RESEARCH.md Pattern 2 verbatim (DDL, PRAGMAS, normalize_query, make_cache_key, get_cached, set_cached, prune_loop). Parameterized queries everywhere (SQL injection mitigation: sha256 hex key is alphanumeric only, but use ? placeholders regardless). No string interpolation in SQL.
    - rate_limit.py: implement GoogleRateLimiter per RESEARCH.md Pattern 1 (Semaphore(1) + monotonic timestamp). acquire() method blocks until GOOGLE_MIN_INTERVAL_S elapsed.
  </behavior>
  <action>
    Create all 8 test files and 2 implementation files. The test files follow the exact function names
    from 02-VALIDATION.md — those names are the contract that execute-plan will verify against.

    For cache.py: DDL schema must include ALL columns from RESEARCH.md Pattern 2: cache_key TEXT PK,
    query TEXT, query_norm TEXT, response_json TEXT, raw_serp_html_a BLOB, raw_serp_html_b BLOB,
    created_at INTEGER, expires_at INTEGER, bytes_total INTEGER. Also CREATE INDEX on expires_at and
    created_at. PRAGMAS must include journal_mode=WAL, synchronous=NORMAL, busy_timeout=5000.

    For rate_limit.py: GoogleRateLimiter must serialize Google fetches via asyncio.Semaphore(1).
    acquire() is async: awaits the semaphore, computes elapsed = monotonic() - _last_fetch, sleeps
    the remainder if elapsed < min_interval, then updates _last_fetch. This ensures ≥60s between
    Google fetches when GOOGLE_MIN_INTERVAL_S=60 (BROWSER-04).

    For test stubs that import from not-yet-existing modules (llm.py, visit.py, main.py): use
    try/except ImportError at the top of the test file and mark those tests as xfail or skip.
    This allows `pytest tests/ -x -q -k "not e2e"` to run without crashing on import errors.

    For test_cache.py tests: use the tmp_db fixture from conftest.py. test_lazy_ttl: set_cached with
    ttl_s=1, sleep 2, call get_cached → must return None.
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/artiscrapper && uv run pytest tests/test_cache.py tests/test_footguns.py::test_no_uvloop_installed tests/test_footguns.py::test_uvloop_absent_from_lock tests/test_footguns.py::test_no_persistent_context_in_codebase -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_cache.py -x -q` exits 0 with 4 passed (test_schema, test_gzip_roundtrip, test_cache_key_deterministic, test_lazy_ttl)
    - `uv run pytest tests/test_footguns.py::test_no_uvloop_installed tests/test_footguns.py::test_uvloop_absent_from_lock tests/test_footguns.py::test_no_persistent_context_in_codebase -x -q` exits 0
    - All 8 test files exist under tests/
    - tests/conftest.py defines mock_app_state and tmp_db fixtures
    - `grep -c 'def test_' tests/test_parser.py` returns ≥3 (cascade_exhausted_alert, canonicalize, blocklist)
    - `grep -c 'def test_' tests/test_footguns.py` returns ≥4 (no_uvloop_installed, uvloop_absent_from_lock, no_persistent_context, d2_fallback_is_dropped)
    - cache.py contains `PRAGMA journal_mode=WAL` and `SELECT response_json, expires_at FROM query_cache WHERE cache_key=?`
    - rate_limit.py contains `asyncio.Semaphore(1)` and `time.monotonic()`
  </acceptance_criteria>
  <done>Cache tests green. Wave 0 test scaffold in place. rate_limit.py and cache.py implemented and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: browser.py + search.py + main.py (FastAPI lifespan + SIGSTOP heartbeat)</name>
  <files>
    src/artiscrapper/browser.py,
    src/artiscrapper/search.py,
    src/artiscrapper/main.py
  </files>
  <read_first>
    - .planning/phases/02-mvp/02-RESEARCH.md — Pattern 1 (lifespan + _recycle_browser_loop WITH SIGSTOP heartbeat, lines 287-402 — copy verbatim); Pattern 7 (launch_async + new_context chain, lines 1070-1115); Pattern 8 (_detect_block markers, lines 1119-1163); Pattern 9 (build_serp_url, lines 1169-1184); Pattern 11 (/health + /health/deep, lines 1232-1292); §Code Examples (parser cascade, canonicalize_url, dedupe, is_junk, rerank, lines 1477-1576)
    - .planning/phases/02-mvp/02-PATTERNS.md — §src/artiscrapper/browser.py (analog: scripts/spike/02_cloak_smoke.py); §src/artiscrapper/search.py (analog: scripts/spike/08_extruct_classifier.py); §src/artiscrapper/main.py (NO-ANALOG — Pattern 1 + 11)
    - .planning/SPIKE.md — §Browser (SIGSTOP NEEDS-PIVOT: `is_connected()` stays True on SIGSTOP; solution: page.evaluate("1") every 10s in _recycle_browser_loop); Cloak import path: `from cloakbrowser import launch_async` (NOT async_playwright)
    - scripts/spike/02_cloak_smoke.py — exact browser launch + ephemeral context pattern (empirically verified)
    - .planning/REQUIREMENTS.md — BROWSER-01 (singleton), BROWSER-02 (no persistent context), BROWSER-03 (recycle 200), BROWSER-04 (rate limit), BROWSER-05 (_detect_block), SEARCH-03 (pws=0&safe=off), SEARCH-04 (cascade)
  </read_first>
  <behavior>
    - browser.py: `from cloakbrowser import launch_async` (NOT async_playwright — SPIKE.md confirmed). create_browser(): `await launch_async(headless=True)`. fetch_serp(browser, url, rate_limiter): calls rate_limiter.acquire(), creates ephemeral ctx via `browser.new_context(locale="es-AR", timezone_id="America/Argentina/Buenos_Aires", viewport={"width": 1366, "height": 768})`, creates page, goto with wait_until="domcontentloaded" timeout=20_000, calls _detect_block(page), returns (html, block_reason). _detect_block(page): checks /sorry/ in URL, g-recaptcha, unusual traffic, captcha_form markers per Pattern 8 verbatim. Returns block_reason string or None.
    - search.py: build_serp_url(query, *, meli=False) per Pattern 9 verbatim (pws=0, safe=off, NO num/tbm/udm/site:). parse_serp(html) per §Code Examples lines 1477-1516 with ORGANIC_SELECTORS and CAROUSEL_SELECTORS; logs `parse_cascade_exhausted` warning when all organic selectors return 0 (D4). _extract_organic(node) and _extract_carousel(node): extract title (h3 text), url (href from <a>), snippet (div.VwiC3b or similar text), price_in_card. _extract_by_h3(tree): generic fallback. canonicalize_url per §Code Examples lines 1521-1547 with STRIP_PARAMS frozenset. dedupe per §Code Examples lines 1538-1547. is_junk per §Code Examples lines 1551-1565 with JUNK_DOMAINS frozenset and JUNK_DOMAIN_SUFFIXES. rerank per §Code Examples lines 1568-1576.
    - main.py: FastAPI app with lifespan per Pattern 1 verbatim (lines 352-402). lifespan: configure_logging first, then sqlite open+PRAGMAS+init_schema, then launch_async, then GoogleRateLimiter, then asyncio.create_task(_recycle_browser_loop) + asyncio.create_task(prune_loop). app.add_middleware(CorrelationIdMiddleware). _recycle_browser_loop: MUST implement SIGSTOP heartbeat per Pattern 1 lines 305-350: primary check is_connected() for SIGKILL, secondary `asyncio.wait_for(page.evaluate("1"), timeout=5.0)` for SIGSTOP every 10s sleep cycle. Recycle after BROWSER_RECYCLE_AFTER uses. /health and /health/deep per Pattern 11. POST /search stub: only cache lookup + request body parse + placeholder 200 response (LLM and visit pass come in 02-02). Bind structlog contextvars at top of /search handler: `structlog.contextvars.clear_contextvars(); structlog.contextvars.bind_contextvars(query_hash=cache_key[:12], stage="search")`.
    - OBS-05: NEVER log title, snippet, url, or reason. Use log_candidate_safe() from logging_setup.py for any candidate logging.
    - D8: `launch_persistent_context` must NOT appear anywhere in src/. `grep -rn launch_persistent_context src/` must return empty.
  </behavior>
  <action>
    browser.py implements fetch_serp + _detect_block + create_browser. The critical SIGSTOP heartbeat
    in _recycle_browser_loop (Phase 1 NEEDS-PIVOT): the loop sleeps 10s, then:
    1. Checks is_connected() — if False, recycle (catches SIGKILL).
    2. Attempts `asyncio.wait_for(page.evaluate("1"), timeout=5.0)` in an ephemeral context — if
       exception, recycle (catches SIGSTOP and frozen Chromium).
    3. Checks browser_uses >= BROWSER_RECYCLE_AFTER — if True, recycle (memory drift prevention).
    This three-check loop is the exact Pattern 1 (lines 305-350) — copy it verbatim.

    search.py: parse_serp uses ORGANIC_SELECTORS list with the 2024-2026 primary selector
    "div.MjjYud div.tF2Cxc" first (Phase 1 confirmed all 10 SERP fixtures use this). The for/else
    construct fires log.warning("parse_cascade_exhausted") ONLY when no organic selector yields
    results (D4 alert). The carousel selectors run independently (separate for loop, does not affect
    organic cascade exhaustion).

    main.py: POST /search stub at this wave: parses SearchRequest body, makes cache_key via
    make_cache_key(), calls get_cached(). If cache hit: returns immediately with cache_hit=true in
    metadata. If cache miss: placeholder response with empty results (LLM + visit fill this in 02-02).
    This is the MINIMUM viable wire — full handler composition happens when 02-02 ships.
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/artiscrapper && uv run pytest tests/test_parser.py tests/test_footguns.py -x -q 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_parser.py -x -q` exits 0: test_cascade_exhausted_alert passes (monkeypatched selectors → warning logged), test_canonicalize passes (utm_source/gclid/ved stripped), test_blocklist passes (youtube.com/fandom.com/wikipedia.org/reddit.com all return True from is_junk())
    - `uv run pytest tests/test_footguns.py::test_no_persistent_context_in_codebase -x -q` exits 0 (no launch_persistent_context in src/)
    - `grep -rn "from cloakbrowser import launch_async" src/artiscrapper/browser.py` returns 1 match (NOT async_playwright)
    - `grep -rn "async_playwright" src/` returns empty (0 matches)
    - `grep -c "parse_cascade_exhausted" src/artiscrapper/search.py` returns 1
    - `grep -c "_recycle_browser_loop" src/artiscrapper/main.py` returns ≥2 (definition + create_task call)
    - `grep -c "page.evaluate" src/artiscrapper/main.py` returns ≥1 (SIGSTOP heartbeat present)
    - `grep -c "pws.*0.*safe.*off\|safe.*off.*pws.*0" src/artiscrapper/search.py` returns ≥1 (D3: pws=0&safe=off in build_serp_url)
    - `uv run pytest tests/ -x -q -k "not e2e"` exits 0 (all non-xfail/non-skip tests pass)
  </acceptance_criteria>
  <done>Parser tests green. FastAPI app with lifespan, SIGSTOP heartbeat, and cache wiring complete. Full test suite passes (non-e2e, non-Wave-2 stubs).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /search | Untrusted query string + max_results + visit_timeout_s from caller |
| Cloak → Google SERP | Google HTML returned from Chromium; may contain challenge pages |
| process → filesystem | sqlite cache file; cache_key is user-derived |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | POST /search input validation | mitigate | `SearchRequest` Pydantic model: q min_length=1 max_length=500, max_results ge=1 le=30, visit_timeout_s ge=3 le=30 — invalid input raises 422 |
| T-02-01-02 | Tampering | SQL cache key injection | mitigate | make_cache_key() uses sha256(normalize_query()).hexdigest() — all-hex output; parameterized `?` placeholders in all SQL — no string interpolation |
| T-02-01-03 | Tampering | D8 persistent context CAPTCHA loop | mitigate | launch_persistent_context absent (grep test); lifespan uses launch_async + ephemeral new_context() per request |
| T-02-01-04 | Information Disclosure | Scraped content in logs (titles, snippets, URLs) | mitigate | OBS-05 allow-list: only url_hash, host, has_price, confidence, freshness_signal permitted in log calls; log_candidate_safe() wrapper in logging_setup.py |
| T-02-01-05 | Elevation of Privilege | Chromium process escape via --no-sandbox | accept | Cloak fingerprint patches don't affect sandbox; --no-sandbox is standard practice inside Docker containers with no kernel namespace concern |
| T-02-01-06 | Denial of Service | Google rate-limit exhaustion (BROWSER-04) | mitigate | GoogleRateLimiter: asyncio.Semaphore(1) + GOOGLE_MIN_INTERVAL_S=60 hard floor; serializes all Google fetches |
| T-02-01-SC | Tampering | npm/pip installs via uv sync | mitigate | All packages pre-audited (RESEARCH.md §Package Legitimacy Audit — [OK] for all 12 packages); uv.lock pinned; CI grep asserts uvloop absent |
</threat_model>

<verification>
Full plan verification (after all tasks complete):

1. `uv run pytest tests/test_parser.py tests/test_footguns.py tests/test_cache.py -x -q` exits 0
2. `uv run pytest tests/ -x -q -k "not e2e"` exits 0 (Wave 2 LLM/visit stubs are xfail — allowed)
3. `grep -v '^#' uv.lock | grep uvloop` returns nothing (D6 clear)
4. `grep -rn "async_playwright" src/` returns nothing (correct import path)
5. `grep -rn "launch_persistent_context" src/` returns nothing (D8 clear)
6. `grep -c "page.evaluate" src/artiscrapper/main.py` returns ≥1 (SIGSTOP heartbeat)
7. `grep -c "pws" src/artiscrapper/search.py` returns ≥1 (D3: pws=0 present)
8. `grep -c "parse_cascade_exhausted" src/artiscrapper/search.py` returns 1 (D4 alert)
9. `python -c "from src.artiscrapper.models import SearchRequest, SearchResponse"` exits 0
10. `python -c "from src.artiscrapper.cache import make_cache_key, get_cached, set_cached"` exits 0
</verification>

<success_criteria>
- pyproject.toml with all 11 production + 6 dev dependencies at pinned versions; no uvloop
- uv.lock checked in; `grep -v '^#' uv.lock | grep uvloop` returns 0 matches
- src/artiscrapper/ package with 8 modules: __init__, config, models, logging_setup, metrics, rate_limit, cache, browser, search, main (plus freshness.py stub for 02-02)
- `uv run pytest tests/test_parser.py -x -q` green (cascade exhausted, canonicalize, blocklist)
- `uv run pytest tests/test_cache.py -x -q` green (schema, gzip roundtrip, deterministic key, lazy TTL)
- `uv run pytest tests/test_footguns.py -x -q` green (no uvloop installed, no uvloop in lock, no persistent context in src/)
- browser.py imports `from cloakbrowser import launch_async` (verified by grep)
- main.py `_recycle_browser_loop` contains page.evaluate("1") SIGSTOP heartbeat
- `uv run pytest tests/ -x -q -k "not e2e"` exits 0 (all tests pass or are xfail/skip)
</success_criteria>

<output>
Create `.planning/phases/02-mvp/02-01-SUMMARY.md` when done.
</output>
