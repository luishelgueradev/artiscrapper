---
phase: 03.1-v0-1-close-hygiene
plan: 03
subsystem: rate-limit+settings-source-of-truth

# Dependency graph
requires:
  - phase: 03-robustness
    provides: "@limiter.limit decorator stack on /search with hardcoded '60/minute' / '10000/day' strings (D-02 — the target of the refactor), settings.API_RATE_PER_MINUTE/DAY (defined but not wired to runtime hot path), slowapi 0.1.9 pinned"
  - phase: 03.1-v0-1-close-hygiene (plan 02)
    provides: "stable test infrastructure (68 passed) so the refactor lands on a green baseline"
provides:
  - "Module-level constants `_RATE_LIMIT_PER_MINUTE` and `_RATE_LIMIT_PER_DAY` computed once at import-time from settings (single source of truth — D-05 permanent fix)"
  - "Decorator stack on POST /search now references the constants (`@limiter.limit(_RATE_LIMIT_PER_MINUTE)` + `@limiter.limit(_RATE_LIMIT_PER_DAY)`) — no more hardcoded literals"
  - "rate_limit_init log line emits the SAME constants (cannot lie by construction); source='module_constants_from_settings' tags the wiring"
  - "Propagation invariant test in tests/test_main.py catches A3 drift (constants must equal f-string of settings)"
  - "D-06 empirical retest evidence: API_RATE_PER_MINUTE=5 in .env → compose up --force-recreate → 6th /search returns 429 + log shows per_minute='5/minute'"
affects: [milestone-v0.1-close, future-rate-limit-policy-changes]

# Tech tracking
tech-stack:
  added: []
  patterns: ["Pattern B (module constants → decorator argument + log line) — chosen after Pattern A (default_limits on Limiter constructor) failed empirically on slowapi 0.1.9 + FastAPI"]

key-files:
  created:
    - tests/test_main.py
    - .planning/phases/03.1-.../03.1-D06-EVIDENCE.md
  modified:
    - src/artiscrapper/main.py
    - compose.yml
    - .env.example

key-decisions:
  - "Pattern B (decorator argument as module constant) instead of Pattern A (default_limits on Limiter). Pattern A non-viable: default_limits is not auto-applied to routes without @limiter.limit decorators in slowapi 0.1.9, and the SlowAPIMiddleware workaround crashes on first request (extension.py:81 AttributeError)"
  - "R-01 mitigation (@limiter.exempt on health endpoints) deemed UNNECESSARY post-Pattern-B: limits only fire on decorated routes, so /health and /health/deep are intrinsically exempt"
  - "rate_limit_init log line emits the module constants directly (not raw settings.API_RATE_PER_MINUTE ints) — log and decorator cannot diverge"
  - "compose.yml + .env.example comments updated to reflect Pattern B wiring (previously said 'informational' / 'literals in main.py')"

patterns-established:
  - "Pattern B: module-level f-string constant computed from settings at import; used by both @limiter.limit() decorator argument AND structlog kwarg in lifespan log; drift surface = 1 line"

requirements-completed: []

# Metrics
duration: ~30min (executor attempt 1 revert + attempt 2 Pattern B + D-06 retest + cleanup)
completed: 2026-06-04
---

# Plan 03.1-03 Summary

**slowapi rate-limit settings-source-of-truth refactor via Pattern B (module constants → decorator argument + log line); Pattern A (default_limits on Limiter) revealed non-viable on slowapi 0.1.9 + FastAPI by empirical regression. D-06 empirical retest PASS: .env bump propagated through 5x 200 + 1x 429 with log showing the new rate**

## Performance

- **Duration:** ~30 min (executor attempt 1 revert ~10min + attempt 2 Pattern B ~5min + D-06 retest ~10min + comment sync ~5min)
- **Started:** 2026-06-04T03:00Z
- **Completed:** 2026-06-04T03:30Z
- **Tasks:** 4 (refactor + propagation test + D-06 + comment sync)
- **Files modified:** 3 source + 1 test (new) + 2 ops files + 1 evidence doc

## Accomplishments

- Pattern B refactor: 2 module constants, log line uses constants, decorators reference constants, hardcoded strings gone
- Pattern A → Pattern B deviation documented in PLAN.md with empirical root cause
- New `tests/test_main.py::test_rate_limit_constants_match_settings` catches A3 drift
- D-06 empirical retest PASS: bump → build → recreate → 5/min returns 429 on 6th request
- compose.yml + .env.example comments updated (no longer claim "informational, literals in main.py")
- Full suite: 68 passed, 2 deselected (e2e)

## Task Commits

1. **Task 1: Pattern B refactor** — `1f44310` (refactor)
2. **Task 2: propagation invariant test** — `f29ee77` (test)
3. **Task 4: D-06 empirical retest evidence + comment sync** — `a3731ac` (docs)
4. **(post-suite hygiene)** ruff --fix + format on phase-touched files — `e243846` (style)

**Plan-level commits:** `1d7af66` (Pattern A deviation note), `783506e` (STATE update)

## Files Modified

- `src/artiscrapper/main.py` — +`_RATE_LIMIT_PER_MINUTE` / `_RATE_LIMIT_PER_DAY` module constants (lines 251-252); decorators on /search reference constants (374-375); rate_limit_init log uses them with `source="module_constants_from_settings"` (172-176)
- `tests/test_main.py` (new) — `test_rate_limit_constants_match_settings` propagation invariant
- `compose.yml` — `API_RATE_PER_*` comment updated to reflect wiring path
- `.env.example` — same comment sync
- `.planning/phases/03.1-.../03.1-D06-EVIDENCE.md` (new) — full procedure + log evidence + request-by-request HTTP codes

## Decisions Made

See `key-decisions` above. Two binding ones:

1. **Pattern B over Pattern A** — verified by executor reading `.venv/lib/python3.13/site-packages/slowapi/extension.py:732,764` and `middleware.py:116-139`. `default_limits` requires `SlowAPIMiddleware` which crashes on FastAPI 0.136 with `AttributeError: 'TypeError' object has no attribute 'detail'`. Pattern B preserves the intent of D-05 (single source of truth) without depending on broken upstream code paths.
2. **R-01 mitigation dropped** — `@limiter.exempt` is unnecessary because limits only fire on decorated routes under Pattern B. The `test_health_never_rate_limited` invariant was also dropped (would be vacuous).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — Empirical fact contradicts research] RESEARCH §1 Pattern A non-viable on slowapi 0.1.9 + FastAPI**
- **Found during:** Task 1 attempt 1 (applied Pattern A, `test_rate_limit_per_minute_returns_429` regressed: request #61 returned 200, not 429)
- **Issue:** `Limiter(default_limits=[...])` accepts kwarg but doesn't auto-apply to routes without `@limiter.limit` decorator (requires `SlowAPIMiddleware`, which itself crashes on first request)
- **Fix:** Reverted (`git checkout`), updated PLAN.md with Deviation Note, re-attempted with Pattern B (decorators reference module constants)
- **Files modified:** `src/artiscrapper/main.py` (Pattern B applied), `03.1-03-PLAN.md` (Deviation Note section)
- **Verification:** D-06 empirical retest PASS (5x 200, 6th 429 with log showing per_minute=5/minute)
- **Committed in:** `1d7af66` (deviation note), `1f44310` (Pattern B refactor)

**2. [Rule 1 — Stale comment] `compose.yml` and `.env.example` claimed API_RATE_PER_* was "informational"**
- **Found during:** Post-D-06, before commit
- **Issue:** Phase 3 comment said "the slowapi decorators in main.py use string literals" — false after D-05
- **Fix:** Updated both comments to reflect Pattern B wiring path through module constants
- **Files modified:** `compose.yml`, `.env.example`
- **Verification:** N/A (comment text — visual review)
- **Committed in:** `a3731ac`

**Total deviations:** 2 auto-fixed (1 empirical research correction, 1 stale-comment sync)
**Impact on plan:** No scope creep. Pattern A → Pattern B preserves the core D-05 intent (single source of truth, drift structurally impossible). The deviation also dropped 2 secondary tasks (R-01 exempt + invariant test) that no longer applied.

## Issues Encountered

- slowapi 0.1.9 `SlowAPIMiddleware` crashes on first request with FastAPI (extension.py:81 AttributeError). Workaround would require a custom middleware shim — outside v0.1 close-hygiene scope. Pattern B sidesteps the issue entirely.

## Next Phase Readiness

D-05 + D-06 closed empirically. All 7 audit `tech_debt[N]` items closed across the 3 plans of Phase 3.1. Ready for `/gsd-verify-work 3.1` and `/gsd-complete-milestone v0.1`.

---
*Phase: 03.1-v0-1-close-hygiene*
*Completed: 2026-06-04*
