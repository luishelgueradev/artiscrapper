---
phase: 3.1
slug: v0-1-close-hygiene
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-04
---

# Phase 3.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: `03.1-RESEARCH.md §Validation Architecture` (lines 1167-1213).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 9.0+ + pytest-asyncio 1.4.0 (pyproject.toml:29-30) |
| **Config file** | `pyproject.toml` `[tool.pytest.ini_options]` lines 40-47 |
| **Quick run command** | `LLM_ROUTER_BEARER_TOKEN=test-token uv run pytest tests/ -x -q -k "not e2e"` |
| **Full suite command** | `LLM_ROUTER_BEARER_TOKEN=test-token uv run pytest tests/ -q` |
| **Phase 3.1-specific** | `pytest tests/integration/test_degraded_mode.py::test_degraded_mode_via_search_endpoint -x` (new D-04 sibling) |
| **Estimated runtime** | ~30s quick / ~90s full |

---

## Sampling Rate

- **After every task commit:** Run `LLM_ROUTER_BEARER_TOKEN=test-token uv run pytest tests/ -x -q -k "not e2e"`
- **After every plan wave:** Run full suite + `pytest tests/integration/ -v`
- **Before `/gsd:verify-work`:** Full suite green AND D-06 empirical retest PASS
- **Max feedback latency:** ~30 seconds

---

## Per-Task Verification Map

| ID | Behavior | Test Type | Automated Command | File Exists | Status |
|----|----------|-----------|-------------------|-------------|--------|
| D-01 | 52 REQUIREMENTS.md rows flip Pending→Complete | text-diff | `grep -c '\| Complete \|' .planning/REQUIREMENTS.md` ≥ 53 AND `grep -c '\| Pending \|' .planning/REQUIREMENTS.md` == 0 | ✅ | ⬜ pending |
| D-02 | 4 SUMMARY frontmatters have `requirements_completed:` | YAML grep | `for f in 02-01 02-02 02-03; do grep -c "^requirements_completed:" .planning/phases/02-mvp/$f-SUMMARY.md; done` returns 1 per file; same for `03-02` | ✅ | ⬜ pending |
| D-03 | `tests/test_e2e.py` POST calls carry X-API-Key | text inspection | `grep -c "X-API-Key" tests/test_e2e.py` ≥ 2 | ✅ | ⬜ pending |
| D-04 | Degraded-mode TestClient sibling test exists + passes | integration | `pytest tests/integration/test_degraded_mode.py::test_degraded_mode_via_search_endpoint -x -q` | ❌ Wave 0 | ⬜ pending |
| D-05 | `default_limits` on Limiter; `rate_limit_init` log reads from same constants | unit + grep | `grep -c "default_limits=" src/artiscrapper/main.py` ≥ 1 AND `grep -c "_RATE_LIMIT_PER_MINUTE" src/artiscrapper/main.py` ≥ 2 | ❌ Wave 0 | ⬜ pending |
| D-06 | Bump-and-recreate propagates to runtime hot path | empirical (UAT) | Recipe in RESEARCH §7 — Claude runs as UAT operator | n/a runtime | ⬜ pending |
| D-07/D-08 | 3 VALIDATION.md files have `status: accepted` + Acceptance Rationale | YAML grep | `grep -c "^status: accepted" .planning/phases/*/0*-VALIDATION.md` returns 3; `grep -c "## Acceptance Rationale" .planning/phases/*/0*-VALIDATION.md` returns 3 | ✅ | ⬜ pending |
| WR-01 | MELI guard uses `tldextract.registered_domain` match | unit | `pytest tests/test_visit.py::test_meli_guard_no_false_positive_on_notmercadolibre -x` | ❌ Wave 0 | ⬜ pending |
| WR-02 | Strong-ref already in place — verify-only | unit (grep) | `grep -c "background_tasks.add" src/artiscrapper/main.py` ≥ 1 | ✅ already passing | ⬜ pending |
| WR-03 | No tautological assertion; settings.BROWSER_RECYCLE_AFTER pinned | unit | `pytest tests/test_footguns.py::test_recycle_triggers -x` | ✅ | ⬜ pending |
| WR-04 | `tests/test_health.py` uses `tmp_path` (no `NamedTemporaryFile` leak) | unit + post-run inspection | `pytest tests/test_health.py -x -q` AND no orphan `/tmp/tmp*` post-run | ✅ | ⬜ pending |
| R-01 | `/health`, `/health/deep` NOT consumed by `/search` quota after D-05 | integration | `pytest tests/test_health.py::test_health_never_rate_limited -x` (new test from RESEARCH §8) | ❌ Wave 0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/integration/test_degraded_mode.py` — APPEND new `test_degraded_mode_via_search_endpoint` (D-04). File exists; new function only.
- [ ] `tests/test_visit.py` — APPEND `test_meli_guard_no_false_positive_on_notmercadolibre` (WR-01 regression). File exists; new function only.
- [ ] `tests/test_health.py` — APPEND `test_health_never_rate_limited` (R-01 invariant from §8). File exists; new function only.
- [ ] Framework install: **not needed** — `pytest`, `pytest-asyncio`, `respx` already pinned.

*(All other "test additions" are edits to existing test files, not new files. The 3 NEW test functions above are the Wave 0 surface area.)*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| D-06 empirical retest of slowapi settings drift | D-05/D-06 | Validates runtime hot path receives `.env` value via `compose up --force-recreate` — cannot be reproduced in pytest TestClient (lifespan singleton + ASGI in-process bypass the env-→-decorator wiring) | RESEARCH §7 — Claude runs the recipe as UAT operator per `feedback_agent_as_uat_operator` |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies declared
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers 3 NEW test functions (D-04, WR-01 regression, R-01 invariant)
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s for quick run
- [ ] `nyquist_compliant: true` set in frontmatter after empirical retest passes

**Approval:** pending
