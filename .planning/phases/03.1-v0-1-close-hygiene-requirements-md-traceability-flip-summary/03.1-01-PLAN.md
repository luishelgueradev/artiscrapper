---
phase: 03.1-v0-1-close-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/02-mvp/02-01-SUMMARY.md
  - .planning/phases/02-mvp/02-02-SUMMARY.md
  - .planning/phases/02-mvp/02-03-SUMMARY.md
  - .planning/phases/03-robustness/03-02-SUMMARY.md
  - .planning/phases/01-spike-empirical-validation/01-VALIDATION.md
  - .planning/phases/02-mvp/02-VALIDATION.md
  - .planning/phases/03-robustness/03-VALIDATION.md
autonomous: true
requirements: []
closes_audit_items:
  - "v0.1-MILESTONE-AUDIT tech_debt[0] — REQUIREMENTS.md traceability flip (52 rows Pending→Complete)"
  - "v0.1-MILESTONE-AUDIT tech_debt[1] — Nyquist wave-0 accept (Phase 1, 2, 3)"
  - "v0.1-MILESTONE-AUDIT tech_debt[5] — SUMMARY frontmatter requirements_completed (4 SUMMARYs)"
closes_context_decisions: [D-01, D-02, D-07, D-08]

must_haves:
  truths:
    - "REQUIREMENTS.md traceability table shows 53/53 v1 reqs as Complete (52 freshly flipped + 1 pre-existing OBS-07)"
    - "02-01-SUMMARY.md, 02-02-SUMMARY.md, 02-03-SUMMARY.md, 03-02-SUMMARY.md each carry a `requirements_completed:` frontmatter field"
    - "01-VALIDATION.md, 02-VALIDATION.md, 03-VALIDATION.md each carry `status: accepted` + a `## Acceptance Rationale (2026-06-04)` body section"
    - "53 unique REQ-IDs appear across all 5 SUMMARYs (02-01, 02-02, 02-03, 03-01, 03-02)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Authoritative 3-source traceability — all 53 v1 reqs in Complete state"
      contains: "| Complete |"
    - path: ".planning/phases/02-mvp/02-01-SUMMARY.md"
      provides: "requirements_completed for core-pipeline subsystem (SEARCH+CACHE+BROWSER)"
      contains: "requirements_completed:"
    - path: ".planning/phases/02-mvp/02-02-SUMMARY.md"
      provides: "requirements_completed for llm-visit-pipeline subsystem (LLM+VISIT+FRESH)"
      contains: "requirements_completed:"
    - path: ".planning/phases/02-mvp/02-03-SUMMARY.md"
      provides: "requirements_completed for observability-deploy-tests subsystem (DEPLOY+OBS+NF)"
      contains: "requirements_completed:"
    - path: ".planning/phases/03-robustness/03-02-SUMMARY.md"
      provides: "requirements_completed clarification (empty + comment explaining hardening-only plan)"
      contains: "requirements_completed:"
    - path: ".planning/phases/01-spike-empirical-validation/01-VALIDATION.md"
      provides: "Accept frontmatter + Phase 1 Acceptance Rationale (spike has no v1 reqs)"
      contains: "status: accepted"
    - path: ".planning/phases/02-mvp/02-VALIDATION.md"
      provides: "Accept frontmatter + Phase 2 Acceptance Rationale (53/53 SATISFIED + parser regression as wave-0)"
      contains: "status: accepted"
    - path: ".planning/phases/03-robustness/03-VALIDATION.md"
      provides: "Accept frontmatter + Phase 3 Acceptance Rationale (6/6 + 21/21 + 10/10 + integration wave-0 inventory)"
      contains: "status: accepted"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/02-mvp/02-VERIFICATION.md"
      via: "REQ-ID coverage table is the source-of-truth for the flip + the requirements_completed lists"
      pattern: "SATISFIED"
    - from: ".planning/phases/02-mvp/02-01-SUMMARY.md"
      to: ".planning/REQUIREMENTS.md"
      via: "requirements_completed bridges plan-level evidence to the global traceability table"
      pattern: "requirements_completed:"
---

<objective>
Close the v0.1 milestone's traceability + Nyquist bookkeeping in a single text-only wave. This plan touches 8 planning artifacts and zero source files — it is the safest wave to execute first and the fastest reviewable diff in the phase.

Purpose: Three Phase 3.1 audit items collapse into one cohesive change set because they all answer the same question ("where is the evidence?") from three different consumer angles — REQUIREMENTS.md is the global view, SUMMARY frontmatters are the per-plan view, VALIDATION.md is the Nyquist view. Doing them together prevents partial-state inconsistencies (e.g., a sibling SUMMARY pointing at a REQ-ID still marked Pending).

Output:
- 52 rows flipped Pending→Complete in REQUIREMENTS.md (D-01)
- 4 SUMMARY frontmatters populated with the exact REQ-ID lists from RESEARCH §2 (D-02)
- 3 VALIDATION.md files transitioned draft→accepted with per-phase Acceptance Rationale sections (D-07 / D-08)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v0.1-MILESTONE-AUDIT.md
@.planning/phases/02-mvp/02-VERIFICATION.md
@.planning/phases/02-mvp/02-01-SUMMARY.md
@.planning/phases/02-mvp/02-02-SUMMARY.md
@.planning/phases/02-mvp/02-03-SUMMARY.md
@.planning/phases/03-robustness/03-01-SUMMARY.md
@.planning/phases/03-robustness/03-02-SUMMARY.md
@.planning/phases/01-spike-empirical-validation/01-VALIDATION.md
@.planning/phases/02-mvp/02-VALIDATION.md
@.planning/phases/03-robustness/03-VALIDATION.md
@.planning/phases/03.1-v0-1-close-hygiene-requirements-md-traceability-flip-summary/03.1-CONTEXT.md
@.planning/phases/03.1-v0-1-close-hygiene-requirements-md-traceability-flip-summary/03.1-RESEARCH.md
@.planning/phases/03.1-v0-1-close-hygiene-requirements-md-traceability-flip-summary/03.1-VALIDATION.md

<interfaces>
<!-- Frontmatter shape for SUMMARY edits (existing key convention is mixed; D-02 uses underscore per RESEARCH §2 / R-07) -->

Existing 02-01-SUMMARY.md frontmatter style (double-quoted values + snake_case keys):
  phase: "02-mvp"
  plan: "01"
  subsystem: "core-pipeline"
  tags: ["fastapi", "cloakbrowser", ...]
  dependency_graph: {...}
  tech_stack: {...}
  key_files: {...}

03-01-SUMMARY.md uses an INCONSISTENT hyphen-case key `requirements-completed:` (line 62).
RESEARCH §2 R-07 + audit-tool key in v0.1-MILESTONE-AUDIT.md mandate the **underscore** form
`requirements_completed:` for all 4 new entries — DO NOT propagate the hyphen typo from 03-01.

VALIDATION.md frontmatter (shared by all 3 phases):
  phase: <int>
  slug: <string>
  status: draft        # ← THIS FLIPS to "accepted"
  nyquist_compliant: <bool>
  wave_0_complete: <bool>   # ← THIS FLIPS to true
  created: 2026-06-NN
  # plan adds: accepted: 2026-06-04 + accepted_by: phase-3.1-v0.1-close-hygiene
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip 52 REQUIREMENTS.md rows Pending→Complete (D-01)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 150-220 = traceability table + footer)
    - .planning/phases/03.1-v0-1-close-hygiene-requirements-md-traceability-flip-summary/03.1-RESEARCH.md §3 (REQUIREMENTS.md flip plan — lines 385-429)
    - .planning/phases/03.1-v0-1-close-hygiene-requirements-md-traceability-flip-summary/03.1-CONTEXT.md (D-01)
    - .planning/v0.1-MILESTONE-AUDIT.md tech_debt[0]
  </read_first>
  <action>
Flip every `| Pending |` cell to `| Complete |` in the traceability table at .planning/REQUIREMENTS.md lines 152-206 (per D-01). The OBS-07 row at line 202 is already `Complete` and MUST remain untouched — verified pre-state is 52 Pending + 1 Complete; post-state must be 0 Pending + 53 Complete. The flip is a single-column change (column 3, `Status`); rows are NOT reordered.

Recommended approach is an in-place sed edit per RESEARCH §3 (one atomic substitution, single commit per R-05):
  sed -i 's/| Pending |/| Complete |/g' .planning/REQUIREMENTS.md

After the flip, append a single provenance line at the end of the file (after line 220 "Last updated:" footer) per RESEARCH §3 / Open Question #3:
  *Status flip: 2026-06-04 by Phase 3.1 (v0.1 close hygiene). 53/53 reqs Complete per VERIFICATION.md evidence.*

DO NOT change any other column, header, footer count ("v1 requirements: 53 total"), or the "Out of Scope" section. The diff must touch exactly 52 lines (the status cells) + 1 appended line (provenance) = 53-line diff. Per R-05, all 52 flips ship in ONE commit so no downstream tool ever sees an intermediate state.
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/artiscrapper &amp;&amp; PEND=$(grep -c '| Pending |' .planning/REQUIREMENTS.md); COMP=$(grep -c '| Complete |' .planning/REQUIREMENTS.md); PROV=$(grep -c 'Status flip: 2026-06-04 by Phase 3.1' .planning/REQUIREMENTS.md); test "$PEND" -eq 0 &amp;&amp; test "$COMP" -eq 53 &amp;&amp; test "$PROV" -eq 1 &amp;&amp; echo "D-01 PASS: pending=0, complete=53, provenance=1" || (echo "D-01 FAIL: pending=$PEND (want 0), complete=$COMP (want 53), provenance=$PROV (want 1)"; exit 1)</automated>
  </verify>
  <done>0 rows with `Pending`, 53 rows with `Complete`, 1 provenance line appended, OBS-07 row text unchanged from pre-state (still `Phase 3 (Robustness)`), and the table footer count line `v1 requirements: 53 total` unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Populate requirements_completed in 4 SUMMARY frontmatters (D-02)</name>
  <files>
    .planning/phases/02-mvp/02-01-SUMMARY.md,
    .planning/phases/02-mvp/02-02-SUMMARY.md,
    .planning/phases/02-mvp/02-03-SUMMARY.md,
    .planning/phases/03-robustness/03-02-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/02-mvp/02-01-SUMMARY.md (full file — frontmatter ends ~line 76; insert new key in frontmatter)
    - .planning/phases/02-mvp/02-02-SUMMARY.md (full file — frontmatter ends ~line 52)
    - .planning/phases/02-mvp/02-03-SUMMARY.md (full file — frontmatter ends ~line 58)
    - .planning/phases/03-robustness/03-02-SUMMARY.md (full file — frontmatter ends ~line 60, currently has empty placeholder)
    - .planning/phases/02-mvp/02-VERIFICATION.md (authoritative REQ-ID→plan mapping table at lines 204-216)
    - .planning/phases/03.1-v0-1-close-hygiene-requirements-md-traceability-flip-summary/03.1-RESEARCH.md §2 (exhaustive REQ-ID lists per SUMMARY — lines 274-381)
    - .planning/phases/03.1-v0-1-close-hygiene-requirements-md-traceability-flip-summary/03.1-CONTEXT.md (D-02 + discretion item on YAML style)
  </read_first>
  <action>
Add a top-level `requirements_completed:` field (UNDERSCORE form per RESEARCH §2 R-07 — NOT the hyphen form used in 03-01-SUMMARY.md, which is itself an inconsistency) to the YAML frontmatter of each of the 4 SUMMARYs. Use flow style `[REQ-01, REQ-02, ...]` to match the existing `tags:` flow style in 02-01/02-02/02-03 and the OBS-07 entry in 03-01.

The exact lists are sourced VERBATIM from RESEARCH §2 (do not re-derive — these were cross-checked against 02-VERIFICATION.md Requirements Coverage table lines 204-216 + each SUMMARY's `key_files.created` field):

**.planning/phases/02-mvp/02-01-SUMMARY.md** (core-pipeline subsystem — 18 reqs from RESEARCH §2.1):
  requirements_completed: [SEARCH-01, SEARCH-02, SEARCH-03, SEARCH-04, SEARCH-05, SEARCH-06, SEARCH-07, SEARCH-08, CACHE-01, CACHE-02, CACHE-03, CACHE-04, CACHE-05, BROWSER-01, BROWSER-02, BROWSER-03, BROWSER-04, BROWSER-05]

**.planning/phases/02-mvp/02-02-SUMMARY.md** (llm-visit-pipeline subsystem — 20 reqs from RESEARCH §2.2):
  requirements_completed: [LLM-01, LLM-02, LLM-03, LLM-04, LLM-05, LLM-06, LLM-07, LLM-08, VISIT-01, VISIT-02, VISIT-03, VISIT-04, VISIT-05, VISIT-06, VISIT-07, VISIT-08, FRESH-01, FRESH-02, FRESH-03, FRESH-04]

**.planning/phases/02-mvp/02-03-SUMMARY.md** (observability-deploy-tests subsystem — 16 reqs from RESEARCH §2.3 — note OBS-07 belongs to Phase 3, NOT this plan):
  requirements_completed: [DEPLOY-01, DEPLOY-02, DEPLOY-03, DEPLOY-04, DEPLOY-05, DEPLOY-06, OBS-01, OBS-02, OBS-03, OBS-04, OBS-05, OBS-06, NF-01, NF-02, NF-03, NF-04]

**.planning/phases/03-robustness/03-02-SUMMARY.md** (per RESEARCH §2.5 — plan introduces ZERO new v1 reqs; keep the empty list but make the clarifying comment explicit):
  requirements_completed: []
  # Phase 3 plan 03-02 introduces NO new v1 requirements.
  # It hardens BROWSER-05 (sorry detection), LLM-06 (degraded mode),
  # D12 (catalog extraction), NF-02 (parser tests), NF-03 (test gates).
  # Their `requirements_completed` lives on the Phase 2 SUMMARY that
  # originally satisfied them.

Insertion point for each file: add the new key as a sibling to existing top-level frontmatter keys (e.g., next to `tags:` or `tech_stack:`). DO NOT nest under another key. DO NOT touch the existing `requirements-completed: [OBS-07]` line in 03-01-SUMMARY.md (that is out of scope for this plan per RESEARCH Open Question #2 — flagged as "Carried Debt" if the hyphen→underscore retroactive flip is later wanted).

Per the discretion item in 03.1-CONTEXT.md, the snake_case underscore form aligns with: (1) the audit-tool key in v0.1-MILESTONE-AUDIT.md, (2) Python identifier convention, (3) other snake_case frontmatter keys (`tech_stack`, `dependency_graph`, `key_files`). 03-01-SUMMARY.md is the outlier; do not propagate its hyphen typo.

After the edits, run the cross-sum sanity check from RESEARCH §2 to confirm 53 unique REQ-IDs span all 5 SUMMARYs (02-01 contributes 18, 02-02 contributes 20, 02-03 contributes 16, 03-01 contributes 1 OBS-07, 03-02 contributes 0 — set-union deduplicates the BROWSER-05 counted once).
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/artiscrapper &amp;&amp; for f in .planning/phases/02-mvp/02-01-SUMMARY.md .planning/phases/02-mvp/02-02-SUMMARY.md .planning/phases/02-mvp/02-03-SUMMARY.md .planning/phases/03-robustness/03-02-SUMMARY.md; do COUNT=$(grep -c "^requirements_completed:" "$f"); test "$COUNT" -eq 1 || (echo "D-02 FAIL: $f has $COUNT requirements_completed lines (want 1)"; exit 1); done &amp;&amp; UNIQ=$(grep -hoE '[A-Z]+-[0-9]+' .planning/phases/02-mvp/02-01-SUMMARY.md .planning/phases/02-mvp/02-02-SUMMARY.md .planning/phases/02-mvp/02-03-SUMMARY.md .planning/phases/03-robustness/03-01-SUMMARY.md .planning/phases/03-robustness/03-02-SUMMARY.md | grep -E '^(SEARCH|LLM|VISIT|FRESH|CACHE|BROWSER|DEPLOY|OBS|NF)-[0-9]+$' | sort -u | wc -l) &amp;&amp; test "$UNIQ" -eq 53 &amp;&amp; echo "D-02 PASS: 4 frontmatters populated, $UNIQ unique REQ-IDs across all 5 SUMMARYs" || (echo "D-02 FAIL: $UNIQ unique REQ-IDs across SUMMARYs (want 53)"; exit 1)</automated>
  </verify>
  <done>Each of the 4 target SUMMARYs has exactly one `^requirements_completed:` line in flow style; the union of REQ-IDs across all 5 SUMMARYs (incl. existing 03-01) equals 53; 03-01-SUMMARY.md's existing `requirements-completed: [OBS-07]` line is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Accept 3 VALIDATION.md files with per-phase Acceptance Rationale (D-07 / D-08)</name>
  <files>
    .planning/phases/01-spike-empirical-validation/01-VALIDATION.md,
    .planning/phases/02-mvp/02-VALIDATION.md,
    .planning/phases/03-robustness/03-VALIDATION.md
  </files>
  <read_first>
    - .planning/phases/01-spike-empirical-validation/01-VALIDATION.md (full file — note current status: draft, nyquist_compliant: false, wave_0_complete: false)
    - .planning/phases/02-mvp/02-VALIDATION.md (full file — note current status: draft, nyquist_compliant: true, wave_0_complete: false)
    - .planning/phases/03-robustness/03-VALIDATION.md (full file — note current status: draft, nyquist_compliant: false, wave_0_complete: false)
    - .planning/phases/03.1-v0-1-close-hygiene-requirements-md-traceability-flip-summary/03.1-RESEARCH.md §4 (VALIDATION.md accept procedure — lines 432-555 — verbatim Acceptance Rationale bodies per phase)
    - .planning/phases/03.1-v0-1-close-hygiene-requirements-md-traceability-flip-summary/03.1-CONTEXT.md (D-07 + D-08 — per-phase justifications)
    - .planning/phases/02-mvp/02-VERIFICATION.md (Phase 2 evidence)
    - .planning/phases/03-robustness/03-VERIFICATION.md (Phase 3 evidence)
  </read_first>
  <action>
For each of the 3 VALIDATION.md files, edit the frontmatter and append a body section. NO sidecar `ACCEPTED.md` file — the accept lives in the existing VALIDATION.md per RESEARCH §4 + the "priorizar correcciones permanentes" user constraint.

**Frontmatter changes (apply to ALL 3 files):**
- `status: draft` → `status: accepted`
- `wave_0_complete: false` → `wave_0_complete: true`
- Add `accepted: 2026-06-04`
- Add `accepted_by: phase-3.1-v0.1-close-hygiene`
- `nyquist_compliant`: flip to `true` on Phase 1 + Phase 3 (per RESEARCH §4 / Open Question 1 — recommended path; Phase 2 already true). Rationale: keeping false while wave_0_complete becomes true creates an inconsistent draft signal that audit-milestone tooling may classify as PARTIAL (R-06).

**Body sections — append to the END of each file's body** (after the current "Approval: pending" line or equivalent). Use the verbatim text from RESEARCH §4:

For .planning/phases/01-spike-empirical-validation/01-VALIDATION.md, append a `## Acceptance Rationale (2026-06-04)` section using the **Phase 1 acceptance body** from RESEARCH §4 lines 488-504 (justifies: spike has no production code; 0 v1 reqs assigned per REQUIREMENTS.md line 208; fixtures ARE the wave-0 artifacts for downstream phases).

For .planning/phases/02-mvp/02-VALIDATION.md, append the **Phase 2 acceptance body** from RESEARCH §4 lines 508-521 (justifies: 02-VERIFICATION.md passed with 53/53 SATISFIED; tests/test_parser.py::test_carousel_extracts_prices_from_fixtures acts as wave-0 anchor; 37 non-e2e tests pass; flag was simply never updated).

For .planning/phases/03-robustness/03-VALIDATION.md, append the **Phase 3 acceptance body** from RESEARCH §4 lines 525-546 (lists the 7 wave-0 test files that already exist + 03-VERIFICATION.md 6/6 + 21/21 + 10/10 + 65 passing tests; flags are stale draft markers).

DO NOT generate retroactive wave-0 tests — per D-08, the justification for accept is precisely that the wave-0 work is empirically DONE; the flags are bookkeeping stale.

Also update each file's final "Approval: pending" line (or sign-off section) to "Approval: accepted 2026-06-04 — phase-3.1-v0.1-close-hygiene" so downstream readers see the resolution without parsing the frontmatter.
  </action>
  <verify>
    <automated>cd /home/luis/proyectos/artiscrapper &amp;&amp; ACC=$(grep -c "^status: accepted" .planning/phases/01-spike-empirical-validation/01-VALIDATION.md .planning/phases/02-mvp/02-VALIDATION.md .planning/phases/03-robustness/03-VALIDATION.md | awk -F: '{s+=$2} END {print s}'); RAT=$(grep -c "## Acceptance Rationale" .planning/phases/01-spike-empirical-validation/01-VALIDATION.md .planning/phases/02-mvp/02-VALIDATION.md .planning/phases/03-robustness/03-VALIDATION.md | awk -F: '{s+=$2} END {print s}'); WAVE=$(grep -c "^wave_0_complete: true" .planning/phases/01-spike-empirical-validation/01-VALIDATION.md .planning/phases/02-mvp/02-VALIDATION.md .planning/phases/03-robustness/03-VALIDATION.md | awk -F: '{s+=$2} END {print s}'); NYQ=$(grep -c "^nyquist_compliant: true" .planning/phases/01-spike-empirical-validation/01-VALIDATION.md .planning/phases/02-mvp/02-VALIDATION.md .planning/phases/03-robustness/03-VALIDATION.md | awk -F: '{s+=$2} END {print s}'); ABY=$(grep -c "^accepted_by: phase-3.1" .planning/phases/01-spike-empirical-validation/01-VALIDATION.md .planning/phases/02-mvp/02-VALIDATION.md .planning/phases/03-robustness/03-VALIDATION.md | awk -F: '{s+=$2} END {print s}'); test "$ACC" -eq 3 &amp;&amp; test "$RAT" -eq 3 &amp;&amp; test "$WAVE" -eq 3 &amp;&amp; test "$NYQ" -eq 3 &amp;&amp; test "$ABY" -eq 3 &amp;&amp; echo "D-07/D-08 PASS: 3 files accepted with frontmatter + body" || (echo "D-07/D-08 FAIL: accepted=$ACC rationale=$RAT wave0=$WAVE nyquist=$NYQ accepted_by=$ABY (want 3 each)"; exit 1)</automated>
  </verify>
  <done>All 3 VALIDATION.md files show `status: accepted`, `nyquist_compliant: true`, `wave_0_complete: true`, `accepted: 2026-06-04`, `accepted_by: phase-3.1-v0.1-close-hygiene` in frontmatter; each body has a `## Acceptance Rationale (2026-06-04)` section with the per-phase justification from RESEARCH §4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan touches only planning artifacts under .planning/; no I/O channels, no runtime surface, no inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-01 | (none) | Planning artifacts | accept | No new threat surface — pure documentation hygiene phase per RESEARCH §Security Domain. Reuses existing API-key auth (V2), no new I/O channels, no new inputs. The text-only edits cannot introduce vulnerabilities. |
</threat_model>

<verification>
**Plan-level gate after all 3 tasks pass their automated checks:**

```bash
# 1. REQUIREMENTS.md state (D-01)
grep -c '| Complete |' .planning/REQUIREMENTS.md   # Expect: 53
grep -c '| Pending |' .planning/REQUIREMENTS.md    # Expect: 0

# 2. SUMMARY frontmatter coverage (D-02)
for f in .planning/phases/02-mvp/02-{01,02,03}-SUMMARY.md .planning/phases/03-robustness/03-02-SUMMARY.md; do
    grep -c "^requirements_completed:" "$f"          # Expect: 1 per file
done

# 3. Unique REQ-IDs across 5 SUMMARYs (RESEARCH §2 sanity check)
grep -hoE '[A-Z]+-[0-9]+' .planning/phases/02-mvp/02-{01,02,03}-SUMMARY.md \
    .planning/phases/03-robustness/03-{01,02}-SUMMARY.md \
    | grep -E '^(SEARCH|LLM|VISIT|FRESH|CACHE|BROWSER|DEPLOY|OBS|NF)-[0-9]+$' \
    | sort -u | wc -l                                # Expect: 53

# 4. VALIDATION.md accept state (D-07 / D-08)
grep -c "^status: accepted" .planning/phases/*/0*-VALIDATION.md      # Expect: 3 across the 3 files
grep -c "## Acceptance Rationale" .planning/phases/*/0*-VALIDATION.md # Expect: 3
```
</verification>

<success_criteria>
- REQUIREMENTS.md traceability table: 0 Pending + 53 Complete + 1 provenance line appended
- All 4 target SUMMARYs carry a `requirements_completed:` (underscore) frontmatter field with the verbatim REQ-ID lists from RESEARCH §2
- Set-union of REQ-IDs across all 5 SUMMARYs equals 53 (the v1 universe)
- 03-01-SUMMARY.md's existing `requirements-completed: [OBS-07]` hyphen-form line is unchanged (deferred to Carried Debt per Open Question #2)
- All 3 VALIDATION.md files: frontmatter accepted + nyquist_compliant true + wave_0_complete true + accepted/accepted_by stamped + body has `## Acceptance Rationale (2026-06-04)` section with per-phase justification
- Zero source files touched (no src/, no tests/, no pyproject.toml) — pure planning-artifact diff
</success_criteria>

<output>
Create `.planning/phases/03.1-v0-1-close-hygiene-requirements-md-traceability-flip-summary/03.1-01-SUMMARY.md` when done. The SUMMARY must:
- Record which audit items closed (tech_debt[0], tech_debt[1], tech_debt[5])
- Record which CONTEXT.md decisions closed (D-01, D-02, D-07, D-08)
- Include `requirements_completed: []` (this plan ships zero new v1 reqs — it closes traceability for previously-satisfied reqs)
- Note the YAML key style discrepancy in 03-01-SUMMARY.md (`requirements-completed` hyphen) is deliberately left as Carried Debt per RESEARCH Open Question #2
- Confirm the 53/53 sanity check passed
</output>
