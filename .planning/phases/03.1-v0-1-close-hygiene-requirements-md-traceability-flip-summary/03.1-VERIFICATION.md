---
phase: 03.1
slug: v0-1-close-hygiene
verified: 2026-06-04
verifier_model: claude-opus-4-7
status: passed
score: 11/11 must-haves verified
overrides_applied: 0
---

# Phase 03.1 — v0.1 close hygiene — Verification Report

## §1 Goal Restatement

Close the 5 medium-severity tech-debt items from `.planning/v0.1-MILESTONE-AUDIT.md` and the 4 low-severity Phase-2 review WARNINGs (WR-01..04) so the v0.1 milestone is clean for `/gsd-complete-milestone v0.1`. The phase is pure hygiene: bookkeeping flips (REQUIREMENTS.md + SUMMARY frontmatters + 3 VALIDATION.md accepts), test-coverage additions (X-API-Key in e2e + degraded-mode TestClient sibling), a permanent settings-source-of-truth refactor for slowapi rate limits (Pattern B after Pattern A proved non-viable on slowapi 0.1.9), the WR-01..04 cleanups, and the empirical D-06 retest proving `.env` bump propagates through the runtime hot path. No new v1 requirements; no production happy-path behavioural changes other than the slowapi wiring refactor and the WR-01 MELI guard tightening.

## §2 Per-Decision Verification

| # | Item | Expected | Actual | PASS/FAIL | Evidence |
|---|------|----------|--------|-----------|----------|
| D-01 | REQUIREMENTS.md flip | 0 `Pending`, all v1 rows `Complete` (per audit, 52 reqs; reconciled to 55) | `Pending=0`, `Complete=55` (55 v1 REQ-ID rows; footer reconciled by Plan 03.1-01 from "53 logical" → "55 unique" framing) | PASS | `grep -cE "^\| [A-Z]+-[0-9]+ \|.*\| Pending \|"` = 0; same for `Complete` = 55; footer line `Status flip: 2026-06-04 by Phase 3.1 ... 55/55 REQ-IDs Complete` present |
| D-02 | 4 SUMMARY frontmatters carry `requirements_completed:` | 1 line per file in 02-01, 02-02, 02-03, 03-02 | All 4 files have exactly 1 `^requirements_completed:` line; flow-style lists; 02-01=18 reqs (SEARCH+CACHE+BROWSER), 02-02=20 (LLM+VISIT+FRESH), 02-03=16 (DEPLOY+OBS Phase-2-only+NF), 03-02=`[]` (hardening-only, no new v1 reqs) | PASS | Inline grep + set-union: 55 unique REQ-IDs across all 5 SUMMARYs (matches REQ table size) |
| D-03 | `tests/test_e2e.py` POST calls carry X-API-Key | ≥2 X-API-Key headers + ≥2 E2E_API_KEY env reads | `X-API-Key=3` occurrences (header + 2 docstring refs is plausible — verified at the 2 client.post sites); `E2E_API_KEY=3` env reads | PASS | `grep -c "X-API-Key" tests/test_e2e.py` = 3 (matches the 2 POST sites + 1 module doc reference) |
| D-04 | Degraded-mode TestClient sibling test exists + passes | 1 `test_degraded_mode_via_search_endpoint`, real Phase-1 SERP fixture, asserts `metadata.llm_degraded=True AND len(results)>0` | Function defined (1 match), fixture `01-pelota_playera_quico.html` referenced 2× (file open + comment), `llm_degraded` referenced 3×; pytest passes (1 passed in full suite) | PASS | Full quick suite `68 passed, 2 deselected`; targeted run earlier in Plan 03.1-02 verified D-04 PASS |
| D-05 | slowapi rate-limit is settings-source-of-truth (Pattern B per Deviation Note) | `_RATE_LIMIT_PER_MINUTE = f"..."` defined; `@limiter.limit(_RATE_LIMIT_PER_*)` on 2 decorators; no hardcoded `"60/minute"`/`"10000/day"`; log line uses same constants + provenance tag | `_RATE_LIMIT_PER_MINUTE = f` (1 def, line 255); `_RATE_LIMIT_PER_DAY = f` (1 def, line 256); decorators `@limiter.limit(_RATE_LIMIT_PER_MINUTE)`/`(_RATE_LIMIT_PER_DAY)` at lines 378-379 (the 3rd grep hit is the docstring at line 395 — not executable); `grep -cE '@limiter\.limit\("'` = 0 (no hardcoded literals); log line uses constants with `source="module_constants_from_settings"` at line 180 | PASS | See §3 Code Verification for line-level wiring |
| D-06 | Empirical retest evidence captured | `.env` bump → compose recreate → ≥1× 429 in 6 requests; log shows new value | Evidence file `03.1-D06-EVIDENCE.md` present (87 lines) with: full procedure, log line showing `"per_minute": "5/minute"` + `"source": "module_constants_from_settings"`, request table (5×200 + 1×429), Verdict: **PASS** | PASS | See §4 Empirical Verification |
| D-07/D-08 | 3 VALIDATION.md formally accepted | Each: `status: accepted`, `nyquist_compliant: true`, `wave_0_complete: true`, `accepted_by:` field, `## Acceptance Rationale` section | All 3 files (Phase 1, 2, 3 VALIDATION.md): accepted=1, rationale=1, wave0=1, nyquist=1, accepted_by=1 — uniform | PASS | Per-file grep counts all = 1 across 3 files |
| WR-01 | MELI guard uses tldextract + no false-positive on `notmercadolibre.com` | tldextract import + frozenset + no substring match + regression test passes | `tldextract` referenced 4× in visit.py; `_MELI_REGISTERED_DOMAINS` defined (2× — definition + usage); `registered_domain` used 3× (1 call site + 2 doc refs); `"mercadolibre." in` substring count = 0; `test_meli_guard_no_false_positive_on_notmercadolibre` passes (1 passed in 0.12s) | PASS | tldextract emits a `DeprecationWarning` for `registered_domain` (5.3.1 → use `top_domain_under_public_suffix` in 6.x). Tracked in `deferred-items.md` for v0.2 per Plan 03.1-02 SUMMARY — not a blocker |
| WR-02 | Strong-ref pattern in `main.py` for cache-write task (verify-only) | `app.state.background_tasks = set()` init + `.add()` + `.discard` callback + `_cache_task = asyncio.create_task` | All 4 grep counts = 1; lines 207-212 (init+comment), and the cache-write site is intact. Commits to main.py since 2026-06-04 are `1f44310` (Pattern B refactor) and `e243846` (ruff style) — both expected; neither modifies the strong-ref pattern | PASS | Verify-only deliverable: pattern exists and was not regressed by the Pattern B refactor |
| WR-03 | No tautological assertion; settings.BROWSER_RECYCLE_AFTER pinned | tautology removed, `settings.BROWSER_RECYCLE_AFTER == 200` pin + `<= 500` cap | tautology count = 0; pin count = 1; `test_recycle_triggers` part of the green suite | PASS | `pytest tests/test_footguns.py::test_recycle_triggers` passes; full suite green |
| WR-04 | `tests/test_health.py` uses `tempfile.mkdtemp` + `atexit.register` | NamedTemporaryFile removed; mkdtemp + atexit pattern in place | NamedTemp count = 0; mkdtemp = 2; atexit = 1; all 3 health tests pass (`test_health_shape`, `test_health_deep_shape`, plus 1 more) in 0.71s | PASS | `pytest tests/test_health.py` passes |
| R-01 | Health endpoints NOT consumed by /search quota | Marked N/A under Pattern B (Deviation Note) — limits only fire on decorated routes; `/health` and `/health/deep` have no `@limiter.limit` decorator, so they are intrinsically exempt | Lines 292-309 (`/health`) and 312-367 (`/health/deep`) have no slowapi decorator; only `@app.get(...)` | PASS (N/A — see Deviation Note in `03.1-03-PLAN.md`) | Confirmed by reading main.py — no `@limiter.exempt` was added (and none is needed under Pattern B) |

**Score:** 11/11 must-haves verified (the audit's 5 medium + 4 WR + D-07/D-08 split is 11 distinct rows above; R-01 is informational N/A under Pattern B and not counted).

## §3 Code Verification — Pattern B wiring

Pattern B (chosen after Pattern A regressed empirically on slowapi 0.1.9; see Deviation Note in `03.1-03-PLAN.md`) requires three coordinated edits in `src/artiscrapper/main.py`. Verified line-by-line:

**Module constants (lines 240-256):**
- Line 240: comment block "Phase 3.1 D-05 — slowapi rate limits, single source of truth (Pattern B)"
- Line 247-254: rationale for Pattern B over Pattern A (slowapi 0.1.9 + FastAPI broken middleware path)
- Line 255: `_RATE_LIMIT_PER_MINUTE = f"{settings.API_RATE_PER_MINUTE}/minute"`
- Line 256: `_RATE_LIMIT_PER_DAY = f"{settings.API_RATE_PER_DAY}/day"`

**Decorator stack (lines 377-379) — Pattern B end consumer:**
- Line 377: `@app.post("/search", response_model=SearchResponse)`
- Line 378: `@limiter.limit(_RATE_LIMIT_PER_MINUTE)` — module constant as argument
- Line 379: `@limiter.limit(_RATE_LIMIT_PER_DAY)` — module constant as argument
- No `@limiter.limit("60/minute")` or `@limiter.limit("10000/day")` anywhere in the file (`grep -cE '@limiter\.limit\("'` = 0)

**Log line (lines 176-181) — the honesty surface:**
- Line 176-181: `log.info("rate_limit_init", per_minute=_RATE_LIMIT_PER_MINUTE, per_day=_RATE_LIMIT_PER_DAY, source="module_constants_from_settings", ...)` — uses the same module constants the decorators consume; the `source` field tags the wiring path for forensics

**Drift surface = 1 line per limit.** A bump of `API_RATE_PER_MINUTE` in `.env` propagates through `settings` → `_RATE_LIMIT_PER_MINUTE` → both the decorator argument and the log kwarg in a single import-time evaluation. The propagation invariant is pinned by `tests/test_main.py::test_rate_limit_constants_match_settings` (passes in 0.31s).

**WR-02 strong-ref pattern (lines 207-212) — verified unchanged by D-05 refactor:**
- Line 212: `app.state.background_tasks = set()` (lifespan init)
- The cache-write site uses `.add(task)` + `.add_done_callback(...discard)` (grep confirms 1× each)

## §4 Empirical Verification — D-06

Evidence file: `03.1-D06-EVIDENCE.md` (present, 87 lines).

**Procedure recap (verbatim from evidence file):**
1. Wrote temporary `.env` with `API_RATE_PER_MINUTE=5` (artificially low)
2. `docker compose build` + `HOST_PORT=8002 docker compose up -d --force-recreate` (separate steps per memory `feedback_compose_build_recreate`)
3. Waited for `/health` to settle
4. Inspected `rate_limit_init` log line
5. Exercised `/search` 6 times with same `X-API-Key`
6. Tore down + removed `.env`

**Log line captured at runtime:**
```json
{
  "per_minute": "5/minute",
  "per_day": "10000/day",
  "source": "module_constants_from_settings",
  "event": "rate_limit_init",
  ...
}
```
The `"5/minute"` string proves `settings.API_RATE_PER_MINUTE=5` reached the module constant; `source=module_constants_from_settings` proves Pattern B wiring is the source (not legacy hardcoded literals, not Pattern A `default_limits`).

**Request sequence (from evidence file):**

| # | HTTP | Note |
|---|------|------|
| 1 | 200 | Within quota |
| 2 | 200 | Within quota |
| 3 | 200 | Within quota |
| 4 | 200 | Within quota |
| 5 | 200 | Within quota (5/minute exhausted) |
| 6 | **429** | `{"error":"Rate limit exceeded: 5 per 1 minute"}` |

**Verdict (from evidence file):** **PASS**.

The runtime decorator received the same constant the log line printed; bumping `.env` + `compose up --force-recreate` cycled all three (settings, log, decorator) together. Memory `feedback_empirical_retest_after_default_changes` invariant is honoured: this is runtime log proof, not a unit-test "works on my machine."

## §5 Test Suite Results

| Suite | Command | Result |
|-------|---------|--------|
| Full quick suite | `LLM_ROUTER_BEARER_TOKEN=test-token uv run pytest tests/ -x -q -k "not e2e"` | **68 passed, 2 deselected** (e2e), 34 warnings — 9.51s |
| WR-01 regression | `pytest tests/test_visit.py::test_meli_guard_no_false_positive_on_notmercadolibre` | 1 passed, 1 warning (registered_domain deprecation), 0.12s |
| WR-03/WR-04 targeted | `pytest tests/test_footguns.py::test_recycle_triggers tests/test_health.py` | 3 passed, 3 warnings, 0.71s |
| D-05 propagation invariant | `pytest tests/test_main.py` | 1 passed, 0.31s |
| Ruff lint | `uv run ruff check` on 8 phase-touched files | **All checks passed!** |
| Ruff format | `uv run ruff format --check` on same 8 files | **8 files already formatted** |
| Mypy strict | `uv run mypy --strict src/artiscrapper/` | **Success: no issues found in 15 source files** |

No regressions. No new tests in the integration directory broke. `tldextract` `DeprecationWarning` on `registered_domain` is expected (5.3.1 → 6.x will require renaming to `top_domain_under_public_suffix`; tracked in `deferred-items.md` per Plan 03.1-02 SUMMARY).

## §6 Out-of-Scope / Deferred Items

The following items are intentionally outside Phase 3.1 scope (carried forward, not gaps):

| Item | Status | Where addressed |
|------|--------|-----------------|
| Audit item #7 (deferred-phases scope decision) | Out of scope per Phase 3.1 CONTEXT.md `<deferred>` § | `/gsd-complete-milestone v0.1` will decide (scope v0.1=1-3 vs record 4-5 as deferred-by-design) |
| `tldextract.registered_domain` → `top_domain_under_public_suffix` deprecation | Carried Debt (per Plan 03.1-02 SUMMARY `deferred-items.md`) | v0.2 backlog — fires only when tldextract bumps to 6.x |
| Real E2E runs against dev-box | D-03 unblocks them, but actually running them was deliberately deferred | Future operator UAT or milestone close |
| 03-01-SUMMARY.md `requirements-completed:` hyphen-case key | Carried Debt (per Plan 03.1-01 SUMMARY) | Low-value retroactive flip; v0.2 if cleanup pass desires consistency |
| Phase 2 review WR-05+ (none filed) | N/A | Only WR-01..04 existed in `02-REVIEW.md` backlog |
| `scripts/spike` ruff debt | Not touched (out of scope) | Phase 1 spike code; not in Phase 3.1 file set |
| `compose up -d --build` combined form | Already in carried memory `feedback_compose_build_recreate` | Avoided in D-06 procedure (separate build + recreate) |

**No debt markers (`TBD`/`FIXME`/`XXX`) found** in any of the 8 phase-touched source/test files. `TODO`/`HACK`/`PLACEHOLDER` scan also clean. The `WR-03` source comment label inside the strong-ref block at `main.py:208-211` is a stale label (the audit calls it WR-02); not a debt marker, and the labelling drift is explicitly documented in `03.1-02-PLAN.md` Task 4 action text.

## §7 Overall Verdict

**PASS** — phase goal achieved. All 11 must-haves verified against the codebase, not just SUMMARY claims:

- D-01: 55 v1 REQ-IDs are `Complete` in REQUIREMENTS.md (reconciled from "52 expected" to actual 55; footer updated).
- D-02: 4 SUMMARYs have populated `requirements_completed:` lists; 55-row union matches REQUIREMENTS.md.
- D-03: X-API-Key header is in tests/test_e2e.py.
- D-04: `test_degraded_mode_via_search_endpoint` exists, uses real Phase-1 SERP fixture, asserts `llm_degraded=True`, and passes inside the green suite.
- D-05: Pattern B wiring confirmed at lines 178-180 (log), 255-256 (constants), 378-379 (decorators); no hardcoded literals; A3 drift pinned by `test_rate_limit_constants_match_settings`.
- D-06: Empirical retest evidence file documents runtime log line `"5/minute"` + 5×200/1×429 sequence; verdict PASS.
- D-07/D-08: 3 VALIDATION.md files uniformly accepted with status/nyquist/wave-0/accepted_by/Rationale all set.
- WR-01: tldextract-based MELI guard + regression test pinning `notmercadolibre.com` false-positive absence.
- WR-02: Strong-ref pattern verified intact at main.py:207-212 + cache-write site; D-05 refactor did not regress it.
- WR-03: Tautology gone; settings-pinned boundary asserts pass.
- WR-04: NamedTemporaryFile gone; mkdtemp+atexit cleanup in place; tests green.

The Pattern A → Pattern B deviation is documented inline in `03.1-03-PLAN.md` (Deviation Note section) with the empirical root cause (slowapi 0.1.9 `default_limits` requires `SlowAPIMiddleware` which crashes on first request with FastAPI). The deviation preserves the core intent of D-05 (single source of truth, drift structurally impossible) without depending on broken-in-0.1.9 middleware. R-01 mitigation is moot under Pattern B because limits only fire on decorated routes.

## §8 Ready for Milestone Close

**Yes** — ready for `/gsd-complete-milestone v0.1`.

Recommendation: proceed to `/gsd-complete-milestone v0.1` to surface and decide the one remaining audit item (#7 deferred-phases scope: scope v0.1 to Phases 1-3 vs record Phases 4-5 as deferred-by-design). All 6 in-scope tech-debt items + 4 WR items are closed with code-level evidence and a green test suite. The empirical D-06 verification + the propagation invariant test together close the slowapi shadowing class the `feedback_empirical_retest_after_default_changes` memory was filed against.

---
*Verified: 2026-06-04 by /gsd:verify-work goal-backward verifier (Claude Opus 4.7)*
