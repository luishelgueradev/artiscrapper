---
phase: 03.1-v0-1-close-hygiene
plan: 02
subsystem: tests+visit-guard+wr-cleanup

# Dependency graph
requires:
  - phase: 03-robustness
    provides: "X-API-Key auth on /search (D-01/D-03 — requires header in e2e), tldextract 5.3.1 already pinned (metrics.py:26), Pattern S5 TestClient + monkeypatched fetch_serp (test_challenge_backoff.py:199 mirror), aiosqlite challenge_state row pattern (shared CACHE_DB_PATH)"
provides:
  - "tests/test_e2e.py POST calls carry X-API-Key (D-03 — unblocks live runs against dev box)"
  - "tests/integration/test_degraded_mode.py::test_degraded_mode_via_search_endpoint full-route TestClient coverage with real SERP fixture (D-04 — hardens LLM-06)"
  - "src/artiscrapper/visit.py MELI guard uses tldextract.registered_domain match (WR-01 — eliminates 'mercadoliberia.com' false-positive class)"
  - "Strong-ref pattern verified in main.py (WR-02 — already implemented Phase 3, this plan documents the verification)"
  - "tests/test_footguns.py::test_recycle_triggers replaces tautology with real BROWSER-03 boundary invariant (WR-03)"
  - "tests/test_health.py uses tempfile.mkdtemp + atexit cleanup (WR-04 — eliminates /tmp leakage on long test runs)"
affects: [milestone-v0.1-close]

# Tech tracking
tech-stack:
  added: []  # tldextract already pinned 5.3.1
  patterns: ["tldextract.registered_domain MELI suffix match (canonical TLD parsing)", "TestClient sibling test for degraded-mode using real Phase 1 fixture", "shared sqlite challenge_state reset helper between integration tests (Rule 1 fix)"]

key-files:
  created:
    - .planning/phases/03.1-.../wr02-verify-only.md
    - .planning/phases/03.1-.../deferred-items.md
  modified:
    - tests/test_e2e.py
    - tests/integration/test_degraded_mode.py
    - src/artiscrapper/visit.py
    - tests/test_visit.py
    - tests/test_footguns.py
    - tests/test_health.py

key-decisions:
  - "WR-02 is verify-only (Phase 3 already implemented strong-ref pattern at main.py:207, 668-670)"
  - "WR-01 reuses tldextract (already pinned 5.3.1) instead of introducing stdlib MELI suffix tuple"
  - "WR-04 uses mkdtemp + atexit (NOT pytest tmp_path) — CACHE_DB_PATH must bind at module-import-time before app import"
  - "D-04 uses 01-pelota_playera_quico.html (PRD §10 canonical query, ≥15 carousel candidates)"
  - "tldextract.registered_domain deprecation tracked in deferred-items.md for v0.2 (top_domain_under_public_suffix is the v6.x successor)"

patterns-established:
  - "test_challenge_backoff.py:106 _reset_challenge_state helper mirrored by test_degraded_mode for shared sqlite isolation"

requirements-completed: []

# Metrics
duration: ~25min
completed: 2026-06-04
---

# Plan 03.1-02 Summary

**Test coverage gaps closed (X-API-Key on e2e + full-route degraded-mode TestClient), MELI guard hardened with tldextract registered_domain (WR-01), and 3 Phase-2 review WARNINGs cleared (WR-03/04 fixes + WR-02 verify-only)**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-06-04T02:45Z
- **Completed:** 2026-06-04T03:10Z
- **Tasks:** 6 (+ 1 Rule 1 follow-up + STATE update)
- **Files modified:** 6 source/test + 2 new planning docs

## Accomplishments

- e2e test calls now carry `X-API-Key` (was 401-blocked since Phase 3 auth landed)
- New full-route degraded-mode test asserts `metadata.llm_degraded=True AND len(results)≥3` via TestClient + real fixture
- MELI guard upgraded to `tldextract.extract(url).registered_domain` membership check (no false-positives on `notmercadolibre.com`)
- WR-02 verified already-in-place (zero source diff per critical constraint #1)
- WR-03 tautology replaced with `settings.BROWSER_RECYCLE_AFTER == 200` + `<= 500` boundary
- WR-04 NamedTemporaryFile leak replaced with mkdtemp + atexit
- Full suite: 67 → 68 passed after the new D-04 test (later 68 after Plan 03's propagation test)

## Task Commits

1. **Task 1: D-03 X-API-Key in e2e POST** — `bd9d176` (test)
2. **Task 2: D-04 full-route degraded-mode TestClient** — `f9f25fc` (test)
3. **Task 3: WR-01 tldextract MELI guard + regression** — `d527e4d` (fix)
4. **Task 4: WR-02 verify-only (no diff)** — `626314c` (chore)
5. **Task 5: WR-03 real boundary invariant** — `aa9937e` (test)
6. **Task 6: WR-04 mkdtemp + atexit** — `e5390e5` (test)

**Rule 1 fix:** `58be58d` (test) — reset shared `challenge_state` sqlite row in degraded-mode test (test-suite-ordering bug surfaced under full-suite run, root-caused at sqlite row not in-memory cache)
**STATE.md update:** `5211552`

## Files Modified

- `tests/test_e2e.py` — +2 X-API-Key headers via `os.getenv("E2E_API_KEY", "test-key-1")`
- `tests/integration/test_degraded_mode.py` — +`test_degraded_mode_via_search_endpoint` + `_reset_challenge_state_dm` helper
- `src/artiscrapper/visit.py` — +tldextract import + `_MELI_REGISTERED_DOMAINS` frozenset + substring guard replaced
- `tests/test_visit.py` — +`test_meli_guard_no_false_positive_on_notmercadolibre` regression
- `tests/test_footguns.py` — tautology → real `settings.BROWSER_RECYCLE_AFTER == 200` + cap pin
- `tests/test_health.py` — NamedTemporaryFile → mkdtemp + atexit + setdefault
- `.planning/phases/03.1-.../wr02-verify-only.md` (new) — documents WR-02 already-done finding
- `.planning/phases/03.1-.../deferred-items.md` (new) — tracks tldextract `registered_domain` deprecation for v0.2

## Decisions Made

See `key-decisions` above. The notable one: WR-04 deliberately uses `mkdtemp` + `atexit` instead of pytest `tmp_path` because the test sets `CACHE_DB_PATH` BEFORE importing the app — pytest fixtures fire too late (function scope vs. module-import-time binding).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Test-suite isolation bug] Shared `challenge_state` sqlite row leaked between tests**
- **Found during:** Plan-level gate (full suite run after Task 2)
- **Issue:** `test_challenge_backoff` writes a `challenge_state` row to the shared CACHE_DB_PATH; subsequent `test_degraded_mode_via_search_endpoint` saw an armed gate and got 503 instead of 200
- **Fix:** Added `_reset_challenge_state_dm(db_path)` mirroring `test_challenge_backoff.py:106` — resets both `_STATE` in-memory and the sqlite row inside the TestClient block
- **Files modified:** `tests/integration/test_degraded_mode.py`
- **Verification:** Full suite green (67 passed at this point)
- **Committed in:** `58be58d` (separate Rule 1 fix commit)

**Total deviations:** 1 auto-fixed (test-side root-cause fix, not assertion relaxation)
**Impact on plan:** Surfaced a real shared-state bug in our own new test; fixed within file scope, no scope creep.

## Issues Encountered

- tldextract 5.3.1 emits `DeprecationWarning: registered_domain → top_domain_under_public_suffix`. Logged in `deferred-items.md` for v0.2 when tldextract bumps to 6.x. Out of scope here; plan prescribed `registered_domain` explicitly.

## Next Plan Readiness

Tests + WR cleanup landed cleanly. Plan 03.1-03 (slowapi refactor) safe to start with stable test infrastructure underfoot.

---
*Phase: 03.1-v0-1-close-hygiene*
*Completed: 2026-06-04*
