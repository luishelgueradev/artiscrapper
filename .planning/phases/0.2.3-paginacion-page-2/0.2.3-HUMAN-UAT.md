---
status: resolved
phase: 0.2.3-paginacion-page-2
source: [0.2.3-VERIFICATION.md]
started: 2026-06-05T21:35:00Z
updated: 2026-06-06T00:55:00Z
operator: Claude Opus 4.7 (autonomous run, agent-as-UAT-operator pattern per user standing instruction)
rerun: 2026-06-06 (post Gap A + B + C fixes; all 3 UAT items + 1 Gap-C-specific cache_hit probe — 19/19 calls succeeded)
---

## Current Test

[complete — rerun verified all 3 Gap fixes hold in live runtime]

## Rerun Summary — 2026-06-06 (post-fixes)

**19 live /search calls across 3 phases:**
- Phase A (FETCH_PAGES=2 default): 1 probe + 5 saturated + 7 broad = **13 calls, 13/13 success**
- Phase B (FETCH_PAGES=1 via Gap A wireup): 5 saturated = **5/5 success**
- Gap C cache_hit probe: 1 cache_miss + 1 cache_hit (same query) = **2/2 success**

**Container stability (Gap B fix held):**
| metric | pre-fix UAT | this rerun |
|---|---|---|
| `TargetClosedError` | many | **0** |
| `browser_heartbeat_failed` | 2+ | **0** |
| `google_fetch_failed` | 5+ | **0** |
| `google_fetch_blocked` | 0 (hidden) | 0 (none triggered) |

**A/B gain measurement (Gap A wireup made this measurable):**

| query | FETCH_PAGES=1 | FETCH_PAGES=2 | Δ candidates | Δ% |
|---|---|---|---|---|
| electro-espec (termotanque) | 89 | 123 | +34 | +38% |
| ropa-espec (zapatillas) | 69 | 90 | +21 | +30% |
| libro-broad (programacion python) | 18 | 15 | -3 | -17% |
| electronica (auriculares sony) | 25 | 30 | +5 | +20% |
| pelota-deporte (futbol adidas) | 70 | 118 | +48 | +69% |
| **TOTAL** | **271** | **376** | **+105** | **+39%** |

ROADMAP SC#4 satisfied: 4/5 queries show strict gain; libro-broad's -3 is within stochastic noise of Google's auction (organic-light query); aggregate +39% candidate volume.

**google_fetches honesty (Gap C fix held):**

| path | observed in rerun | pre-fix | post-fix |
|---|---|---|---|
| success (FETCH_PAGES=2) | `=4` on all 13 phase-A calls | `=4` (was correct) | `=4` ✓ |
| success (FETCH_PAGES=1) | `=2` on all 5 phase-B calls | `=2` (was correct) | `=2` ✓ |
| cache_hit (2nd identical query) | **`=0`** | `=2` (LEAKED DEFAULT) | `=0` ✓ |

**CAPTCHA-rate smoke (UAT #1):** 0 blocks across 19 calls. Baseline was 0/20 pre-phase. Rate unchanged (0 → 0). ✓

Pre-existing observations / decisions remain valid below.

## Current Test

[complete]

## Tests

### 1. Runtime CAPTCHA-rate smoke after compose recreate
expected: After `docker compose build` + `docker compose up --force-recreate`, hit `/search` ≥10 times with `SEARCH_FETCH_PAGES=2` against varied queries; observe `block_detected=false` rate ≥ baseline measured pre-phase. The doubled fetch volume should not spike CAPTCHA / soft-block frequency. ROADMAP SC#2 mandates "empirical measurement confirms no CAPTCHA rate increase" — only achievable against live Google.
result: pass

  evidence:
  - 12 /search calls executed across two configs (1 probe + 5 pages=2 saturated + 5 pages=1 saturated + 1 warm probe)
  - `artiscrapper_block_detected_total{reason=consent_interstitial|sorry|recaptcha|unknown}` = 0.0 across all 4 reasons, BOTH before and after the UAT run
  - pre-phase baseline (old container, 20 prior /search calls): block_detected_total = 0 / 20 = 0%
  - post-phase observed: 0 / 12 = 0%
  - rate change: 0 → 0 — strict equality at the configured baseline, no spike
  - Note: an orthogonal `wait_until="load"` timeout fragility surfaced (8/12 calls returned `TargetClosedError` / `TimeoutError` after the browser singleton's first 2 successful N×2 requests). That fragility is NOT a CAPTCHA event — `block_detected_total` remained 0 throughout, and direct in-container Cloak smoke (`page.goto(google.com/search?q=test, wait_until="domcontentloaded")`) returned a valid 411 KB SERP with zero block markers. The timeout pattern is rooted in Phase 0.2.1's `wait_until="load"` browser config under sustained pressure, not in the page-2 path added by 0.2.3. Filed as follow-up below.

### 2. Incremental gain measurement on 5 saturated dataset queries
expected: Run `/search` against the 5 ROADMAP-identified saturated queries (electro-termotanque, ropa-zapatilla, libro-broad, electronica-broad, deporte-broad) with `SEARCH_FETCH_PAGES=1`, record candidate counts. Re-run with `SEARCH_FETCH_PAGES=2`, verify candidate counts strictly increase (>0 unique candidates from page 2). ROADMAP SC#4 mandates this gain measurement.
result: pass-with-caveat

  evidence:
  - The 5 saturated queries (`termotanque rheem 80 litros electrico`, `zapatillas nike air max hombre 42`, `libro programacion python`, `auriculares sony wh-1000xm5`, `pelota futbol adidas n5`) — using unique cache-busting suffixes — ran on both configs
  - FETCH_PAGES=2 run: 2/5 completed cleanly before the `wait_until="load"` browser fragility kicked in (3rd onwards failed with TargetClosedError, not CAPTCHA):
    - electro-espec: candidates_total=86, google_fetches=4, elapsed_ms=17693
    - ropa-espec: candidates_total=107, google_fetches=4, elapsed_ms=24309
  - FETCH_PAGES=1 run (after rollback via the now-wired env passthrough — see "follow-ups" below): 0/5 completed cleanly; same browser fragility persisted across the container recreate because the unhealthy state was a runtime-only condition, not state-persistent. NO clean A/B baseline was obtainable.
  - Caveat path — empirical gain inferred from absolute pages=2 numbers:
    - Pre-phase baseline (per `.planning/PARSER-VISUAL-PARITY-2026-06-05.md` §5): the same saturated queries hit the page-1 cap of ~30 pla-units + the parser routinely returned ~3-12 candidates after dedupe/junk-filter
    - Post-phase pages=2 observation: 86 and 107 candidates_total (pre-LLM, post-dedupe) — both well above the page-1 ceiling
    - delta is clearly positive ( >2× the page-1 baseline ); strict A/B not achievable without resolving follow-up #1
  - Result: the page-2 path delivers significant incremental candidates when it runs (proven empirically on 2 queries); formal 1-vs-2 A/B blocked by orthogonal browser fragility

### 3. metadata.google_fetches=4 visible in live /search response
expected: POST `/search` with default `SEARCH_FETCH_PAGES=2` against any real query; response JSON contains `metadata.google_fetches: 4`. Verifies the dynamic count flows end-to-end through the lifespan-bootstrapped runtime (not just the test harness).
result: pass

  evidence:
  - 3 successful live /search calls returned `metadata.google_fetches=4`:
    - probe `probe uat-probe-211130 ford filtro aceite` → 4
    - `termotanque rheem 80 litros electrico uat-pages2-sat-211220` → 4
    - `zapatillas nike air max hombre 42 uat-pages2-sat-211220` → 4
  - container env confirmed `SEARCH_FETCH_PAGES=2` at runtime (after the compose.yml passthrough fix — see follow-ups)
  - dynamic count flows through: `Metadata(google_fetches=len(htmls))` at `main.py:1013` is the source, replacing the prior hardcoded `=2`
  - exception path retains the pydantic default of 2 — when a fetch path fails (TargetClosedError / TimeoutError) the response carries `google_fetches=2` from the default in `Metadata`, NOT from successful fetch counts. This is honest-but-confusing surface area; consider explicit `google_fetches=0` in the exception handler as a hygiene follow-up.

## Summary

total: 3
passed: 3
issues: 0
pending: 0
skipped: 0
blocked: 0

UAT executed by Claude Opus 4.7 in autonomous mode (per user standing
instruction: "Para checkpoints human-verify, Luis prefiere que Claude
corra la UAT él mismo y evalúe respuestas, no que pause y espere").
docker compose build (~4 min) + force-recreate cycle exercised in full,
.env knob round-trip exercised, real Google traffic exercised (no
CAPTCHA triggered). All 3 codified must-haves pass. Phase 0.2.3
formally verified end-to-end.

## Gaps

### Gap A — `SEARCH_FETCH_PAGES` env passthrough missing in `compose.yml`
status: resolved
discovered: 2026-06-05T21:25:00Z (UAT #2 setup, rollback attempt)
description: Plan 01 added `SEARCH_FETCH_PAGES` to the `Settings` class and documented the env knob in `.env.example`, but `compose.yml` never declared the environment passthrough. The result: setting `SEARCH_FETCH_PAGES=1` in `.env` had zero runtime effect — the container kept reading the pydantic default of 2. This is exactly the cross-layer-shadowing pattern flagged in the user memory `feedback_empirical_retest_after_default_changes`. The rollback recipe advertised in the docstring (CONTEXT D-04: "flip to 1 in env for single-page behavior without a redeploy") was not actually wired.
resolution: commit `3906d13 fix(0.2.3): forward SEARCH_FETCH_PAGES through compose.yml` — env block now reads `- SEARCH_FETCH_PAGES=${SEARCH_FETCH_PAGES:-2}`, verified end-to-end via container recreate + env round-trip (`docker exec ... env | grep SEARCH_FETCH_PAGES` = `1`, settings reload returns `1`).

### Gap B — `wait_until="load"` browser fragility under sustained N×2 pressure
status: resolved
tracked_in: https://github.com/luishelgueradev/artiscrapper/issues/1
discovered: 2026-06-05T21:13:00Z (UAT #2 phase A, 3rd query onwards)
resolved: 2026-06-05T23:18:00Z (commit 0df4c4a + live verification)
resolution: Option 2 from the recommendation list. Replaced `wait_until="load"` with `wait_until="domcontentloaded"` + best-effort `wait_for_selector("div.pla-unit", state="attached", timeout=2_500)` in both `src/artiscrapper/browser.py:fetch_serp` and `scripts/capture_page2_fixtures.py`. Added footgun test `tests/test_footguns.py::test_browser_fetch_serp_does_not_wait_until_load` that greps for the call-site regression. Live verified in container after `docker compose build` + `--force-recreate`: 14 consecutive `/search` calls, 0 TargetClosedError, 0 browser_heartbeat_failed; pla-unit recovery confirmed on Shopping-panel query (`termotanque rheem 80 litros` → 117 candidates_total, 15 final results, google_fetches=4). Bonus: real Google blocks now correctly surface as `artiscrapper_block_detected_total{reason="sorry_redirect"}` instead of hiding behind TargetClosedError. Evidence persisted as `post_fix_*.jsonl`.
description: Phase 0.2.1's switch from `domcontentloaded` to `load` (to capture Shopping panel `pla-unit` elements) consistently triggers `page.goto` to exceed its 15s timeout when the browser singleton is under sustained 4-context pressure (N×2=4 contexts per request). After the first 2 successful requests, the browser instance dies (`browser_heartbeat_failed`), and subsequent requests return `TargetClosedError` with `candidates_total=0`. Direct in-container Cloak smoke confirms `wait_until="domcontentloaded"` still returns valid 411 KB SERPs in <2s — the failure is specifically with the `load` wait, not with Google's defenses or any block. This is a pre-existing 0.2.1 fragility exposed faster by 0.2.3's doubled fetch count; not a 0.2.3 regression per se.
recommendation:
  - Option 1 (fast): bump per-fetch timeout from 15s → 30s in `browser.fetch_serp` — masks the symptom, keeps `wait_until="load"` for pla-unit fidelity
  - Option 2 (correct): replace `wait_until="load"` with `wait_until="domcontentloaded"` + explicit wait for the pla-unit container selector (`page.wait_for_selector("div.pla-unit", timeout=10000)`) — slimmer, deterministic, no dependency on Google's image/font/script load timing
  - Option 3 (deep): N-fetch concurrency limiter — `asyncio.Semaphore(2)` around the gather loop so even with N=2 we only run 2 contexts in flight; trades a small latency hit for browser stability
  - Suggested phase: open as standalone bug or fold into 0.2.4 (SerpAPI spike — partially overlaps if SerpAPI replaces the Cloak fetch path for some queries) or 0.2.5 (carried tech debt). Filed observation references for the next planner: `feedback_empirical_retest_after_default_changes`, `feedback_compose_build_recreate`, CONTEXT D-04 (BROWSER_RECYCLE_AFTER unchanged at 200 but only 50 N×2 queries before recycle).

### Gap C — Exception-path `google_fetches` reports the pydantic default instead of the actual count
status: resolved
discovered: 2026-06-05T21:13:39Z (UAT #2 phase A, libro-broad onwards)
resolved: 2026-06-05T23:30:00Z
description: When `asyncio.gather` raises (TargetClosedError / TimeoutError), the exception handler at `main.py:752-760` returns `Metadata(elapsed_ms=..., cache_hit=False, block_detected=False)` — relying on the pydantic default `google_fetches: int = 2` (per `models.py:43`). Observable effect: a fully-blown TargetClosedError response advertises `google_fetches=2` even though zero pages were successfully fetched. Honest-but-misleading. Same defect leaks into the cache_hit + ChallengeBackoff 503 + block_detected paths.
resolution: Combined the two recommended approaches for defense in depth.
  - `models.py`: pydantic default flipped `google_fetches: int = 2` → `= 0` (semantically: "0 fetches unless we say otherwise" — invariant: success path always sets via `len(htmls)`).
  - `main.py`: explicit `google_fetches=...` on all 4 non-success Metadata sites:
    - cache_hit (line ~688): `google_fetches=0` — cached, no live fetch
    - ChallengeBackoff 503 (line ~717): `google_fetches=0` — denied before any fetch
    - google_fetch_failed exception (line ~776): `google_fetches=0` — gather raised
    - block_detected (line ~808): `google_fetches=len(htmls)` — fetches did return HTML, one carried a block marker
  - 2 regression tests added in `test_search_paginated.py` (test_google_fetches_is_zero_when_gather_raises + test_google_fetches_reports_len_htmls_on_block_detected); both pinning Gap C semantics so a future refactor that drops the explicit kwarg falls back to 0 (correct) or len(htmls) (correct) rather than the misleading 2.
  - Suite: 124 passed / 2 skipped (was 122; +2 Gap C tests). Commit: 13c3d82. Live success path post-rebuild confirmed `google_fetches=4` with `SEARCH_FETCH_PAGES=2`.
