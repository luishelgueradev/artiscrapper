---
phase: 0.2.5
slug: carried-tech-debt-v0-1
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-06-06
---

# Phase 0.2.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. All 4 TECHDEBT items have existing test coverage; no Wave 0 scaffolding is needed.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 8.x (asyncio-mode=AUTO, anyio-4.x, respx, cov) |
| **Config file** | `pyproject.toml` `[tool.pytest.ini_options]` |
| **Quick run command** | `.venv/bin/pytest tests/ -q -x --no-header` |
| **Full suite command** | `.venv/bin/pytest tests/ -q` |
| **Estimated runtime** | ~15 seconds (124 tests baseline post-0.2.3) |

---

## Sampling Rate

- **After every task commit:** Run `.venv/bin/pytest tests/ -q -x` (fail-fast)
- **After every plan wave:** Run `.venv/bin/pytest tests/ -q` (full)
- **Before `/gsd:verify-work`:** Full suite green + `pytest -q -W error::DeprecationWarning -m "not slow"` green
- **Max feedback latency:** 20 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Secure Behavior | Test Type | Automated Command | Status |
|---------|------|------|-------------|-----------------|-----------|-------------------|--------|
| 01-1.1 | 01 (tldextract) | 1 | TECHDEBT-01 | MELI guard still blocks `notmercadolibre.com` post-rename (WR-01 invariant) | unit | `.venv/bin/pytest tests/test_visit.py -k meli -v` | ⬜ pending |
| 01-1.2 | 01 (tldextract) | 1 | TECHDEBT-01 | No stale `.registered_domain` references in `src/` | grep | `grep -rn '\.registered_domain' src/artiscrapper/ \| grep -v "#"` returns 0 lines | ⬜ pending |
| 02-1.1 | 02 (ORJSONResponse) | 1 | TECHDEBT-02 | `/search` end-to-end still returns 200 + valid `metadata` shape | integration | `.venv/bin/pytest tests/integration/test_search_paginated.py -q` | ⬜ pending |
| 02-1.2 | 02 (ORJSONResponse) | 1 | TECHDEBT-02 | `ORJSONResponse` removed from `main.py` (import + kwarg) | grep | `grep -n 'ORJSONResponse' src/artiscrapper/main.py` returns 0 lines | ⬜ pending |
| 03-1.1 | 03 (httpx2) | 1 | TECHDEBT-03 | checkpoint:human-verify — confirm httpx2 legitimacy (Pydantic-published, Starlette imports by name) | checkpoint | manual evidence in SUMMARY | ⬜ pending |
| 03-1.2 | 03 (httpx2) | 2 | TECHDEBT-03 | `httpx2` installed as dev dep | shell | `uv pip list \| grep httpx2` shows version | ⬜ pending |
| 03-1.3 | 03 (httpx2) | 2 | TECHDEBT-03 | StarletteDeprecationWarning silenced | grep | `.venv/bin/pytest tests/ -q 2>&1 \| grep -c "httpx2"` returns 0 | ⬜ pending |
| 03-1.4 | 03 (httpx2) | 2 | TECHDEBT-03 | `filterwarnings = ["error::DeprecationWarning"]` in pyproject.toml + full suite still green under it | command | `.venv/bin/pytest tests/ -q -W error::DeprecationWarning -m "not slow"` exits 0 | ⬜ pending |
| 04-1.1 | 04 (ruff scripts/spike) | 1 | TECHDEBT-04 | `ruff check scripts/spike/` exits 0 cleanly | shell | exit code 0 + 0 issues | ⬜ pending |
| post | all | n/a | regression | Full suite remains 124 passed / 2 skipped (baseline) | command | `.venv/bin/pytest tests/ -q` count >= 124 passed | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

*Existing infrastructure covers all phase requirements. No Wave 0 test scaffolding needed:*

- `tests/test_visit.py` — already exercises the MELI guard (WR-01, Phase 3.1)
- `tests/integration/test_search_paginated.py` — covers `/search` end-to-end (validates ORJSONResponse removal doesn't break response shape)
- `pyproject.toml` — pytest already configured; only adding `filterwarnings` knob
- No new test files. No new fixtures. No new dev dependencies beyond `httpx2`.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| httpx2 legitimacy verification | TECHDEBT-03 | slopcheck flagged `[SUS]` (25-day-old package, name close to `httpx`). Dispositive evidence is non-codifiable: (1) Pydantic team published it, (2) Starlette's testclient imports it by name. Operator must confirm via `pip show httpx2` (author/owner check), `pypi.org/project/httpx2/` (publisher check), and `grep "import httpx2" .venv/lib/.../starlette/testclient.py` (consumer chain). Per agent-as-UAT-operator memory: Claude runs these checks itself. | See checkpoint task 03-1.1 in plan 03 |

---

## Validation Sign-Off

- [x] All tasks have automated verify or explicit checkpoint
- [x] Sampling continuity: every task has a check
- [x] Wave 0 requirements: none — existing infra covers
- [x] No watch-mode flags
- [x] Feedback latency < 20s
- [x] `nyquist_compliant: true` — ratified 2026-06-06 after planner produced all 4 plans with verifiable `must_haves` blocks

**Approval:** approved 2026-06-06
