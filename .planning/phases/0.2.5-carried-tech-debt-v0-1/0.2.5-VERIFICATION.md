---
phase: 0.2.5-carried-tech-debt-v0-1
verified: 2026-06-06T14:00:00Z
status: passed
score: 4/4 must-haves verified
overrides_applied: 0
---

# Phase 0.2.5: Carried Tech Debt v0.1 — Verification Report

**Phase Goal:** Cerrar los 4 ítems de tech debt arrastrados desde v0.1 antes de que se acumule más en v0.3+ (TECHDEBT-01..04 closed: tldextract rename, ORJSONResponse cleanup, httpx2 migration, scripts/spike ruff debt).
**Verified:** 2026-06-06T14:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | tldextract `.registered_domain` rename: `top_domain_under_public_suffix` substituted at MELI guard callsite; WR-01 (MELI guard) tests green; zero `.registered_domain` references in production code | VERIFIED | `src/artiscrapper/visit.py:364` reads `ext.top_domain_under_public_suffix.lower()`. `grep -vE '^\s*#' src/artiscrapper/visit.py \| grep -c '\.registered_domain'` → 0. Defensive comment landed at `src/artiscrapper/metrics.py:143`. `pytest tests/test_visit.py -k meli -v` → 3 passed (incl. `test_meli_guard_no_false_positive_on_notmercadolibre` = WR-01). NOTE: tldextract 6.x does not yet exist on PyPI; SC#1 satisfied by rename per RESEARCH §Q4. |
| 2 | FastAPI ORJSONResponse decision documented (full removal) + code aligned | VERIFIED | `src/artiscrapper/main.py` has zero live-code `ORJSONResponse` / `default_response_class` references (`grep -vE '^\s*#' ... \| grep -cE 'ORJSONResponse\|default_response_class'` → 0). Decision documented in Plan 02 + SUMMARY 02 `key-decisions`: full removal → relies on Pydantic-Rust fast-path via `DefaultPlaceholder(JSONResponse)`. Runtime introspection confirms: `app.router.default_response_class` resolves to `DefaultPlaceholder / JSONResponse`. |
| 3 | `pytest -q -W error::DeprecationWarning -m "not slow"` exits 0 | VERIFIED | Explicit-flag form: `124 passed, 2 skipped in 13.62s` (exit 0). Implicit form via `pyproject.toml` `filterwarnings = ["error::DeprecationWarning"]` always-on gate: `124 passed, 2 skipped in 14.12s`. Zero `StarletteDeprecationWarning`, `FastAPIDeprecationWarning`, or `registered_domain` warning emissions. httpx2 installed at 2.3.0 (`python -c "import httpx2"` → 2.3.0). |
| 4 | `ruff check scripts/spike/` exits 0 without warnings | VERIFIED | `.venv/bin/ruff check scripts/spike/` → "All checks passed!" (exit 0). `--select F841 / E741 / F541 / F401 / I001` → 0 violations each. Zero `[tool.ruff.lint.per-file-ignores]` section, zero `exclude` directive, zero `# noqa: E741/F841` comments. |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/artiscrapper/visit.py` | MELI guard with `ext.top_domain_under_public_suffix` | VERIFIED | 3 occurrences (line 37 docstring, line 360 comment, line 364 callsite). Zero live-code `.registered_domain` references. |
| `src/artiscrapper/metrics.py` | Defensive comment near `_host_for_metric` | VERIFIED | Line 143 contains the locked `# NOTE: uses ext.domain + ext.suffix ... Do NOT introduce .registered_domain here ...` comment. |
| `tests/test_visit.py` | Docstring updated to reference new property name | VERIFIED | Line 205 docstring updated. WR-01 invariant test (`test_meli_guard_no_false_positive_on_notmercadolibre`) still passes byte-identically. |
| `src/artiscrapper/main.py` | FastAPI() without `default_response_class`, no `ORJSONResponse` import | VERIFIED | grep -vE '^\s*#' returns 0 live references for either symbol. Runtime introspection confirms `DefaultPlaceholder / JSONResponse`. |
| `pyproject.toml` | `httpx2==2.3.0` in `[dependency-groups].dev` + `filterwarnings = ["error::DeprecationWarning"]` in `[tool.pytest.ini_options]` | VERIFIED | Line 36: `"httpx2==2.3.0",`. Lines 44–45: `filterwarnings = [` / `"error::DeprecationWarning",`. Zero `ignore::DeprecationWarning:*` narrow ignores. |
| `uv.lock` | httpx2 entries present | VERIFIED | `grep -c 'name = "httpx2"' uv.lock` → 3 (canonical package row + dev requirement + transitive marker). |
| `scripts/spike/05_router_probe.py` | 2 `l → line` renames + F841 cleanups | VERIFIED | `ruff check scripts/spike/05_router_probe.py --select E741` → 0 violations. F841 sites at lines 163, 166, 381, 406, 433 resolved (LHS-drop + whole-line delete). |
| `scripts/spike/09_label_cards.py` | 3 `l → line` renames | VERIFIED | `ruff check scripts/spike/09_label_cards.py --select E741` → 0 violations. |
| `scripts/spike/02_cloak_smoke.py` | F841 LHS-drop on `results = await smoke(out_dir)` | VERIFIED | File ruff-clean per `ruff check scripts/spike/`. |
| `scripts/spike/03_pws_consent_probe.py` | F841 LHS-drop on `new_results = await top_up_fixtures(...)` | VERIFIED | File ruff-clean per `ruff check scripts/spike/`. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `visit.py:364` MELI guard | `_MELI_REGISTERED_DOMAINS` frozenset | `ext.top_domain_under_public_suffix.lower() in _MELI_REGISTERED_DOMAINS` | WIRED | Pattern verified at line 364; WR-01 invariant + 2 other MELI guard tests pass. |
| `src/artiscrapper/main.py` FastAPI() | Framework-default response class (Pydantic-Rust fast path) | No `default_response_class` kwarg → `DefaultPlaceholder(JSONResponse)` triggers Pydantic-Rust fast path | WIRED | Runtime introspection: `app.router.default_response_class` → `DefaultPlaceholder / JSONResponse`. |
| `tests/test_*.py` `TestClient` | starlette.testclient → httpx2 transport | starlette `try: import httpx2 as httpx; except: import httpx + warn` (testclient.py:40,43) | WIRED | Zero `StarletteDeprecationWarning` emissions in pytest output (was 1 pre-Plan-03). httpx2==2.3.0 installed and importable. |
| ruff lint coverage | `scripts/spike/` | `ruff check` (no exclude, no per-file-ignores) | WIRED | `ruff check scripts/spike/` → All checks passed! Zero ignore mechanisms added to pyproject.toml. |

### Data-Flow Trace (Level 4)

Not applicable — Phase 0.2.5 is a tech-debt closure phase (rename / removal / config / lint cleanup). No new data-rendering artifacts were introduced. The MELI guard rename is byte-equivalent on every URL in scope (RESEARCH §Code Examples TECHDEBT-01) — no behavioral data flow change.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Quick test suite under always-on deprecation gate | `.venv/bin/pytest tests/ -q -m "not slow"` | `124 passed, 2 skipped in 14.12s` | PASS |
| Quick test suite under explicit `-W error::DeprecationWarning` (ROADMAP SC#3) | `.venv/bin/pytest tests/ -q -W error::DeprecationWarning -m "not slow"` | `124 passed, 2 skipped in 13.62s` | PASS |
| MELI guard WR-01 invariant | `.venv/bin/pytest tests/test_visit.py -k meli -v` | 3 passed (incl. `test_meli_guard_no_false_positive_on_notmercadolibre`) | PASS |
| Ruff clean on scripts/spike/ (ROADMAP SC#4) | `.venv/bin/ruff check scripts/spike/` | All checks passed! | PASS |
| FastAPI app boots without ImportError + correct response class | `.venv/bin/python -c "from src.artiscrapper.main import app; print(app.title, app.version, type(app.router.default_response_class).__name__)"` | `artiscrapper 0.1.1 DefaultPlaceholder` | PASS |
| httpx2 installed and importable | `.venv/bin/python -c "import httpx2; print(httpx2.__version__)"` | `2.3.0` | PASS |
| Zero residual DeprecationWarning emissions | `.venv/bin/pytest tests/ -q -m "not slow" 2>&1 \| grep -ciE 'StarletteDeprecationWarning\|FastAPIDeprecationWarning\|registered_domain'` | `0` | PASS |

### Probe Execution

Not applicable — Phase 0.2.5 is a tech-debt closure phase. No `scripts/*/tests/probe-*.sh` declared in PLANs or SUMMARYs; success criteria are entirely test-suite / lint / runtime checks (all executed under Behavioral Spot-Checks above).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| TECHDEBT-01 | 0.2.5-01 | tldextract `.registered_domain` rename in `visit.py` (and any callsite); WR-01 MELI guard tests still pass | SATISFIED | `visit.py:364` uses `top_domain_under_public_suffix`; WR-01 test passes; production package zero `.registered_domain` references. |
| TECHDEBT-02 | 0.2.5-02 | FastAPI ORJSONResponse cleanup — decide default global vs explicit per endpoint, document | SATISFIED | Decision = full removal (RESEARCH Q3 + Plan 02 frontmatter). `main.py` cleansed; runtime confirms Pydantic-Rust fast path; documented in SUMMARY 02 `key-decisions`. |
| TECHDEBT-03 | 0.2.5-03 | httpx2 test migration — tests using old httpx API updated; deprecation warnings clean | SATISFIED | httpx2==2.3.0 installed; `filterwarnings = ["error::DeprecationWarning"]` always-on; explicit `-W error::DeprecationWarning` form exits 0 with 124 passed (ROADMAP SC#3). Zero `tests/` edits required (Starlette transparently swaps transport). |
| TECHDEBT-04 | 0.2.5-04 | scripts/spike/ ruff debt cleanup — decide archive or keep under lint | SATISFIED | `ruff check scripts/spike/` exits 0. Decision = keep under lint with zero per-file-ignores / exclude / noqa shortcuts (RESEARCH Q2). 39 errors → 0 via 28 auto-fix + 5 E741 source-rename + 6 F841 surgical edits. |

All 4 requirement IDs declared in plan frontmatter are mapped to REQUIREMENTS.md (line 72: `| TECHDEBT-01..04 | 0.2.5 | Pending |`) and verified SATISFIED in the codebase. Zero orphaned requirements.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `scripts/spike/06_capture_catalog.py` | 148, 153 | String literals containing the word `TBD` (`classification = "TBD"`, `f"... | TBD | TBD |..."`) | Info | These are intentional **data values** written to the generated `MANIFEST.md` markdown table as placeholder cells for rows pending classification — NOT source-code debt-marker annotations. Introduced in commit `5c3a3d5` (prior phase `01-03`, empirical-spike work, well before 0.2.5). Plan 04's only modification to this file was an auto-fix F541 (f-prefix removal). The spike README explicitly documents `scripts/spike/` as "Throwaway probe scripts for empirical validation. These are NOT production code." Phase 0.2.5 introduced ZERO new debt markers in production code (verified via `git diff e321a2d^..313c7e0 -- src/ tests/ pyproject.toml`). |

No blocker-class anti-patterns. The `TBD` strings are not auditable-debt markers — they are runtime-generated cell values in a generated markdown manifest table.

### Human Verification Required

None — all goal achievement claims are programmatically verifiable via test suite, runtime introspection, grep, and ruff. No visual / real-time / external-service items were promised by this phase.

### Gaps Summary

No gaps found. All 4 ROADMAP Success Criteria for Phase 0.2.5 verified against the codebase:

1. **TECHDEBT-01 (tldextract rename):** `visit.py:364` calls `top_domain_under_public_suffix`; defensive comment locked in `metrics.py:143`; WR-01 MELI guard invariant passes; zero `.registered_domain` references in production package. Per the verification context note: tldextract 6.x does NOT yet exist on PyPI (latest 5.3.1), so "bump to 6.x" becomes a future no-op; the rename closes the deprecation root cause now (verified by zero `DeprecationWarning.*registered_domain` emissions in pytest output).

2. **TECHDEBT-02 (ORJSONResponse cleanup):** Decision = full removal (documented in SUMMARY 02 + Plan 02). `main.py` cleansed (import deleted, kwarg deleted). Runtime introspection confirms FastAPI app router resolves `default_response_class` to `DefaultPlaceholder / JSONResponse` — the Pydantic-Rust fast-path placeholder per FastAPI PR #14964. Zero `FastAPIDeprecationWarning` emissions.

3. **TECHDEBT-03 (httpx2 migration + always-on gate):** `pyproject.toml` has both `httpx2==2.3.0` dev dep and `filterwarnings = ["error::DeprecationWarning"]` always-on gate. `uv.lock` regenerated (3 `httpx2` entries). `pytest -q -W error::DeprecationWarning -m "not slow"` exits 0 with 124 passed / 2 skipped — exactly matching the baseline. Zero narrow ignores added (escalation gate did not fire). Starlette's testclient transparently swaps to httpx2 (zero `tests/` edits — verified untouched between Plan 03 commit `41b4331` and HEAD `313c7e0`).

4. **TECHDEBT-04 (scripts/spike/ ruff clean):** `ruff check scripts/spike/` exits 0 with "All checks passed!". All 8 spike files modified; 28 errors auto-fixed (F541 + F401 + I001), 5 E741 resolved by source rename `l → line` (2 in `05_router_probe.py` + 3 in `09_label_cards.py` — distribution exactly as predicted), 6 F841 resolved by surgical LHS-drop/whole-line-delete. ZERO `[tool.ruff.lint.per-file-ignores]` TOML section, ZERO `exclude` directive on `scripts/spike/`, ZERO `# noqa: E741/F841` shortcuts — lint coverage preserved end-to-end as the plan's must_haves contract required.

**Test suite baseline preserved exactly:** 124 passed / 2 skipped under both implicit (always-on) and explicit (`-W error::DeprecationWarning`) gates. No regressions from the 0.2.3 baseline.

**Code review verdict** (commit `313c7e0`): 0 critical / 2 warnings / 4 info — advisory only per verifier context, NOT a verification blocker. The 2 warnings are pre-existing or informational, not introduced by this phase.

The pre-existing `TBD` string-literal data values in `06_capture_catalog.py` (a throwaway spike script per its README) are NOT debt markers under the gate semantics — they are intentional placeholder cells in a generated MANIFEST.md markdown table, introduced by a prior phase (commit `5c3a3d5`), and Phase 0.2.5 introduced ZERO new debt markers in production code (verified via `git diff` of the full phase-0.2.5 commit range against `src/`, `tests/`, `pyproject.toml`).

---

*Verified: 2026-06-06T14:00:00Z*
*Verifier: Claude (gsd-verifier)*
