---
phase: 0.2.5-carried-tech-debt-v0-1
plan: 03
type: execute
wave: 2
depends_on:
  - 0.2.5-01
  - 0.2.5-02
files_modified:
  - pyproject.toml
  - uv.lock
autonomous: false
requirements:
  - TECHDEBT-03

must_haves:
  truths:
    - "`httpx2==2.3.0` legitimacy explicitly verified (3 evidence channels) BEFORE install (slopcheck [SUS] gate cleared by operator/agent-as-UAT)"
    - "`httpx2==2.3.0` listed under `[dependency-groups].dev` in pyproject.toml after legitimacy gate clears"
    - "Starlette's testclient.py auto-detects httpx2 (no tests/ code edits)"
    - "`filterwarnings = [\"error::DeprecationWarning\"]` is registered under `[tool.pytest.ini_options]` in pyproject.toml (always-on gate, per RESEARCH Open Question 1 recommendation)"
    - "`pytest -q -W error::DeprecationWarning -m \"not slow\"` exits 0 — the v0.2.5 milestone success criterion #3"
  artifacts:
    - path: "pyproject.toml"
      provides: "httpx2 dev dep + always-on deprecation gate"
      contains: "httpx2==2.3.0"
    - path: "uv.lock"
      provides: "Deterministic lock for reproducible builds with httpx2 resolved"
      contains: "httpx2"
  key_links:
    - from: "tests/test_*.py via fastapi.testclient.TestClient"
      to: "starlette.testclient (auto-switches to httpx2 when installed)"
      via: "starlette `try: import httpx2 as httpx; except: import httpx + warn`"
      pattern: "import httpx2 as httpx"
---

<objective>
Close TECHDEBT-03: install `httpx2==2.3.0` as a dev dependency (after clearing the slopcheck [SUS] flag via a blocking human-verify checkpoint), and add `filterwarnings = ["error::DeprecationWarning"]` to `[tool.pytest.ini_options]` so the v0.2.5 milestone success criterion `pytest -q -W error::DeprecationWarning -m "not slow"` exits 0 by default on every test run (not only when the CLI flag is passed).

Purpose: Starlette's `testclient.py` (shipped inside the version FastAPI 0.136.3 pins) does `try: import httpx2 as httpx; except: import httpx + warn` at module import — meaning a dev-dep install of `httpx2` silently flips the transport, eliminating the `StarletteDeprecationWarning` with ZERO test-file edits. This is THE simplest closure in the phase: 2 lines added to `pyproject.toml`, `uv sync` regenerates `uv.lock`. The catch: slopcheck flags `httpx2` as `[SUS]` (25 days old, name close to `httpx`) — a typosquat-shaped signal that must be cleared BEFORE install by 3 channels of dispositive evidence (Pydantic org publishes, Starlette imports it by name, PyPI publisher metadata). Plan splits into Wave 1 (legitimacy verification — autonomous=false) and Wave 2 (install + always-on deprecation gate — autonomous=true after Wave 1 clears).

Output: pyproject.toml has `httpx2==2.3.0` in dev deps + `filterwarnings = ["error::DeprecationWarning"]` in pytest config; `uv.lock` regenerated and committed; full quick suite green under the always-on deprecation gate; SUMMARY.md records the 3-channel legitimacy evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/0.2.5-carried-tech-debt-v0-1/0.2.5-RESEARCH.md
@.planning/phases/0.2.5-carried-tech-debt-v0-1/0.2.5-VALIDATION.md

# Files this plan modifies
@pyproject.toml

# Dispositive evidence files (do NOT modify; read to verify legitimacy)
@.venv/lib/python3.13/site-packages/starlette/testclient.py

# Prior plans this depends on (their changes must be in main.py + visit.py BEFORE the gate flips on)
@.planning/phases/0.2.5-carried-tech-debt-v0-1/0.2.5-01-PLAN.md
@.planning/phases/0.2.5-carried-tech-debt-v0-1/0.2.5-02-PLAN.md

<interfaces>
<!-- Empirically verified 2026-06-06 — see RESEARCH §"Package Legitimacy Audit" -->
<!-- Starlette's testclient.py inside the running .venv: -->
<!--   Line 40: `    import httpx2 as httpx`        (preferred path) -->
<!--   Line 43: `        import httpx2 as httpx`    (retry inside try) -->
<!--   Line 46: `            import httpx`          (fallback that emits StarletteDeprecationWarning) -->

<!-- pyproject.toml current state (verified): -->
<!--   `[dependency-groups].dev` has 7 entries; no httpx2 -->
<!--   `[tool.pytest.ini_options]` has `asyncio_mode = "auto"` + markers + testpaths + pythonpath -->
<!--   NO `filterwarnings` key currently -->

<!-- httpx2 metadata (slopcheck output): -->
<!--   Version: 2.3.0 -->
<!--   Released: 2026-06-01 (25 days old at research time, 26 days at planning time) -->
<!--   Publisher: Pydantic org -->
<!--   Repo: github.com/pydantic/httpx2 -->
<!--   Verdict: [SUS] — must be cleared by 3-channel verification -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1 (Wave 1): Clear the slopcheck [SUS] verdict on httpx2 via 3-channel legitimacy evidence — agent-as-UAT operator</name>

  <what-built>
    No code changes yet. This task is the SECURITY GATE for the install step that follows. It collects three independent channels of dispositive evidence that `httpx2==2.3.0` is a legitimate package authored by the Pydantic team — NOT a typosquat of `httpx` — and records the evidence in the plan's eventual SUMMARY.md.

    Per the project memory `feedback_agent_as_uat_operator` (Luis prefers Claude run UAT checks itself rather than pause and wait), the executor agent runs all three verification commands directly, evaluates each one against the expected positive result documented in the research, and only flags for human attention if ANY of the three checks returns negative evidence. In autonomous mode this is effectively self-clearing when all three positives land; in conservative mode (or if any check is ambiguous) the agent surfaces evidence to the operator and pauses.

    This checkpoint is NEVER auto-advanceable regardless of `workflow.auto_advance` setting (per planner-source-audit rule: "Legitimacy checkpoints are never auto-approvable").
  </what-built>

  <how-to-verify>
    Run all three commands in sequence and record the literal output of each. The expected result is documented next to each command. If any command produces an unexpected output, STOP and escalate.

    (1) **GitHub source repository exists and is under the Pydantic org**
        Command: `curl -sf -o /dev/null -w "%{http_code}\n" https://github.com/pydantic/httpx2`
        Expected: `200`
        Evidence channel: Pydantic org is the legitimate stewardship handoff for the httpx ecosystem.

    (2) **Starlette inside our running `.venv` imports `httpx2` by name** (the dispositive proof — starlette/fastapi maintainers authored the test client against this exact package name)
        Command: `grep -n 'import httpx2' /home/luis/proyectos/artiscrapper/.venv/lib/python3.13/site-packages/starlette/testclient.py`
        Expected: at least 2 matches, on lines around 40 and 43, each reading `import httpx2 as httpx` (or `import httpx2 as httpx` inside a try block).
        Evidence channel: starlette's own import path is the canonical proof — if `httpx2` were a typosquat, starlette would not reference it.

    (3) **PyPI publisher metadata via pip index (no install required for this check)**
        Command: `.venv/bin/pip index versions httpx2 2>&1 | head -5`
        Expected: `httpx2 (2.3.0)` appears in the version listing.
        Evidence channel: confirms the public-registry record matches the version this plan pins.

    **Decision rule**:
      - ALL THREE positive → record evidence in SUMMARY.md, resume to Task 2 (Wave 2 install).
      - ANY ONE negative or ambiguous → STOP, do NOT proceed to install. Surface the failed command + actual output and ask for operator guidance. Fallback (per RESEARCH §"Claude's Discretion") = add a narrow `filterwarnings` ignore for `StarletteDeprecationWarning` instead of installing httpx2; this is a documented fallback but leaves the SC #3 gate failing on the starlette warning surface.

    Record in SUMMARY.md a 3-row evidence table with: command, expected, actual output (literal, truncated to 200 chars), pass/fail.
  </how-to-verify>

  <resume-signal>
    All three checks positive → record evidence + proceed to Wave 2 (Task 2). If autonomous mode AND all three positive, the agent self-clears and continues without operator input. If ANY negative, pause and report.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2 (Wave 2): Add httpx2 dev dep + filterwarnings always-on gate; regenerate uv.lock; confirm deprecation gate exits 0</name>
  <files>pyproject.toml, uv.lock</files>

  <read_first>
    - pyproject.toml — current state of `[dependency-groups].dev` (7 entries, no httpx2) and `[tool.pytest.ini_options]` (asyncio_mode + markers + testpaths + pythonpath; NO filterwarnings key)
    - .planning/phases/0.2.5-carried-tech-debt-v0-1/0.2.5-RESEARCH.md §"TECHDEBT-03" + §"Pattern 3: Starlette transparent transport swap" + §"Pitfall 4: -W error::DeprecationWarning surfaces warnings from third-party libs we don't own"
    - .venv/lib/python3.13/site-packages/starlette/testclient.py — the import block at lines ~39-58 (already verified in Task 1 channel 2)
    - The SUMMARY.md evidence table from Task 1 — three positive results MUST be recorded before this task runs
  </read_first>

  <action>
    Apply two edits to `pyproject.toml`, then regenerate the lockfile, then verify the always-on deprecation gate exits 0.

    (1) Inside `[dependency-groups].dev`, ADD the line `    "httpx2==2.3.0",` at the end of the existing dev-deps list (after `"pytest-cov",`). Preserve the exact pin `2.3.0` — do NOT widen the range; the package was 25 days old at research time and the pin is part of the legitimacy contract.

    (2) Inside `[tool.pytest.ini_options]`, ADD a new key after the existing `asyncio_mode = "auto"` line:

            filterwarnings = [
                "error::DeprecationWarning",
            ]

        Place this block BEFORE the existing `markers = [` list so the section reads: asyncio_mode → filterwarnings → markers → testpaths → pythonpath. This makes the v0.2.5 milestone success criterion `pytest -q -W error::DeprecationWarning -m "not slow"` exits 0 enforceable on EVERY pytest invocation, not only when the CLI flag is passed (per RESEARCH Open Question 1 recommendation: always-on prevents local-run regressions from sneaking past CI).

    (3) Regenerate the lockfile: `uv lock` (or `uv sync` which also installs into `.venv`). This must produce a determining `uv.lock` update with `httpx2==2.3.0` resolved. Commit BOTH `pyproject.toml` AND `uv.lock` together — never one without the other (deterministic-build constraint per PROJECT.md §Constraints).

    (4) Run the full quick suite under the (now-default) deprecation gate: `.venv/bin/pytest tests/ -q -m "not slow"`. Expected: exit 0, ≥124 passed.

    **Critical sequencing**: this task runs in Wave 2 because the `filterwarnings = ["error::DeprecationWarning"]` gate will fail the suite if Plans 01 (visit.py rename) and 02 (ORJSONResponse removal) have not landed first. The 5 currently-failing tests under the gate are rooted at `visit.py:364` (closed by Plan 01); the FastAPIDeprecationWarning at `fastapi/routing.py:120` is closed by Plan 02. Only the StarletteDeprecationWarning (closed by THIS task via httpx2 install) is left for Plan 03.

    **Fallback if a third-party transitive emits a NEW DeprecationWarning** (RESEARCH Pitfall 4): add a NARROW per-module ignore to the filterwarnings list, e.g. `"ignore::DeprecationWarning:offending.module"`. Never a blanket `"ignore::DeprecationWarning"` (defeats the gate). Record any narrow ignore in SUMMARY.md with the offending module + reason.

    **Do NOT edit any tests/** file. Per RESEARCH §"Pattern 3" and §"Anti-Patterns", starlette's testclient.py does the import switch internally; any `import httpx2` line inside `tests/` is noise.
  </action>

  <acceptance_criteria>
    - `grep -n 'httpx2==2.3.0' pyproject.toml` returns ≥1 match inside the `[dependency-groups].dev` block.
    - `grep -nA 3 'filterwarnings' pyproject.toml` shows the `["error::DeprecationWarning"]` entry inside `[tool.pytest.ini_options]`.
    - `grep -c 'httpx2' uv.lock` returns ≥1 (lockfile records the pinned version).
    - `.venv/bin/pip show httpx2 2>&1 | grep -i '^Version'` prints `Version: 2.3.0` (confirms install reflects pin).
    - `.venv/bin/pip show httpx2 2>&1 | grep -i '^Author\|^Home-page\|^Project-URL' | head -3` shows Pydantic/pydantic.dev metadata (publisher confirmation post-install).
    - `.venv/bin/pytest tests/ -q -m "not slow"` exits 0 — note this is THE SAME command as before, but it now implicitly enforces `-W error::DeprecationWarning` because of the `filterwarnings` config. ≥124 passed required.
    - `.venv/bin/pytest tests/ -q -W error::DeprecationWarning -m "not slow"` exits 0 (explicit-flag form — must match the implicit form's exit code, proving the always-on filter is wired).
    - `.venv/bin/pytest tests/ -q -m "not slow" 2>&1 | grep -ci 'StarletteDeprecationWarning'` returns 0 (the warning this task closes).
    - `.venv/bin/pytest tests/ -q -m "not slow" 2>&1 | grep -ci 'FastAPIDeprecationWarning'` returns 0 (Plan 02's warning, confirms Wave 1 fully closed).
    - `.venv/bin/pytest tests/ -q -m "not slow" 2>&1 | grep -ci 'registered_domain'` returns 0 (Plan 01's warning, confirms Wave 1 fully closed).
    - No file inside `tests/` was modified by this task (`git diff tests/` empty).
  </acceptance_criteria>

  <verify>
    <automated>cd /home/luis/proyectos/artiscrapper && grep -n 'httpx2==2.3.0' pyproject.toml && grep -nA 3 'filterwarnings' pyproject.toml && uv lock --frozen 2>&1 || uv sync && .venv/bin/pip show httpx2 | grep -E '^(Version|Author|Home-page)' && .venv/bin/pytest tests/ -q -m "not slow" && .venv/bin/pytest tests/ -q -W error::DeprecationWarning -m "not slow" && bash -c '! git diff --quiet tests/ || echo "tests/ clean (expected)"'</automated>
  </verify>

  <done>
    - `pyproject.toml` has `"httpx2==2.3.0"` under `[dependency-groups].dev`.
    - `pyproject.toml` has `filterwarnings = ["error::DeprecationWarning"]` under `[tool.pytest.ini_options]`.
    - `uv.lock` regenerated with httpx2 resolved; both files committed together.
    - Full quick suite green at ≥124 passed under BOTH the implicit (always-on) and explicit-flag forms of the deprecation gate.
    - Zero `StarletteDeprecationWarning`, `FastAPIDeprecationWarning`, or `registered_domain`-related DeprecationWarning surfaces in pytest output.
    - Zero edits in `tests/`.
    - SUMMARY.md records: (a) the 3-channel evidence table from Task 1, (b) the always-on filter rationale, (c) any narrow per-module ignores added with offending-module justification (expected: none).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI dependency manifest | New dev dep `httpx2` enters the lockfile; the registry → developer machine path is the slopcheck-flagged attack surface |
| pytest CI/local invariant gate | `filterwarnings = ["error::DeprecationWarning"]` makes the gate enforceable on every test run, defending against silent regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0.2.5-SC | Tampering / Spoofing | pyproject.toml dev-dep install of `httpx2==2.3.0` | mitigate | Slopcheck [SUS] flag cleared via Task 1's 3-channel verification (GitHub repo under Pydantic org + starlette imports by name from .venv + PyPI publisher metadata). Pin exact version `2.3.0`. NEVER auto-advance this checkpoint regardless of `workflow.auto_advance`. |
| T-0.2.5-05 | Repudiation | Future DeprecationWarning regressions sneaking past CI | mitigate | `filterwarnings = ["error::DeprecationWarning"]` always-on in `pyproject.toml` — every pytest invocation, local or CI, enforces the gate. New transitive-dep warnings surface immediately (in the test run that introduces them), not 6 months later. |
| T-0.2.5-06 | Tampering | Lockfile drift (pyproject pinned but uv.lock stale) | mitigate | Acceptance criterion requires BOTH `pyproject.toml` AND `uv.lock` to be edited+committed together. `uv lock --frozen` (or `uv sync`) regenerates the lock. CI build reproducibility depends on lock being in sync. |
</threat_model>

<verification>
- All Task 2 `<verify>` block commands pass.
- Task 1 evidence table recorded in SUMMARY.md (3 rows, all "pass").
- Negative greps confirm zero residual DeprecationWarning surfaces in pytest output for the 3 root causes Phase 0.2.5 owns (registered_domain, ORJSONResponse, StarletteDeprecationWarning).
</verification>

<success_criteria>
- ROADMAP success criterion 3 satisfied: `pytest -q -W error::DeprecationWarning -m "not slow"` exits 0 — BOTH explicitly via the CLI flag AND implicitly via the always-on `filterwarnings` config.
- httpx2 install legitimacy documented with 3 channels of dispositive evidence; the slopcheck [SUS] verdict is cleared on the historical record.
- Zero edits required in any `tests/` file (Starlette swaps the transport transparently).
- Full quick suite remains green at ≥124 passed under the always-on gate.
</success_criteria>

<output>
Create `.planning/phases/0.2.5-carried-tech-debt-v0-1/0.2.5-03-SUMMARY.md` when done. SUMMARY.md MUST include the 3-channel legitimacy evidence table from Task 1.
</output>
</content>
</invoke>