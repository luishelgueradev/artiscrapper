---
phase: 0.2.5-carried-tech-debt-v0-1
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/spike/02_cloak_smoke.py
  - scripts/spike/03_pws_consent_probe.py
  - scripts/spike/05_router_probe.py
  - scripts/spike/06_capture_catalog.py
  - scripts/spike/07_falabella_403.py
  - scripts/spike/08_extruct_classifier.py
  - scripts/spike/09_label_cards.py
  - scripts/spike/labels.py
autonomous: true
requirements:
  - TECHDEBT-04

must_haves:
  truths:
    - "All 39 ruff errors in `scripts/spike/` are resolved (22 F541 + 6 F841 + 5 E741 + 4 F401 + 2 I001)"
    - "Auto-fixable errors (28) are resolved via `ruff check --fix`"
    - "Residual errors (11: 5×E741 + 6×F841) are resolved via SOURCE RENAME / removal (per RESEARCH Open Question 2 recommendation), NOT per-file-ignores"
    - "`ruff check scripts/spike/` exits 0 (ROADMAP success criterion 4)"
    - "Full quick test suite remains green at ≥124 passed (spike scripts are not imported by tests, so no regression risk)"
  artifacts:
    - path: "scripts/spike/05_router_probe.py"
      provides: "2 of 5 E741 instances of `l` renamed to `line` (lines 292, 539 — both inside list comprehensions)"
    - path: "scripts/spike/09_label_cards.py"
      provides: "3 of 5 E741 instances of `l` renamed to `line` (lines 372, 373 in `sum(1 for l in ...)` generators; line 390 in `[l.strip() for l in ...]` list comprehension)"
    - path: "scripts/spike/02_cloak_smoke.py"
      provides: "Unused `results = await smoke(out_dir)` assignment removed (F841)"
    - path: "scripts/spike/03_pws_consent_probe.py"
      provides: "Unused `new_results = await top_up_fixtures(...)` assignment removed (F841)"
  key_links:
    - from: "ruff lint coverage"
      to: "scripts/spike/"
      via: "ruff check (no exclude, no per-file-ignores)"
      pattern: "scripts/spike/"
---

<objective>
Close TECHDEBT-04: bring `scripts/spike/` to ruff-clean state via the recommended sequence — (1) `ruff check --fix scripts/spike/` to auto-resolve 28 safe-fixable errors (22 F541 + 4 F401 + 2 I001), (2) source-rename `l → line` to resolve the 5 E741 ambiguous-variable-name errors (split across TWO files — 2 sites in `05_router_probe.py`, 3 sites in `09_label_cards.py`), (3) delete the 6 dead `results = ...` assignments to resolve the F841 unused-variable errors. Decision per RESEARCH Open Question 2: source rename over `per-file-ignores` (cheaper than the lifetime cost of losing regression lint coverage if any spike script is ever revived).

Purpose: ROADMAP success criterion 4 demands `ruff check scripts/spike/` exits 0. The 39 errors are dead-code residue from Phase 1 empirical-validation cycles. Empirical breakdown (verified 2026-06-06): 22 F541 (f-string without placeholders — auto-fixable), 6 F841 (assigned-but-never-used locals — manual delete), 5 E741 (ambiguous `l` for line — source rename, distributed 2+3 across two files), 4 F401 (unused imports — auto-fixable), 2 I001 (unsorted imports — auto-fixable). NONE of these scripts is imported by production code or tests (verified by grep), so cleanup carries zero regression risk.

Output: `scripts/spike/` ruff-clean; full quick suite unchanged; ZERO `tool.ruff.lint.per-file-ignores` entries added (the spike-code lint coverage is preserved for future Phase-1-style validation work).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/0.2.5-carried-tech-debt-v0-1/0.2.5-RESEARCH.md
@.planning/phases/0.2.5-carried-tech-debt-v0-1/0.2.5-VALIDATION.md

# Files this plan modifies — auto-fix touches up to all 8; manual edits target the 4 named below
@scripts/spike/02_cloak_smoke.py
@scripts/spike/03_pws_consent_probe.py
@scripts/spike/05_router_probe.py
@scripts/spike/09_label_cards.py

<interfaces>
<!-- Empirical ruff statistics (verified 2026-06-06 via `.venv/bin/ruff check scripts/spike/ --statistics`): -->
<!--   22 F541  [*] f-string-missing-placeholders        (auto-fixable) -->
<!--    6 F841  [-] unused-variable                       (manual; safe to delete) -->
<!--    5 E741  [ ] ambiguous-variable-name `l`           (manual; rename to `line`) -->
<!--    4 F401  [-] unused-import                         (auto-fixable) -->
<!--    2 I001  [*] unsorted-imports                      (auto-fixable) -->
<!--   Total: 39 errors, 28 fixable with --fix -->

<!-- E741 sites (verified live 2026-06-06 via `ruff check scripts/spike/ --select E741`): -->
<!--   ──── scripts/spike/05_router_probe.py (2 sites): ─────────────────────────── -->
<!--     Line 292:23  filtered = [l for l in summary_text.splitlines() if not l.startswith("json_mode_first_try_rate:")] -->
<!--     Line 539:76  missing = [k for k in required_keys if not any(l.startswith(k) for l in summary_text.splitlines())] -->
<!--   ──── scripts/spike/09_label_cards.py (3 sites): ─────────────────────────── -->
<!--     Line 372:27  positives = sum(1 for l in labelled_records if '"expected_is_product":true' in l) -->
<!--     Line 373:27  negatives = sum(1 for l in labelled_records if '"expected_is_product":false' in l) -->
<!--     Line 390:28  lines = [l.strip() for l in jsonl_path.read_text().splitlines() if l.strip()] -->
<!--   Total: 5 sites = 2 in 05_router_probe.py + 3 in 09_label_cards.py. Both files MUST be edited. -->

<!-- F841 sites (verified — 3 confirmed; 3 more in spike scripts): -->
<!--   scripts/spike/02_cloak_smoke.py:230   results = await smoke(out_dir)              (dead assignment) -->
<!--   scripts/spike/03_pws_consent_probe.py:229  new_results = await top_up_fixtures(...)  (dead) -->
<!--   scripts/spike/05_router_probe.py:166  parsed = None                                  (dead inside except block) -->
<!--   (3 more F841 to be found by `ruff check scripts/spike/ --select F841` in the executor) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean scripts/spike/ to ruff-zero via auto-fix + source edits for E741 and F841 residue</name>
  <files>scripts/spike/02_cloak_smoke.py, scripts/spike/03_pws_consent_probe.py, scripts/spike/05_router_probe.py, scripts/spike/06_capture_catalog.py, scripts/spike/07_falabella_403.py, scripts/spike/08_extruct_classifier.py, scripts/spike/09_label_cards.py, scripts/spike/labels.py</files>

  <read_first>
    - Output of `.venv/bin/ruff check scripts/spike/ --statistics` (to confirm the 39-error baseline)
    - Output of `.venv/bin/ruff check scripts/spike/ --select E741 --show-source` (enumerates the 5 E741 sites with surrounding code — expect 2 in `05_router_probe.py` + 3 in `09_label_cards.py`)
    - Output of `.venv/bin/ruff check scripts/spike/ --select F841 --show-source` (enumerates the 6 F841 sites with surrounding code)
    - scripts/spike/02_cloak_smoke.py around line 230 (the `results = await smoke(out_dir)` dead assignment)
    - scripts/spike/03_pws_consent_probe.py around line 229 (the `new_results = await top_up_fixtures(...)` dead assignment)
    - scripts/spike/05_router_probe.py around lines 166 (parsed=None dead assignment), 292, 539 (the two E741 sites in this file)
    - scripts/spike/09_label_cards.py around lines 372, 373, 390 (the three E741 sites in this file)
    - .planning/phases/0.2.5-carried-tech-debt-v0-1/0.2.5-RESEARCH.md §"TECHDEBT-04" + §"Pattern 4: Ruff scoped clean-up with `--fix` audit" + §"Pitfall 5: scripts/spike `ruff --fix` autofix is unsafe-fixes-disabled by default" + §"Open Questions (RESOLVED)" Q2
  </read_first>

  <action>
    Execute the cleanup in four ordered steps. Steps 1-2 are mechanical; steps 3-4 require small human-eye edits per F841/E741 occurrence.

    (1) **Preview auto-fix diff** (Pattern 4, RESEARCH §"Code Examples TECHDEBT-04"):
        `.venv/bin/ruff check --fix --diff scripts/spike/ > /tmp/spike_ruff_autofix.diff`
        Read the diff before applying. Verify all proposed changes are within: removing `f` prefix from f-strings without placeholders (F541), removing unused imports (F401), reordering imports alphabetically (I001). If the diff proposes anything outside these three categories, STOP and report.

    (2) **Apply auto-fix**:
        `.venv/bin/ruff check --fix scripts/spike/`
        Expected output: "Found 39 errors (28 fixed, 11 remaining)." If the count does not match (e.g., upstream ruff added new rules and there are 12 remaining), inspect what `ruff check scripts/spike/` reports next and triage.

    (3) **Resolve the 5 E741 residue via source rename `l → line` across TWO files** (per RESEARCH Open Question 2 recommendation — cheaper than per-file-ignores for a 1-letter delta). The 5 sites are distributed as **2 in `05_router_probe.py` + 3 in `09_label_cards.py`** (verified live via `.venv/bin/ruff check scripts/spike/ --select E741`):

        **In `scripts/spike/05_router_probe.py` (2 sites):**
          - Line ~292 list comprehension: rename `l` to `line` in BOTH occurrences inside the comprehension. The line becomes:
                `filtered = [line for line in summary_text.splitlines() if not line.startswith("json_mode_first_try_rate:")]`
          - Line ~539 generator expression: rename `l` to `line` in BOTH occurrences:
                `missing = [k for k in required_keys if not any(line.startswith(k) for line in summary_text.splitlines())]`

        **In `scripts/spike/09_label_cards.py` (3 sites):**
          - Line ~372 generator expression: rename `l` to `line` in BOTH occurrences:
                `positives = sum(1 for line in labelled_records if '"expected_is_product":true' in line)`
          - Line ~373 generator expression: rename `l` to `line` in BOTH occurrences:
                `negatives = sum(1 for line in labelled_records if '"expected_is_product":false' in line)`
          - Line ~390 list comprehension: rename `l` to `line` in BOTH occurrences (note: this site has `l` used in THREE positions — the LHS strip, the iterator var, and the filter — all become `line`):
                `lines = [line.strip() for line in jsonl_path.read_text().splitlines() if line.strip()]`

        After the edits above, run `.venv/bin/ruff check scripts/spike/ --select E741` and confirm zero matches. If any residual E741 site remains (e.g., upstream ruff version changed reporting), apply the same `l → line` rename at the additional site. Total expected: 5 E741 reports resolved across 2 files.

        Do NOT rename `l` outside list comprehensions or generator expressions where it is genuinely a local. Do NOT add `# noqa: E741`.

    (4) **Resolve the 6 F841 residue by deleting the dead assignment** (per RESEARCH Assumption A2 — manual inspection of the 3 confirmed sites shows they are refactor leftovers with no side-effect):
        - `scripts/spike/02_cloak_smoke.py:~230`: remove `results = ` from `results = await smoke(out_dir)`. The call is still made for its file-write side-effect; only the discarded binding goes away. Resulting line: `await smoke(out_dir)`.
        - `scripts/spike/03_pws_consent_probe.py:~229`: same pattern. Replace `new_results = await top_up_fixtures(out_dir, existing_count, target=10)` with `await top_up_fixtures(out_dir, existing_count, target=10)`. Preserve indentation inside the `if existing_count < 10:` block.
        - `scripts/spike/05_router_probe.py:~166`: inside the `except json.JSONDecodeError:` block, simply DELETE the line `parsed = None`. There is no consumer of `parsed` downstream (verified by reading the function body).
        - For the remaining 3 F841 sites (identify via `ruff check --select F841 scripts/spike/`), apply the same surgical rule: if the assignment has a meaningful side-effect (await call, file write) → drop only the LHS binding; if it is a pure dead assignment (`= None`, `= []`) → delete the entire line. Per Assumption A2 caveat: review each F841 site briefly; if uncertain whether a side-effect matters, prefer dropping the LHS (`results = expr` → `expr`) over deleting the line entirely.

        Do NOT mark with `# noqa: F841`. Do NOT prefix the variable with `_` as a convention shortcut (per RESEARCH Open Question 2 — source rename is the chosen route, and underscore prefix would lose semantic intent in spike-replay scenarios).

    (5) **Verify zero residue**: `.venv/bin/ruff check scripts/spike/`. Expected: `All checks passed!` and exit 0.

    Do NOT add `[tool.ruff.lint.per-file-ignores]` to `pyproject.toml` (per RESEARCH Open Question 2 + Anti-Patterns + the must_haves contract above).
    Do NOT add `exclude = ["scripts/spike"]` to `[tool.ruff]` (per Anti-Patterns — loses regression coverage if a spike script is revived).
  </action>

  <acceptance_criteria>
    - `.venv/bin/ruff check scripts/spike/` exits 0 with output `All checks passed!`.
    - `.venv/bin/ruff check scripts/spike/ --select E741` reports 0 violations.
    - `.venv/bin/ruff check scripts/spike/05_router_probe.py --select E741` reports 0 violations (file 1 of 2 with E741 residue).
    - `.venv/bin/ruff check scripts/spike/09_label_cards.py --select E741` reports 0 violations (file 2 of 2 with E741 residue).
    - `.venv/bin/ruff check scripts/spike/ --select F841` reports 0 violations.
    - `.venv/bin/ruff check scripts/spike/ --select F541` reports 0 violations.
    - `.venv/bin/ruff check scripts/spike/ --select F401` reports 0 violations.
    - `.venv/bin/ruff check scripts/spike/ --select I001` reports 0 violations.
    - `grep -nE '^\[tool\.ruff(\.lint)?\.per-file-ignores\]' pyproject.toml` returns 0 matches (per the must_haves: no per-file-ignores TOML section added; comments containing the literal string are allowed).
    - `grep -nA 5 '\[tool\.ruff\]' pyproject.toml | grep -E '^[[:space:]]*exclude' | wc -l` returns 0 (no `exclude` directive added to ruff config; tolerates commented mentions).
    - `grep -rn 'noqa: E741\|noqa: F841' scripts/spike/` returns 0 matches (no noqa shortcuts).
    - `.venv/bin/pytest tests/ -q -m "not slow"` exits 0 with ≥124 passed (regression invariant — spike scripts are not imported by tests, so this is a sanity check, not a sensitivity test).
    - `bash -c "cd /home/luis/proyectos/artiscrapper && for f in scripts/spike/*.py; do python -c \"import ast; ast.parse(open('$f').read())\"; done"` exits 0 (Python syntax sanity for every spike script post-edit — catches any over-aggressive deletion that produced invalid syntax).
  </acceptance_criteria>

  <verify>
    <automated>cd /home/luis/proyectos/artiscrapper && .venv/bin/ruff check scripts/spike/ && .venv/bin/ruff check scripts/spike/ --select E741 && .venv/bin/ruff check scripts/spike/05_router_probe.py --select E741 && .venv/bin/ruff check scripts/spike/09_label_cards.py --select E741 && .venv/bin/ruff check scripts/spike/ --select F841 && bash -c '! grep -nE "^\[tool\.ruff(\.lint)?\.per-file-ignores\]" pyproject.toml' && bash -c '! grep -rn "noqa: E741\|noqa: F841" scripts/spike/' && bash -c 'for f in scripts/spike/*.py; do .venv/bin/python -c "import ast; ast.parse(open(\"$f\").read())" || exit 1; done' && .venv/bin/pytest tests/ -q -m "not slow"</automated>
  </verify>

  <done>
    - `ruff check scripts/spike/` exits 0.
    - All 5 E741 errors resolved by source rename (`l → line`) — **2 sites in `05_router_probe.py` + 3 sites in `09_label_cards.py`** (both files edited; total 5).
    - All 6 F841 errors resolved by deleting dead assignments (LHS-drop where the RHS has a side-effect; whole-line delete where it is a pure dead store).
    - All 22 F541 + 4 F401 + 2 I001 errors resolved via `ruff check --fix`.
    - ZERO `[tool.ruff.lint.per-file-ignores]` TOML section added; ZERO `exclude` directive on `scripts/spike/`; ZERO `# noqa` comments added.
    - Full quick test suite remains green at ≥124 passed (regression sanity check).
    - All 8 spike scripts still parse as valid Python.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lint coverage / regression detection | `scripts/spike/` is throwaway probe code per the spike README, but the project chose to keep it under lint to catch regressions if scripts are revived; this plan preserves that boundary by refusing to `exclude` or `per-file-ignore` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0.2.5-07 | Repudiation | Future spike-script revival drift | mitigate | Cleanup is in-source (rename + delete), NOT via per-file-ignores or exclude. If a future Phase-1-style validation cycle revives a spike script and introduces new lint debt, it surfaces immediately on the next `ruff check` rather than being silently swallowed by an ignore directive. |
| T-0.2.5-08 | Tampering | Over-aggressive F841 deletion accidentally removes a side-effect | accept | Mitigation: action step 4 requires brief review of each F841 site; the conservative LHS-drop pattern (`results = expr` → `expr`) preserves side-effects; only pure dead stores (`= None`) get full-line delete. Acceptance criterion runs `ast.parse` on every spike script post-edit to catch syntactic regressions. |
</threat_model>

<verification>
- All Task 1 `<verify>` block commands pass.
- ROADMAP success criterion 4 satisfied: `ruff check scripts/spike/` exits 0 cleanly.
- No `[tool.ruff.lint.per-file-ignores]` TOML section or `exclude` directive added.
- No `# noqa` shortcuts added.
- Full quick suite regression: ≥124 passed.
</verification>

<success_criteria>
- `ruff check scripts/spike/` exits 0.
- Zero `# noqa` comments added; zero per-file-ignores TOML sections added; zero exclude directives added — the lint coverage on spike scripts is preserved end-to-end (per RESEARCH §Anti-Patterns "Excluding `scripts/spike/` from ruff entirely. Loses regression coverage if any spike script is revived").
- All 8 spike files still importable / parseable (no over-aggressive deletion).
- Full quick test suite remains green at ≥124 passed.
</success_criteria>

<output>
Create `.planning/phases/0.2.5-carried-tech-debt-v0-1/0.2.5-04-SUMMARY.md` when done. SUMMARY MUST list each E741 callsite (with file + line + chosen rename target) and each F841 callsite (with the chosen resolution: rename / LHS-drop / whole-line delete) so the historical record is auditable. The E741 table MUST show the 2+3 distribution across `05_router_probe.py` and `09_label_cards.py`.
</output>
</content>
