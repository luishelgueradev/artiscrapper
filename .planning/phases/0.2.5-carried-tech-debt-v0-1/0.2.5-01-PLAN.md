---
phase: 0.2.5-carried-tech-debt-v0-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/artiscrapper/visit.py
  - src/artiscrapper/metrics.py
  - tests/test_visit.py
autonomous: true
requirements:
  - TECHDEBT-01

must_haves:
  truths:
    - "MELI guard at visit.py uses the public-suffix-aware sister API (not the deprecated alias)"
    - "No `.registered_domain` calls remain in src/artiscrapper/"
    - "Phase 3.1 WR-01 invariant holds: `notmercadolibre.com` is NOT flagged; live MELI subdomains ARE flagged"
    - "All 5 currently-failing pytest tests under `-W error::DeprecationWarning` (rooted at visit.py:364) pass"
  artifacts:
    - path: "src/artiscrapper/visit.py"
      provides: "MELI guard with `ext.top_domain_under_public_suffix` (line ~364)"
      contains: "top_domain_under_public_suffix"
    - path: "src/artiscrapper/metrics.py"
      provides: "Defensive comment near `_host_for_metric` documenting why this file does NOT use `.registered_domain`"
      contains: "top_domain_under_public_suffix"
    - path: "tests/test_visit.py"
      provides: "Docstring updated to reference the new public-suffix-aware property name"
  key_links:
    - from: "src/artiscrapper/visit.py:364"
      to: "_MELI_REGISTERED_DOMAINS frozenset comparison"
      via: "ext.top_domain_under_public_suffix.lower()"
      pattern: "top_domain_under_public_suffix\\.lower\\(\\) in _MELI_REGISTERED_DOMAINS"
---

<objective>
Close TECHDEBT-01: rename the deprecated `tldextract` attribute `.registered_domain` to its sister property `.top_domain_under_public_suffix` at the single in-codebase callsite (`src/artiscrapper/visit.py:364`), and update tracker text (comments, docstrings, test docstring) to match.

Purpose: tldextract 5.3.0+ emits `DeprecationWarning` on `.registered_domain`; the warning is currently the root cause of 5 failing tests under `pytest -W error::DeprecationWarning` (verified empirically 2026-06-06). The rename is provably behavior-equivalent on every URL the MELI guard cares about (verified interactively against MELI subdomains AND the regression invariant `notmercadolibre.com`). Closing this debt is a prerequisite for TECHDEBT-03's `filterwarnings = ["error::DeprecationWarning"]` always-on gate to ship green.

Output: `src/artiscrapper/visit.py` calls the public-suffix-aware sister property; `src/artiscrapper/metrics.py` documents why it intentionally uses `ext.domain + ext.suffix` (not the deprecated alias) so a future refactor does not reintroduce the bug; `tests/test_visit.py` docstring matches; the Phase 3.1 WR-01 regression invariant `test_meli_guard_no_false_positive_on_notmercadolibre` continues to pass; no `.registered_domain` calls survive anywhere under `src/artiscrapper/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/0.2.5-carried-tech-debt-v0-1/0.2.5-RESEARCH.md
@.planning/phases/0.2.5-carried-tech-debt-v0-1/0.2.5-VALIDATION.md

# Source files this plan modifies (executor MUST read each before editing)
@src/artiscrapper/visit.py
@src/artiscrapper/metrics.py
@tests/test_visit.py

<interfaces>
<!-- Key behavior contract: tldextract.extract(url).<sister-property> -->
<!-- Empirically verified 2026-06-06 — see RESEARCH §Code Examples TECHDEBT-01 -->

`tldextract.extract(url).registered_domain` and `tldextract.extract(url).top_domain_under_public_suffix` return BYTE-IDENTICAL strings on every URL the MELI guard handles, including:
- `https://articulo.mercadolibre.com.ar/MLA-1`  → `"mercadolibre.com.ar"` for both
- `https://notmercadolibre.com/foo`              → `"notmercadolibre.com"` for both

The deprecated alias emits `DeprecationWarning("'registered_domain' is an alias for the 'top_domain_under_public_suffix' property; use the latter")` from `tldextract.py` on every call.

`_MELI_REGISTERED_DOMAINS` (in visit.py) is a frozenset of 8 strings — semantics are pure string membership; no schema change needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename `.registered_domain` → `.top_domain_under_public_suffix` at the MELI guard callsite</name>
  <files>src/artiscrapper/visit.py, src/artiscrapper/metrics.py, tests/test_visit.py</files>

  <read_first>
    - src/artiscrapper/visit.py — lines 19 (tldextract import), 33-44 (module docstring referencing "registered_domain"), 358-367 (the MELI guard block containing the deprecated call at line 364)
    - src/artiscrapper/metrics.py — lines surrounding the `_host_for_metric` helper that uses `ext.domain` + `ext.suffix` (verified by RESEARCH §Pattern 1 audit; the file does NOT call `.registered_domain` today)
    - tests/test_visit.py — line ~205 (docstring referencing `tldextract.registered_domain`) and the `test_meli_guard_no_false_positive_on_notmercadolibre` test that pins the WR-01 invariant
    - .planning/phases/0.2.5-carried-tech-debt-v0-1/0.2.5-RESEARCH.md §"TECHDEBT-01" + §"Pattern 1: Surgical rename with sister-method audit" + §"Pitfall 1: tldextract has *two* sister properties"
  </read_first>

  <action>
    Apply exactly three text-substitution edits, in this order:

    (1) `src/artiscrapper/visit.py`, in the MELI guard block at line ~364: replace the property access `ext.registered_domain` with `ext.top_domain_under_public_suffix`. Preserve `.lower()` chain and the `in _MELI_REGISTERED_DOMAINS` comparison verbatim. Do NOT touch line 363 (`ext = tldextract.extract(url)`). Do NOT use the sister property `top_domain_under_registry_suffix` — that is a different semantic and would regress the WR-01 invariant (see RESEARCH Pitfall 1).

    (2) `src/artiscrapper/visit.py`, in the surrounding documentation: update the two prose references to `registered_domain` (module docstring near line 33-44 and the inline comment near line 358-362) to read `top_domain_under_public_suffix` instead, preserving the surrounding sentence structure. The comments explain WHY tldextract is used (public-suffix-aware) — keep that intent intact.

    (3) `src/artiscrapper/metrics.py`: this file does NOT call `.registered_domain`; it uses `ext.domain + ext.suffix` directly. Add a single-line inline comment immediately above the `_host_for_metric` function explaining: `# NOTE: uses ext.domain + ext.suffix directly (not the public-suffix-aware sister API top_domain_under_public_suffix); intentional — _host_for_metric labels a Prometheus metric, not a security guard. Do NOT introduce .registered_domain here (deprecated in tldextract 5.3.0).` This addresses RESEARCH Open Question 4 — defensive future-proofing only, no behavioral change.

    (4) `tests/test_visit.py`: update the docstring at line ~205 that says `tldextract.registered_domain match` to read `tldextract.top_domain_under_public_suffix match` so the test's self-description matches the new property name. Do not change any assertions or test logic.

    Decision rationale: full rename (per RESEARCH Recommendation), not pinning `tldextract<5.3.0` (regression per Anti-Patterns), not adding `-W ignore::DeprecationWarning` (defeats TECHDEBT-03 gate per Anti-Patterns).
  </action>

  <acceptance_criteria>
    - `grep -n 'top_domain_under_public_suffix' src/artiscrapper/visit.py` returns ≥1 match (the renamed callsite, formerly line 364).
    - `grep -v '^#' src/artiscrapper/visit.py | grep -c '\.registered_domain'` equals 0 (no `.registered_domain` call survives in non-comment code; protects against `grep -c` counting docstring tokens per the planner grep-gate hygiene rule).
    - `grep -v '^#' src/artiscrapper/ -r | grep -c '\.registered_domain'` equals 0 across the entire production package (verifies no callsite outside visit.py).
    - `grep -n 'top_domain_under_public_suffix' src/artiscrapper/metrics.py` returns ≥1 match (the defensive comment).
    - `grep -n 'top_domain_under_public_suffix' tests/test_visit.py` returns ≥1 match (the updated docstring).
    - `.venv/bin/pytest tests/test_visit.py -k meli -v` exits 0 with all MELI guard tests passing, including `test_meli_guard_no_false_positive_on_notmercadolibre` (the WR-01 invariant).
    - `.venv/bin/pytest tests/test_visit.py -k 'meli or skip_if_price' -v 2>&1` produces ZERO output lines matching `DeprecationWarning.*registered_domain` (the source of the 5 currently-failing tests under the deprecation gate).
    - `.venv/bin/pytest tests/ -q -m "not slow"` exits 0 and reports ≥124 passed (the 124-pass / 2-skip baseline must NOT regress; see RESEARCH Assumption A7).
  </acceptance_criteria>

  <verify>
    <automated>cd /home/luis/proyectos/artiscrapper && .venv/bin/pytest tests/test_visit.py -k meli -v && bash -c '! grep -v "^#" src/artiscrapper/visit.py | grep "\.registered_domain"' && bash -c '! grep -rn "\.registered_domain" src/artiscrapper/' && .venv/bin/pytest tests/ -q -m "not slow"</automated>
  </verify>

  <done>
    - `src/artiscrapper/visit.py` MELI guard calls `ext.top_domain_under_public_suffix.lower()` instead of `ext.registered_domain.lower()`.
    - All MELI-related tests pass (including the WR-01 regression invariant).
    - Full quick test suite remains green at ≥124 passed.
    - Negative grep for `.registered_domain` returns 0 matches in production package.
    - Defensive comment landed in `metrics.py` near `_host_for_metric`.
    - Test docstring in `tests/test_visit.py:~205` updated.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MELI guard input | URL strings of arbitrary origin enter `visit.py` from the search pipeline; the guard decides whether to flag `meli_skip` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0.2.5-01 | Tampering | visit.py MELI guard | mitigate | Use the public-suffix-aware sister property `top_domain_under_public_suffix` (verified equivalent to `.registered_domain` on every URL in scope). Do NOT use `top_domain_under_registry_suffix` (different semantic — would regress WR-01 for private suffixes like blogspot.com). |
| T-0.2.5-02 | Tampering | tests/test_visit.py invariant `test_meli_guard_no_false_positive_on_notmercadolibre` | mitigate | Existing test pins the public-suffix-aware behavior; this plan does NOT modify the assertion, only the docstring. The test failing post-rename would be a hard regression of Phase 3.1 WR-01. |
</threat_model>

<verification>
- All test-level acceptance commands listed in Task 1's `<verify>` block.
- Phase regression invariant: `.venv/bin/pytest tests/ -q -m "not slow"` reports ≥124 passed.
- Negative grep across `src/artiscrapper/` for `.registered_domain` returns 0 hits.
</verification>

<success_criteria>
- The single line `src/artiscrapper/visit.py:364` (or wherever the MELI guard call lives after surrounding edits) reads `ext.top_domain_under_public_suffix.lower()`.
- Full quick suite green at ≥124 passed under both `pytest -q -m "not slow"` (this phase's per-task gate) AND, after TECHDEBT-03 lands the always-on filter, `pytest -q -W error::DeprecationWarning -m "not slow"` (the phase merge gate that Plan 03 owns).
- No `.registered_domain` calls remain in `src/artiscrapper/`.
- WR-01 invariant `test_meli_guard_no_false_positive_on_notmercadolibre` still passes byte-identically.
</success_criteria>

<output>
Create `.planning/phases/0.2.5-carried-tech-debt-v0-1/0.2.5-01-SUMMARY.md` when done.
</output>
</content>
</invoke>