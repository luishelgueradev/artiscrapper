---
milestone: v0.1
audited: 2026-06-03T23:08:00Z
status: tech_debt
scores:
  requirements: 53/53
  phases: 3/3
  integration: WIRED (5/5 E2E flows pass structurally)
  flows: 5/5
  nyquist: 0/3 compliant, 3/3 partial, 0/3 missing
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - category: traceability-hygiene
    severity: medium
    description: REQUIREMENTS.md traceability table shows 52/53 reqs as "Pending" despite Phase 2 VERIFICATION.md marking all 53 SATISFIED. SUMMARY.md `requirements_completed` frontmatter is `[]` in 7 of 8 SUMMARYs (only 03-01 lists OBS-07). 3-source cross-reference fails the workflow's Status Determination Matrix (verified=passed + summary=missing + traceability=Pending → "partial"). Root cause: execute-phase did not populate frontmatter and phase transitions did not flip traceability table. Code is correct; bookkeeping is stale.
    affected_reqs: ["SEARCH-01..08", "LLM-01..08", "VISIT-01..08", "FRESH-01..04", "CACHE-01..05", "BROWSER-01..05", "DEPLOY-01..06", "OBS-01..06", "NF-01..04"]
    fix: Flip REQUIREMENTS.md traceability table Status column from "Pending" → "Complete" for the 52 reqs that VERIFICATION.md confirms (Phase 2: 52 reqs; OBS-07 already "Complete"). Populate `requirements_completed` lists in the 02-01/02/03 and 03-02 SUMMARY frontmatters. One-time hygiene fix before milestone close.

  - category: nyquist-coverage
    severity: medium
    description: All 3 phases have VALIDATION.md but none are nyquist_compliant. Phase 1 (false/false), Phase 2 (compliant=true but wave_0_complete=false → still PARTIAL per workflow), Phase 3 (false/false). Status field is "draft" on all three. Nyquist validation is enabled in config (workflow.nyquist_validation=true).
    affected_phases: ["01-spike-empirical-validation", "02-mvp", "03-robustness"]
    fix: Run /gsd:validate-phase 1, 2, 3 (one each) to either complete wave-0 coverage or formally accept the gaps. Phase 1 is a spike with no production code — most likely candidate for explicit acceptance.

  - category: test-quality
    severity: medium
    description: "tests/test_e2e.py POST /search calls omit X-API-Key header. Phase 3 made auth mandatory. The two e2e tests (test_serp_pelota, test_cache_hit) are env-gated via @pytest.mark.skipif(not E2E) so CI is green, but Luis cannot run them against the live dev-box as written without manual edits."
    affected_reqs: ["NF-01", "CACHE-05", "OBS-01"]
    fix: Add `headers={\"X-API-Key\": os.getenv(\"E2E_API_KEY\",\"test-key-1\")}` to the two TestClient calls in tests/test_e2e.py:46 and tests/test_e2e.py:87.

  - category: test-coverage-gap
    severity: medium
    description: tests/integration/test_degraded_mode.py validates the trigger (router_health_check → False) and reimplements _heuristic_only_survivors but does NOT POST /search via TestClient. A regression where main.py's degraded branch drops metadata.llm_degraded=true or returns 503 would not be caught.
    affected_reqs: ["LLM-06"]
    fix: Add a sibling test that monkeypatches router_health_check, POSTs /search via TestClient with mocked fetch_serp returning realistic SERP HTML, and asserts metadata.llm_degraded is True AND len(results) > 0. Pattern mirrors tests/integration/test_challenge_backoff.py:199.

  - category: configuration-drift
    severity: medium
    description: "slowapi rate-limit strings hardcoded in @limiter.limit(\"60/minute\") + @limiter.limit(\"10000/day\") decorators at main.py:355-356, not derived from settings.API_RATE_PER_MINUTE / settings.API_RATE_PER_DAY. config.py:42 documents this. The rate_limit_init log line reports settings values, not the actual decorator values — so docker compose logs | grep rate_limit_init would NOT surface the divergence if Luis bumps env. This is exactly the shadowing class Phase 2 memory feedback_empirical_retest_after_default_changes warns about."
    affected_reqs: ["D-02"]
    fix: Either (a) build a single source-of-truth lookup at module-load that wires settings into the decorators via slowapi parse_many, or (b) log the actual decorator strings inside rate_limit_init so docker compose logs would catch the divergence.

  - category: phase-warnings-deferred
    severity: low
    description: 4 WARNINGs from Phase 2 review (WR-01..04) were acknowledged but not fixed. WR-01 mercadolibre. substring match (overcautious), WR-02 asyncio.create_task without strong ref (GC risk), WR-03 tautological assertion in test_footguns.py:62, WR-04 NamedTemporaryFile leak in test_health.py:18-21.
    affected_files: ["tests/test_footguns.py:62", "tests/test_health.py:18-21", "src/artiscrapper/main.py:457", "src/artiscrapper/visit.py:330"]
    fix: Open a cleanup phase or carry forward to the next milestone as known low-severity debt.

  - category: deferred-phases
    severity: informational
    description: ROADMAP defines v0.1 as Phases 1-5, but Phase 4 (Production Operations) is "deferred — on demand from prod telemetry" and Phase 5 (Expansion) is "deferred — until Sánchez grows past current scope". Both carry ZERO v1 requirements. Phase 4 (Grafana, Loki, tracing) is on-demand; Phase 5 (per-supplier adapters, residential proxy, async+SSE, multi-tenant auth) is gated on triggers. Treating v0.1 as "Phases 1-3 shipped" is the workflow-clean interpretation. /gsd:complete-milestone v0.1 will need to scope-down to 1-3 OR explicitly record Phases 4-5 as deferred-by-design.
    affected_phases: ["04-production-operations", "05-expansion"]
    fix: At /gsd:complete-milestone v0.1, choose: (a) scope v0.1 = phases 1-3 only and carry 4-5 into v0.2 ROADMAP, or (b) record 4-5 as "deferred-by-design — re-evaluated for v0.2 trigger conditions" in MILESTONES.md.
---

# Milestone v0.1 Audit — artiscrapper

**Status:** `tech_debt` — no critical blockers; 7 hygiene/coverage items need decisions before close.

**Scope:** Phases 1, 2, 3 of v0.1 milestone (Phase 4 + 5 = deferred-by-design, zero v1 requirements).

**Audited by:** /gsd-audit-milestone on 2026-06-03.

---

## Score Card

| Dimension | Score | Notes |
|---|---|---|
| Requirements | **53/53 satisfied** | Per VERIFICATION.md coverage tables + integration evidence. 3-source cross-reference WARNS on REQUIREMENTS.md traceability ("Pending" everywhere) and SUMMARY frontmatter (empty in 7/8 SUMMARYs) — bookkeeping debt, not code gaps. |
| Phases | **3/3 complete** | Phase 1 spike (no VERIFICATION.md by design — produces SPIKE.md instead, 0 v1 reqs); Phase 2 VERIFICATION.md status=passed (53/53); Phase 3 VERIFICATION.md status=passed (6/6 success criteria + 20/20 D-NN decisions + 21/21 threats). |
| Integration | **WIRED** | Phase 1 → Phase 2 handoff verified (fixtures consumed, D1+D12 honored); Phase 2 → Phase 3 hardening overlay clean (auth Depends, slowapi decorators, /metrics mount, ChallengeBackoff hooks, metrics bridge). |
| E2E Flows | **5/5 PASS** | Cold happy path, cache-hit, degraded LLM-down, blocked path, observability — all structurally verified. |
| Nyquist | **0 compliant / 3 partial / 0 missing** | All 3 phases have VALIDATION.md draft with `wave_0_complete: false`. |

---

## Requirements Coverage (3-Source Cross-Reference)

| REQ Block | Phase | VERIFICATION.md | SUMMARY frontmatter | REQUIREMENTS.md Status | Final |
|---|---|---|---|---|---|
| SEARCH-01..08 (8) | 2 | passed (SATISFIED) | empty | Pending | **partial bookkeeping → satisfied per evidence** |
| LLM-01..08 (8) | 2 | passed (SATISFIED) | empty | Pending | **partial bookkeeping → satisfied per evidence** |
| VISIT-01..08 (8) | 2 | passed (SATISFIED) | empty | Pending | **partial bookkeeping → satisfied per evidence** |
| FRESH-01..04 (4) | 2 | passed (SATISFIED) | empty | Pending | **partial bookkeeping → satisfied per evidence** |
| CACHE-01..05 (5) | 2 | passed (SATISFIED) | empty | Pending | **partial bookkeeping → satisfied per evidence** |
| BROWSER-01..05 (5) | 2 | passed (SATISFIED) | empty | Pending | **partial bookkeeping → satisfied per evidence** |
| DEPLOY-01..06 (6) | 2 | passed (SATISFIED) | empty | Pending | **partial bookkeeping → satisfied per evidence** |
| OBS-01..06 (6) | 2 | passed (SATISFIED) | empty | Pending | **partial bookkeeping → satisfied per evidence** |
| OBS-07 (1) | 3 | passed (SATISFIED) | listed (03-01-SUMMARY) | Complete | **satisfied** |
| NF-01..04 (4) | 2 | passed (SATISFIED, NF-01 latency human_needed) | empty | Pending | **partial bookkeeping → satisfied per evidence** (NF-01 latency verified by operator UAT 2026-06-03) |

**No orphaned REQ-IDs.** Every requirement maps to a phase with code-verified evidence + integration-verified wiring.

**Bookkeeping debt:** 52 reqs need REQUIREMENTS.md Status flip + SUMMARY frontmatter population. See `tech_debt[0]` in frontmatter.

---

## Cross-Phase Integration (gsd-integration-checker output)

### Phase 1 → Phase 2 handoffs

| Artifact | Consumer | Status |
|---|---|---|
| `tests/fixtures/serp/*.html` (10 SERP) | `tests/test_parser.py` `FIXTURES_DIR` + `len==10` assertion | WIRED |
| `tests/fixtures/catalog/*/*.html` (10 PDP) | `tests/test_visit.py` + `tests/integration/test_catalog_extraction.py` (≥85% jsonld floor) | WIRED |
| `tests/fixtures/llm/labelled.jsonl` (50 records) | `tests/integration/conftest.py` + `tests/integration/test_degraded_mode.py` (`len==50`) | WIRED |
| D1 docker pin `cloakhq/cloakbrowser:0.3.31` | `Dockerfile:22` + `pyproject.toml` cloakbrowser==0.3.31 | WIRED |
| D12 hand-roll (no extruct) | `src/artiscrapper/visit.py:8` ("Do NOT import extruct"); pyproject.toml has no extruct | WIRED |
| SPIKE.md NEEDS-PIVOT (libnspr4/libnss3) | `Dockerfile:2-25` apt-installs both | WIRED |
| SPIKE.md SIGSTOP heartbeat | `main.py:5-7` `_recycle_browser_loop` documented | WIRED |
| SPIKE.md `/healthz` (with z) | `llm.py:261-268` `client.head(f"{router_url}/healthz")` | WIRED |

### Phase 2 → Phase 3 hardening overlay

| Concern | Evidence | Status |
|---|---|---|
| `/metrics` ASGI mount before CorrelationIdMiddleware (D-10) | `main.py:257` mount BEFORE `main.py:261` middleware | WIRED |
| `verify_api_key` Depends on `/search` | `main.py:361` `Depends(verify_api_key)` | WIRED |
| `verify_api_key` Depends on `/health/deep` (CR-03 hardening) | `main.py:292` | WIRED |
| `/health` cheap unauthenticated | `main.py:269-286` no Depends | WIRED |
| Stacked slowapi limiters | `main.py:355-356` `@limiter.limit("60/minute") + @limiter.limit("10000/day")` | WIRED (see config-drift WARNING) |
| 429 carries digit-string Retry-After | `tests/integration/test_rate_limit.py:122-131` end-to-end | WIRED |
| `check_gate` inside `search_elapsed.time()` | `main.py:394` histogram open → `main.py:429` check_gate call | WIRED |
| `record_block` after `_detect_block` branch | `main.py:502` inside `block_detected=True` branch | WIRED |
| `record_success` only when ≥1 candidate survives (WR-07) | `main.py:537-541` | WIRED |
| `inc_block_detected` dual-write | `main.py:490` + `metrics.py:133-136` | WIRED |
| `inc_llm_fallback` hooks (6 reasons) | `llm.py:196,214,232,234,237,240` | WIRED |
| Histograms wrap actual work (context-manager form, D-12) | `main.py:394`, `llm.py:293`, visit.py 3 stages | WIRED |
| Sentry init non-fatal (WRN-04 invariant) | `main.py:167-171` reads `is_active()` not env | WIRED |
| `/metrics` returns ≥6 artiscrapper_* families | Counter pre-seeding at `metrics.py:145-163` → `test_metrics_endpoint.py:65-80` regex-asserts ≥6 | WIRED |

### E2E Flow Verdicts

| Flow | Verdict | Test Evidence |
|---|---|---|
| Cold happy path (auth → rate-limit → cache miss → 2× Google fetch → parse → dedupe → junk filter → LLM curate → visit → freshness → rerank → cache write → 200) | **PASS structurally** | `main.py:354-700` 10 steps inlined; `tests/integration/test_rate_limit.py` exercises full route 60× via TestClient |
| Cache-hit fast path (<500ms, `metadata.cache_hit=true`) | **PASS** (logic verified, live deferred to E2E gate) | `main.py:404-417` early-return; covered in `tests/test_cache.py` |
| Degraded LLM-down (router HEAD → 503 → heuristic-only → `metadata.llm_degraded=true`) | **PASS** | `main.py:550-582`; `tests/integration/test_degraded_mode.py` validates trigger (test-coverage WARNING — does not exercise /search via TestClient) |
| Blocked path (sorry HTML → 200+block_detected → next request 503+Retry-After) | **PASS** | `tests/integration/test_challenge_backoff.py:157-246` two real POST /search calls via TestClient |
| Observability (/metrics ≥6 artiscrapper_* families) | **PASS** (cold-scrape variant) | Counter pre-seeding guarantees families exist before first /search; `tests/integration/test_metrics_endpoint.py:65-80` |

---

## Tech Debt (7 items)

1. **traceability-hygiene** (medium) — REQUIREMENTS.md table shows 52/53 "Pending" despite VERIFICATION.md confirming SATISFIED. SUMMARY frontmatter `requirements_completed: []` in 7/8 files. → Flip 52 traceability statuses + populate 4 SUMMARY frontmatters as one-time hygiene fix before close.

2. **nyquist-coverage** (medium) — All 3 phases have VALIDATION.md draft, none nyquist_compliant. → `/gsd:validate-phase 1, 2, 3` to complete wave-0 or formally accept gaps.

3. **test-quality** (medium) — `tests/test_e2e.py` omits X-API-Key, will 401 against live dev-box after Phase 3 auth. Affected: NF-01, CACHE-05, OBS-01. → Add header to two TestClient calls.

4. **test-coverage-gap** (medium) — `tests/integration/test_degraded_mode.py` validates trigger but never POSTs /search via TestClient. Affected: LLM-06. → Add sibling test exercising the full degraded route.

5. **configuration-drift** (medium) — slowapi rate-limit strings hardcoded; settings env vars ignored by hot path; `rate_limit_init` log reports settings, masking divergence. Affected: D-02. → Either derive decorators from settings OR log the actual decorator strings (the latter catches the class the `feedback_empirical_retest_after_default_changes` memo warns about).

6. **phase-warnings-deferred** (low) — Phase 2 WR-01..04 acknowledged but not fixed. Cosmetic / GC risk / overcautious heuristic. → Carry to v0.2 or cleanup phase.

7. **deferred-phases** (informational) — Phase 4 + 5 carry zero v1 reqs and are deferred-by-design. → At /gsd:complete-milestone, scope v0.1 to phases 1-3 OR record 4-5 as deferred in MILESTONES.md.

---

## Nyquist Compliance

| Phase | VALIDATION.md | nyquist_compliant | wave_0_complete | Classification | Action |
|---|---|---|---|---|---|
| 01-spike-empirical-validation | exists (draft) | false | false | PARTIAL | `/gsd:validate-phase 1` — likely candidate for explicit acceptance (spike has no production code) |
| 02-mvp | exists (draft) | true | false | PARTIAL | `/gsd:validate-phase 2` — promote `wave_0_complete: false` → true or accept |
| 03-robustness | exists (draft) | false | false | PARTIAL | `/gsd:validate-phase 3` |

**Recommendation:** Phase 1 has 0 v1 reqs and produced fixtures + SPIKE.md instead of code. Nyquist wave-0 doesn't naturally apply — formal acceptance is the right close. Phases 2 and 3 may genuinely need wave-0 work or formal accept based on what's missing.

---

## Decision Required Before /gsd:complete-milestone v0.1

The audit surfaces 7 tech-debt items, of which **2 are blocking from a workflow-hygiene perspective** (#1 traceability, #7 deferred-phases scope decision). The other 5 are real but acceptable to carry as known debt into v0.2.

**Recommended path to clean close:**

1. **Fix #1 (traceability hygiene)** — flip REQUIREMENTS.md Status column for the 52 reqs and populate SUMMARY frontmatters. ~5 minutes of edits.
2. **Decide #7 (deferred phases)** — scope v0.1 to Phases 1-3 only (cleanest); Phases 4 + 5 move into v0.2 ROADMAP.
3. **Decide #2 (Nyquist)** — either run `/gsd:validate-phase` for each of 1-3 OR formally accept and record in v0.2 backlog.
4. **Carry #3, #4, #5, #6** — known low-medium debt; record in MILESTONES.md `### Carried Debt` section.

After steps 1-2 land, re-run `/gsd:complete-milestone v0.1` — status will become `passed` (53/53 reqs cleanly satisfied across 3 phases with WIRED integration).

---

## Next Steps

The audit-milestone workflow routes `tech_debt` status to two paths:

**Option A — Complete milestone, accept debt:**

```
/gsd:complete-milestone v0.1
```

**Option B — Plan a cleanup phase:**

```
/gsd:phase --insert 4 "v0.1 close hygiene: traceability table flip + SUMMARY frontmatters + test_e2e auth header + degraded-mode TestClient coverage + nyquist accept"
/gsd:discuss-phase 4
/gsd:plan-phase 4
/gsd:execute-phase 4
```

Given the debt is hygiene + test-quality not blockers, **Option A with traceability fix inline** is the lowest-friction path. Workflow allows the `/gsd:complete-milestone` step to record carried debt in MILESTONES.md `### Carried Debt`.
